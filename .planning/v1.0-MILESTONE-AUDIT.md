---
milestone: v1.0
audited: 2026-08-18T17:40:00Z
status: tech_debt
milestone_complete: false
phases_executed: 3
phases_planned: 8
scores:
  requirements: 27/27 in-scope satisfied (27 of 74 milestone requirements are in scope; 47 belong to unbuilt Phases 4-8)
  phases: 3/3 verified passed
  integration: 4/4 cross-phase seams wired
  flows: 1/1 buildable E2E flow works (CSV -> compile-data -> bundle -> run-backtest / gate test)
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: ["03"]
  partial_phases: []
  not_validated_phases: ["01", "02"]
  missing_phases: []
  overall: partial
tech_debt:
  - phase: 01-performance-spike-and-budget-lock
    items:
      - "WINDOWS #2 (waived, not fixed): calibration under-corrects for CI runner speed variance; normalized figures carry ~10 percentage points of runner noise. Waiver text says the deferral EXPIRES BEFORE PHASE 4 PLANNING, because Phase 4 books PERF-07 and PERF-08 budget decisions through the same normalize() path."
      - "WR-04: measureMinOfN's floor check lacks the !Number.isFinite guard its sibling calibrationScore check has."
      - "WR-05: persistEnvironment is last-write-wins across three bench files sharing one .raw/environment.json (diagnostic only)."
      - "D-20 escalation on record: PERF-03 fails outright on a 2-logical-core runner (1032ms vs 1000ms budget), observed in a real Actions run."
      - "01-VALIDATION.md is status: draft -- validate-phase has never reconciled it."
  - phase: 02-compiled-data-bundle
    items:
      - "WR-01: buildTotalReturnSeries has no explicit precondition check when a price series does not reach the real total-return series' first date; fails via a Number.isFinite guard with a less direct message."
      - "WR-02: splitCsvFields does not handle RFC4180 double-quote escaping (latent; no committed vendor file exercises it)."
      - "WR-03: applyGapPolicy's doc comment overstates 'one run names every violation' -- true only within one classification tier."
      - "02-VALIDATION.md is status: draft -- validate-phase has never reconciled it."
  - phase: 03-simulation-kernel-and-the-upro-tqqq-gate
    items:
      - "TQQQ's margin against TRACKING_ERROR_TOLERANCE is thin (3.5331% measured vs 3.9550% tolerance, ~11% headroom), and 352 of the 395.5bp tolerance comes from one measured mechanism derived from the same two funds the gate tests. Legitimate under D-15, but the margin is real rather than generous."
      - "FINANCING_SPREAD_RANGE remains ASSUMED confidence; no fund itemizes swap financing spread in any public disclosure found across four EDGAR retrieval routes."
      - "D-10 was amended mid-phase (synthetic now built from the total-return index). Flagged forward to Phase 5's VALID-04 view."
  - phase: cross-phase
    items:
      - "bench/kernel.ts (runSpikeBacktest), tests/kernel.test.ts and bench/sweep.worker.ts still run the Phase 1 throwaway kernel. Phase 7 is scheduled to replace it. The file self-documents as carrying no correctness claim, so the integration checker rated the mistake-it-for-production risk low."
---

# Milestone v1.0 — Audit

## Scope caveat

**This milestone is not finished.** ROADMAP.md defines eight phases for v1.0; three are executed.
Phases 4 through 8 (browser UI, attribution and credibility surface, heatmap design pass, sweep
engine, export and permalinks) do not exist. This audit therefore verifies **what has been built**,
and does **not** clear v1.0 for `/gsd-complete-milestone`. 47 of the 74 milestone requirements are
assigned to unbuilt phases and are correctly `Pending`, not gaps.

## Requirements Coverage (3-source cross-reference)

Sources: REQUIREMENTS.md traceability table, each phase VERIFICATION.md, each plan SUMMARY.md
`requirements-completed` frontmatter.

| Phase | Requirements | VERIFICATION | SUMMARY frontmatter | Traceability | Verdict |
|-------|--------------|--------------|---------------------|--------------|---------|
| 01 | PERF-01, PERF-01a, PERF-10, PERF-11 | passed, 7/7 | all four listed | Complete | 4/4 satisfied |
| 02 | DATA-01…DATA-07, DATA-09 | passed, 5/5 | all eight listed | Complete | 8/8 satisfied |
| 03 | PERF-02, SIM-01…SIM-12, VALID-01…VALID-03 | passed, 16/16 | all sixteen listed | was Pending, corrected by this audit | 16/16 satisfied |

**In scope: 27/27 satisfied. Unsatisfied: 0. Orphaned: 0.**

### Bookkeeping corrected by this audit

Twelve Phase 3 requirements (PERF-02, SIM-01…SIM-08, SIM-10…SIM-12) read `[ ]` / `Pending` in
REQUIREMENTS.md despite `03-VERIFICATION.md` recording 16/16 verified and every plan SUMMARY listing
them. Per the status-determination matrix (`passed` + listed + `[ ]` → satisfied, update checkbox),
all twelve checkboxes and all twelve traceability rows were flipped to complete. This is the same
class of drift `02-VERIFICATION.md` caught for DATA-02, which has since been corrected.

## Phase Verification

| Phase | Status | Score | Human verification |
|-------|--------|-------|--------------------|
| 01 Performance Spike and Budget Lock | passed | 7/7 must-haves | Resolved 2026-08-17 — a GitHub remote was configured afterward; real Actions runs prove both green and red `bench` checks |
| 02 Compiled Data Bundle | passed | 5/5 must-haves | None required |
| 03 Simulation Kernel and the UPRO/TQQQ Gate | passed | 16/16 requirement IDs, 5/5 ROADMAP criteria | Both items closed by evidence |

Zero phases missing a VERIFICATION.md. Zero unverified phases.

## Cross-Phase Integration

| Seam | Status | Evidence |
|------|--------|----------|
| Phase 2 → Phase 3 binary decode | WIRED | `src/data/kernel-inputs.ts` imports `decodeHeader`/`seriesView`/`calendarView` from `tools/bundle-compiler/src/binary-format.ts` and passes the expected `BUNDLE_VERSION` (`45a9f1ae6444`); a mismatch throws `BundleVersionMismatchError` rather than degrading silently |
| Phase 2 → Phase 3 bundle contents | WIRED | All nine required series present: SPX/NDX/UPRO/TQQQ price-return and total-return, plus `@rate/rate`. The rate series ends 2026-08-14 against price data through 2026-08-17; D-29 truncation takes the intersection and reports `truncatedForRateCoverage=true` rather than reading past the end |
| Phase 1 → Phase 3 PERF-02 subject | WIRED | `bench/kernel.bench.test.ts` imports `runBacktest` from `src/kernel/backtest.ts`; the bench row reports `source=production` and is gated through the shared `assertWithinBudget`/`checkBudget` helper |
| Phase 1 → Phase 2/3 bench completeness | WIRED | `buildFullRowSet()` synthesizes a row for every `PERF_BUDGETS` id, so unbuilt paths print `verdict=unmeasured` instead of vanishing (PERF-10) |

**Broken flows: none.** The one E2E flow buildable today runs clean: raw CSVs → `npm run compile-data`
→ committed bundle → `npm run run-backtest` prints a real dated equity curve → `npm run test` runs the
UPRO/TQQQ gate against that same bundle. 385/385 unit tests pass; the gate is GREEN for both funds.

### Seam readiness for unbuilt phases (observations, not gaps)

- `src/validation/tracking-error.ts` takes only `Float64Array` and scalars and imports nothing from
  the kernel, data or chart layers, so Phase 5's VALID-04 in-app view can call it unchanged.
- The kernel's typed-array in/out interface is Worker-callable as-is for Phase 7's sweep.

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Verdict | Action |
|-------|---------------|--------|-------------------|---------|--------|
| 01 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 01` |
| 02 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 02` |
| 03 | exists | validated | true | COMPLIANT | none |

Phases 1 and 2 were seeded by plan-phase and never reconciled by validate-phase. `nyquist_compliant:
false` there is not authoritative and is not a compliance failure — it is a coverage TODO. Both
phases have substantial passing test suites already, so validation will likely promote them without
new tests.

## Tech Debt Summary

See the `tech_debt` block in the frontmatter for the itemized list. The one with a deadline attached:

**WINDOWS #2 (calibration noise) is waived, not fixed, and its waiver expires before Phase 4
planning.** Phase 4 measures PERF-07 and PERF-08 for the first time, and both run through the same
`normalize()` path that carries roughly 10 percentage points of runner noise. The waiver was granted
only because Phase 3's own measurements sit far from their budget lines (PERF-02 at 0.22ms against
16ms). That argument does not transfer to Phase 4's cold-load and decode budgets.

Total: 13 items across 3 phases plus 1 cross-phase item. Zero blockers.

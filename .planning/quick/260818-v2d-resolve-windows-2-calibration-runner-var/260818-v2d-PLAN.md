---
phase: quick-260818-v2d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - perf-budgets.ts
  - bench/sweep-pool.ts
  - bench/sweep.bench.test.ts
  - bench/report.ts
  - bench/calibration.ts
  - tests/sweep-pool.test.ts
  - tests/report.test.ts
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [QUICK-260818-v2d]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PERF-03's sweep runs at the declared 4-core baseline pool width (workerCount 3) on every host, not at whatever width the host happens to offer, so the figure means what perf-budgets.ts already says it means."
    - "A bench run on a host whose recorded hardwareConcurrency differs from the declared baseline does not render a PERF-03 verdict: the row reads unmeasured, the table carries an explicit withheld banner naming both widths, and the measured figure survives in the PERF-03 info line."
    - "The withholding is not bypassable from a single bench file: assertRunInvariants throws if a PERF-03 verdict is present on an off-baseline host, and throws outright when an off-baseline host is claimed as an authoritative CI baseline run."
    - "The change is a demonstrated no-op at the declared baseline width: a committed unit test proves the pinned width equals what auto-resolution produced on a 4-core host, and a 4-core-constrained bench run measures PERF-03 within run-to-run noise of the same run made before the change, with the verdict still rendered."
    - "NOMINAL_REFERENCE_MS is still 40, no thresholdMs or anchorMs in perf-budgets.ts moved, and no substitute budget-denominating constant was introduced: nothing added divides a measured value."
    - "bench/calibration.ts's normalize() doc comment states the measured noise band conditioned on a 4-core runner (residual 6.36% relative over 13 recorded runs, single run good to roughly +/-13%, two-run comparison to roughly +/-20%) and the superseded three-run limitation paragraph is gone."
    - "bench/canonical-calibration.ts still imports only vitest/browser commands and ./calibration.ts, so the browser bundle is untouched."
    - "npm run bench exits 0 and its total runtime stays under BENCH_TOTAL_RUNTIME_CAP_MS on both a 4-core-constrained run and an unconstrained run."
    - "WINDOWS.md entry 2 reads fixed in the frontmatter counters, the markdown table row and the JSON block, all written by gsd-tools rather than by hand."
    - "STATE.md and ROADMAP.md no longer carry the BEFORE-PHASE-4 prerequisite as outstanding, and both carry forward the measured band that Phase 4's criterion 5 must plan against."
  artifacts:
    - perf-budgets.ts
    - bench/sweep-pool.ts
    - bench/sweep.bench.test.ts
    - bench/report.ts
    - bench/calibration.ts
    - tests/sweep-pool.test.ts
    - tests/report.test.ts
    - .planning/WINDOWS.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - "perf-budgets.ts PERF_03_BASELINE_HARDWARE_CONCURRENCY -> bench/sweep-pool.ts BASELINE_WORKER_COUNT (via workerCountForCores) -> bench/sweep.bench.test.ts runSpikeSweep options: one declared width, two consumers, no second literal"
    - "environment-block.ts hardwareConcurrency (already recorded on every run) -> bench/sweep.bench.test.ts verdict suppression -> bench/report.ts assertRunInvariants host guard -> renderTable withheld banner: the guard needs no new instrumentation"
    - "bench/report.ts assertRunInvariants verdict-fail check runs BEFORE the host guard, so bench/selftest/over-budget.selftest.ts (hardwareConcurrency 1) still fails for its own PERF-05 reason and tests/perf-budgets.selftest.test.ts's assertions still match"
    - "tests/sweep-pool.test.ts workerCountForCores(4) === BASELINE_WORKER_COUNT -> the committed proof that pinning is a no-op on the D-17 baseline, where every recorded run resolved workerCount=3"
    - "bench/calibration.ts normalize() measured band -> STATE.md decision bullet -> ROADMAP.md Phase 4 prerequisite line: the +/-13% single-run limit reaches Phase 4 planning in the document Phase 4 planning reads"
---

<objective>
Close WINDOWS.md entry 2 by fixing the yardstick's actual defect: PERF-03's pool width is pinned to
the declared 4-core baseline and its verdict is withheld on any other host width, and normalize()'s
doc comment is replaced with the measured, correctly-conditioned noise band.

Purpose: 260818-v2d-RESEARCH.md measured two distinct problems the ledger conflated. Problem A, the
scalar anchor is structurally blind to available parallel width, is latent and catastrophic: with
everything else held fixed the anchor moves 0.4% across a 4.5x change in width while normalized
PERF-03 moves 6.1x, and it has already fired once (a 2-core CI runner failed PERF-03 outright).
Problem B, each workload has its own elasticity to host interference so one scalar cannot correct
three of them, is chronic and moderate at 6.36% residual CV. The ledger's specific 10-point claim
came from a 3-run sample containing the single most anomalous run of 13 and does not survive the
larger sample. Problem A gets fixed. Problem B gets an honest measured band, because more runs, not
a cleverer anchor, is its only remedy.

Output: a PERF-03 figure that is denominated against the baseline it claims, a run-level guard that
makes an off-baseline figure impossible to present as a verdict, a superseded doc comment replaced
by measured numbers, a closed Broken Window, and an unblocked Phase 4.

No tracer task is emitted as a separate slice. Task 1 IS the vertical slice: it threads the declared
width from perf-budgets.ts through the pool, the measurement, the row, the run-level invariant and
the rendered table, and is proven end to end by a real `npm run bench` on both a baseline-width host
and an off-baseline host. Splitting it would leave a half-wired guard behind a passing typecheck.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260818-v2d-resolve-windows-2-calibration-runner-var/260818-v2d-RESEARCH.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@perf-budgets.ts
@bench/calibration.ts
@bench/sweep-pool.ts
@bench/sweep.bench.test.ts
@bench/report.ts
@bench/environment-block.ts
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<established_figures>
## Every number this plan writes, already measured in 260818-v2d-RESEARCH.md

Do NOT re-derive these. Re-measuring the local bench is required (Task 1's verification), but no
figure written into a comment or a document may be invented here.

**The n=13 CI dataset** (every recorded `ubuntu-latest` run, all of them `hardwareConcurrency: 4`,
all of them `workerCount=3 chunkCount=12`):

| metric | raw CV | normalized CV |
|---|---|---|
| PERF-03 (pool) | 11.03% | 6.36% |
| PERF-02 (single-thread) | 11.10% | 7.12% |
| PERF-05 (canvas) | 14.48% | 12.59% |

Within the 9-run pre-phase-2 cohort with the one anomalous run excluded: PERF-02 normalized CV
0.31%, PERF-03 normalized CV 3.07% (raw 7.31%). Cross-metric residual correlation within that
cohort: 0.874. Correlation of each residual with the anchor's own level: 0.057 and -0.007.

**Power at 6.36% residual CV** (two-sample, alpha .05, 80% power): 5% change needs ~26 runs per
arm, 10% needs ~7, 15% needs ~3, 20% needs ~2. So a single run supports a headroom claim only to
roughly +/-13% (2 sd) and a two-run comparison only to roughly +/-20%.

**The width effect, same machine, same HEAD, only affinity varied:**

| affinity | hardwareConcurrency | workerCount | anchor score | PERF-03 norm, auto width | PERF-03 norm, width pinned to 3 |
|---|---|---|---|---|---|
| all 9 cores | 9 | 8 | 0.6100 | 314.4 | 675.2 |
| `0-3` | 4 | 3 | 0.6125 | 678.9 | 683.4 |
| `0-1` | 2 | 1 | 0.6075 | 1917.2 (exit 1) | 1015.7 |

Pinning collapses the 9-core-vs-4-core spread from 116% to 1.2%.

**Elasticity to host interference** (4 memory-streaming spinners, same machine, same HEAD): anchor
score +7.8%, PERF-02 raw +12.1%, PERF-05 raw +24.5%, PERF-03 raw +60.2%. Residual after
normalize(): PERF-02 +4.0%, PERF-05 +15.5%, PERF-03 +48.6%.

**Measured dead, do not implement:** log-log OLS of PERF-03 on the anchor has slope 1.051 (95% CI
0.626 to 1.476, r2 0.681); slope 1 cannot be rejected and the fitted slope buys 0.02pp.
</established_figures>

<hard_prohibitions>
1. **`NOMINAL_REFERENCE_MS` stays 40.** No calibration constant changes. Adding any new constant
   that a measured value is divided by is the same prohibition reached by a different route and is
   equally forbidden. `PERF_03_BASELINE_HARDWARE_CONCURRENCY` is legal precisely because nothing is
   ever divided by it: it selects a workload width and gates a verdict.
2. **No `thresholdMs` or `anchorMs` value in `perf-budgets.ts` changes.** No `relaxationReason` is
   added. This task fixes the yardstick, not the thresholds.
3. **`bench/canonical-calibration.ts` is not touched**, and nothing it imports may come to depend on
   `node:fs` or `node:os`. It runs in the browser bundle.
4. **Do not implement candidates (a), (b) or (c)** from the research: a pool-shaped anchor, a
   two-term calibration, or a regression-calibrated coefficient. (c) is measured dead. (a) and (b)
   introduce a second budget-denominating constant and are separately blocked by the score-coherence
   check at `bench/report.ts:322`.
5. **Do not re-waive.** The evidence was obtainable and was obtained.
6. **Never emit the U+2014 character** in any file written, including commit messages. Use a comma,
   colon, parentheses, or two sentences.
7. **Minimize ternaries; never nest them.** New branching in `bench/report.ts` and
   `bench/sweep.bench.test.ts` uses `if`/`else`.
</hard_prohibitions>

<interfaces>
## The exact shape Task 1 builds, so Tasks 2 and 3 can cite it

```ts
// perf-budgets.ts
export const PERF_03_BASELINE_HARDWARE_CONCURRENCY = 4

// bench/sweep-pool.ts
export function workerCountForCores(cores: number): number   // Math.max(1, cores - 1)
export function resolveWorkerCount(): number                  // workerCountForCores(navigator.hardwareConcurrency || 1)
export const BASELINE_WORKER_COUNT: number                    // workerCountForCores(PERF_03_BASELINE_HARDWARE_CONCURRENCY) === 3

// bench/report.ts
export function hostMatchesPerf03Baseline(environment: EnvironmentBlock): boolean
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin PERF-03 to the declared 4-core pool width and withhold its verdict off that width</name>
  <files>perf-budgets.ts, bench/sweep-pool.ts, bench/sweep.bench.test.ts, bench/report.ts, tests/sweep-pool.test.ts, tests/report.test.ts</files>
  <precondition>`node_modules/` is installed and `public/data/` is present in the working tree, and the Playwright chromium cache under `~/.cache/ms-playwright` is populated: this task's verification runs the real browser bench twice. If `node_modules/` is absent (a fresh worktree), run `npm ci` first; if it cannot be installed, halt and report rather than substituting a reasoned argument for the measured no-op proof.</precondition>
  <reversibility rating="reversible">A workload parameter and a reporting gate. Nothing is persisted, no budget moves, and reverting the commit restores auto-resolution exactly.</reversibility>
  <read_first>
Read `bench/sweep-pool.ts` (resolveWorkerCount, SweepOptions, runSpikeSweep), `bench/sweep.bench.test.ts` (the PERF-03 test at the bottom), `bench/report.ts` (assertRunInvariants and renderTable), `bench/environment-block.ts` (EnvironmentBlock fields), `perf-budgets.ts` (the PERF-03 entry), `tests/report.test.ts` (the `row()` helper and the `environment` fixture, which is already hardwareConcurrency 4 / ci true), and `bench/selftest/over-budget.selftest.ts` (which persists hardwareConcurrency 1).
  </read_first>
  <behavior>
Write the tests in `tests/sweep-pool.test.ts` and `tests/report.test.ts` first and watch them fail
before implementing. They are the no-op proof and must not be written to fit an implementation that
already passes.

`tests/sweep-pool.test.ts` (new file, unit project, Node; `bench/sweep-pool.ts` imports cleanly in
Node, verified):
- `workerCountForCores(4)` is 3, and `BASELINE_WORKER_COUNT` equals `workerCountForCores(PERF_03_BASELINE_HARDWARE_CONCURRENCY)`. Name this test so it reads as what it is: the pinned width is exactly the width auto-resolution produced on the D-17 baseline, where all 13 recorded runs resolved workerCount 3, so pinning cannot move the baseline figure.
- `workerCountForCores(1)` is 1 and `workerCountForCores(0)` is 1: the floor survives the extraction.
- `workerCountForCores(9)` is 8: the rule itself is unchanged, only its input is now fixed for PERF-03.

`tests/report.test.ts` additions:
- `assertRunInvariants` does not throw for the existing baseline `environment` fixture (hardwareConcurrency 4, ci true) with a PERF-03 row carrying verdict pass. This is the guard-is-silent-on-the-baseline proof.
- `assertRunInvariants` throws for `{ ...environment, hardwareConcurrency: 2 }` and for `{ ...environment, hardwareConcurrency: 8 }` while ci is true, and the message names both the recorded width and the declared width.
- `assertRunInvariants` throws when ci is false, hardwareConcurrency is 9, and the PERF-03 row carries verdict pass: a bench file cannot restore a verdict the host does not support.
- `assertRunInvariants` does not throw when ci is false, hardwareConcurrency is 9, and the PERF-03 row is unmeasured.
- `assertRunInvariants` still throws `/failed budget/` naming PERF-05 when a row fails AND the host is off-baseline with ci true: the verdict gate keeps precedence over the host guard, which is what keeps `bench/selftest/over-budget.selftest.ts` (hardwareConcurrency 1) failing for its own reason.
- `renderTable` emits a line containing `PERF-03 VERDICT WITHHELD` plus both widths when the environment is off-baseline, and emits no such line for the baseline fixture.
  </behavior>
  <action>
Five edits, one declared width.

**perf-budgets.ts.** Export `PERF_03_BASELINE_HARDWARE_CONCURRENCY = 4` immediately above the
`PERF_BUDGETS` table, with a doc comment stating: D-17 makes GitHub Actions `ubuntu-latest` the
baseline machine and every recorded baseline run reported four logical cores; PERF-03's own
description already claims a 4-core baseline, so the width belongs beside the budget it conditions;
and, explicitly, that this is a workload parameter and a reporting gate, never a divisor, so it is
not budget-denominating in the sense `NOMINAL_REFERENCE_MS` is. Change nothing else in this file.

**bench/sweep-pool.ts.** Extract the pure rule as `workerCountForCores(cores: number): number`
returning `Math.max(1, cores - 1)`, reimplement `resolveWorkerCount()` as
`workerCountForCores(navigator.hardwareConcurrency || 1)` so the flooring rule exists once, and
export `BASELINE_WORKER_COUNT = workerCountForCores(PERF_03_BASELINE_HARDWARE_CONCURRENCY)` importing
the constant from `../perf-budgets.ts`. Document on `BASELINE_WORKER_COUNT` that PERF-03 measures at
this width on every host: the anchor in `bench/calibration.ts` is single-threaded and cannot see
available parallelism at all, so a width that follows the host makes the metric's dominant variance
term invisible to the correction that is supposed to absorb it (measured: the anchor moves 0.4%
across a 4.5x width change while normalized PERF-03 moves 6.1x). `runSpikeSweep`'s own default stays
`resolveWorkerCount()`: the correctness and failure-path tests exercise the production resolution,
only the measured arm is pinned.

**bench/sweep.bench.test.ts.** In the PERF-03 test, pass `{ workerCount: BASELINE_WORKER_COUNT }` to
`runSpikeSweep`. Read `navigator.hardwareConcurrency` into a local and compare it against
`PERF_03_BASELINE_HARDWARE_CONCURRENCY` with an `if`, not a ternary. On the baseline width the row is
built exactly as it is today. Off it, the row carries `measuredMs: null`, `normalizedMs: null` and
`verdict: 'unmeasured'`, because a figure produced at a pinned width on a host that cannot supply
that width is not a PERF-03 measurement and must never be compared against the budget. Either way
the figure is disclosed: extend the existing `PERF-03-sweep` info line to carry `workerCount`,
`chunkCount`, the host's `hardwareConcurrency`, the declared baseline width, the raw and normalized
milliseconds (via `formatMeasured`), and whether the verdict was rendered or withheld. Keep the
existing `expect(() => assertWithinBudget(row)).not.toThrow()` diagnostic; on a withheld row it is a
no-op by `assertWithinBudget`'s own contract for an unmeasured value.

**bench/report.ts.** Export
`hostMatchesPerf03Baseline(environment: EnvironmentBlock): boolean`, comparing
`environment.hardwareConcurrency` against `PERF_03_BASELINE_HARDWARE_CONCURRENCY`. In
`assertRunInvariants`, add the host guard at the top of the existing `if (environment)` block, which
is already positioned after the verdict-fail gate: that ordering is load-bearing and must be stated
in a comment, since `bench/selftest/over-budget.selftest.ts` records hardwareConcurrency 1 and must
keep failing on its own PERF-05 breach rather than on the host width. When the host is off-baseline
and `environment.ci` is true, throw, naming the recorded width and the declared width and saying
plainly that a run on a host that is not the declared baseline cannot be presented as an
authoritative D-17 baseline run. When the host is off-baseline and `environment.ci` is false, throw
only if a PERF-03 row carries a verdict other than `unmeasured`, naming the offending verdict: this
is what makes the suppression non-bypassable, so deleting the bench file's guard turns the run red
instead of silently restoring a misleading verdict. In `renderTable`, after the existing
authoritative/informational banner, emit a line when the host is off-baseline whose text contains
`PERF-03 VERDICT WITHHELD`, both widths, and the reason (three workers timesharing fewer cores is an
irreducible cost no anchor can or should erase; a wider host is simply not the machine the budget is
denominated against).

Answer to the research's open design question, recorded here because the executor must not re-decide
it: withhold locally, hard-fail on CI. Withholding on CI would leave PERF-03 ungated on exactly the
runs that gate the project, and a CI run that drew an off-baseline runner is already an invalid
baseline run whichever way it is reported. On a developer machine the banner already says the run is
informational, so withholding is sufficient there and keeps `npm run bench` usable on any hardware.
  </action>
  <verify>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; taskset -c 0-3 npm run bench > /tmp/v2d-post-4core.log 2>&amp;1 || { tail -40 /tmp/v2d-post-4core.log; exit 1; } ; cp .bench/bench-results.json /tmp/v2d-post-4core.json &amp;&amp; node --input-type=commonjs -e 'const pre=require("/tmp/v2d-pre-4core.json"),post=require("/tmp/v2d-post-4core.json");const pick=o=>o.rows.find(r=>r.budgetId==="PERF-03");const a=pick(pre),b=pick(post);if(post.environment.hardwareConcurrency!==4)throw new Error("host reported width "+post.environment.hardwareConcurrency+", not the declared baseline");if(b.verdict!=="pass")throw new Error("verdict is "+b.verdict+" at the baseline width: the guard fired when it must stay silent");if(!post.infoLines.some(l=>l.includes("workerCount=3")))throw new Error("pool width was not pinned to 3");const d=Math.abs(b.measuredMs-a.measuredMs)/a.measuredMs;if(d>0.15)throw new Error("PERF-03 raw moved "+(d*100).toFixed(1)+"% at the baseline width: pinning is not a no-op");console.log("PASS baseline no-op: pre="+a.measuredMs.toFixed(1)+"ms post="+b.measuredMs.toFixed(1)+"ms delta="+(d*100).toFixed(1)+"%")'</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; npm run bench > /tmp/v2d-post-host.log 2>&amp;1 || { tail -40 /tmp/v2d-post-host.log; exit 1; } ; node --input-type=commonjs -e 'const o=require("/workspace/-Users-abarcinski-myrepos-leverage-simulator/.bench/bench-results.json");const r=o.rows.find(x=>x.budgetId==="PERF-03");if(o.environment.hardwareConcurrency===4)throw new Error("this host reported the baseline width, so the off-baseline arm proved nothing: rerun it under an affinity mask that is not 4 cores");if(r.verdict!=="unmeasured")throw new Error("verdict "+r.verdict+" was rendered on an off-baseline host");if(r.measuredMs!==null||r.normalizedMs!==null)throw new Error("a withheld row must not carry a measured figure");const info=o.infoLines.find(l=>l.startsWith("PERF-03 sweep"));if(!info||!info.includes("workerCount=3")||!info.includes("hardwareConcurrency="))throw new Error("the withheld figure was not disclosed in the info line: "+info);console.log("PASS off-baseline: "+info)' &amp;&amp; grep -qF "PERF-03 VERDICT WITHHELD" /tmp/v2d-post-host.log &amp;&amp; echo "PASS withheld banner rendered"</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; test "$(git diff -U0 -- perf-budgets.ts | grep -cE '^[+-][[:space:]]*(thresholdMs|anchorMs|relaxationReason):')" = "0" &amp;&amp; test "$(git diff -U0 -- bench/calibration.ts | grep -cE '^[+-]const NOMINAL_REFERENCE_MS')" = "0" &amp;&amp; grep -qF 'const NOMINAL_REFERENCE_MS = 40' bench/calibration.ts &amp;&amp; test "$(git diff -- bench/canonical-calibration.ts | wc -l)" = "0" &amp;&amp; test "$(git diff -U0 | grep '^+' | LC_ALL=C grep -c $'\xe2\x80\x94' || true)" = "0" &amp;&amp; echo "PASS prohibitions intact"</automated>
  </verify>
  <done>
`npm run typecheck` and `npm test` pass with the new tests in `tests/sweep-pool.test.ts` and
`tests/report.test.ts`. A 4-core-constrained bench run exits 0, renders a PERF-03 verdict, reports
workerCount 3, and measures PERF-03 raw within 15% of the same run captured before the change. An
unconstrained bench run on this host exits 0, carries a PERF-03 row with verdict unmeasured and no
measured figure, prints the withheld banner, and still discloses the raw and normalized figures plus
both widths in the PERF-03 info line. No budget value, no calibration constant, and no line of
`bench/canonical-calibration.ts` changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace normalize()'s superseded limitation note with the measured, 4-core-conditioned band</name>
  <files>bench/calibration.ts</files>
  <reversibility rating="reversible">A doc comment. No executable line in this file changes.</reversibility>
  <action>
Delete the KNOWN LIMITATION paragraph from `normalize()`'s doc comment in its entirety, the
three-run spread claim and the noise-magnitude sentence that follows it, and write the measured band
in its place. That paragraph's numeric claim was drawn from a 3-run sample that included the single
most anomalous run of the 13 now on record, and it does not survive the larger sample, so leaving it
standing next to the corrected figures would be worse than having no note at all.

The replacement must carry, using only figures from `<established_figures>`:

- That over the 13 recorded D-17 baseline runs (write the sample size as the literal `n=13`, which
  the verification greps for) `normalize()` cuts PERF-03's CV from 11.03% raw to
  6.36% normalized and PERF-02's from 11.10% to 7.12%, and that within the largest same-HEAD cohort
  with the one anomalous run excluded normalized PERF-02 sits at 0.31% CV and PERF-03 at 3.07%. The
  correction works; it is partial, not absent.
- That the band is conditional on a 4-core runner and is wrong off it, because all 13 recorded runs
  drew 4 cores and the parallel-width term was therefore constant across the whole dataset. Point at
  where that term is now handled instead: `bench/sweep-pool.ts` pins PERF-03's pool to
  `BASELINE_WORKER_COUNT` and `bench/report.ts` withholds its verdict off the declared width.
- What the residual actually is: a shared per-run factor the anchor's single sample did not capture
  (cross-metric residual correlation 0.874 within cohort, while the residuals' correlation with the
  anchor's own level is 0.057 and -0.007), not each metric's own sampling tail.
- Why one scalar cannot do better: each workload has its own elasticity to host interference,
  measured under memory contention at anchor +7.8%, PERF-02 raw +12.1%, PERF-05 raw +24.5%, PERF-03
  raw +60.2%, leaving post-normalize residuals of +4.0%, +15.5% and +48.6%. This is why PERF-05
  normalizes worst of the three.
- How to read a normalized figure: a single run supports a headroom claim only to roughly +/-13%
  (2 sd) and a two-run comparison only to roughly +/-20%; detecting a true 5% change at 80% power
  needs roughly 26 runs per arm and 10% needs roughly 7. State plainly that the remedy for the
  residual is more runs, not a cleverer anchor, and that a regression-calibrated coefficient was
  measured and rejected (OLS slope 1.051, 95% CI 0.626 to 1.476, worth 0.02pp).
- That retuning `NOMINAL_REFERENCE_MS` is still not the remedy, for the reason already recorded:
  it is budget-denominating under PERF-01a.

Cite `quick-260818-v2d` and 260818-v2d-RESEARCH.md so the numbers are traceable. Touch no
executable line: `normalize()`'s body, `calibrationScore()`, and every constant stay byte-identical.
  </action>
  <verify>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; for tok in "6.36" "11.03" "7.12" "3.07" "0.874" "n=13" "4-core" "260818-v2d" "+/-13" "+/-20" "48.6" "1.051"; do grep -qF "$tok" bench/calibration.ts || { echo "MISSING in bench/calibration.ts: $tok"; exit 1; }; done &amp;&amp; echo "PASS measured band present"</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; test "$(grep -cF '856.4' bench/calibration.ts)" = "0" &amp;&amp; test "$(grep -ciF 'percentage points' bench/calibration.ts)" = "0" &amp;&amp; test "$(grep -cF 'KNOWN LIMITATION' bench/calibration.ts)" = "0" &amp;&amp; echo "PASS superseded note gone"</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; npm run typecheck &amp;&amp; npx vitest run --project unit tests/calibration.test.ts &amp;&amp; test "$(git diff -U0 -- bench/calibration.ts | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vcE '^[+-]([[:space:]]*(\*|/\*\*|\*/).*)?$')" = "0" &amp;&amp; echo "PASS comment-only diff"</automated>
  </verify>
  <done>
`normalize()`'s doc comment states the measured band with its 4-core condition, names the
correlation evidence for what the residual is, points at where the parallel-width term is now
handled, and says what number of runs a given claim needs. The superseded three-run paragraph is
gone. `git diff` on this file touches comment lines only, `npm run typecheck` passes, and
`tests/calibration.test.ts` still passes unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close WINDOWS entry 2 through gsd-tools and clear the Phase 4 prerequisite</name>
  <files>.planning/WINDOWS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <reversibility rating="reversible">Ledger and planning-document state, all recoverable from git.</reversibility>
  <action>
Do this only after Tasks 1 and 2 are complete and their verifications passed.

**WINDOWS.md.** Run `gsd-tools windows fixed 2` (the CLI, resolved through the standard launcher, or
`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 2`). Do not hand-edit the frontmatter
counters, the markdown table row, or the JSON block: the tool owns all three and keeping them
consistent by hand is how they drift.

**STATE.md.** Remove the outstanding prerequisite bullet from `### Pending Todos` and leave that
section reading `- None`. Append a bullet to the `### Decisions` list, tagged `quick-260818-v2d`,
recording:
- That the entry named one problem and there were two. The anchor cannot see parallel width, which
  is latent and worth up to 6.1x, and was dormant in all 13 recorded CI runs because every one of
  them drew a 4-core runner. Separately, each workload has its own elasticity to host interference,
  which is chronic and worth 6.36% residual CV.
- That the first is fixed by measuring PERF-03 at the declared 4-core baseline width on every host
  and withholding its verdict when the recorded `hardwareConcurrency` differs, and that this is
  provably a no-op on the D-17 baseline, where every recorded run already resolved workerCount 3.
- That the second is documented as a measured band rather than fixed, because more runs is its only
  remedy: a single run supports a headroom claim only to roughly +/-13%.
- That the ledger's own numeric claim did not survive the larger sample: the three runs it cited
  included the single most anomalous run of the 13, and that run inflated the single-threaded
  PERF-02 by the same amount it inflated PERF-03, which a pool-specific mechanism cannot explain.
- That `NOMINAL_REFERENCE_MS` stayed 40 and no budget value moved.

**ROADMAP.md.** Replace the Phase 4 `**Prerequisite (added 2026-08-18, ...)**` block with a single
`**Prerequisite RESOLVED (2026-08-18, quick-260818-v2d):**` block. It must state that the entry is
closed and how, and then carry forward the one fact Phase 4 planning actually needs: on the D-17
baseline the normalized residual is 6.36% relative over 13 recorded runs, so a single CI run supports
a headroom claim only to roughly +/-13% and a two-run comparison only to roughly +/-20%, and
criterion 5's PERF-07/PERF-08 decisions must be read against that band rather than against a single
figure. Keep it to that: the prerequisite is discharged, not restated.
  </action>
  <verify>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; node --input-type=commonjs -e 'const fs=require("fs");const t=fs.readFileSync(".planning/WINDOWS.md","utf8");const m=t.match(/`{3,}json\n([\s\S]*?)\n`{3,}/);if(!m)throw new Error("json block not found");const e=JSON.parse(m[1]).find(x=>x.id===2);if(!e)throw new Error("entry 2 missing");if(e.status!=="fixed")throw new Error("entry 2 status is "+e.status);if(!/waived_count:\s*0/.test(t))throw new Error("frontmatter waived_count is not 0");if(!/fixed_count:\s*3/.test(t))throw new Error("frontmatter fixed_count is not 3");if(!/\n\|\s*2\s*\|[^\n]*\|\s*fixed\s*\|/.test(t))throw new Error("markdown table row 2 does not read fixed");console.log("PASS windows entry 2 closed in all three representations")'</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; node --input-type=commonjs -e 'const fs=require("fs");const s=fs.readFileSync(".planning/STATE.md","utf8");const sec=s.split("### Pending Todos")[1].split("###")[0];const bullets=sec.split("\n").filter(l=>l.trim().startsWith("- "));if(bullets.length!==1||bullets[0].trim()!=="- None")throw new Error("Pending Todos is not cleared, found: "+JSON.stringify(bullets));if(!s.includes("quick-260818-v2d"))throw new Error("STATE.md records no quick-260818-v2d decision");const r=fs.readFileSync(".planning/ROADMAP.md","utf8");const p4=r.split("### Phase 4")[1].split("### Phase 5")[0];const pre=p4.split("\n").filter(l=>l.startsWith("**Prerequisite"));if(pre.length!==1)throw new Error("Phase 4 carries "+pre.length+" Prerequisite lines, expected 1");if(!pre[0].startsWith("**Prerequisite RESOLVED"))throw new Error("Phase 4 prerequisite is still outstanding: "+pre[0]);if(!p4.includes("6.36"))throw new Error("Phase 4 does not carry the measured band forward");console.log("PASS state and roadmap cleared")'</automated>
    <automated>cd /workspace/-Users-abarcinski-myrepos-leverage-simulator &amp;&amp; test "$(git diff -U0 -- .planning/ | grep '^+' | LC_ALL=C grep -c $'\xe2\x80\x94' || true)" = "0" &amp;&amp; echo "PASS no new U+2014"</automated>
  </verify>
  <done>
`gsd-tools windows fixed 2` has run and WINDOWS.md reads fixed for entry 2 in the frontmatter
counters, the table row and the JSON block. STATE.md's Pending Todos section reads `- None` and its
Decisions list carries the quick-260818-v2d resolution including the correction to the ledger's own
numeric claim. ROADMAP.md's Phase 4 carries exactly one Prerequisite line, it reads RESOLVED, and it
hands the +/-13% single-run limit to whoever plans criterion 5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner allocation to the bench gate | The host width is chosen by GitHub, not by this repo, and it silently changes what PERF-03 measures. This is the only untrusted input in scope. |
| bench file to run-level invariant | A per-file `expect()` can be removed or weakened; `assertRunInvariants` is the boundary that must not be bypassable from a bench file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2d-01 | Spoofing | An off-baseline runner presenting itself as a D-17 baseline result | high | mitigate | `assertRunInvariants` throws when `environment.ci` is true and `hardwareConcurrency` differs from `PERF_03_BASELINE_HARDWARE_CONCURRENCY`, naming both widths (Task 1) |
| T-v2d-02 | Tampering | A future edit deletes the bench file's verdict suppression and silently restores a misleading PERF-03 verdict | high | mitigate | The run-level guard rejects any PERF-03 row carrying a verdict other than `unmeasured` on an off-baseline host, so the deletion turns the run red rather than green (Task 1) |
| T-v2d-03 | Information disclosure | A withheld verdict hides the measured figure, leaving a developer with no signal at all | medium | mitigate | The PERF-03 info line always carries workerCount, chunkCount, both widths, and the raw and normalized milliseconds; the table prints an explicit withheld banner (Task 1) |
| T-v2d-04 | Repudiation | A normalized figure is cited later as more precise than the data supports | medium | mitigate | The measured band, its 4-core condition, and the per-claim run counts live in `normalize()`'s own doc comment and in ROADMAP.md's Phase 4 block (Tasks 2 and 3) |
| T-v2d-05 | Elevation of privilege | A gate change quietly un-trips the D-20 escalation | high | mitigate | The pinned width equals the width every recorded baseline run already resolved, proven by a committed unit test and by a before/after 4-core bench comparison; hard prohibitions forbid touching `NOMINAL_REFERENCE_MS`, any threshold, or introducing a substitute divisor (Task 1 verification) |
| T-v2d-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency; `package.json` is not in `files_modified`, so the supply-chain surface is unchanged and no legitimacy checkpoint is owed |
</threat_model>

<verification>
Run in order. The first command must run BEFORE any file is edited.

1. **Capture the pre-change baseline-width reference** (Task 1 depends on it):
   `taskset -c 0-3 npm run bench && cp .bench/bench-results.json /tmp/v2d-pre-4core.json`
   Confirm it exits 0 and that the captured JSON reports `environment.hardwareConcurrency` 4. If it
   does not report 4, the affinity mask is not reaching the browser and the no-op proof is not
   available: halt and report rather than proceeding without it.
2. Task 1's four verify commands, in order.
3. Task 2's three verify commands.
4. Task 3's three verify commands.
5. Full suite once more at the end: `npm run typecheck && npm test && npm run bench:selftest`
   (the selftest is expected to exit non-zero by design; assert its output still contains PERF-05
   and `failed budget`, which proves the host guard did not displace the verdict gate).
</verification>

<success_criteria>
- PERF-03 is measured at the declared 4-core baseline pool width on every host, and a
  4-core-constrained run measures it within 15% of the pre-change figure with the verdict still
  rendered.
- An off-baseline run renders no PERF-03 verdict, prints the withheld banner, still discloses the
  figure, and exits 0 locally; an off-baseline CI run fails the run naming both widths.
- Removing the bench file's suppression cannot restore the verdict: the run-level invariant rejects
  it.
- `bench/calibration.ts` carries the measured 4-core-conditioned band and no superseded claim, with
  a comment-only diff.
- `NOMINAL_REFERENCE_MS` is 40, no `thresholdMs`/`anchorMs` moved, no new divisor was introduced, and
  `bench/canonical-calibration.ts` is unchanged.
- WINDOWS.md entry 2 reads fixed in all three representations, written by gsd-tools.
- STATE.md and ROADMAP.md discharge the Phase 4 prerequisite and hand the +/-13% single-run limit
  forward.
- `npm run typecheck`, `npm test` and `npm run bench` all pass; bench total runtime stays under
  `BENCH_TOTAL_RUNTIME_CAP_MS`.
</success_criteria>

<output>
Create `.planning/quick/260818-v2d-resolve-windows-2-calibration-runner-var/260818-v2d-SUMMARY.md`
when done. It must record: the pre-change and post-change 4-core PERF-03 raw figures with their
delta (the measured no-op proof), the off-baseline run's withheld-row evidence, and confirmation
that `NOMINAL_REFERENCE_MS` and every budget value are unchanged. If any verification could not be
run, say so explicitly and record a Broken Window rather than substituting an argument for a
measurement.
</output>

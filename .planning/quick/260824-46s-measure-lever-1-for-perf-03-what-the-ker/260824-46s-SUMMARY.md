---
phase: quick-260824-46s
plan: 01
subsystem: testing
tags: [performance, benchmarking, kernel, measurement, perf-03]

requires:
  - phase: 07.1-sweep-pool-tuning-inserted
    provides: 07.1-PERF-03-BASELINE.md section 9's naming of the kernel's write-only per-bar output arrays as an unspent, reasoned-not-measured lever, and section 2's five-run PERF-03 failing baseline (1120.86-1411.05ms normalized)
provides:
  - A measured ratio for PERF-03 lever 1 (variant/shipped kernel time): min=0.9810 median=0.9841 max=0.9904 across five samples over the real ~25,000-bar SPX series
  - A reusable bit-identical equivalence proof (bench/kernel-scalar-arrays.bench.test.ts) gating any future timing claim about the kernel's output arrays
  - A refutation of 07.1-PERF-03-BASELINE.md section 9's reasoning: the write-only arrays cost roughly 1-2% of kernel compute time, not a meaningful fraction
affects: []

actuals:
  tokens: 6791
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Equivalence-before-timing gated by a file-scoped pass counter, not declaration order: the timing test throws unless all equivalence cases proved in the same run, so a faster wrong answer can never be reported as an improvement"

key-files:
  created:
    - bench/backtest-scalar-only.ts
    - bench/kernel-scalar-arrays.bench.test.ts
    - .planning/quick/260824-46s-measure-lever-1-for-perf-03-what-the-ker/260824-46s-FINDINGS.md
  modified: []

key-decisions:
  - "Lever 1 measured, not spent: the scalar-only variant is a bench-tree-only measurement artifact, never wired into src/sweep/, and src/kernel/backtest.ts stays byte-identical throughout."
  - "The measured ratio (0.9810-0.9904) refutes 07.1-PERF-03-BASELINE.md section 9's reasoning that the write-only arrays are a worthwhile lever; the projected best case (~1105-1113ms normalized) stays above the 1000ms budget and above the best of the five failing D-17 runs."

patterns-established:
  - "A measurement task with a HARD prohibition on shipping the thing it measures: proof-before-timing gate plus a per-task git diff --exit-code check against the shipped file, so a tempting-but-unmeasured lever cannot leak into production code via the same plan that measures it."

requirements-completed: [QUICK-260824-46s, PERF-03]

coverage:
  - id: D1
    description: "A scalar-only variant of runBacktest exists in the bench tree and is bit-identical, field by field under Object.is, to the shipped kernel over the real bundled series across four parameter sets (zero contribution, contribution schedule, ruin, leverage below 1)"
    requirement: PERF-03
    verification:
      - kind: unit
        ref: "bench/kernel-scalar-arrays.bench.test.ts (4 equivalence tests, all pass)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The equivalence proof runs and passes before any timing figure is produced, enforced by a gate (equivalenceCasesProven === 4), not merely file order"
    requirement: PERF-03
    verification:
      - kind: unit
        ref: "bench/kernel-scalar-arrays.bench.test.ts (timing test throws if equivalenceCasesProven !== 4; observed to pass gated on all four)"
        status: pass
    human_judgment: false
  - id: D3
    description: "An A/B ratio (variant/shipped) is reported across five independent samples with min/median/max, produced by measureBatchedMinOfN, normalized, disclosing batchSize"
    requirement: PERF-03
    verification:
      - kind: other
        ref: ".bench/bench-results.json infoLines: PERF-03-lever1-scalar-arrays entry, batchSize=250 sampleCount=5 minRatio=0.9810 medianRatio=0.9841 maxRatio=0.9904"
        status: pass
    human_judgment: false
  - id: D4
    description: "src/kernel/backtest.ts stays byte-identical; no budget, threshold, calibration constant, cap, or grid dimension moved; FINDINGS.md states the verdict plainly, including that a refutation is a successful outcome"
    requirement: PERF-03
    verification:
      - kind: other
        ref: "git diff --exit-code -- src/kernel/backtest.ts perf-budgets.ts bench/calibration.ts .planning/ROADMAP.md (exits 0); grep -rl backtest-scalar-only src/ (empty); npm run typecheck (0 errors); 260824-46s-FINDINGS.md section 6"
        status: pass
    human_judgment: false

duration: 3min
completed: 2026-08-24
status: complete
---

# Quick Task 260824-46s: Measure PERF-03 Lever 1 (Kernel Write-Only Per-Bar Arrays) Summary

**Lever 1 measured and refuted: the scalar-only variant is only 1-2% faster than the shipped kernel (ratio min=0.9810 median=0.9841 max=0.9904 across five samples over the real ~25,000-bar SPX series), too small to close PERF-03's 1120.86-1411.05ms normalized figure to under the 1000ms budget on its own.**

## Performance

- **Duration:** 3 min (task-commit span; total session including planning-context review was longer)
- **Started:** 2026-08-24T03:15:14Z
- **Completed:** 2026-08-24T03:18:06Z
- **Tasks:** 3
- **Files modified:** 3 (all created)

## Accomplishments

- `bench/backtest-scalar-only.ts`: a faithful copy of `runBacktest` with the eleven write-only per-bar array assignments removed and the four `outValue` readback sites (three points plus `finalValue`) replaced with the scalar substitutions the array writes already made redundant. No arithmetic, operator, order-of-operations, constant, or branch condition differs from the shipped kernel.
- `bench/kernel-scalar-arrays.bench.test.ts`: four equivalence tests proving the variant bit-identical (`Object.is`, per field) to the shipped kernel across zero-contribution, a contribution schedule, a run that ruins, and leverage below 1, each with its own non-vacuity assertion, over the real ~24,772-bar SPX series. A file-scoped `equivalenceCasesProven` counter gates the fifth (timing) test: it throws if fewer than four cases proved in the same run.
- The timing test measured five independent A/B samples (arm order alternated, elision-guarded via an accumulator sink, one shared batch size of 250 sized against the faster arm) over the zero-contribution branch, the failing PERF-03 headline row.
- `260824-46s-FINDINGS.md`: states the measured ratio, projects it against 07.1-PERF-03-PROFILE.md's own kernel-compute/overhead split, and concludes plainly that lever 1 cannot close the gap alone and that 07.1-PERF-03-BASELINE.md section 9's reasoning was wrong.

## Task Commits

Each task was committed atomically:

1. **Task 1: Scalar-only variant plus a bit-identical equivalence proof over the real series** - `f7cbe55` (test)
2. **Task 2: A/B timing over the real series, gated on the proof, reported as a ratio with its spread** - `8cf231f` (test)
3. **Task 3: Write FINDINGS.md, stating plainly whether lever 1 can close the gap** - `a9c1feb` (docs)

**Plan metadata:** committed by the orchestrator, not this executor, per the harness's explicit instruction not to commit docs artifacts (SUMMARY.md, STATE.md, PLAN.md).

## Files Created/Modified

- `bench/backtest-scalar-only.ts` - Measurement-only variant of `runBacktest`; not reachable from `src/`
- `bench/kernel-scalar-arrays.bench.test.ts` - Four equivalence tests plus the gated A/B timing test, diagnostic-only (info-line channel)
- `.planning/quick/260824-46s-measure-lever-1-for-perf-03-what-the-ker/260824-46s-FINDINGS.md` - The measured ratio, its projection, and the plain verdict

## Decisions Made

- The equivalence proof's non-vacuity preconditions used the real series as-is rather than synthetic fixtures: the ruin case's leverage-sweep loop (starting at 20, doubling if needed) turned out to ruin at exactly leverage 20 on the real history, so no sweep was needed in practice, but the loop is retained as written since the plan required it not be assumed.
- The projection in FINDINGS.md section 5 used all three of the measured ratio's min/median/max points (not just the median) against 07.1-PERF-03-PROFILE.md's kernel-compute/overhead split, because Phase 7.1's own record shows understating variance was the specific failure mode to avoid.

## Deviations from Plan

None - plan executed exactly as written. One in-flight correction during Task 2 (not a deviation from the plan's substance): the first `recordInfoLine` call's text used "lever 1" with a space, which did not satisfy the plan's own verify script's `includes('lever1')`/`includes('PERF-03-lever1-scalar-arrays')` check; corrected to lead the line with the literal `PERF-03-lever1-scalar-arrays` id before the human-readable description, then re-verified. No test logic, measurement methodology, or committed figure changed as a result.

## Issues Encountered

None. `npx vitest run --project bench` exits 1 on both task runs due to `assertRunInvariants`'s PERF-08 coverage check (unmeasured `DATA-BUNDLE-BYTES`/`PERF-08a/b/c` on a single-file run). This is expected and documented in the plan's own context fact 7: not a failure of this measurement, and the per-file test-pass count and info-line content were used to judge success instead of the process exit code.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- PERF-03 lever 1 is now MEASURED and refuted. Whoever plans the next PERF-03 phase should not budget on lever 1 alone; 260824-46s-FINDINGS.md section 6 states this plainly and section 5 shows the projected best case (~1105-1113ms normalized) still sits above both the 1000ms budget and the best of the five failing D-17 runs.
- The two remaining named-but-unspent levers from 07.1-PERF-03-BASELINE.md section 9 (D-03's coarser default grid, and Newton-with-bisection-fallback for `solveIrr`) are untouched by this task and remain in reserve.
- PR #7 and PR #8 stay unmerged; the merge bar (PERF-03 at or under 1000ms normalized on two consecutive D-17 runs) is unaffected by this task, as intended.
- No blockers. `npm run typecheck` exits 0. `src/kernel/backtest.ts`, `perf-budgets.ts`, `bench/calibration.ts`, and `.planning/ROADMAP.md` are all byte-identical to this task's starting point.

## Self-Check: PASSED

All three created files confirmed present on disk; all three task commit hashes (`f7cbe55`, `8cf231f`, `a9c1feb`) confirmed in `git log`.

---
*Phase: quick-260824-46s*
*Completed: 2026-08-24*

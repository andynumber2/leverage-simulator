---
phase: quick-260824-r5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/backtest-ablation-variants.ts
  - bench/chunk-metrics-kernel-ablation.ts
  - bench/kernel-ablation.bench.test.ts
  - .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md
autonomous: true
requirements: [QUICK-260824-r5d, PERF-03]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every ablation candidate is measured at the 17-column chunk shape (REAL_CHUNK_COLS=17 columns x SWEEP_ROWS=50 rows, 850 cells), through a harness that reproduces computeChunkMetrics' own per-column series resolution and per-cell call pattern. No ablation ratio is derived from a tight single-series runBacktest loop."
    - "The 17-column harness is PROVEN faithful by measurement: with the shipped kernel injected, its multiples, drawdowns, annualized and flags arrays are bit-identical (Object.is, element by element) to the real computeChunkMetrics output at the same 17-column request, and its own wall clock against the real computeChunkMetrics is recorded so the injected-callee polymorphism tax is a measured number rather than an assumption."
    - "Every candidate declared bit-preserving is PROVEN bit-identical on all eight KernelResult fields under Object.is, over the real bundled ~25,000-bar series, across the same four parameter cases 260824-46s used: zero contribution, contribution schedule, ruin, and leverage below 1."
    - "The reciprocal-multiply candidate is identified as NOT bit-preserving and its maximum absolute and maximum relative deviation per numeric KernelResult field is measured and recorded across all four cases."
    - "The correctness proof runs and passes BEFORE any timing figure is produced, gated at runtime by a file-scoped proven-case counter the timing test checks and throws on, not by declaration order alone."
    - "Per-candidate A/B ratios against the shipped kernel are recorded at the 17-column span with the sample count and the observed min, median and max stated, from rounds in which every arm runs once and the arm order rotates between rounds."
    - "A combined arm containing every bit-preserving candidate together is MEASURED as its own variant at the 17-column span. The combined figure is never summed or multiplied from the individual ratios."
    - "The arm is gated off in the default bench suite. Its added cost to a default npm run bench is measured back to back with the file absent and present, and disclosed. BENCH_TOTAL_RUNTIME_CAP_MS stays 30000 and is not raised."
    - "src/kernel/backtest.ts, src/sweep/sweep-pool.ts, src/sweep/sweep.worker.ts, bench/sweep.bench.test.ts, perf-budgets.ts and bench/calibration.ts are all byte-identical at task end: git diff --exit-code against each exits 0. Nothing under src/ references any ablation variant."
    - "No kernel change ships. No budget, threshold, calibration constant (NOMINAL_REFERENCE_MS stays 40), run-level cap or grid dimension is touched, and D-03's coarser default grid is not spent."
    - "260824-r5d-FINDINGS.md states, in this order: the per-candidate table (ratio, spread, bit-preserving yes or no, deviation if no); the measured combined bit-preserving cut; the verdict against BOTH the 10.8% and the 21.3% thresholds in plain words; the projection onto the D-17 headline marked as a projection; and, if the cut clears neither threshold, an unambiguous statement that the 1000ms budget is unreachable by every lever this project has now tested."
    - "npm run typecheck exits 0 and a default npm run bench completes under the unchanged cap with the gated PERF-03 row unperturbed."
  artifacts:
    - bench/backtest-ablation-variants.ts
    - bench/chunk-metrics-kernel-ablation.ts
    - bench/kernel-ablation.bench.test.ts
    - .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md
  key_links:
    - "computeChunkMetrics (src/sweep/sweep.worker.ts, already exported and already called serially at span 17 by bench/sweep-residual.bench.test.ts) -> the bench-side clone computeChunkMetricsWithKernel: the clone is what makes an injected kernel measurable at the real 17-column span without editing the byte-identical-protected worker."
    - "260824-52h's spanRatio=0.51 (span17PerCellUs=132.82 versus span2PerCellUs=260.00) -> why every arm here runs at 17 columns: measuring this ablation at a narrow span is the exact error that produced the phantom 273.98ms residual."
    - "bench/backtest-scalar-only.ts (already exists, already proven bit-identical by 260824-46s) -> candidate 6's arm: reused as-is rather than reimplemented, so the array-write ablation is re-measured at the 17-column span without re-litigating it."
    - "bench/kernel-scalar-arrays.bench.test.ts's equivalenceCasesProven counter and its throw in the timing test -> this file's own proof-before-timing gate: the mechanism is copied because Vitest continues after a failed test, so declaration order alone is not a gate."
    - "resolveBenchResultsDir (bench/accumulator-store.ts, reads process.env.BENCH_RESULTS_DIR, rejects absolute and parent-directory paths) -> BENCH_RESULTS_DIR=.bench/ablation for the flagged run: the ablation invocation writes its artifact inside the already-gitignored .bench/ tree and cannot clobber .bench/bench-results.json."
    - "assertRunInvariants' PERF-08 coverage gate (bench/report.ts) -> why the flagged single-file invocation exits non-zero: a single-file bench run leaves the due PERF-08 rows unmeasured. The artifact is written before that gate throws, so the info line is always recoverable, and the non-zero exit is structural, never a budget verdict."
    - "commands.recordInfoLine -> the results JSON infoLines array: the diagnostic-only channel (bench/sweep-pool-profile.bench.test.ts's precedent). A PERF-03-kernel-ablation budget id does not exist in perf-budgets.ts, so recording a MeasurementRow here would trip assertRunInvariants' unknown-budget-id check."
---

<objective>
Measure, by ablation at the real 17-column chunk shape, whether per-cell kernel compute is
reducible, and report the answer against the two thresholds a passing PERF-03 run actually needs.

Purpose: quick-260824-52h established that real per-chunk compute divided by worker count explains
98.3% of sweep wall clock on its sandbox, that buffer allocation, merge and wire total 0.14ms
across the whole 10,000-cell grid, that drain imbalance is 7.50ms and concurrencyFactor is 1.05.
No pool-overhead lever remains. Its section 5 names real per-cell kernel compute at the 17-column
chunk shape as the only place remaining headroom could come from, and states that nobody has
measured whether that cost is reducible. This task measures exactly that, and nothing else.

Output: three bench-tree measurement artifacts and one FINDINGS document. No kernel change. A
refuted result is a successful outcome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

@.planning/quick/260824-52h-isolate-the-273-98ms-unexplained-residua/260824-52h-FINDINGS.md
@.planning/quick/260824-46s-measure-lever-1-for-perf-03-what-the-ker/260824-46s-FINDINGS.md
@src/kernel/backtest.ts
@bench/backtest-scalar-only.ts
@bench/kernel-scalar-arrays.bench.test.ts
</context>

<constraints_for_every_task>
Binding on all three tasks below. Each task's `<verify>` re-asserts them; none is a one-time check.

- Ship NO kernel change. Adopting any candidate is a separate, later decision, and for any
  candidate that is not bit-preserving it requires its own Key Decision.
- These six files must be byte-identical at task end, enforced by `git diff --exit-code` against
  each: `src/kernel/backtest.ts`, `src/sweep/sweep-pool.ts`, `src/sweep/sweep.worker.ts`,
  `bench/sweep.bench.test.ts`, `perf-budgets.ts`, `bench/calibration.ts`.
- Nothing under `src/` may reference any ablation artifact:
  `grep -rl 'backtest-ablation-variants\|chunk-metrics-kernel-ablation' src/` must return nothing.
- Do NOT touch any budget, threshold, calibration constant (`NOMINAL_REFERENCE_MS` stays 40),
  run-level cap (`BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000) or grid dimension. Do NOT spend D-03's
  coarser default grid.
- Do NOT merge, mark ready, push to, comment on, or otherwise act on PR #7 or PR #8. Do NOT
  `git push`. Do NOT update ROADMAP.md.
- No em dash characters in any file this plan produces.
- Record results exclusively through `commands.recordInfoLine`, never `commands.recordMeasurement`.
- If a measurement contradicts an expectation stated in this plan, the measurement wins and
  FINDINGS records the contradiction. Do not manufacture a hopeful conclusion.
</constraints_for_every_task>

<reference_arithmetic>
Precomputed here so FINDINGS is mechanical rather than re-derived under time pressure.

Threshold 1, against CI run 32669644628 (`raw=1179.70`, `calibrationScore=1.0525`): a pass needs
`raw <= 1052.5`, a cut of 127.20ms, **10.8%**.

Threshold 2, against CI run 32686531154 (`raw=1156.60`, `calibrationScore=0.9100`): a pass needs
`raw <= 910.0`, a cut of 246.60ms, **21.3%**. The user's merge bar requires two consecutive passes
and the observed calibration range spans 0.9100 to 1.0525, so 21.3% is the robust target.

Projection rule, using 260824-52h's measured kernel share of wall clock (0.983, a sandbox figure):

  projected cut fraction = 1 - (0.983 * rCombined + 0.017)

Which gives the decision rule FINDINGS must apply to the MEASURED combined ratio `rCombined`:

  - `rCombined <= 0.7833` clears the 21.3% threshold (and therefore both).
  - `rCombined <= 0.8907` clears the 10.8% threshold only.
  - `rCombined > 0.8907` clears neither.

Every figure derived from this rule is a projection and must be labelled one in FINDINGS.
</reference_arithmetic>

<candidates>
Nine arms. Arm 0 is the shipped kernel, re-run inside every round as that round's own denominator.

| Arm | Variant export | Candidate | Expected classification |
|---|---|---|---|
| 0 | `runBacktest` (shipped) | baseline denominator | n/a |
| 1 | `runBacktestNoGuards` | 1: the `?? 0` guards on `calendarDaysElapsed[i]`, `returns[i]`, `shortRate[i]` | bit-preserving |
| 2 | `runBacktestDayCountLut` | 2: the two per-bar divisions by 360 and 365, replaced by a precomputed exact-quotient lookup | bit-preserving |
| 3 | `runBacktestDayCountReciprocal` | 2 variant: `gap * (1/360)` and `gap * (1/365)` | NOT bit-preserving |
| 4 | `runBacktestDrawdownSkip` | 3: the `1 - value / peakValue` division at four sites | bit-preserving |
| 5 | `runBacktestPeelBarZero` | 4: the `if (i === 0)` branch evaluated on all ~24,772 iterations | bit-preserving |
| 6 | `runBacktestDedupDrawdown` | 5: the four duplicated copies of the drawdown-update block | bit-preserving |
| 7 | `runBacktestScalarOnly` (existing, `bench/backtest-scalar-only.ts`) | 6: the per-bar array writes | bit-preserving (already proven by 260824-46s) |
| 8 | `runBacktestCombined` | all bit-preserving candidates at once (1, 2-lut, 3, 4, 5, 6) | bit-preserving |

Expected classifications are expectations, not claims. Every one is proven or refuted by the
equivalence arm before any clock starts. If a candidate expected to be bit-preserving is not, it
is reclassified and reported as such, never quietly dropped.
</candidates>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One arm end to end at the 17-column span, gated off by default</name>
  <files>bench/backtest-ablation-variants.ts, bench/chunk-metrics-kernel-ablation.ts, bench/kernel-ablation.bench.test.ts</files>
  <read_first>
    src/kernel/backtest.ts (the loop under ablation, lines 92 to 179),
    src/sweep/sweep.worker.ts (computeChunkMetrics: confirm for yourself that per-cell work in the
    zero-contribution branch is one runBacktest call plus two float writes plus solveCagr, and
    that per-column work is one resolveColumnSeries call),
    bench/backtest-scalar-only.ts (the variant-artifact pattern and its header discipline),
    bench/kernel-scalar-arrays.bench.test.ts (the equivalence gate, the elision sink, the info-line
    channel, the arm-order alternation),
    bench/sweep-residual.bench.test.ts lines 78 to 180 and 660 to 690 (REAL_CHUNK_COLS,
    baseParams, buildEarlyEntryDates, chunkRequestFor, and how it times a serial span-17
    computeChunkMetrics call)
  </read_first>
  <behavior>
    Tests written before the arm is trusted, in this order inside bench/kernel-ablation.bench.test.ts:
    - Harness fidelity: computeChunkMetricsWithKernel(bundle, request17, runBacktest) returns
      multiples, drawdowns, annualized and flags element-by-element bit-identical (Object.is) to
      computeChunkMetrics(bundle, request17), over a real 17-column request on the real bundle.
      Non-vacuity: cellCount is exactly 17 * SWEEP_ROWS, and at least one multiple is nonzero.
    - Equivalence for arm 1 (runBacktestNoGuards) on all four parameter cases, all eight
      KernelResult fields, Object.is via toBe, with 260824-46s' own non-vacuity assertions carried
      over unchanged (barCount above 20000; totalContributed above initialInvestment; ruined true
      with maxDrawdown exactly 1; ruined false on the leverage-below-1 case).
    - Gate: the timing test throws unless the proven-case counter equals its expected value.
  </behavior>
  <action>
Create three files. This task wires ONE variant through every layer so the whole path is proven
before Task 2 widens it. Task 2 adds the remaining arms and the sampling loop; it must not need to
change any structure this task establishes.

(a) `bench/backtest-ablation-variants.ts`. Header comment in the same discipline as
`bench/backtest-scalar-only.ts`: this is NOT the shipped kernel, `src/kernel/backtest.ts` remains
the one simulation kernel under SIM-10, nothing under `src/` may import this file, it exists only
to be timed against the shipped kernel by `bench/kernel-ablation.bench.test.ts` gated on a
correctness proof that runs before any clock starts. Re-declare
`FINANCING_DAY_COUNT_BASIS = 360` and `EXPENSE_DAY_COUNT_BASIS = 365` locally, exactly as both the
shipped kernel and `backtest-scalar-only.ts` do, so the emitted module has no runtime imports.
Export a shared `AblationKernel` function type matching `runBacktest`'s exact signature
(`(params: KernelParams, series: KernelSeries, outputs: KernelOutputs) => KernelResult`).

In this task, implement only `runBacktestNoGuards`: a faithful copy of `runBacktest` whose ONLY
difference is that the three `?? 0` guards on `calendarDaysElapsed[i]`, `returns[i]` and
`shortRate[i]` become non-null assertions. Every other statement, operator, constant, branch
condition, order of operations and pre-loop length guard is character-for-character the shipped
kernel's. Document the one difference in a comment above the function and state why it is expected
bit-preserving (a Float64Array or Int32Array in-range read never yields undefined, so `?? 0` is a
redundant runtime check, not a semantic one).

(b) `bench/chunk-metrics-kernel-ablation.ts`. Export
`computeChunkMetricsWithKernel(bundle: LoadedBundle, request: SweepChunkRequest, kernel:
AblationKernel): SweepChunkMetrics`. It is a faithful clone of `computeChunkMetrics` from
`src/sweep/sweep.worker.ts` with exactly one difference: the `runBacktest(kernelParams, resolution,
outputs)` call becomes `kernel(kernelParams, resolution, outputs)`. Everything else is preserved,
including the columns-outside/rows-inside order, the once-per-column `resolveColumnSeries` call,
the once-per-column `calendarDays` computation, the D-28/D-20 incomplete-column path, the scratch
`KernelOutputs` growth discipline, the D-24 `useIrr` resolution and both annualized branches, and
the `cell = colPos * rowCount + rowPos` indexing. Import `resolveColumnSeries`, `leverageForRow`,
`ANNUALIZED_UNDEFINED`, `CELL_FLAG_INCOMPLETE`, `CELL_FLAG_RUINED`, `solveCagr`, `buildCashFlows`,
`solveIrr`, `toDaysSinceEpoch` and `LONG_GAP_FLAG_MIN_DAYS` from the same modules the worker
imports them from. Import the `SweepChunkRequest` and `SweepChunkMetrics` types from
`src/sweep/sweep.worker.ts` rather than redeclaring them, so a future shape change breaks the
typecheck instead of silently diverging. State in the header that the clone exists because
`src/sweep/sweep.worker.ts` is byte-identical-protected for this task, that its faithfulness is
proven by measurement in the bench file rather than asserted, and that injecting the kernel makes
this call site polymorphic across arms, which is a known and separately measured effect.

(c) `bench/kernel-ablation.bench.test.ts`. Structure, in this order:

  - Header comment: measurement arm, informational channel only, no MeasurementRow, no kernel
    change, the six protected files untouched, and why every arm runs at 17 columns (260824-52h's
    spanRatio=0.51 makes narrow-span measurement systematically misleading).
  - The default-run gate. Define `const ABLATION_ENABLED = import.meta.env.VITE_PERF03_ABLATION ===
    '1'` and wrap EVERY test in this file so it is skipped when the flag is unset (`describe.skipIf`
    or `test.skipIf` on each test, whichever keeps the file's declaration order legible). The flagged
    invocation is
    `BENCH_RESULTS_DIR=.bench/ablation VITE_PERF03_ABLATION=1 npx vitest run --project bench bench/kernel-ablation.bench.test.ts`.
    Document in the header that this invocation exits non-zero because `assertRunInvariants`'
    PERF-08 coverage gate finds the due PERF-08 rows unmeasured in a single-file run, that the
    results artifact is written before that gate throws so the info line is always recoverable from
    `.bench/ablation/bench-results.json`, and that the non-zero exit is structural rather than a
    budget verdict. CONTINGENCY, if and only if the flag does not reach the browser context: do NOT
    ship an ungated arm and do NOT raise the cap. Instead rename the file to
    `bench/kernel-ablation.ablation.ts` (outside the bench project's `bench/**/*.bench.test.ts`
    include glob) and add a new `vitest.ablation.config.ts` that imports the default config,
    locates the project named `bench` and replaces only its `include` with
    `['bench/**/*.ablation.ts']`, then invoke with `--config vitest.ablation.config.ts`. Record in
    the SUMMARY which mechanism was used and why.
  - `beforeAll`: `loadBundleFromFetch()` once, and build the entry-date axis with the same
    `buildEarlyEntryDates` construction `bench/sweep-residual.bench.test.ts` uses (extended-tier
    bounds, the first `SWEEP_COLS` real trading sessions), plus `baseParams()` at
    `contributionAmount: 0`. Also read the per-series kernel payload through
    `commands.readKernelSeries()` for the equivalence cases, exactly as
    `bench/kernel-scalar-arrays.bench.test.ts` does.
  - `REAL_CHUNK_COLS = 17` and a `chunkRequestFor` helper, same construction as
    `bench/sweep-residual.bench.test.ts` (column indices 0..16, all `SWEEP_ROWS` row indices).
  - Harness fidelity test, per `<behavior>`. Compare all four output arrays element by element with
    `Object.is`, failing with the first differing index and both values named. Record both calls'
    wall clock (real `computeChunkMetrics` versus the clone with the shipped kernel injected) into
    module scope for the info line, so the injected-callee polymorphism tax is a measured number.
    Increment the proven-case counter on success.
  - Equivalence test for arm 1, per `<behavior>`, reusing 260824-46s' four cases verbatim including
    the leverage-doubling ruin probe and every non-vacuity assertion. Increment the proven-case
    counter once per case proven.
  - Timing test, gated: throw with a named message if the proven-case counter is not exactly the
    expected value for this task's arm set. Then resolve the run calibration
    (`resolveRunCalibration`), record the environment block (`commands.recordEnvironment`), run one
    warm-up round over arm 0 and arm 1, then ONE measured round: time
    `computeChunkMetricsWithKernel(bundle, request17, runBacktest)` and
    `computeChunkMetricsWithKernel(bundle, request17, runBacktestNoGuards)` with
    `performance.now()` deltas around each single call (a span-17 call is roughly 113ms on this
    sandbox, comfortably clear of `MIN_MEASUREMENT_MS`, so no batching is needed, matching
    `bench/sweep-residual.bench.test.ts`' own span-arm note). Guard both arms against elision with a
    shared file-scoped accumulator sink read after the timed region and asserted finite and nonzero.
    Emit one `commands.recordInfoLine` under the key `PERF-03-kernel-ablation` carrying:
    `hardwareConcurrency`, `calibrationScore`, `barCount`, `cellCount`, the fidelity pair, and the
    arm-1 ratio. Report ratios as `variant / shipped` so a value below 1 means faster, matching
    260824-46s' convention.

Do not add any budget id, do not call `commands.recordMeasurement`, and do not import anything from
this file into `src/`.
  </action>
  <verify>
    <automated>npm run typecheck && git diff --exit-code -- src/kernel/backtest.ts src/sweep/sweep-pool.ts src/sweep/sweep.worker.ts bench/sweep.bench.test.ts perf-budgets.ts bench/calibration.ts && test -z "$(grep -rl 'backtest-ablation-variants\|chunk-metrics-kernel-ablation' src/ || true)" && BENCH_RESULTS_DIR=.bench/ablation VITE_PERF03_ABLATION=1 npx vitest run --project bench bench/kernel-ablation.bench.test.ts; node -e "const r=require('./.bench/ablation/bench-results.json');const l=r.infoLines.join('\n');if(!/PERF-03-kernel-ablation/.test(l))throw new Error('no ablation info line recorded');console.log(l)"</automated>
    <automated>npx vitest run --project bench bench/kernel-ablation.bench.test.ts 2>&1 | grep -qiE 'skipped|0 passed' && echo "gate holds: unflagged run executes no arm"</automated>
  </verify>
  <done>The 17-column harness is proven bit-identical to the real computeChunkMetrics with the shipped kernel injected; arm 1 is proven bit-identical on all four cases before any clock starts; one arm-1 ratio at the 17-column span is recorded in .bench/ablation/bench-results.json; the unflagged run executes no arm; all six protected files are byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All nine arms, three rounds, and the measured cost of the arm</name>
  <files>bench/backtest-ablation-variants.ts, bench/kernel-ablation.bench.test.ts</files>
  <behavior>
    - Equivalence, per variant, per case: all eight KernelResult fields bit-identical under
      Object.is for arms 1, 2, 4, 5, 6, 7 and 8, across all four parameter cases. Proven-case
      counter reaches 4 per bit-preserving variant plus the fidelity case; the timing test throws
      on any shortfall.
    - Deviation, arm 3 (reciprocal): finalValue, maxDrawdown, droppedContributionsTotal and
      totalContributed recorded as max absolute and max relative deviation from the shipped
      result across the four cases; ruined, ruinBarIndex, longGapBarCount and barCount asserted
      exactly equal, and relative deviation on finalValue asserted below 1e-9, so a broken variant
      fails loudly while a genuine rounding difference is recorded rather than hidden.
    - Timing: three rounds; in each round every one of the nine arms runs exactly once at the
      17-column span; arm order rotates by round index so no arm sits systematically early or late;
      each round's ratios are computed against that same round's own shipped-arm time.
  </behavior>
  <action>
Extend the two files from Task 1. Add no new file.

(a) `bench/backtest-ablation-variants.ts`: add the remaining six variants named in `<candidates>`.
Each is a faithful copy of `runBacktest` differing in exactly the documented way, with a comment
above it naming the difference and the reason it is or is not expected bit-preserving.

  - `runBacktestDayCountLut`: build, at module scope, two `Float64Array` lookup tables of length 32
    whose entry at index g is computed by the SAME divisions the shipped kernel performs
    (`g / FINANCING_DAY_COUNT_BASIS` and `g / EXPENSE_DAY_COUNT_BASIS`), so every table entry is
    bit-identical to the division it replaces by construction. In the loop, index the table when
    `calendarGap` is a non-negative integer below the table length and fall back to the division
    otherwise, so correctness does not depend on the observed gap range (typically 1 to 4, the
    largest in the data being the 1933 bank holiday at 12 days). State in the comment that whether
    a table lookup is actually cheaper than a division on this hardware is the measurement, not an
    assumption.
  - `runBacktestDayCountReciprocal`: `calendarGap * FINANCING_RECIPROCAL` and
    `calendarGap * EXPENSE_RECIPROCAL` with the two reciprocals precomputed at module scope. Comment
    must state plainly that neither 1/360 nor 1/365 is exactly representable in binary64, so this
    variant is NOT bit-preserving and adopting it would change displayed figures.
  - `runBacktestDrawdownSkip`: keep every drawdown-update site's arithmetic identical, but add a
    `minValueSincePeak` scalar so the `1 - barValue / peakValue` division is evaluated only when the
    bar's value is strictly below the smallest value seen since the current peak was set, resetting
    that scalar whenever the peak advances. Comment must carry the bit-preservation argument: for a
    fixed positive peak, `1 - v / peak` is monotone non-increasing in v under correctly-rounded IEEE
    division, so every skipped candidate is less than or equal to one already compared, and the
    running maximum is therefore preserved exactly, not approximately. The `peakValue > 0` guard is
    preserved verbatim.
  - `runBacktestPeelBarZero`: perform the bar-0 anchor work once before the loop and start the loop
    at `i = 1`, removing the `if (i === 0)` branch from the ~24,772-iteration body. Guard the peeled
    block on `barCount > 0`. No arithmetic changes.
  - `runBacktestDedupDrawdown`: collapse the four duplicated drawdown-update blocks into a single
    update site reached by every path, driven by a `barValue` scalar that each path assigns before
    reaching it. Restructure the `continue` statements only as far as this requires. No arithmetic,
    operator, constant or comparison changes.
  - `runBacktestCombined`: every bit-preserving candidate at once (no-guards, day-count LUT,
    drawdown skip, peeled bar 0, deduplicated drawdown update, and the scalar-only substitution
    of the per-bar array writes exactly as `bench/backtest-scalar-only.ts` performs it, including
    its four documented readback substitutions). This is the arm the combined figure comes from.
    The combined figure is MEASURED here and is never summed or multiplied from the individual
    ratios, because the candidates interact.

(b) `bench/kernel-ablation.bench.test.ts`:

  - Generalize the equivalence test into one that iterates a table of `{ name, kernel,
    expectedBitPreserving }` entries over the four cases. For entries expecting bit preservation,
    assert all eight fields with `toBe`. For arm 3, compute and stash the per-field max absolute and
    max relative deviations and apply the assertions named in `<behavior>`. Keep every non-vacuity
    assertion from Task 1. Set the expected proven-case count from the table length rather than a
    hand-written literal, so adding an arm without proving it cannot pass the gate.
  - Generalize the timing test into `SAMPLE_COUNT = 3` rounds over all nine arms with the rotation
    described in `<behavior>`, preceded by exactly one full warm-up round over all nine arms whose
    timings are discarded. Compute per-arm min, median and max ratio across the three rounds.
  - Extend the `PERF-03-kernel-ablation` info line to carry, for every arm: the arm name, the
    per-round ratios, min, median and max, and its bit-preserving classification. Carry the arm-3
    deviation figures, the fidelity pair from Task 1, `armWallClockMs` for the whole file, the
    sample count, `cellCount`, `barCount`, `hardwareConcurrency` and `calibrationScore`. Keep the
    single `recordInfoLine` key.
  - Keep the elision sink and the proof-before-timing gate. Both must still be live.

(c) Measure the cost this arm adds to a DEFAULT run, the way 260824-52h measured its own: run
`npm run bench` with `bench/kernel-ablation.bench.test.ts` temporarily renamed out of the include
glob, then again with it restored (gate unset, so every arm is skipped), same host session, back to
back. Record both `totalRuntimeMs` figures and the delta. Expected delta is collection cost only,
well inside the 2103ms of headroom 260824-52h measured. If the measured delta ever pushes
`totalRuntimeMs` over the unchanged 30000ms cap, do NOT raise the cap: move the file out of the
`bench/**/*.bench.test.ts` include glob using Task 1's contingency and re-measure. Also record the
flagged run's own wall clock.
  </action>
  <verify>
    <automated>npm run typecheck && git diff --exit-code -- src/kernel/backtest.ts src/sweep/sweep-pool.ts src/sweep/sweep.worker.ts bench/sweep.bench.test.ts perf-budgets.ts bench/calibration.ts && BENCH_RESULTS_DIR=.bench/ablation VITE_PERF03_ABLATION=1 npx vitest run --project bench bench/kernel-ablation.bench.test.ts; node -e "const r=require('./.bench/ablation/bench-results.json');const l=r.infoLines.join('\n');for(const a of ['noGuards','dayCountLut','dayCountReciprocal','drawdownSkip','peelBarZero','dedupDrawdown','scalarOnly','combined'])if(!l.includes(a))throw new Error('missing arm in info line: '+a);console.log(l)"</automated>
    <automated>npm run bench 2>&1 | tail -40 | grep -E 'Total bench runtime' && node -e "const r=require('./.bench/bench-results.json');if(r.totalRuntimeMs>30000)throw new Error('default run exceeded the unchanged cap: '+r.totalRuntimeMs);console.log('default totalRuntimeMs='+r.totalRuntimeMs)"</automated>
  </verify>
  <done>All nine arms are measured at the 17-column span across three rotated rounds with per-arm min, median and max ratios recorded; every bit-preserving classification is proven, not asserted; arm 3's deviation is measured and recorded; the combined arm is measured as its own variant; the default suite's added cost is measured back to back and stays under the unchanged 30000ms cap; all six protected files are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: FINDINGS with the verdict against both thresholds</name>
  <files>.planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md</files>
  <action>
Write FINDINGS from the recorded info line, in exactly this order. Every number comes from
`.bench/ablation/bench-results.json`, never from memory or from this plan's expectations.

1. **Per-candidate table.** One row per arm: arm name, candidate number, measured ratio
   (min / median / max across the three rounds), observed spread in percentage points,
   bit-preserving yes or no, and for arm 3 the max absolute and max relative deviation per numeric
   `KernelResult` field. State the sample count (3 rounds, one call per arm per round, one
   discarded warm-up round) and state plainly that three samples is a small sample, naming
   Phase 7.1's own four-run "roughly 4%" spread that became 26% on a fifth sample as the reason the
   min and max are reported rather than the median alone.

2. **The measured combined bit-preserving cut.** The combined arm's ratio, stated as measured, with
   an explicit sentence that it was measured as its own variant and not summed or multiplied from
   the individual ratios because the candidates interact. If the combined ratio is worse than the
   best individual ratio, say so.

3. **The verdict against BOTH thresholds, in plain words.** Apply this plan's `<reference_arithmetic>`
   decision rule to the measured `rCombined`: whether the achievable cut clears 21.3%, clears only
   10.8%, or clears neither. Restate the two thresholds and where they come from (run 32669644628
   at `calibrationScore=1.0525` and run 32686531154 at `calibrationScore=0.9100`), and why 21.3% is
   the robust target given the merge bar requires two consecutive passes across that calibration
   range.

4. **The projection onto the D-17 headline, marked as a projection.** Show the arithmetic
   (`projected raw = raw * (0.983 * rCombined + 0.017)`) against both baseline raw figures, and
   state that 0.983 is 260824-52h's sandbox kernel share, not a D-17 measurement. Note that every
   derived-not-measured projection tested in Phase 7.1 was refuted by later measurement.

5. **The closing statement.** If the combined cut clears neither threshold, state unambiguously and
   in plain words that the 1000ms budget is unreachable by every lever this project has now tested,
   listing them: pool and dispatch overhead (measured negligible, 260824-52h), worker count
   (measured no improvement, 07.1-06), the per-bar output arrays (measured 1 to 2%, 260824-46s),
   and per-cell kernel compute at the real chunk shape (measured, this task). If it clears a
   threshold, say which, and state explicitly that adoption is a separate decision this task does
   not make, and that adopting any non-bit-preserving candidate requires its own Key Decision
   because it would change displayed figures.

6. **Measurement caveats, as their own section.** The host block
   (`hardwareConcurrency`, `deviceMemory`, `calibrationScore`, os, ci) and the statement that this
   is a sandbox figure, not the D-17 4-core baseline, which is why the deliverable is a
   dimensionless ratio. The measured fidelity pair, and what it shows about the injected-callee
   polymorphism tax: if the clone with the shipped kernel injected is materially slower than the
   real `computeChunkMetrics`, say that the ablation arms may understate gains that depend on the
   kernel being inlined, and say it as a measured caveat rather than a hedge.

7. **The cost this arm adds.** The two back-to-back default-run `totalRuntimeMs` figures with the
   file absent and present, the delta, the flagged run's own wall clock, and the statement that the
   arm is gated off by default, that `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000 and was not raised,
   and that the flagged invocation exits non-zero only because a single-file bench run leaves the
   due PERF-08 rows unmeasured, which is a structural coverage gate rather than a budget verdict.

8. **What did not move.** `NOMINAL_REFERENCE_MS` stays 40. `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000.
   No budget or threshold in `perf-budgets.ts` changed. No grid dimension changed. D-03's coarser
   default grid was not spent. `src/kernel/backtest.ts`, `src/sweep/sweep-pool.ts`,
   `src/sweep/sweep.worker.ts`, `bench/sweep.bench.test.ts`, `perf-budgets.ts` and
   `bench/calibration.ts` are unmodified, with the `git diff --exit-code` evidence named. No kernel
   change shipped. PR #7 and PR #8 were not merged, marked ready, commented on or pushed to.

A refuted result is a successful outcome. Do not soften a refutation and do not manufacture a
hopeful branch. No em dash characters anywhere in the document.
  </action>
  <verify>
    <automated>test -s .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md && ! grep -qP '\x{2014}' .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md && grep -q "21.3" .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md && grep -q "10.8" .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md && grep -qi "projection" .planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-FINDINGS.md && git diff --exit-code -- src/kernel/backtest.ts src/sweep/sweep-pool.ts src/sweep/sweep.worker.ts bench/sweep.bench.test.ts perf-budgets.ts bench/calibration.ts</automated>
  </verify>
  <done>FINDINGS.md exists with the per-candidate table, the measured combined cut, an explicit verdict against both the 10.8% and 21.3% thresholds in plain words, the D-17 projection labelled as a projection, the measurement caveats, the disclosed cost, and the what-did-not-move section. No em dashes. All six protected files byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench tree to src tree | A measurement variant must never become reachable from production code |
| ablation run to the gated bench artifact | A diagnostic arm must never perturb or overwrite the gated PERF-03 measurement |
| measured figure to reported conclusion | A faster but wrong kernel must never be reportable as an improvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r5d-01 | Tampering | `src/kernel/backtest.ts` and the five other protected files | critical | mitigate | `git diff --exit-code` against all six in every task's verify; no task lists any of them in `<files>` |
| T-r5d-02 | Elevation of privilege | ablation variants leaking into production | high | mitigate | `grep -rl 'backtest-ablation-variants\|chunk-metrics-kernel-ablation' src/` must return nothing, asserted in every task's verify |
| T-r5d-03 | Spoofing | a faster wrong kernel reported as an improvement | critical | mitigate | Proof-before-timing runtime gate: the timing test throws unless the proven-case counter equals the arm-table-derived expected value; bit-identity via `Object.is`, not tolerance, for every candidate claimed bit-preserving |
| T-r5d-04 | Information disclosure | a non-bit-preserving candidate adopted without the conclusion change being surfaced | high | mitigate | Arm 3 classified NOT bit-preserving by measurement, its per-field deviation recorded, and FINDINGS required to state that adopting it would change displayed figures and needs its own Key Decision |
| T-r5d-05 | Denial of service | the arm pushing the suite past `BENCH_TOTAL_RUNTIME_CAP_MS` | medium | mitigate | Gated off by default, added cost measured back to back, cap never raised, contingency is to move the file out of the include glob |
| T-r5d-06 | Tampering | the flagged run clobbering `.bench/bench-results.json` | medium | mitigate | `BENCH_RESULTS_DIR=.bench/ablation` on every flagged invocation, an existing seam that rejects absolute and parent-directory paths |
| T-r5d-07 | Repudiation | a span-17 claim that is not actually span 17 | high | mitigate | Harness fidelity proven bit-identical against the real `computeChunkMetrics` at a real 17-column request, with the polymorphism tax separately measured |
| T-r5d-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; every import is an existing repo module or an already-installed dependency |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `git diff --exit-code` exits 0 against `src/kernel/backtest.ts`, `src/sweep/sweep-pool.ts`,
  `src/sweep/sweep.worker.ts`, `bench/sweep.bench.test.ts`, `perf-budgets.ts` and
  `bench/calibration.ts`.
- `grep -rl 'backtest-ablation-variants\|chunk-metrics-kernel-ablation' src/` returns nothing.
- A default `npm run bench` completes with `totalRuntimeMs` under the unchanged 30000ms cap and the
  gated PERF-03 row unperturbed.
- The flagged invocation records a single `PERF-03-kernel-ablation` info line naming all nine arms.
- FINDINGS contains an explicit verdict against both 10.8% and 21.3%.
</verification>

<success_criteria>
The question "is per-cell kernel compute reducible at the 17-column chunk shape, and by enough" is
answered by measurement, with every bit-preserving claim proven bit-identical before any clock
started, and the answer stated against both thresholds in plain words. No kernel change ships. A
refuted result satisfies this plan in full.
</success_criteria>

<output>
Create `.planning/quick/260824-r5d-measure-whether-per-cell-kernel-compute-/260824-r5d-SUMMARY.md`
when done, naming which gating mechanism was used (the `VITE_PERF03_ABLATION` flag or the
contingency config) and why.
</output>

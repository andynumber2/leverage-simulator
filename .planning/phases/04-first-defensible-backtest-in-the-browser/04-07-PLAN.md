---
phase: 04-first-defensible-backtest-in-the-browser
plan: 07
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - src/app/permalink.ts
  - src/app/state.ts
  - src/app/App.tsx
  - src/app/components/ParameterColumn/CopyLinkButton.tsx
  - src/app/components/ParameterColumn/ParameterColumn.tsx
  - src/app/components/ParameterColumn/HoldingModeControl.tsx
  - src/app/components/ResultColumn/BundleVersionBanner.tsx
  - src/app/components/ResultColumn/ValidationExplanation.tsx
  - tests/app/permalink.test.ts
  - tests/app/permalink.browser.test.ts
autonomous: false
requirements: [SHARE-01, SHARE-02, SHARE-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Copying the URL and opening it in a fresh browser reproduces the exact run, enforced by a determinism test (SHARE-01, SHARE-03, ROADMAP criterion 4)."
    - "Every parameter is emitted in the URL, always, as flat readable query params, with no default elided and nothing omitted, so changing a default in a later release cannot retroactively change what an existing link means (D-13)."
    - "Hold-to-today encodes both the mode and the end date resolved at creation, so replay is frozen and faithful by default while a one-click 'Run to today' honours the open-ended intent (D-14)."
    - "The link carries the data-bundle version; a mismatch computes against the deployed bundle and banners the change naming both versions (SHARE-02, D-15)."
    - "`decode(encode(params))` returns identical params across the generated parameter space, asserted by a fast-check property, and committed golden URLs assert their metric outputs to a stated tolerance, which catches computation drift a round-trip cannot see (SHARE-03, D-16)."
    - "A URL carrying the same query key twice is rejected by the decoder with a named error identifying the duplicated key, rather than silently resolving to one of the two values (SHARE-01 adjacency edge)."
    - "An empty query string loads the default landing run, and a query string missing any required key renders a named decode error identifying the missing key rather than filling it from a default (SHARE-01 empty edge)."
    - "The encoder emits query keys in one fixed declaration order, so the same parameter set always produces a byte-identical query string and two links to the same run compare equal as strings (SHARE-01 ordering edge)."
    - "The URL is updated through `history.replaceState`, never `pushState`, so scrubbing the leverage slider does not spam the browser's back-button history with thousands of entries."
    - "Copy link is disabled until a result exists, so there is no window where it copies a link to nothing (UI-SPEC E10 loading)."
    - "A clipboard write rejection is reported in place: the button label swaps to a failure state and the permalink is shown as selectable text as the fallback; a silent no-op is not acceptable for the phase's core action (UI-SPEC E10 error)."
    - "The button label is fixed width and the permalink is never rendered inline except in the clipboard-failure fallback, where it is a scrollable single-line monospace field (UI-SPEC E10 overflow)."
    - "The Copy link label and its transient confirmation and failure states are authored to the same character budget so the button does not resize on state change (UI-SPEC E10 long-text)."
  artifacts:
    - path: "src/app/permalink.ts"
      provides: "The one canonical serialization format per field, used by both directions (D-13 through D-16)"
      exports: ["PermalinkParams", "PERMALINK_KEYS", "encodeParams", "decodeParams"]
    - path: "src/app/components/ParameterColumn/CopyLinkButton.tsx"
      provides: "The phase's one explicit user action (SHARE-01)"
    - path: "src/app/components/ResultColumn/BundleVersionBanner.tsx"
      provides: "The D-15 mismatch notice naming both versions"
    - path: "tests/app/permalink.test.ts"
      provides: "The fast-check round-trip property plus committed golden-run fixtures (D-16)"
    - path: "tests/app/permalink.browser.test.ts"
      provides: "Fresh-load reproduction, replaceState behaviour, and the clipboard failure fallback"
  key_links:
    - from: "src/app/state.ts"
      to: "src/app/permalink.ts"
      via: "seeds initial state from decodeParams on boot and re-serializes through encodeParams into history.replaceState on every change"
      pattern: "replaceState"
    - from: "src/app/components/ResultColumn/BundleVersionBanner.tsx"
      to: "src/data-bundle.generated.ts"
      via: "compares the URL's bundleVersion against the imported BUNDLE_VERSION"
      pattern: "BUNDLE_VERSION"
    - from: "tests/app/permalink.test.ts"
      to: "src/data/load-bundle-node.ts"
      via: "golden fixtures decode a full URL and run it end to end through buildKernelInputs and runBacktest"
      pattern: "loadBundleFromDisk"
  prohibitions:
    - requirement_id: SHARE-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT let an already-shared permalink be silently reinterpreted under a later release's defaults or a later bundle's data. A link whose meaning changes when a default changes turns the tool's core function, two people comparing links, into a source of false agreement, which is the exact failure PITFALLS G4 names. Every parameter is emitted so no default can be inherited later, and a bundle-version mismatch is announced on screen rather than absorbed."
    - requirement_id: SHARE-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT route the permalink through any external service: no URL shortener, no share-tracking redirect, no server-side link resolver. The link is the user's argument, and APP-03 forbids a runtime external call regardless."
---

<objective>
The link. Every parameter encoded as flat readable query params, decoded back into the exact same
run, with the bundle version carried and a mismatch announced rather than absorbed.

Purpose: this is the half of the phase goal the tracer deliberately deferred. "Get an outcome they
can hand to someone else as a link" is not satisfied by a chart; it is satisfied by a URL that
reproduces the chart in a fresh browser, and by a determinism test that keeps proving it.

Output: the codec, the copy-link action, the mismatch banner, the "Run to today" refresh, and two
test files, one property-and-golden in Node and one reproduction-and-clipboard in the browser.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-CONTEXT.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-PATTERNS.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-05-SUMMARY.md
</context>

<interfaces>
From `src/data/kernel-inputs.ts`, `BacktestRequest`'s ten fields, which the permalink is a near
direct projection of: `symbol`, `dividendReinvest`, `leverage`, `entryDate`, `holdingPeriodBars`,
`initialInvestment`, `contributionAmount`, `contributionFrequency`, `expenseRatioPercent`,
`financingSpreadPercent`.

From `src/data-bundle.generated.ts`: `BUNDLE_VERSION` is `'45a9f1ae6444'` in the committed bundle,
and `MANIFEST_PATH` points at exactly one content-hashed manifest, which is why D-15's "states
clearly that the data has changed" is the only reachable branch of SHARE-02 today. Retaining older
bundles is a recorded Deferred Idea, not this plan's work.

From plan 04-01's `src/app/state.ts`: the `BacktestRequest`-shaped signal, the `scale` signal with
values `log` and `linear`, and the load-status signal.

From plan 04-04's `src/app/bounds.ts`: `resolveEntryDateBounds` takes a `tier` typed as the union of
`strict` and `extended`, so a `tier` query param has a type to decode into even though Phase 4 only
emits `strict`.

From plan 04-05's `ValidationExplanation`: the bundle-mismatch variant slot and the stacking order
already exist; this plan fills the slot rather than reordering the surface.

From `tools/bundle-compiler/tests/roundtrip.test.ts` lines 182 to 186: the established fast-check
round-trip shape, `fc.assert(fc.property(arbitrary, (value) => { ... }))`, with an `fc.oneof` over a
`fc.double({ noNaN: true, noDefaultInfinity: true })` and an explicit extreme-values constant set.
Copy that structure with `BacktestRequest`-shaped arbitraries instead of raw float arrays.
</interfaces>

<!-- planner-discipline-allow: pushState -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the published permalink parameter contract before it ships</name>
  <read_first>
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-CONTEXT.md`, decisions D-13,
      D-14, D-15 and the "Claude's Discretion" bullet assigning exact query-param names and value
      formats
    - `src/data/kernel-inputs.ts` lines 34 to 46 (`BacktestRequest`'s ten fields)
  </read_first>
  <decision>
    The exact query-param key set, their order, and each field's canonical value format.
  </decision>
  <context>
    D-13 locks the policy (every parameter, always, flat and readable, no default elided) and rates it
    `one-way`: once links are in circulation the key set and the key names are a published contract,
    and renaming or dropping a key breaks every link already shared. The policy is settled and is not
    being re-asked. What is not settled is the concrete schema, which CONTEXT.md assigns to Claude's
    Discretion, and that schema is the thing that becomes irreversible the moment a link leaves the
    machine. It is cheap to confirm now and expensive to change later, which is the whole reason this
    gate exists.

    Proposed key set, in the fixed emission order, fifteen keys, all always present:

    | # | key | format | source |
    |---|-----|--------|--------|
    | 1 | `symbol` | bare scope string, e.g. `SPX` | `BacktestRequest.symbol` |
    | 2 | `dividendReinvest` | `true` or `false` | `BacktestRequest.dividendReinvest` |
    | 3 | `leverage` | fixed two decimals, e.g. `3.00` | `BacktestRequest.leverage` |
    | 4 | `entryDate` | ISO `YYYY-MM-DD` | `BacktestRequest.entryDate` |
    | 5 | `holdMode` | `fixed` or `today` | D-14 |
    | 6 | `holdingPeriodBars` | integer, always present; in `today` mode it carries the bar count that hold-to-today resolved to at creation | D-13 plus D-14 |
    | 7 | `resolvedEndDate` | ISO `YYYY-MM-DD`, always present | D-14 |
    | 8 | `initialInvestment` | fixed two decimals | `BacktestRequest.initialInvestment` |
    | 9 | `contributionAmount` | fixed two decimals | `BacktestRequest.contributionAmount` |
    | 10 | `contributionFrequency` | one of `none`, `daily`, `monthly`, `quarterly`, `yearly` | `ContributionFrequency` |
    | 11 | `expenseRatioPercent` | fixed four decimals | `BacktestRequest.expenseRatioPercent` |
    | 12 | `financingSpreadPercent` | fixed four decimals | `BacktestRequest.financingSpreadPercent` |
    | 13 | `tier` | `strict` (Phase 4 emits only this; Phase 5 adds `extended`) | D-09 |
    | 14 | `scale` | `log` or `linear` | D-19 |
    | 15 | `bundleVersion` | the twelve-character hex string | D-15 |

    Two judgment calls inside the discretion area are worth your explicit yes or no:

    First, `holdingPeriodBars` is emitted even in `today` mode, carrying the resolved bar count.
    RESEARCH.md's sketch omitted it in that mode. Emitting it keeps D-13's "nothing omitted" literally
    true and makes the decoder's required-key check uniform, at the cost of one redundant-looking key.

    Second, `scale` is included. It is a display choice rather than a run parameter, but a recipient
    who opens a link on a linear axis when the sender was reading a log axis is not seeing the same
    picture, and PITFALLS E6 is specifically about how much the axis choice changes the reading.
    Including it now is also the cheap moment: adding a key later is a contract change.

    Estimated length at the default landing run: roughly 210 characters, well inside every browser's
    practical URL limit, so no compression or custom encoding is needed and `URLSearchParams` is
    sufficient.
  </context>
  <options>
    <option id="as-proposed">
      <name>Ship the fifteen-key schema exactly as proposed</name>
      <pros>Nothing omitted, uniform decoder, the axis choice travels with the link, no later contract change</pros>
      <cons>One key (`holdingPeriodBars` in `today` mode) looks redundant to a human reading the URL</cons>
    </option>
    <option id="drop-scale">
      <name>Ship fourteen keys, dropping `scale`</name>
      <pros>Strictly run parameters only, shortest URL</pros>
      <cons>A recipient can see the same run on a different axis; adding `scale` later is a published-contract change</cons>
    </option>
    <option id="omit-bars-in-today">
      <name>Ship fifteen keys but omit `holdingPeriodBars` when `holdMode` is `today`</name>
      <pros>Reads more naturally to a human</pros>
      <cons>Breaks D-13's "nothing omitted" literally, and makes the decoder's required-key set conditional on another key's value</cons>
    </option>
    <option id="revise">
      <name>Revise the schema (name the changes)</name>
      <pros>Any key naming or formatting concern is cheapest to fix right now</pros>
      <cons>Requires stating the replacement</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A human has selected exactly one option by id, or stated a revision.
    - The selected schema is recorded verbatim in the plan summary, so the published contract has a
      written origin rather than living only in the code.
    - Task 2 does not begin before this resolves.
  </acceptance_criteria>
  <resume-signal>Select: as-proposed, drop-scale, omit-bars-in-today, or revise (and name the changes).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The permalink codec, its round-trip property, and the committed golden runs</name>
  <reversibility rating="one-way">The key set and key names become a published contract the moment a
  link is shared; renaming or dropping one breaks every link already in circulation.</reversibility>
  <read_first>
    - `tools/bundle-compiler/src/binary-format.ts`, the `encodeHeader` and `decodeHeader` pair (the
      repo's existing symmetric-codec convention: one file owns both directions and one canonical
      format)
    - `tools/bundle-compiler/tests/roundtrip.test.ts` in full, especially the fast-check block at
      lines 182 to 186 and the earlier byte-exact golden assertions
    - `src/data/kernel-inputs.ts` lines 34 to 46 and the D-32 throw sites
    - `src/data/contribution-schedule.ts` (`ContributionFrequency`)
    - `src/data-bundle.generated.ts`
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-RESEARCH.md` Pitfall 5 and the
      permalink codec Code Example
    - Task 1's resolved schema
  </read_first>
  <behavior>
    - `decodeParams(encodeParams(p))` deep-equals `p` for every generated parameter set, including
      fractional leverage values that would round-trip differently through a bare `toString`.
    - `encodeParams` called twice on the same parameter set produces byte-identical query strings.
    - The emitted key order matches the declared order exactly, asserted against the declared key
      array rather than a literal string.
    - An empty `URLSearchParams` decodes to a named "no parameters" result the caller reads as
      "use the default landing run", not to a partially defaulted parameter set.
    - A query string missing one required key decodes to a named error identifying that key.
    - A query string carrying one key twice decodes to a named error identifying the duplicated key.
    - A query string carrying an unknown extra key decodes to a named error identifying it, rather
      than ignoring it, because an ignored key is how a future contract change silently loses meaning.
    - A leverage value of `0`, a negative leverage, a non-numeric leverage and an out-of-band date
      each decode to a named error rather than throwing.
    - `decodeParams` never throws for any input, including an adversarial query string built from
      prototype-polluting key names; it is a total function over arbitrary `URLSearchParams`.
    - Each committed golden URL, run end to end, reproduces its recorded IRR, CAGR, maximum drawdown
      and final value within the stated tolerance.
  </behavior>
  <action>
    Create `src/app/permalink.ts` as the single owner of the wire format, opening with the file-level
    header comment convention every module under `src/` uses and naming D-13 through D-16.

    Export `PERMALINK_KEYS` as a readonly array of the key names in the fixed emission order Task 1
    resolved. Both directions read the order from that one array, and the round-trip test asserts
    against it, so an added or renamed key cannot drift between encode and decode. Export
    `PermalinkParams` as the decoded shape: `BacktestRequest`'s ten fields plus `holdMode`,
    `resolvedEndDate`, `tier`, `scale` and `bundleVersion`.

    Export `encodeParams(params): URLSearchParams`. One canonical format per field type, fixed in this
    one place and used by both directions, never inferred ad hoc at a call site: leverage, the two
    money fields and the two cost fields render with a fixed decimal count rather than through
    `toString`, dates render as ISO `YYYY-MM-DD`, booleans render as the literals `true` and `false`,
    and unions render as their bare member string. A float that would round-trip differently through
    `toString`, such as a leverage arriving as 2.9999999999999996 from a slider, is exactly what the
    fixed decimal count exists to normalize.

    Export `decodeParams(qs): PermalinkParams | { error: string }` as a total function over arbitrary
    input. It never throws past the caller: a malformed or hand-edited URL routes to the
    clear-and-explain path plan 04-05 built, not to an unhandled rejection that blanks the page.
    Validate against `PERMALINK_KEYS` as an allow-list: reject an unknown key by name, reject a
    duplicated key by name (read through `URLSearchParams.getAll` and check the length, since `get`
    silently returns the first), and reject a missing key by name. Never assign a URL-derived string
    as a property name onto an object; build the result by reading each declared key explicitly.

    Create `tests/app/permalink.test.ts` in the `unit` project. First block: the fast-check round-trip
    property, following `tools/bundle-compiler/tests/roundtrip.test.ts`'s structure, with arbitraries
    generating in-range values for every field plus an explicit constant set of awkward values (a
    leverage with fifteen significant decimals, an initial investment at the float64 extreme, a
    zero contribution, the earliest and latest valid entry dates). Second block: the negative cases
    from `<behavior>`, each asserting the error names the offending key. Third block: the committed
    golden runs.

    Commit at least four golden fixtures as full URL strings with their expected metric outputs and a
    stated absolute tolerance. Cover, at minimum: the default landing run; a monthly-contribution run
    so IRR and CAGR genuinely differ; a high-leverage early-entry run that ruins, so the -100% IRR
    and the drawdown of 1 are pinned; and a fixed-holding-period run so the `holdMode` `fixed` branch
    is exercised. Each golden test decodes the URL, loads the committed bundle through
    `loadBundleFromDisk`, builds the inputs, runs the kernel, derives the metrics, and asserts each
    against the recorded value within the stated tolerance. State the tolerance as a named constant
    with a one-line reason, not as a bare number scattered through the assertions. This is the half
    of D-16 that catches computation drift a round-trip cannot see.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project unit tests/app/permalink.test.ts` exits 0 with every `<behavior>`
      case represented by a named test.
    - `PERMALINK_KEYS.length` equals the key count Task 1 resolved, and both `encodeParams` and
      `decodeParams` iterate or reference that array rather than repeating the key list.
    - `encodeParams` output is byte-identical across two calls on the same input, asserted by string
      equality.
    - `decodeParams` returns an error result rather than throwing for every negative case, asserted by
      wrapping each call in a try-catch that fails the test if it catches.
    - `grep -c 'getAll' src/app/permalink.ts` is at least 1, proving duplicate keys are detected
      rather than silently collapsed by `get`.
    - `grep -cE '\\[[a-zA-Z]+\\] *=' src/app/permalink.ts` shows no dynamic property assignment from a
      URL-derived key.
    - At least four golden URL fixtures are committed, each asserting IRR, CAGR, maximum drawdown and
      final value within the named tolerance constant.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project unit tests/app/permalink.test.ts</automated>
  </verify>
  <done>One file owns the wire format in both directions, a property proves the round trip over the generated parameter space, four committed URLs prove the numbers behind them, and no malformed input can throw.</done>
  <files>src/app/permalink.ts, tests/app/permalink.test.ts</files>
</task>

<task type="auto">
  <name>Task 3: URL sync, the copy-link action, the mismatch banner, and fresh-browser reproduction</name>
  <read_first>
    - `src/app/permalink.ts` from Task 2
    - `src/app/state.ts` after plan 04-05 (the store, the rAF effect, the load status)
    - `src/app/components/ResultColumn/ValidationExplanation.tsx` after plan 04-05 (the mismatch
      variant slot and the stacking order)
    - `src/app/components/ParameterColumn/HoldingModeControl.tsx` after plan 04-05 (where the
      "Run to today" secondary action belongs)
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md`, the Copywriting
      Contract rows for "Primary CTA", "Secondary action" and "Bundle version mismatch", plus UI
      Considerations row E10 in full
    - `src/data-bundle.generated.ts`
  </read_first>
  <action>
    Wire the codec into `src/app/state.ts`. On boot, read `window.location.search`, call
    `decodeParams`, and seed the store from the result. An empty query string seeds the default
    landing run. A decode error seeds the default landing run and raises the explanation, naming the
    offending key, through the surface plan 04-05 built. On every parameter change, re-serialize
    through `encodeParams` and write the URL with `history.replaceState`, never `pushState`: the rAF
    recompute fires on every slider-drag frame, and `pushState` there would put thousands of entries
    behind the back button in a single scrub.

    Create `src/app/components/ParameterColumn/CopyLinkButton.tsx`, the phase's one explicit action,
    labelled "Copy link" and rendered in `--color-accent`. It is disabled until a completed result
    exists, so there is no window where it copies a link to nothing. On success the label swaps to a
    transient confirmation. On a clipboard write rejection (permission denied, insecure context) the
    label swaps to a failure state and the permalink is rendered beside it as a scrollable
    single-line monospace field the user can select manually. A silent no-op is not acceptable for
    the action the whole phase exists to deliver. All three labels are authored to the same character
    budget so the button does not resize as it changes state, and the permalink is never rendered
    inline in any other state.

    Create `src/app/components/ResultColumn/BundleVersionBanner.tsx` implementing D-15. Compare the
    decoded `bundleVersion` against the imported `BUNDLE_VERSION`. On a mismatch, compute the run
    against the deployed bundle anyway and render the Copywriting Contract's copy naming both
    versions and stating that the underlying data has changed since the link was made. Do not block
    the render behind a click-through. The check runs after the manifest decodes, so the banner can
    only appear once loading has finished and never flashes during load. Fill the mismatch slot plan
    04-05 reserved in the explanation stack; do not reorder the stack.

    Add the "Run to today" secondary action to `HoldingModeControl`, visible only in hold-to-today
    mode. It re-resolves the end date to the current last supported bar and re-emits the URL, which is
    what makes D-14's frozen replay refreshable rather than merely frozen.

    Create `tests/app/permalink.browser.test.ts` in the `app` project asserting: loading a committed
    golden URL in a fresh page reproduces the same rendered IRR, CAGR, drawdown and final-value
    strings as the same run reached by driving the controls; the URL after a slider scrub carries the
    scrubbed leverage and `window.history.length` did not increase across the scrub; a URL carrying a
    `bundleVersion` that does not match `BUNDLE_VERSION` renders the mismatch banner naming both
    versions while the chart and metrics still render; a URL missing a required key renders the named
    decode explanation and no chart; the Copy link button is disabled while the load status is
    `loading`; and, with clipboard permission denied, the button shows its failure label and the
    permalink appears as selectable text.
  </action>
  <acceptance_criteria>
    - `npm run test:app` exits 0 and `tests/app/permalink.browser.test.ts` covers every case named in
      the action's final paragraph.
    - `grep -c 'pushState' src/app` is 0 and `grep -rc 'replaceState' src/app/state.ts` is at least 1.
    - After a scrub of at least 30 slider input events, `window.history.length` is unchanged from
      before the scrub, asserted numerically.
    - A golden URL loaded in a fresh page produces rendered metric strings identical to those produced
      by driving the controls to the same parameters, compared as strings.
    - The mismatch banner's rendered text contains both the URL's `bundleVersion` value and the
      imported `BUNDLE_VERSION`, asserted against the imported constant rather than a literal.
    - The Copy link button's rendered width is unchanged across its default, confirmation and failure
      states, asserted from `getBoundingClientRect`.
    - `grep -rc 'innerHTML' src/app` is 0.
    - `npm run typecheck` and `npm run build` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run test:app</automated>
  </verify>
  <done>Copying the URL and opening it in a fresh browser reproduces the exact run; a stale link says so on screen instead of pretending; and scrubbing a slider does not fill the back button.</done>
  <files>src/app/state.ts, src/app/App.tsx, src/app/components/ParameterColumn/CopyLinkButton.tsx, src/app/components/ParameterColumn/ParameterColumn.tsx, src/app/components/ParameterColumn/HoldingModeControl.tsx, src/app/components/ResultColumn/BundleVersionBanner.tsx, src/app/components/ResultColumn/ValidationExplanation.tsx, tests/app/permalink.browser.test.ts</files>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string to app state | The single largest untrusted-input surface in the phase; anyone can craft it |
| App state to clipboard | The user's parameters leave the app on an explicit action |
| Decoded bundleVersion to displayed claim | A version string from a URL becomes on-screen text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `src/app/permalink.ts` `decodeParams` | high | mitigate | Every field parses through an explicit allow-list read from `PERMALINK_KEYS`; unknown, duplicated and missing keys are each rejected by name; no dynamic property assignment from a URL-derived key, asserted by grep; no `eval` and no `Function` |
| T-04-02 | Information Disclosure | `ValidationExplanation` and `BundleVersionBanner` rendering URL-derived strings | high | mitigate | Rendered through Solid JSX text interpolation, which escapes by default; no `innerHTML` anywhere in `src/app`, asserted by grep; no template-literal HTML construction from any `URLSearchParams` value |
| T-04-03 | Denial of Service (client) | A malformed or adversarial permalink | high | mitigate | `decodeParams` is a total function proven by a try-catch-wrapped negative-case battery including prototype-polluting key names; a decode failure routes to the explanation path rather than blanking the page |
| T-04-30 | Spoofing | A link claiming a bundle version the deployed bundle does not carry | medium | mitigate | The banner names both versions and states the data has changed; the run is computed against the deployed bundle, which `decodeHeader` already pins to `BUNDLE_VERSION` |
| T-04-31 | Information Disclosure | Clipboard write of the permalink | low | accept | The clipboard write is an explicit user action on data the user typed; no third party is involved, and APP-03 forbids a runtime external call regardless |

Canon-referral breadcrumbs, recorded rather than minted as bespoke prohibitions: reflected XSS via
query params and prototype pollution via URL-derived key names are both canon, covered by
`/gsd-secure-phase` and by eslint security rules, and are carried above as T-04-02 and T-04-01
mitigations rather than as separate `must_haves.prohibitions` entries.
</threat_model>

<artifacts_this_phase_produces>
**New files:** `src/app/permalink.ts`,
`src/app/components/ParameterColumn/CopyLinkButton.tsx`,
`src/app/components/ResultColumn/BundleVersionBanner.tsx`, `tests/app/permalink.test.ts`,
`tests/app/permalink.browser.test.ts`.

**New exported symbols:** `PermalinkParams`, `PERMALINK_KEYS`, `encodeParams`, `decodeParams`,
`CopyLinkButton`, `BundleVersionBanner`.

**New published contract:** the fifteen permalink query-param keys and their canonical value
formats, confirmed at Task 1.

**Extended symbol:** `HoldingModeControl` gains the "Run to today" secondary action.
</artifacts_this_phase_produces>

<flagged_assumptions>
Probe rows that stayed `unresolved` (category `unclassified`), surfaced rather than dropped. Two
land in this plan.

- **SHARE-02 (unclassified, unresolved).** No shape category was proposed for "The URL carries a
  data-bundle version, and a permalink created against an older bundle either reproduces faithfully
  or states clearly that the data has changed". Planner assumption: only the second branch is
  reachable today, because `MANIFEST_PATH` addresses exactly one manifest and no older bundle is
  served. D-15 records this as a deliberate scope call, and retaining the last N bundles is a
  recorded Deferred Idea. If faithful reproduction is expected in this phase, the deferral is what
  changes, not the banner.
- **SHARE-03 (unclassified, unresolved).** No shape category was proposed for "Permalink
  reproducibility is enforced by a determinism test". Planner assumption: D-16's two halves together
  are the test, a round-trip property plus tolerance-based golden runs, and the tolerance is an
  absolute one stated as a named constant rather than exact equality, because float64 metric
  derivation is not bit-reproducible across engines. If exact equality is expected instead, the
  golden assertions tighten and the constant is removed.
</flagged_assumptions>

<verification>
- `npm run typecheck && npm test && npm run test:app && npm run build` all exit 0.
- `grep -rn 'pushState' src/app` returns nothing.
- Every golden fixture URL in `tests/app/permalink.test.ts` decodes, runs and matches its recorded
  metrics within the named tolerance.
</verification>

<success_criteria>
- Every parameter is encoded in the URL as readable query params, so a pasted link reproduces the
  exact run (SHARE-01).
- The URL carries a data-bundle version and a mismatch states clearly that the data has changed
  (SHARE-02).
- Permalink reproducibility is enforced by a determinism test (SHARE-03).
</success_criteria>

<output>
Create `.planning/phases/04-first-defensible-backtest-in-the-browser/04-07-SUMMARY.md` when done.
</output>

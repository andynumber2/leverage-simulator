---
phase: 04-first-defensible-backtest-in-the-browser
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/kernel/backtest.types.ts
  - src/kernel/backtest.ts
  - tests/kernel/drawdown.test.ts
  - src/metrics/irr.ts
  - src/metrics/cagr.ts
  - src/metrics/format.ts
  - tests/metrics/irr.test.ts
  - tests/metrics/format.test.ts
  - src/app/components/ResultColumn/MetricsPanel.tsx
  - src/app/components/ResultColumn/RuinBanner.tsx
  - src/app/components/ResultColumn/LogScaleToggle.tsx
  - src/app/components/ResultColumn/EquityCurveChart.tsx
  - src/app/App.tsx
  - src/app/state.ts
  - tests/app/metrics.browser.test.ts
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04, METR-05, VIZ-08]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The result panel's headline slot is annualized return (IRR) in every state, and never changes identity when a contribution schedule is turned on or off (D-05, METR-01)."
    - "CAGR occupies a secondary slot in every state and carries the qualifier 'CAGR is misleading with contributions - see IRR above' whenever contributions are non-zero (D-05, METR-02)."
    - "With zero contributions the headline carries the muted inline note 'IRR equals CAGR for a single cash flow' (D-05)."
    - "Final value as a multiple uses `finalValue / totalContributed`, money actually invested; `droppedContributionsTotal` is its own line naming the ruin date and is never folded into the ratio (D-06, METR-04)."
    - "A ruined run changes the state of the whole result panel: a categorical banner naming the ruin date sits above metrics that are retained but subordinate to it, never a badge beside otherwise-normal numbers (D-07, METR-05)."
    - "Maximum drawdown is computed inside the kernel loop as two additional scalars, with no new array and no allocation, and is returned on `KernelResult` (METR-03, F-01)."
    - "Max drawdown is exactly 0 for a run whose value never falls below its running peak, and exactly 1 for a run that ruins, and strictly between 0 and 1 for every other run (METR-03 boundary edge)."
    - "Max drawdown is carried as an unrounded float64 fraction on `KernelResult`; rounding happens only in `src/metrics/format.ts` at render time via `toFixed`, whose tie behaviour follows the exact binary double rather than the decimal literal, so the stored value and the displayed value are never conflated (METR-03 precision edge)."
    - "IRR is solved by bounded bisection over the annualized bracket -0.9999 to 10.0; when the bracket does not straddle zero the panel prints 'IRR undefined for this cash-flow pattern' in the headline slot and no NaN, Infinity or bare dash ever reaches the screen (D-08, UI-SPEC E7 error)."
    - "A ruined run's IRR is exactly -100%, returned as the definitional total-loss result rather than searched for, because a terminal inflow of zero gives the NPV no sign change to bisect (D-08)."
    - "The equity curve terminates at the bar before ruin with a destructive-coloured terminator marker at the ruin date, so a log axis is never handed a zero value; the scale toggle stays available and the ruin banner explains the truncation (UI-SPEC E6 error, PITFALLS E6)."
    - "The log and linear choice is a persistent visible on-screen control showing which scale is active, defaulting to log (VIZ-08, D-19)."
    - "The metrics panel is hidden entirely, not rendered with blank slots, whenever the result is cleared; metrics and chart appear and disappear together (UI-SPEC E7 empty)."
    - "The metrics panel is hidden during cold load with the LoadingNotice occupying the result column, and shows no skeleton placeholders, which would imply values that do not exist (UI-SPEC E7 loading)."
    - "The metrics panel shows five rows in a fixed identity and order: IRR headline at 28px monospace, CAGR secondary with its qualifier, maximum drawdown, final value as a multiple of total contributed, and the dropped-contributions line (UI-SPEC E7 populated)."
    - "When contributions are zero, CAGR carries the IRR-equals-CAGR note and the dropped-contributions row is omitted rather than shown as zero; the headline slot is IRR regardless (UI-SPEC E7 partial)."
    - "The metric row set is fixed rather than a variable-length list, so there is no plural-copy variance to handle (UI-SPEC E7 zero-one-many)."
    - "Metric values are formatted before render: percentages to two decimals, multiples to two decimals with scientific notation above 1e6, so no metric string exceeds a bounded width regardless of the underlying number (UI-SPEC E7 long-text)."
    - "Ruin banner copy is fixed except for an interpolated ISO date of fixed 10-character width, so total banner length is bounded and known at design time (UI-SPEC E8 long-text)."
    - statement: "At the narrowest supported viewport, extreme values (a 20x run over 90 years, or a near-zero final multiple) do not wrap or clip the 28px headline, and a wrapping secondary metric does not push the panel out of the D-20 screenshot region (UI-SPEC E7 overflow)."
      verification: backstop
    - statement: "At the narrowest supported viewport the ruin banner wraps and never clips the ruin date (UI-SPEC E8 overflow)."
      verification: backstop
  artifacts:
    - path: "src/metrics/irr.ts"
      provides: "Bounded-bisection money-weighted return over dated cash flows (D-08, METR-01)"
      exports: ["CashFlow", "solveIrr", "buildCashFlows"]
    - path: "src/metrics/cagr.ts"
      provides: "Closed-form compound annual growth rate over the run's calendar span (METR-02)"
      exports: ["solveCagr"]
    - path: "src/metrics/format.ts"
      provides: "The single formatting contract for percentages, multiples and currency"
      exports: ["formatPercent", "formatMultiple", "formatCurrency"]
    - path: "src/kernel/backtest.types.ts"
      provides: "KernelResult gains a maxDrawdown scalar"
      contains: "maxDrawdown"
    - path: "src/app/components/ResultColumn/MetricsPanel.tsx"
      provides: "The five fixed metric rows (METR-01 through METR-04)"
    - path: "src/app/components/ResultColumn/RuinBanner.tsx"
      provides: "The categorical ruin state change (METR-05, D-07)"
    - path: "src/app/components/ResultColumn/LogScaleToggle.tsx"
      provides: "The visible log and linear choice (VIZ-08)"
    - path: "tests/kernel/drawdown.test.ts"
      provides: "METR-03 boundary and precision assertions against the real bundled series"
  key_links:
    - from: "src/app/components/ResultColumn/MetricsPanel.tsx"
      to: "src/metrics/irr.ts"
      via: "renders solveIrr's number-or-null into the headline slot"
      pattern: "solveIrr"
    - from: "src/metrics/irr.ts"
      to: "src/kernel/backtest.types.ts"
      via: "buildCashFlows reads KernelResult.ruinBarIndex so post-ruin contributions never enter the sequence"
      pattern: "ruinBarIndex"
    - from: "src/app/components/ResultColumn/EquityCurveChart.tsx"
      to: "src/app/components/ResultColumn/RuinBanner.tsx"
      via: "both read KernelResult.ruinBarIndex; the chart truncates at it and the banner names its date"
      pattern: "ruinBarIndex"
  prohibitions:
    - requirement_id: METR-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT choose which annualized figure occupies the headline slot based on which number reads better for the run. The headline is IRR in every state and CAGR carries its qualifier whenever contributions are non-zero; a headline that changes identity, or a qualifier that is dropped, lets the tool flatter or damn leverage by presentation rather than by arithmetic, and makes two screenshots of the same tool non-comparable."
    - requirement_id: METR-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT adjust any cost parameter, financing spread, expense ratio, or tracking-error tolerance in `src/validation/cost-parameters.ts` while extending the kernel, and MUST NOT let the UPRO or TQQQ gate in `tests/validation/upro-tqqq-gate.test.ts` go red or be loosened. VALID-03 prohibits closing a measured gap by moving a sourced constant, and the carried-forward high-rate sub-window drift recorded in STATE.md is deliberately not acted on until Phase 5."
---

<objective>
Turn the tracer's rendered curve into a result a reader can quote: the five-row metrics panel with
IRR as the permanent headline, the categorical ruin state, maximum drawdown computed where it
belongs, and the visible log and linear choice.

Purpose: the equity curve alone settles nothing. METR-01 through METR-05 are what a person pastes
into an argument, and D-05 through D-08 fix exactly how each is framed so the framing cannot be the
thing doing the arguing.

Output: `maxDrawdown` on `KernelResult`, a pure IRR module, a pure CAGR module, one formatting
contract, and three result-column components wired into the existing slot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-CONTEXT.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-RESEARCH.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-PATTERNS.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md
@.planning/phases/04-first-defensible-backtest-in-the-browser/04-01-SUMMARY.md
</context>

<interfaces>
From `src/kernel/backtest.types.ts` (current, before this plan's edit):

- `KernelResult { finalValue: number; ruined: boolean; ruinBarIndex: number;
  droppedContributionsTotal: number; totalContributed: number; longGapBarCount: number;
  barCount: number }` and its docstring calls it "The small summary object `runBacktest` builds
  once, after the loop."
- `KernelSeries { returns: Float64Array; shortRate: Float64Array; calendarDaysElapsed: Int32Array;
  contributionFlags: Uint8Array }`
- `KernelOutputs { outValue: Float64Array; outRuined: Uint8Array; outLongGap: Uint8Array }`

From `src/kernel/backtest.ts`: the per-bar loop already tracks six scalars, `value`, `ruined`,
`ruinBarIndex`, `droppedContributionsTotal`, `totalContributed` and `longGapBarCount`. Bar 0 is a
cost-free anchor written as exactly `initialInvestment` and `continue`s before the main body. A
ruined bar and every bar after it writes `outValue[i] = 0`.

Verified this session: no file in the repository constructs a `KernelResult` object literal. Every
consumer calls `runBacktest` and reads fields off the returned object, and
`tests/data/kernel-inputs.test.ts` line 242 deep-compares two returned results against each other.
Adding a field is therefore additive, and RESEARCH.md Pitfall 1's feared literal-construction sweep
does not apply. Confirm with a grep before assuming otherwise.

From `src/data/kernel-inputs.ts`: `KernelInputs.window { entryIndex, barCount, firstDate, lastDate }`
and `KernelInputs.meta { seriesId, bundleVersion, truncatedForRateCoverage, contributionCount,
contributionNominalDates }`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Maximum drawdown as two in-loop scalars on KernelResult (F-01, METR-03)</name>
  <read_first>
    - `src/kernel/backtest.ts` in full, especially the header comment's allocation-discipline
      paragraph and the per-bar loop from the `for (let i = 0; ...)` line to the `return` statement
    - `src/kernel/backtest.types.ts` in full, especially the `KernelResult` block and the per-field
      doc-comment style that names the governing Decision on every field
    - `tests/kernel/ruin.test.ts` (the existing pattern for a kernel test that loads the real bundle
      and asserts on a returned `KernelResult`)
    - `tests/kernel/allocation.test.ts` (the SIM-11 heap-delta proof this change must not break)
    - `tests/kernel/module-boundary.test.ts` (asserts `backtest.ts` imports only
      `./backtest.types.ts`; adding a scalar field does not violate it, but the test must stay green)
  </read_first>
  <behavior>
    - A monotonically non-decreasing value series yields `maxDrawdown` exactly 0.
    - A run that ruins yields `maxDrawdown` exactly 1, because the ruin bar's value is exactly 0
      against a strictly positive prior peak.
    - A series that falls 40% from its peak and then recovers to a new high yields `maxDrawdown`
      equal to 0.4 within 1e-12, not the drawdown from the later, higher peak.
    - Two separate peaks with the deeper trough occurring after the second peak yields the larger of
      the two fractional declines.
    - `runBacktest` still allocates nothing per call beyond the single returned result object:
      `tests/kernel/allocation.test.ts` stays green unchanged.
    - The real bundled SPX total-return series over the full strict tier at 3x yields a
      `maxDrawdown` strictly between 0 and 1.
  </behavior>
  <action>
    Add a `maxDrawdown` field to `KernelResult` in `src/kernel/backtest.types.ts`, documented in the
    same inline doc-comment style every other field on that interface uses, naming the phase and the
    finding it resolves: the largest peak-to-trough fractional decline of the portfolio value series,
    tracked in-loop (Phase 4 F-01). State in the comment that the peak is the peak of the portfolio
    value including applied contributions, not a contribution-adjusted or money-weighted peak, so the
    definition cannot drift later.

    In `src/kernel/backtest.ts`, declare two more scalars alongside the existing six, a running peak
    seeded to `initialInvestment` and a running maximum drawdown seeded to 0. Update them once per
    bar after `outValue[i]` is finalized for that bar, on every path that writes `outValue[i]`
    including the post-ruin path and the ruin bar itself: when the bar's value exceeds the running
    peak, raise the peak; otherwise, when the peak is strictly positive, compute one minus the ratio
    of the bar's value to the peak and raise the running maximum when it is larger. Two `let`
    bindings and no new array: SIM-11's allocation discipline is the binding constraint, and the
    existing header comment's allocation paragraph must be extended to name these two so the file's
    own documentation stays true.

    Add `maxDrawdown` to the object literal the function returns after the loop.

    Create `tests/kernel/drawdown.test.ts` in the `unit` project, following
    `tests/kernel/ruin.test.ts`'s structure. Cover every case in this task's `<behavior>` block. For
    the synthetic cases build `KernelSeries` and `KernelOutputs` by hand as
    `tests/kernel/fixtures.ts` already does elsewhere; for the real-bundle case load through
    `loadBundleFromDisk` from `src/data/load-bundle-node.ts` and `buildKernelInputs`.
  </action>
  <acceptance_criteria>
    - `grep -c 'maxDrawdown' src/kernel/backtest.types.ts` is at least 2 (the field and its doc
      comment) and `grep -c 'maxDrawdown' src/kernel/backtest.ts` is at least 3.
    - `src/kernel/backtest.ts` contains no `new Float64Array`, `new Uint8Array`, `new Array`, `[]`
      literal or `.push(` inside the per-bar loop.
    - `npx vitest run --project unit tests/kernel/drawdown.test.ts` exits 0 with every
      `<behavior>` case represented by a named test.
    - `npx vitest run --project unit tests/kernel/allocation.test.ts` exits 0 unchanged.
    - `npx vitest run --project unit tests/kernel/module-boundary.test.ts` exits 0 unchanged.
    - `npx vitest run --project unit tests/validation/upro-tqqq-gate.test.ts` exits 0 and no file
      under `src/validation/` was modified by this task.
    - `npm run bench` exits 0 and the PERF-02 row's verdict is still `pass`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project unit tests/kernel/drawdown.test.ts tests/kernel/allocation.test.ts tests/kernel/module-boundary.test.ts tests/validation/upro-tqqq-gate.test.ts &amp;&amp; git diff --exit-code -- src/validation</automated>
  </verify>
  <done>`KernelResult.maxDrawdown` is a documented in-loop scalar; the boundary values 0 and 1 are asserted; the SIM-11 allocation proof and the Phase 3 UPRO and TQQQ gate are both still green and no cost parameter moved.</done>
  <files>src/kernel/backtest.types.ts, src/kernel/backtest.ts, tests/kernel/drawdown.test.ts</files>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IRR by bounded bisection, CAGR, and the one formatting contract</name>
  <read_first>
    - `src/validation/tracking-error.ts` and `src/validation/cost-parameters.ts` header comment
      (the pure-numeric-module convention: file-level header naming the Decisions it implements,
      exported pure functions with explicit types, no I/O)
    - `src/kernel/backtest.types.ts` (the `ruinBarIndex: -1` sentinel convention this module mirrors
      with its own null return)
    - `src/data/kernel-inputs.ts` (`KernelSeries.calendarDaysElapsed` is the per-bar calendar gap
      the day offsets are summed from; index 0 is 0)
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-RESEARCH.md` Pattern 2 (the
      bisection implementation to follow closely) and Pitfall 5
    - `.planning/research/PITFALLS.md` section D4 (IRR root-finding failure modes)
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md` Copywriting
      Contract rows for "IRR undefined", "IRR-equals-CAGR note", "CAGR qualifier", and the
      "MetricsPanel long-text" row of UI Considerations
  </read_first>
  <behavior>
    - A single outflow at day 0 and a single inflow at day 365 that doubles the money solves to an
      IRR of 1.0 within 1e-6.
    - The same single-cash-flow case solves to a value equal to the closed-form CAGR within 1e-9,
      which is what makes D-05's IRR-equals-CAGR note true rather than decorative.
    - A monthly-contribution schedule ending above total contributed solves to a positive IRR
      strictly less than the naive ratio-based return, because later money compounds for less time.
    - A run whose terminal inflow is exactly 0 returns exactly -1, the definitional total-loss
      result.
    - A cash-flow sequence whose NPV has the same sign at both bracket ends and a non-zero terminal
      inflow returns null, never NaN and never Infinity.
    - `buildCashFlows` never emits a contribution scheduled at or after `ruinBarIndex`, so the
      sequence has exactly one sign change and bisection cannot converge on a wrong root.
    - `formatPercent` renders 0.0342 as `3.42%` and -1 as `-100.00%`; `formatMultiple` renders
      1.5 as `1.50x` and 4.2e7 as scientific notation rather than a fifteen-character integer.
  </behavior>
  <action>
    Create `src/metrics/irr.ts` as a pure numeric module with no I/O, opening with the file-level
    header comment convention every module under `src/` uses: name the file path, the phase, and
    D-08 as the Decision it implements, and state that the bracket, the tolerance and the iteration
    cap are Claude's Discretion subject to D-08's bracket and undefined-result requirement.

    Export `interface CashFlow { daysSinceEntry: number; amount: number }` where a negative amount is
    money going in and a positive amount is money coming out, and the initial investment sits at day
    0. Export `buildCashFlows(params, series, outputs, result)` producing the sequence for one run:
    the initial investment as a negative amount at day 0; each bar whose `contributionFlags` is 1 and
    whose index is strictly before `ruinBarIndex` (or all of them when `ruinBarIndex` is -1) as a
    negative amount at that bar's cumulative sum of `calendarDaysElapsed`; and the final value as a
    single positive amount at the last bar's cumulative day offset. Post-ruin contributions are
    excluded, which is what D-21 already guarantees at the kernel level and what makes the single
    sign change true here.

    Export `solveIrr(flows)` returning `number | null`. Declare the bracket lower bound -0.9999 and
    upper bound 10.0, an iteration cap of 100 and a tolerance of 1e-9 as named module constants.
    Discount each flow by dividing by one plus the rate raised to the flow's day offset over 365,
    XIRR style, because contributions land on trading days with irregular calendar gaps and a
    bars-per-year exponent would be wrong.

    Handle the total-loss case explicitly and deliberately, before bisecting: when the terminal
    inflow is exactly 0, return exactly -1. Record in the code comment why this is not the
    special-casing D-08's rationale hoped to avoid: with a terminal inflow of zero the NPV is
    negative at every rate in the bracket, so there is no sign change to bisect and the boundary
    result must be returned by definition rather than found. The displayed figure is -100%, total
    loss of invested capital, which is the outcome D-08 locks.

    Otherwise evaluate the NPV at both bracket ends. When both have the same sign, return null: the
    panel prints the undefined copy rather than a number. Otherwise bisect, returning the midpoint
    once the NPV magnitude falls below tolerance or the iteration cap is reached. Never return NaN or
    Infinity from any path.

    Create `src/metrics/cagr.ts` exporting `solveCagr(initialInvestment, finalValue, calendarDays)`
    returning `number | null`: the ratio of final to initial raised to 365 over the calendar span,
    minus one. Return null when the initial investment is not strictly positive, when the calendar
    span is not strictly positive, or when the final value is negative. A final value of exactly 0
    returns exactly -1, matching the IRR total-loss convention so the two rows never disagree about
    a ruined run.

    Create `src/metrics/format.ts` as the single formatting contract, so no component formats a
    number inline. Export `formatPercent(fraction)` rendering a fraction as a percentage to exactly
    two decimals with a trailing percent sign; `formatMultiple(ratio)` rendering to exactly two
    decimals with a trailing lowercase x, switching to two-significant-digit scientific notation
    above 1e6 so no metric string exceeds a bounded width; and `formatCurrency(amount)` rendering a
    grouped figure with no fractional cents. Every one of them takes the null case and returns the
    UI-SPEC's exact undefined copy or an em-free placeholder the caller substitutes; none of them can
    return a string containing NaN or Infinity.

    Create `tests/metrics/irr.test.ts` covering every `<behavior>` case for `solveIrr`,
    `buildCashFlows` and `solveCagr`. Add a fast-check property, following
    `tools/bundle-compiler/tests/roundtrip.test.ts`'s use of `fc.assert` and `fc.property`, asserting
    that for any single-outflow-single-inflow pair with a positive inflow and a positive day span,
    `solveIrr` and `solveCagr` agree within 1e-6. Create `tests/metrics/format.test.ts` asserting the
    formatting cases in `<behavior>` plus that no formatter output matches the strings NaN or
    Infinity for any input including 0, negative zero, and the float64 extremes.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project unit tests/metrics/` exits 0.
    - `src/metrics/irr.ts` exports `solveIrr` with a declared return type of `number | null` and
      contains no `throw` for an unsolvable bracket.
    - `grep -c 'Newton' src/metrics/irr.ts` is 0: the solver is bisection only, per D-08.
    - `solveIrr` returns exactly -1 for a flow sequence whose terminal amount is 0, asserted by a
      named test.
    - `formatPercent`, `formatMultiple` and `formatCurrency` never return a string containing NaN or
      Infinity, asserted over the float64 extremes by a named test.
    - `src/metrics/irr.ts`, `src/metrics/cagr.ts` and `src/metrics/format.ts` import nothing from
      `src/app/`, `src/data/` or `tools/`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project unit tests/metrics/</automated>
  </verify>
  <done>IRR, CAGR and the formatting contract are three pure modules with property and example coverage; an unsolvable bracket is a null the UI can name, and a total loss is exactly -100%.</done>
  <files>src/metrics/irr.ts, src/metrics/cagr.ts, src/metrics/format.ts, tests/metrics/irr.test.ts, tests/metrics/format.test.ts</files>
</task>

<task type="auto">
  <name>Task 3: The five-row metrics panel, the ruin state change, and the visible scale choice</name>
  <precondition>`npm run test:app` from plan 04-01 passes, so the `app` Vitest project and the mounted result slot both exist.</precondition>
  <read_first>
    - `src/app/App.tsx` and `src/app/state.ts` (plan 04-01's layout slot and reactive store, which
      this task extends rather than replaces)
    - `src/app/components/ResultColumn/EquityCurveChart.tsx` (the uPlot instance gaining the ruin
      terminator and the scale switch)
    - `src/app/styles.css` (the token names to use; do not introduce a new color literal)
    - `.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md`, the full
      Copywriting Contract table, the Typography table, the Color table's "Destructive / ruin"
      reservation, and UI Considerations rows E6 error, E7 and E8
    - `src/kernel/backtest.types.ts` after Task 1 (the `maxDrawdown` field)
    - `src/metrics/irr.ts`, `src/metrics/cagr.ts`, `src/metrics/format.ts` after Task 2
  </read_first>
  <action>
    Extend `src/app/state.ts` with derived values computed once per completed run, not per render:
    the IRR from `buildCashFlows` plus `solveIrr`, the CAGR from `solveCagr` over
    `window.firstDate` to `window.lastDate`, the final-value multiple as `finalValue` divided by
    `totalContributed`, and the ruin date resolved from `ruinBarIndex` through the run window's
    dates. Add nothing to the rAF callback that allocates per frame beyond these scalars.

    Create `src/app/components/ResultColumn/MetricsPanel.tsx` rendering exactly five rows in a fixed
    identity and order that never varies with the result. Row 1 is the headline: label "Annualized
    return (IRR)", value at 28px weight 600 in the monospace stack, the only element on the screen at
    that size. When contributions are zero it carries the muted inline note "IRR equals CAGR for a
    single cash flow". When `solveIrr` returned null it prints "IRR undefined for this cash-flow
    pattern" in place of a number. Row 2 is CAGR at 16px weight 600 monospace, carrying the muted
    qualifier "CAGR is misleading with contributions - see IRR above" whenever the contribution
    amount is non-zero. Row 3 is maximum drawdown. Row 4 is final value as a multiple of total
    contributed, formatted by `formatMultiple`. Row 5 is the dropped-contributions line, rendered
    only when `droppedContributionsTotal` is non-zero, naming the amount and the ruin date; when
    contributions are zero it is omitted rather than shown as a zero. Every value goes through
    `src/metrics/format.ts`; no component formats a number inline. The panel renders nothing at all,
    not blank slots and not skeletons, when there is no completed run.

    Create `src/app/components/ResultColumn/RuinBanner.tsx` rendering the copy "Position ruined on
    {date} - value reached zero and stays there." in `--color-destructive`, with the date as an ISO
    `YYYY-MM-DD` string. In `App.tsx`, when `result.ruined` is true, place the banner above the
    metrics panel and mark the metrics subordinate to it visually; the metrics stay on screen, and
    the banner is a state change of the whole panel rather than a badge sitting beside otherwise
    normal numbers.

    Create `src/app/components/ResultColumn/LogScaleToggle.tsx`, a two-state control labelled "log"
    and "linear" whose active state is rendered in `--color-accent`. It reads and writes the `scale`
    signal plan 04-01 put in `state.ts`. Minimum hit area 44 by 44 pixels regardless of visual size.
    The control is persistently visible beside the chart, because VIZ-08 requires the choice of scale
    to be visible, not merely available.

    Extend `EquityCurveChart.tsx`: switch the y scale distribution between uPlot's logarithmic and
    linear distributions from the `scale` signal, updating the scale configuration rather than only
    the data, since a distribution change does not take effect from a data update alone. When
    `ruinBarIndex` is not -1, plot the series only up to the bar before ruin and draw a separate
    terminator marker at the ruin date in `--color-destructive`. Never pass the ruined bar's zero
    value into a log-distributed series: the natural logarithm of zero is negative infinity, and
    uPlot either throws inside its scale-range computation or renders a nonsensical spike. The scale
    toggle stays available in the ruined state; the banner is what explains the truncation.

    Create `tests/app/metrics.browser.test.ts` in the `app` project asserting, against the mounted
    app: the headline slot's label is the IRR label in both the zero-contribution and non-zero
    contribution states; the CAGR qualifier text is present when contributions are non-zero and
    absent when they are zero; the IRR-equals-CAGR note is present when contributions are zero; five
    metric rows render in the fixed order for a non-ruined run and the dropped-contributions row is
    absent; and for a deliberately ruined run (a high-leverage entry chosen so the position genuinely
    reaches zero) the ruin banner renders above the metrics, the metrics are still present, the
    canvas renders without throwing on the log scale, and the rendered IRR string is `-100.00%`.
  </action>
  <acceptance_criteria>
    - `npm run test:app` exits 0 and `tests/app/metrics.browser.test.ts` covers every case named in
      the action's final paragraph.
    - `grep -c 'toFixed\|toLocaleString\|toPrecision' src/app/components/ResultColumn/MetricsPanel.tsx`
      is 0: all formatting routes through `src/metrics/format.ts`.
    - The strings `#C4341F` and `#E2604B` appear only in `src/app/styles.css`, never in a `.tsx`
      file: `grep -rl '#C4341F' src/app --include=*.tsx` returns nothing.
    - `grep -rc 'innerHTML' src/app` is 0 across every file.
    - For a ruined run the chart's plotted y series contains no value equal to 0.
    - `src/app/components/ResultColumn/LogScaleToggle.tsx` renders both the log and linear labels in
      the DOM at all times, so the active choice is readable from a screenshot.
    - `npm run typecheck` and `npm run build` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run test:app</automated>
  </verify>
  <done>A reader sees IRR as a permanent headline with CAGR qualified beside it, maximum drawdown, the final multiple, the dropped-contributions line when it applies, a categorical ruin banner when the position dies, and a visible log or linear choice.</done>
  <files>src/app/state.ts, src/app/App.tsx, src/app/components/ResultColumn/MetricsPanel.tsx, src/app/components/ResultColumn/RuinBanner.tsx, src/app/components/ResultColumn/LogScaleToggle.tsx, src/app/components/ResultColumn/EquityCurveChart.tsx, tests/app/metrics.browser.test.ts</files>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kernel output to rendered DOM | Computed numbers become text a reader will quote as fact |
| Float64 arithmetic to displayed string | A formatting slip changes what the number means |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | `src/kernel/backtest.ts` in-loop drawdown addition | high | mitigate | Task 1 gates on `tests/validation/upro-tqqq-gate.test.ts` staying green and on `git diff --exit-code -- src/validation` proving no cost parameter moved in the same change |
| T-04-11 | Information Disclosure | `MetricsPanel` rendering computed values | medium | mitigate | All values pass through `src/metrics/format.ts`, which is asserted never to emit NaN or Infinity for any float64 input; Solid JSX text interpolation escapes by default and no `innerHTML` exists anywhere in `src/app` |
| T-04-12 | Denial of Service (client) | `EquityCurveChart` on a ruined run under a log distribution | medium | mitigate | The series is truncated at the bar before ruin and a terminator marker is drawn instead, asserted by the browser test's deliberately-ruined case; no zero value ever enters a log-distributed series |
| T-04-13 | Repudiation | No server-side record of a displayed figure | low | accept | The tool has no accounts and no server log; the self-describing permalink from plan 04-07 is the project's own accountability mechanism |
</threat_model>

<assumption_delta_decision>
The deterministic assumption-delta detector fired on this phase's ROADMAP section with two
`pluralization` signals, the words "alongside" and "multiple", both from criterion 2's sentence
"leads with IRR as the annualized figure whenever contributions are non-zero, alongside maximum
drawdown and final value as a multiple of total contributed".

- **Primary noun:** unchanged. The identity model here is one `KernelResult` per run, keyed by the
  parameter set the permalink encodes.
- **Decision:** `no-change`. Both trigger words are prose artifacts rather than a singular-to-plural
  transition. "Multiple" is a ratio (final value over total contributed, METR-04), not a plurality,
  and "alongside" lists sibling metrics of one run rather than introducing a second run, a second
  symbol or a second source of truth. Nothing gained a second case and no field went from required
  to optional or from derived to chosen.
- **Accepted debt:** none, because nothing was added alongside anything.

Recorded rather than acted on; this checkpoint is advisory and never blocks.
</assumption_delta_decision>

<artifacts_this_phase_produces>
**New files:** `src/metrics/irr.ts`, `src/metrics/cagr.ts`, `src/metrics/format.ts`,
`src/app/components/ResultColumn/MetricsPanel.tsx`,
`src/app/components/ResultColumn/RuinBanner.tsx`,
`src/app/components/ResultColumn/LogScaleToggle.tsx`, `tests/kernel/drawdown.test.ts`,
`tests/metrics/irr.test.ts`, `tests/metrics/format.test.ts`, `tests/app/metrics.browser.test.ts`.

**New exported symbols:** `CashFlow`, `buildCashFlows`, `solveIrr`, `solveCagr`, `formatPercent`,
`formatMultiple`, `formatCurrency`, `MetricsPanel`, `RuinBanner`, `LogScaleToggle`.

**New field on an existing type:** `KernelResult.maxDrawdown`.
</artifacts_this_phase_produces>

<flagged_assumptions>
Probe rows that stayed `unresolved` (category `unclassified`), surfaced rather than dropped. Four
land in this plan.

- **METR-01 (unclassified, unresolved).** No shape category was proposed for "IRR is computed and is
  the default annualized metric whenever contributions are non-zero". Planner assumption: "default"
  means the permanent headline slot in every state, per D-05, not a conditional swap. If that
  reading is wrong, D-05 is what changes, not this plan.
- **METR-02 (unclassified, unresolved).** Planner assumption: "suppressed or explicitly qualified"
  is discharged by the qualifier route, since D-05 explicitly rejects suppression and rejects
  strike-through. No edge was proposed for what a borderline-misleading schedule is; the plan treats
  any non-zero contribution amount as triggering the qualifier.
- **METR-04 (unclassified, unresolved).** Planner assumption: the denominator is
  `totalContributed`, money actually invested, per D-06, and the dropped amount is a separate line.
  No edge was proposed for a zero denominator; a run with a non-positive initial investment is
  rejected upstream by `buildKernelInputs`, so the ratio cannot divide by zero.
- **METR-05 (unclassified, unresolved).** Planner assumption: "presented as a categorical outcome"
  is discharged by D-07's whole-panel state change plus the chart terminator, and is not verifiable
  by a source assertion alone; the browser test asserts the banner's presence and the metrics'
  retention, which is the closest mechanical proxy.
</flagged_assumptions>

<verification>
- `npm run typecheck && npm test && npm run test:app && npm run bench && npm run build` all exit 0.
- The Phase 3 UPRO and TQQQ gate is still green and `git diff` shows no change under
  `src/validation/`.
- PERF-02 in `.bench/bench-results.json` still reports verdict `pass`.
</verification>

<success_criteria>
- IRR leads as the annualized figure with contributions non-zero (METR-01) and CAGR is qualified
  (METR-02).
- Maximum drawdown is computed and reported (METR-03).
- Final value as a multiple of total contributed is computed and reported (METR-04).
- The ruin flag is reported as a categorical outcome distinct from a merely bad number (METR-05).
- The choice of scale is visible (VIZ-08).
</success_criteria>

<output>
Create `.planning/phases/04-first-defensible-backtest-in-the-browser/04-02-SUMMARY.md` when done.
</output>

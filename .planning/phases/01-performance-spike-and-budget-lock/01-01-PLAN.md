---
phase: 01-performance-spike-and-budget-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - perf-budgets.ts
  - bench/calibration.ts
  - bench/environment-block.ts
  - bench/report.ts
  - bench/global-setup.ts
  - bench/canvas-repaint.bench.test.ts
  - .github/workflows/ci.yml
  - tests/perf-budgets.selftest.test.ts
  - tests/report.test.ts
autonomous: true
requirements: [PERF-01, PERF-01a, PERF-10, PERF-11]

user_setup:
  - service: github-actions
    why: "Phase criterion 5 requires a live CI gate on pull requests. The workflow file is inert until the repository has a GitHub remote and Actions is enabled for it."
    dashboard_config:
      - task: "Ensure the repository has a GitHub remote and that Actions is enabled"
        location: "GitHub repo -> Settings -> Actions -> General"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # Goal-backward truths
    - "Running `npm run bench` from a clean checkout prints one row group for each of PERF-02 through PERF-09, so no metric can be silently omitted (D-05)"
    - "Every printed row shows its numeric budget threshold and its perception anchor, whether or not the metric was measured this run (D-05)"
    - "At least one row carries a real wall-clock number measured inside headless Chromium against a real Canvas, normalized by a calibration score, and compared against its budget by an ordinary Vitest expect call (D-02, D-03, D-06)"
    - "Every run stamps a full environment block naming the machine, hardwareConcurrency, browser version, OS, calibration score and CI flag, so an unlabelled figure is structurally impossible (D-18, PERF-11)"
    - "A budget breach fails `npm run bench` with a non-zero exit code through the normal Vitest failure path, with no separate reporting pipeline (D-03, PERF-01)"
    - "A permanent self-test feeds the budget checker a deliberately over-budget fixture and asserts it produces a fail verdict, so the gate cannot rot into a no-op when the harness is refactored (D-09)"
    - "Every budget whose threshold exceeds its perception anchor carries a non-empty relaxation reason, asserted mechanically rather than left to code review (PERF-01a, D-21)"
    - "The bench run asserts its own total wall-clock against a declared cap and prints that runtime, so repeat cost cannot creep silently (D-08)"
    - "The CI workflow runs `npm run bench` on pull requests and on pushes to main, and uploads the machine-readable results JSON as a build artifact (D-01, D-04)"
    # Edge-probe derived (spec-less fallback, verification: explicit)
    - "A measured value exactly equal to its budget threshold passes; only a value strictly greater than the threshold fails, matching PERF-01's wording that CI fails when a measured value exceeds its budget [edge: PERF-01 adjacency]"
    - "A budget id registered by a measurement module with no matching entry in PERF_BUDGETS fails the run with a named error rather than being skipped, and a budget entry with no registered measurement module prints unmeasured with its threshold and anchor still shown and runs no assertion [edge: PERF-01 empty]"
    - "Report rows are emitted in a fixed order sorted by budget id ascending, identical across runs and independent of measurement registration order or object key insertion order [edge: PERF-01 ordering]"
    - "A measured value exactly equal to 70 percent of its budget triggers the escalation marker; a value below 70 percent does not. The escalation marker is printed and recorded but does not fail the run, because the budget itself is not breached [edge: PERF-01a boundary, D-20]"
    - "A budget whose thresholdMs equals its anchorMs requires no relaxationReason; a budget whose thresholdMs is strictly greater than its anchorMs requires a non-empty relaxationReason [edge: PERF-01a adjacency]"
    - "A relaxationReason that is present but empty or whitespace-only is treated as absent and fails the self-test [edge: PERF-01a empty]"
    - "Budgets sharing a requirementId print grouped under that requirement id in ascending budget-id order [edge: PERF-01a ordering]"
    - "Budget comparison uses the unrounded float derived from performance.now deltas against the integer threshold. The displayed value is rounded half-up to two decimal places for display only, and the displayed rounded value is never the value compared [edge: PERF-01a precision]"
    - "Two metrics with identical measured values print as separate rows in budget-id order, and neither is deduplicated nor merged [edge: PERF-10 adjacency]"
    - "`npm run bench` with zero registered measurement modules exits non-zero with a named error rather than printing an all-unmeasured table and exiting 0, because a harness that measures nothing is broken rather than passing [edge: PERF-10 empty]"
    - "The table prints requirement groups in ascending requirement-id order from PERF-02 through PERF-09, and the completeness assertion requires all eight group headers regardless of how many budget rows each group contains [edge: PERF-10 ordering]"
    # Edge-probe derived (spec-less fallback, verification: backstop)
    - statement: "The bench project runs with no test-level parallelism so no two timed measurements execute concurrently, and the results JSON is written to a temporary path then renamed, so an interrupted run leaves either no artifact or a complete one and never a truncated one"
      verification: backstop
    - statement: "Calibration and metric measurements are sized so that a single repeat spans at least 10 milliseconds, keeping performance.now timer coarsening below one percent of the measured value"
      verification: backstop

  prohibitions:
    - requirement_id: PERF-01a
      category: values
      statement: "MUST NOT make a failing budget pass by moving the goalposts. Neither raising a thresholdMs nor altering the calibration reference loop or its scaling may be done in response to a failing measurement. A budget that cannot be met is an architecture escalation under PERF-01a and D-20, recorded as a Key Decision."
    - requirement_id: PERF-11
      category: transparency
      statement: "MUST NOT present a spike-synthetic measurement as if it were a production measurement. Every reported figure states its source and the machine and core count it was measured on."
    - requirement_id: PERF-01
      category: transparency
      statement: "MUST NOT downgrade a budget breach to a warning, a non-blocking annotation, or a skipped test. A breach fails the run."

  artifacts:
    - path: "perf-budgets.ts"
      provides: "Typed budget module, single source of truth for every performance threshold"
      exports: ["PerfBudget", "BudgetId", "RequirementId", "PERF_BUDGETS", "ESCALATION_TRIGGER_RATIO", "BENCH_TOTAL_RUNTIME_CAP_MS"]
    - path: "bench/report.ts"
      provides: "Row shape, table renderer, budget checker, run-level invariant assertions"
      exports: ["MeasurementRow", "Verdict", "MeasurementSource", "checkBudget", "escalationTriggered", "renderTable", "assertRunInvariants", "formatMeasured"]
    - path: "bench/calibration.ts"
      provides: "Reference loop, minimum-of-N repeats, normalization"
      exports: ["REPEAT_COUNT", "MIN_MEASUREMENT_MS", "calibrationScore", "measureMinOfN", "normalize"]
    - path: "bench/environment-block.ts"
      provides: "Machine, cores, browser, OS, CI stamping"
      exports: ["EnvironmentBlock", "captureEnvironment"]
    - path: "bench/canvas-repaint.bench.test.ts"
      provides: "Hand-rolled Canvas 2D fillRect repaint measurement registered against PERF-05"
    - path: ".github/workflows/ci.yml"
      provides: "Pull-request and push-to-main CI gate running the bench suite"
    - path: "tests/perf-budgets.selftest.test.ts"
      provides: "Permanent gate-liveness self-test and the PERF-01a anchor invariant"

  key_links:
    - from: "bench/canvas-repaint.bench.test.ts"
      to: "perf-budgets.ts"
      via: "imports PERF_BUDGETS and asserts the normalized measurement against the threshold"
      pattern: "PERF_BUDGETS\\['PERF-05'\\]"
    - from: "bench/canvas-repaint.bench.test.ts"
      to: "bench/calibration.ts"
      via: "normalizes the raw minimum-of-5 measurement by the calibration score before comparing"
      pattern: "normalize\\("
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "runs the bench npm script as the authoritative gate"
      pattern: "npm run bench"
    - from: "tests/perf-budgets.selftest.test.ts"
      to: "bench/report.ts"
      via: "feeds checkBudget an over-budget fixture and asserts a fail verdict"
      pattern: "checkBudget\\("
---

<!-- planner-discipline-allow: pull_request_target -->

## Phase Goal

ROADMAP `**Goal:**` for this phase reads: "The architecture is decided by a measured number instead of an estimate, and performance becomes checkable with one command from this point forward."

This is not in the canonical `As a / I want to / so that` user-story format that MVP mode expects. No story has been invented in its place. Run `/gsd mvp-phase 01` if a user-story goal line is wanted before execution.

## Artifacts this phase produces

Every symbol, file, script and constant below is created by Phase 01 and does not exist before it. Nothing in this list is pre-existing project surface.

**New files**

`package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `.gitignore`, `perf-budgets.ts`, `.github/workflows/ci.yml`, `bench/calibration.ts`, `bench/environment-block.ts`, `bench/report.ts`, `bench/global-setup.ts`, `bench/canvas-grid.ts`, `bench/canvas-repaint.bench.test.ts`, `bench/synthetic-data.ts`, `bench/kernel.ts`, `bench/sweep.worker.ts`, `bench/sweep-pool.ts`, `bench/sweep.bench.test.ts`, `bench/kernel.bench.test.ts`, `tests/perf-budgets.selftest.test.ts`, `tests/report.test.ts`, `tests/canvas-grid.test.ts`, `spike/wasm-microbench/Cargo.toml`, `spike/wasm-microbench/src/lib.rs` (both deleted at phase end per D-13), `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md`.

**New npm scripts**

`bench`, `test`, `typecheck`.

**New exported symbols**

- `perf-budgets.ts`: `PerfBudget`, `BudgetId`, `RequirementId`, `PERF_BUDGETS`, `ESCALATION_TRIGGER_RATIO`, `BENCH_TOTAL_RUNTIME_CAP_MS`
- `bench/report.ts`: `MeasurementRow`, `Verdict`, `MeasurementSource`, `checkBudget`, `escalationTriggered`, `renderTable`, `assertRunInvariants`, `formatMeasured`, `recordMeasurement`
- `bench/calibration.ts`: `REPEAT_COUNT`, `MIN_MEASUREMENT_MS`, `calibrationScore`, `measureMinOfN`, `normalize`
- `bench/environment-block.ts`: `EnvironmentBlock`, `captureEnvironment`
- `bench/canvas-grid.ts`: `GRID_COLS`, `GRID_ROWS`, `makeGridValues`, `mapValueToRgba`, `paintFillRect`, `paintPutImageData`
- `bench/synthetic-data.ts`: `BAR_COUNT`, `SyntheticSeries`, `makeSeededGbmSeries`
- `bench/kernel.ts`: `SpikeKernelParams`, `SpikeKernelResult`, `runSpikeBacktest`, `SWEEP_COLS`, `SWEEP_ROWS`
- `bench/sweep-pool.ts`: `runSpikeSweep`, `resolveWorkerCount`

**New runtime paths**

`.bench/bench-results.json` (gitignored, uploaded as a CI artifact).

## Flagged planner assumptions

- **PERF-11 edge probe returned `unclassified` and is left `unresolved`.** PERF-11 is a build-order and process requirement ("performance is measured on real hardware from the first executable phase onward, and the build order places measurement before architecture commitment") with no data-shape edge to classify. No edge acceptance criterion was authored for it and none was fabricated with a backstop marker. It is satisfied structurally by this phase existing first in the roadmap and by the environment block asserted in Task 1, not by a per-input edge criterion.
- **Canon prohibition drops (breadcrumbs, not minted).** Fork-PR privilege escalation through the privileged pull-request trigger variant is canon CI supply-chain, covered by `/gsd-secure-phase` and by the `T-01-02` row in this plan's threat model; not minted as a prohibition. Dependency typosquatting and install-time network access from package postinstall scripts are canon supply-chain, covered by `/gsd-secure-phase` and by the Package Legitimacy Audit in `01-RESEARCH.md`; not minted as a prohibition.

<objective>
Stand up the whole measurement chain end to end with one real metric flowing through it: `npm run bench` runs inside headless Chromium, takes a real Canvas 2D repaint measurement of a 10,000-cell grid, normalizes it against a calibration score, compares it to a real threshold in the typed budget file, prints it in a table alongside every other PERF-02 through PERF-09 row, writes a machine-readable JSON artifact, and fails the run on breach. Then close the CI leg so that gate is live on every pull request and provably red on a breach.

Purpose: this is the substrate every later phase is graded against. Nothing in Phases 2 through 8 has a performance gate until this chain works.
Output: a compiling TypeScript project with a working `npm run bench`, a locked budget file covering all eight PERF metrics, a CI workflow, and a permanent gate-liveness self-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md
@.planning/phases/01-performance-spike-and-budget-lock/01-RESEARCH.md
@.planning/phases/01-performance-spike-and-budget-lock/SKELETON.md
@.claude/CLAUDE.md
</context>

<interfaces>
This plan creates every interface it uses. There is no pre-existing code in this repository outside `.planning/`, `CLAUDE.md` and `.claude/CLAUDE.md`.

The budget record set that Task 1 must produce, derived from each requirement's own numeric anchor in REQUIREMENTS.md (not from a blanket 16/100/1000 triad), is eleven entries across eight requirement ids:

| budget id | requirementId | thresholdMs | anchorMs | anchorLabel | implementedInPhase |
|---|---|---|---|---|---|
| PERF-02 | PERF-02 | 16 | 16 | one frame | 3 |
| PERF-03 | PERF-03 | 1000 | 1000 | holds attention | 7 |
| PERF-04 | PERF-04 | 100 | 100 | feels instant | 7 |
| PERF-05 | PERF-05 | 16 | 16 | one frame | 7 |
| PERF-06 | PERF-06 | 16 | 16 | one frame | 7 |
| PERF-07a | PERF-07 | 50 | 50 | long task threshold | 4 |
| PERF-07b | PERF-07 | 16 | 16 | one frame | 4 |
| PERF-08a | PERF-08 | 1500 | 1500 | cold load ceiling | 4 |
| PERF-08b | PERF-08 | 1000 | 1000 | holds attention | 4 |
| PERF-08c | PERF-08 | 300 | 300 | warm load ceiling | 4 |
| PERF-09 | PERF-09 | 16 | 16 | one frame | 7 |

Every threshold equals its anchor, so no entry carries a `relaxationReason` and no relaxation Key Decision is owed under PERF-01a.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `npm run bench` measures a real 10,000-cell Canvas repaint in headless Chromium and gates it against the locked budget file</name>

  <reversibility rating="costly">Implements D-02 (single measurement environment), D-06 (calibration normalization), D-17 (ubuntu-latest as the 4-core baseline) and D-21 (typed budget module). Every figure this phase records and every budget locked here is denominated in this environment and this schema; changing either later invalidates the historical numbers the Key Decisions cite and forces every registered measurement module to be touched. All four were chosen by the developer during discuss-phase, so no checkpoint re-asks them.</reversibility>

  <read_first>
    - `.planning/phases/01-performance-spike-and-budget-lock/01-RESEARCH.md` sections "Standard Stack", "Architecture Patterns" (Patterns 1 through 5), "Common Pitfalls" (Pitfall 1 and Pitfall 2), "Code Examples" and "Environment Availability". Pattern 4's anti-pattern note governs how unmeasured rows are derived.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md` `<decisions>` block, decisions D-02 through D-08 and D-14, D-15, D-18, D-19, D-20, D-21.
    - `.planning/REQUIREMENTS.md` section "Performance", requirements PERF-01 through PERF-11. The numeric anchors in PERF-07 and PERF-08 are the source for the multi-row budget entries in the table above.
    - `.claude/CLAUDE.md` section "Version Compatibility" for the pinned versions, and section "Q2 - Charting" for the documented grounds on which every charting library was rejected for the heatmap.
    - `.planning/research/PITFALLS.md` section F, entries F1 and F2, for the allocation-free and monomorphic constraints the calibration reference loop must respect.
    - After `npm install` completes, read the installed type declarations under `node_modules/@vitest/browser-playwright` and `node_modules/vitest` to confirm the browser-mode config keys and the browser-to-Node command API actually exist in 4.1.10 before writing code against them. RESEARCH.md flags this as assumption A1 and as a MEDIUM-confidence claim.
  </read_first>

  <behavior>
    - `checkBudget` on a row whose normalized value is strictly greater than the threshold returns verdict `fail`.
    - `checkBudget` on a row whose normalized value is exactly equal to the threshold returns verdict `pass`, because PERF-01 fails only when a value exceeds its budget.
    - `checkBudget` on a row with a null measurement returns verdict `unmeasured`.
    - `escalationTriggered` returns true when the normalized value is greater than or equal to threshold times `ESCALATION_TRIGGER_RATIO`, and false below it.
    - `renderTable` emits rows sorted by budget id ascending, grouped under requirement id ascending, and the order does not change when the input array order changes.
    - `formatMeasured` rounds half-up to two decimal places for display and is never used as the value compared against a threshold.
    - `assertRunInvariants` throws when any of the eight requirement group headers is absent, when zero rows are measured, when a row references a budget id absent from `PERF_BUDGETS`, or when the total run wall-clock exceeds `BENCH_TOTAL_RUNTIME_CAP_MS`.
  </behavior>

  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, .gitignore, perf-budgets.ts, bench/calibration.ts, bench/environment-block.ts, bench/report.ts, bench/global-setup.ts, bench/canvas-repaint.bench.test.ts</files>

  <action>
Create the project scaffold and the full measurement chain in one slice. Do not create product code, a UI framework dependency, or a separate `vite.config.ts`; this phase ships no application.

Install exactly `vitest@4.1.10`, `@vitest/browser-playwright@4.1.10`, `playwright@1.62.1` as devDependencies and `comlink@4.4.2` as a dependency, using `--save-exact`, then run `npx playwright install --with-deps chromium`. Commit `package-lock.json`. Add no charting library and no WASM tooling to `package.json`: the heatmap renderer is hand-rolled Canvas 2D per D-14, and the Rust toolchain for the WASM arm is installed outside npm per the Package Legitimacy Audit in RESEARCH.md.

Add npm scripts `typecheck` running `tsc --noEmit`, `test` running `vitest run --project unit`, and `bench` running `vitest run --project bench`. `tsconfig.json` uses `strict: true`, `noUncheckedIndexedAccess: true`, `module` and `moduleResolution` set for a bundler-resolved ESM project, and `lib` including `DOM` and `WebWorker`.

`vitest.config.ts` defines two projects. The `unit` project runs in Node against `tests/**/*.test.ts` and is the fast feedback loop. The `bench` project runs in browser mode against `bench/**/*.bench.test.ts` using the Playwright provider imported from `@vitest/browser-playwright` with a single chromium instance and `headless: true`, per D-02 and D-03. Set the bench project to run with no file-level parallelism so that no two timed measurements execute concurrently. Register `bench/global-setup.ts` as the bench project's `globalSetup`.

`perf-budgets.ts` (D-21) exports the interface `PerfBudget` with fields `id`, `requirementId`, `description`, `thresholdMs`, `anchorMs`, `anchorLabel`, `implementedInPhase` and optional `relaxationReason`; the union types `BudgetId` and `RequirementId`; the constant record `PERF_BUDGETS` populated with exactly the eleven entries in this plan's `<interfaces>` table; `ESCALATION_TRIGGER_RATIO` set to `0.7` per D-20; and `BENCH_TOTAL_RUNTIME_CAP_MS` set to `30000` per D-08. Thirty seconds is roughly double the 5 to 15 second repeat cost this phase's arms are estimated to carry, which leaves room for the arms Phases 2, 3, 4, 6 and 7 will add without leaving so much slack that creep goes unnoticed; raising it later requires a recorded decision. Add a compile-time exhaustiveness check that the set of distinct `requirementId` values across `PERF_BUDGETS` is exactly the eight ids PERF-02 through PERF-09, so a dropped requirement cannot compile.

`bench/calibration.ts` exports `REPEAT_COUNT` set to `5` and `MIN_MEASUREMENT_MS` set to `10` per D-07; `measureMinOfN(n, fn)` which runs `fn` n times and returns the minimum elapsed milliseconds from `performance.now()` deltas; `calibrationScore()` which runs a fixed-iteration, allocation-free, monomorphic `Float64Array` arithmetic reference loop `REPEAT_COUNT` times and returns the minimum, scaled so that a nominal reference machine scores `1.0`; and `normalize(rawMs, score)` returning `rawMs / score` per D-06. Size the reference loop's iteration count so a single repeat spans at least `MIN_MEASUREMENT_MS`, and sum into a value that is returned so the JIT cannot eliminate the loop, per RESEARCH.md Pitfall 1.

`bench/environment-block.ts` exports the `EnvironmentBlock` interface and `captureEnvironment()` returning `hardwareConcurrency`, `userAgent`, `os`, `deviceMemory` where the browser exposes it, `calibrationScore`, `ci` as a boolean, and an ISO `timestamp`, per D-18.

`bench/report.ts` exports `MeasurementRow` with fields `budgetId`, `requirementId`, `measuredMs`, `normalizedMs`, `budgetMs`, `anchorMs`, `anchorLabel`, `source` and `verdict`; the `Verdict` union `pass | fail | unmeasured`; the `MeasurementSource` union `spike-synthetic | production`; `checkBudget`, `escalationTriggered`, `formatMeasured`, `renderTable` and `assertRunInvariants` behaving exactly as the `<behavior>` block above specifies; and `recordMeasurement(row)` which routes a completed row from the browser context to the Node-side accumulator. `renderTable` emits the columns metric, source, measured, budget, anchor, verdict, escalate. The `source` and `escalate` columns are additions beyond D-04's five columns: `source` exists because a Phase 1 figure comes from throwaway spike code against synthetic input and must never read as a production measurement, and `escalate` surfaces D-20's 70 percent trigger. Above the table, `renderTable` prints the environment block and a banner line stating whether this run is the authoritative `ubuntu-latest` baseline (when `ci` is true, per D-17) or an informational dev-machine run.

Derive unmeasured rows from an explicit registry of which budget ids have a registered measurement module this run. Do not wrap a measurement call in try/catch and print unmeasured on a thrown error: that converts a real crash into a false unmeasured and defeats D-05, per RESEARCH.md Pattern 4's anti-pattern.

`bench/global-setup.ts` runs in the Node context, provides the accumulator sink that `recordMeasurement` writes into, and in its teardown prints `renderTable`'s output to stdout, writes the accumulated environment block and rows to `.bench/bench-results.json` by writing to a temporary path in the same directory then renaming, prints the run's own total wall-clock, and calls `assertRunInvariants`, throwing on failure so the run exits non-zero. Per D-04 the JSON is never committed.

For the browser-to-Node channel that `recordMeasurement` uses, prefer Vitest browser custom commands (declared under the bench project's browser `commands` config and invoked from the test context) after confirming that API exists in the installed 4.1.10 type declarations. If it does not, fall back to a single-line stdout marker protocol using the prefix `##GSD-BENCH##` followed by the JSON payload for one row, parsed by the global-setup teardown from the forwarded browser console output. Record which mechanism was used in the plan SUMMARY.

`bench/canvas-repaint.bench.test.ts` is the one real measurement in this slice. It creates an offscreen or detached `<canvas>` sized for a 200 by 50 cell grid, builds a deterministic 10,000-value `Float64Array` from a fixed seed, and paints every cell with one `fillRect` call per cell, hand-rolled with no charting library per D-14. It measures that full repaint with `measureMinOfN(REPEAT_COUNT, ...)`, normalizes by `calibrationScore()`, records the row against budget id `PERF-05` with `source` set to `spike-synthetic`, and asserts the normalized value against `PERF_BUDGETS['PERF-05'].thresholdMs` using an ordinary Vitest `expect` with a less-than-or-equal comparison, so a value exactly at the threshold passes. Set `imageSmoothingEnabled` to false on the context so cell edges stay crisp.

Add `.gitignore` covering `node_modules/`, `.bench/`, `dist/`, and Playwright's local caches.
  </action>

  <verify>
    <automated>npm run typecheck &amp;&amp; npm run bench</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `perf-budgets.ts` contains a compile-time exhaustiveness check over `requirementId`, so `npm run typecheck` passing is the proof that all eight requirement ids are present. Task 2 adds the runtime assertion that `PERF_BUDGETS` has exactly 11 entries across exactly 8 distinct `requirementId` values.
    - `npm run bench` exits 0.
    - `npm run bench` stdout contains a group header line for each of the eight requirement ids PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, PERF-08, PERF-09.
    - `npm run bench` stdout shows a numeric measured value and a verdict of `pass` or `fail` for budget id PERF-05.
    - `npm run bench` stdout shows verdict `unmeasured` with a visible threshold and anchor for budget ids PERF-02, PERF-03, PERF-04, PERF-06, PERF-07a, PERF-07b, PERF-08a, PERF-08b, PERF-08c and PERF-09.
    - `npm run bench` stdout contains an environment line whose `hardwareConcurrency`, `userAgent`, `os`, `calibrationScore` and `ci` fields are all non-empty.
    - `npm run bench` stdout prints the run's own total wall-clock in milliseconds.
    - `.bench/bench-results.json` exists after a run and `node -e "const r=require('./.bench/bench-results.json'); if(!r.environment||!Array.isArray(r.rows))process.exit(1)"` exits 0.
    - `git status --porcelain` after a bench run shows no entry under `.bench/`.
    - `node -e "const p=require('./package.json');const d=Object.keys({...p.dependencies,...p.devDependencies});const banned=['uplot','echarts','plotly.js','@observablehq/plot','wasm-pack'];if(d.some(k=>banned.includes(k)))process.exit(1)"` exits 0.
  </acceptance_criteria>

  <done>`npm run bench` runs in headless Chromium, prints all eight PERF requirement groups with thresholds and anchors, carries one real calibration-normalized Canvas repaint figure for PERF-05 asserted against its budget, stamps the environment, and writes a gitignored JSON artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the CI leg and prove the budget gate is live</name>

  <read_first>
    - `bench/report.ts` and `perf-budgets.ts` as created in Task 1, so the self-test asserts against the real exported signatures rather than assumed ones.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-RESEARCH.md` sections "Code Examples" (the GitHub Actions Playwright caching pattern) and "Security Domain" (the three threat patterns this workflow must not reintroduce).
    - `.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md` decisions D-01, D-04, D-09 and D-17.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-VALIDATION.md`, the "Per-Task Verification Map" and "Manual-Only Verifications" tables.
  </read_first>

  <behavior>
    - Feeding `checkBudget` a fixture row whose normalized value is above the threshold yields verdict `fail`.
    - Feeding `checkBudget` a fixture row whose normalized value is exactly the threshold yields verdict `pass`.
    - Feeding `checkBudget` a fixture row with a null measurement yields verdict `unmeasured`.
    - `assertRunInvariants` throws when passed a row set missing any of the eight requirement groups.
    - `assertRunInvariants` throws when passed a row set in which every row is unmeasured.
    - `assertRunInvariants` throws when passed a total runtime above `BENCH_TOTAL_RUNTIME_CAP_MS`.
    - Every entry in `PERF_BUDGETS` whose `thresholdMs` is strictly greater than its `anchorMs` has a `relaxationReason` that is a non-empty, non-whitespace string; an empty or whitespace-only reason fails the assertion.
    - `renderTable` output for the same rows supplied in two different array orders is byte-identical.
    - `formatMeasured(16.005)` renders `16.01` and `formatMeasured` is not used inside `checkBudget`.
  </behavior>

  <files>.github/workflows/ci.yml, tests/perf-budgets.selftest.test.ts, tests/report.test.ts</files>

  <action>
Write `tests/perf-budgets.selftest.test.ts` in the fast Node `unit` project as the permanent gate-liveness self-test required by D-09. It constructs a deliberately over-budget `MeasurementRow` fixture, feeds it to `checkBudget`, and asserts the returned verdict is `fail` with the expected message naming the budget id and both numbers. It also asserts the PERF-01a anchor invariant across every entry in `PERF_BUDGETS`: any entry whose `thresholdMs` exceeds its `anchorMs` must carry a non-empty, non-whitespace `relaxationReason`. This turns what VALIDATION.md lists as a manual-only code-review item into a mechanical assertion that runs on every pull request forever. Also assert `Object.keys(PERF_BUDGETS).length` is 11 and that the distinct `requirementId` set is exactly the eight ids PERF-02 through PERF-09.

Write `tests/report.test.ts` in the same `unit` project covering the remaining behaviors listed above: the exactly-at-threshold pass, the null-measurement unmeasured verdict, `escalationTriggered` at exactly and just below the 70 percent boundary, `assertRunInvariants` throwing on a missing requirement group, on an all-unmeasured row set and on a runtime above the cap, order independence of `renderTable`, and `formatMeasured` half-up rounding to two decimal places.

Write `.github/workflows/ci.yml` implementing D-01. Trigger on `pull_request` and on `push` restricted to the `main` branch. Use the standard pull-request trigger only, never the privileged fork-PR variant that grants untrusted code access to repository secrets and a write token; this job needs no secrets and no write access to run the bench suite against untrusted contributor code (threat `T-01-02`). Set workflow-level `permissions` to `contents: read`. The single job runs on `ubuntu-latest`, which per D-17 is the authoritative 4-core baseline machine that PERF-03 names. Steps: `actions/checkout@v4`; `actions/setup-node@v4` pinned to Node 22 with npm caching; `npm ci`; `actions/cache@v4` keyed on `runner.os` plus the `package-lock.json` hash restoring `~/.cache/ms-playwright`; on a cache miss run `npx playwright install --with-deps chromium`, on a hit run `npx playwright install-deps chromium`; `npm run typecheck`; `npm test`; `npm run bench`; finally `actions/upload-artifact@v4` with `if: always()` uploading `.bench/bench-results.json`, which satisfies D-04's machine-readable artifact leg.
  </action>

  <verify>
    <automated>npx vitest run --project unit &amp;&amp; test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'pull_request_target')" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project unit` exits 0 and reports a non-zero passing test count.
    - `tests/perf-budgets.selftest.test.ts` contains an assertion that a deliberately over-budget fixture yields verdict `fail`, and `npx vitest run --project unit -t "over-budget"` exits 0.
    - Temporarily editing `checkBudget` to always return `pass` makes `npx vitest run --project unit` exit non-zero. Revert the edit after confirming.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'pull_request_target'` prints `0`.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'npm run bench'` prints a value of at least 1.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'ubuntu-latest'` prints a value of at least 1.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'contents: read'` prints a value of at least 1.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'upload-artifact'` prints a value of at least 1.
  </acceptance_criteria>

  <done>The unit project asserts the budget gate produces a fail verdict on an over-budget fixture and that every relaxed budget carries a written reason, and a CI workflow runs the bench suite on every pull request and push to main under a read-only token, uploading the results JSON as an artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted pull-request code to GitHub Actions runner | A fork contributor's branch content executes on the runner during `npm ci` and `npm run bench` |
| npm registry to local and CI `node_modules` | Package tarballs and their lifecycle scripts execute at install time on both developer machines and CI |
| bench run to CI artifact store | The results JSON leaves the runner and is retained on GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `package.json` and `package-lock.json` dev dependency tree | high | mitigate | Install only the four packages cleared in the `01-RESEARCH.md` Package Legitimacy Audit, pinned with `--save-exact`; commit `package-lock.json`; CI installs with `npm ci` so a resolved tree cannot drift between local and CI |
| T-01-02 | Elevation of Privilege | `.github/workflows/ci.yml` trigger configuration | high | mitigate | Trigger on the standard pull-request event only; workflow-level `permissions: contents: read`; no secrets referenced anywhere in the job, so untrusted fork code executes with no write token and nothing to exfiltrate. Asserted by the Task 2 verify gate |
| T-01-03 | Tampering, Information Disclosure | npm install-time lifecycle scripts | medium | mitigate | The Rust and WASM toolchain never enters `package.json`; the one package whose npm wrapper fetches a binary from a releases host at install time is installed through rustup and cargo instead, entirely outside the JS dependency tree. Asserted by the Task 1 banned-dependency check |
| T-01-04 | Information Disclosure | `.bench/bench-results.json` uploaded as a CI artifact | low | accept | The JSON contains CPU model, core count, browser user agent, OS and timing figures. No secrets, no credentials and no user data exist in this phase. Machine characteristics of an ephemeral GitHub-hosted runner are not sensitive, and D-18 requires them to be attached to every figure |
| T-01-SC | Tampering | npm installs | high | mitigate | Package-legitimacy gate satisfied: `01-RESEARCH.md` carries a completed `## Package Legitimacy Audit` table with every package Approved. The one `SUS` verdict (`playwright`) is a documented false positive from a latest-publish-date heuristic against an eleven-year-old Microsoft package, verified this research pass against the registry creation date, so no blocking human checkpoint is owed. No `[SLOP]` package is present |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npx vitest run --project unit` exits 0 with a non-zero passing count.
- `npm run bench` exits 0, prints all eight PERF requirement groups, carries one real measured PERF-05 figure, and prints an environment block and its own total runtime.
- `.bench/bench-results.json` parses and is not tracked by git.
- The CI workflow uses the standard pull-request trigger with a read-only token and runs `npm run bench`.
</verification>

<success_criteria>
The measurement chain works end to end on one real metric: one command, run in a real browser, produces a real number, normalized against a real calibration score, compared against a real threshold in the budget file, printed alongside every other locked budget, and enforced by a CI job that a permanent self-test proves will go red on a breach.
</success_criteria>

<output>
Create `.planning/phases/01-performance-spike-and-budget-lock/01-01-SUMMARY.md` when done. Record which browser-to-Node measurement channel was used, and the `BENCH_TOTAL_RUNTIME_CAP_MS` value along with the bench suite's actual measured runtime.
</output>

---
phase: 01-performance-spike-and-budget-lock
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - spike/wasm-microbench/Cargo.toml
  - spike/wasm-microbench/src/lib.rs
  - spike/wasm-microbench/README.md
  - .planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [PERF-01a, PERF-11]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A throwaway Rust microbenchmark built with wasm-pack runs the same branchy per-bar recurrence over the same seeded 25,000-bar series as the JS arm, producing a JS-versus-WASM ratio that is a prediction of what escalating to Rust would actually buy rather than a conservative floor (D-11, D-12)"
    - "The Rust arm's computed output is proven element-wise equal to the JS arm's output before the ratio is trusted, so the two arms are timed on the same work"
    - "The Rust crate is deleted at phase end and leaves no npm dependency, no CI step and no permanent toolchain cost behind (D-13)"
    - "A SPIKE-RESULTS document records the method, the raw numbers, the machine and its core count, and reproduction steps, so the deleted arm's finding survives its code (D-13, criterion 1)"
    - "The plain-JS-versus-WASM decision and the hand-rolled-Canvas-versus-charting-library decision are each recorded in PROJECT.md as a Key Decision citing the measured figure and the machine that settled it (criterion 2)"
    - "Each measured figure is evaluated against the 70 percent escalation trigger, and any trigger produces a deliberate escalation recorded as a Key Decision rather than a relaxed budget (D-20, PERF-01a)"
    - "Every figure in SPIKE-RESULTS is labelled with the machine, the core count and whether it came from the authoritative ubuntu-latest baseline or an informational dev-machine run (D-17, D-18)"

  artifacts:
    - path: "spike/wasm-microbench/src/lib.rs"
      provides: "Rust port of the branchy per-bar recurrence and the seeded series generator, compiled to WASM via wasm-pack. Deleted in Task 2 per D-13"
    - path: ".planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md"
      provides: "Method, raw numbers, machine and core count, reproduction steps, the JS-versus-WASM ratio, both canvas arm figures, and the D-20 escalation evaluation"
    - path: ".planning/PROJECT.md"
      provides: "Two new Key Decisions rows, each citing the measured figure and the machine that settled it"

  key_links:
    - from: "spike/wasm-microbench/src/lib.rs"
      to: "bench/kernel.ts"
      via: "ports the identical per-bar recurrence including the branchy parts, so the ratio compares like with like"
      pattern: "runSpikeBacktest"
    - from: ".planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md"
      to: ".bench/bench-results.json"
      via: "the recorded figures are transcribed from an actual bench run's JSON artifact, not retyped from memory"
      pattern: "bench-results"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md"
      via: "each Key Decision row cites the measured figure and links to the spike results document holding the method and reproduction steps"
      pattern: "01-SPIKE-RESULTS"
---

## Phase Goal

ROADMAP `**Goal:**` for this phase reads: "The architecture is decided by a measured number instead of an estimate, and performance becomes checkable with one command from this point forward."

This is not in the canonical `As a / I want to / so that` user-story format that MVP mode expects. No story has been invented in its place. Run `/gsd mvp-phase 01` if a user-story goal line is wanted before execution.

## Artifacts this phase produces

Every symbol, file, script and constant below is created by Phase 01 and does not exist before it. Nothing in this list is pre-existing project surface.

**New files**

`package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `.gitignore`, `perf-budgets.ts`, `.github/workflows/ci.yml`, `bench/calibration.ts`, `bench/environment-block.ts`, `bench/report.ts`, `bench/global-setup.ts`, `bench/canvas-grid.ts`, `bench/canvas-repaint.bench.test.ts`, `bench/synthetic-data.ts`, `bench/kernel.ts`, `bench/sweep.worker.ts`, `bench/sweep-pool.ts`, `bench/sweep.bench.test.ts`, `bench/kernel.bench.test.ts`, `tests/perf-budgets.selftest.test.ts`, `tests/report.test.ts`, `tests/canvas-grid.test.ts`, `tests/kernel.test.ts`, `spike/wasm-microbench/Cargo.toml`, `spike/wasm-microbench/src/lib.rs`, `spike/wasm-microbench/README.md` (all three deleted at phase end per D-13), `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md`.

**New npm scripts**

`bench`, `test`, `typecheck`.

**New exported symbols**

- `perf-budgets.ts`: `PerfBudget`, `BudgetId`, `RequirementId`, `PERF_BUDGETS`, `ESCALATION_TRIGGER_RATIO`, `BENCH_TOTAL_RUNTIME_CAP_MS`
- `bench/report.ts`: `MeasurementRow`, `Verdict`, `MeasurementSource`, `checkBudget`, `escalationTriggered`, `renderTable`, `assertRunInvariants`, `formatMeasured`, `recordMeasurement`
- `bench/calibration.ts`: `REPEAT_COUNT`, `MIN_MEASUREMENT_MS`, `calibrationScore`, `measureMinOfN`, `normalize`
- `bench/environment-block.ts`: `EnvironmentBlock`, `captureEnvironment`
- `bench/canvas-grid.ts`: `GRID_COLS`, `GRID_ROWS`, `makeGridValues`, `mapValueToRgba`, `paintFillRect`, `paintPutImageData`
- `bench/synthetic-data.ts`: `BAR_COUNT`, `SyntheticSeries`, `makeSeededGbmSeries`
- `bench/kernel.ts`: `SpikeKernelParams`, `SpikeKernelResult`, `runSpikeBacktest`, `SWEEP_COLS`, `SWEEP_ROWS`
- `bench/sweep-pool.ts`: `runSpikeSweep`, `resolveWorkerCount`
- `spike/wasm-microbench/src/lib.rs`: `run_spike_backtest`, `make_seeded_gbm_series`, `bench_full_series` (all deleted at phase end)

**New runtime paths**

`.bench/bench-results.json` (gitignored, uploaded as a CI artifact).

<objective>
Close the phase: get the JS-versus-WASM ratio from a throwaway Rust microbenchmark, delete the Rust and preserve its finding in a spike-results document, then record the two architecture Key Decisions in PROJECT.md each citing the measured figure that settled it, and evaluate every measured figure against the 70 percent escalation trigger.

Purpose: criterion 2 is not met by a decision, it is met by a decision that cites a number. This plan is where the numbers become decisions.
Output: a JS-versus-WASM ratio, `01-SPIKE-RESULTS.md`, two new PROJECT.md Key Decisions rows, an updated STATE.md, and no Rust left in the repository.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md
@.planning/phases/01-performance-spike-and-budget-lock/01-RESEARCH.md
@.planning/phases/01-performance-spike-and-budget-lock/SKELETON.md
@.planning/phases/01-performance-spike-and-budget-lock/01-02-SUMMARY.md
@.planning/phases/01-performance-spike-and-budget-lock/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Throwaway Rust microbenchmark of the same branchy recurrence, yielding the JS-versus-WASM ratio</name>

  <precondition>`cargo --version`, `rustc --version` and `wasm-pack --version` all succeed, and `rustup target list --installed` includes `wasm32-unknown-unknown`</precondition>

  <reversibility rating="reversible">The Rust arm is deliberately throwaway and behind no interface. If the ported recurrence turns out wrong, the fix is local to one file that is deleted in the next task anyway.</reversibility>

  <read_first>
    - `bench/kernel.ts` and `bench/synthetic-data.ts` as created by plan 01-02. The Rust port must reproduce their arithmetic exactly, including the generator algorithm, seed constant, state update and normal transform documented in the `bench/synthetic-data.ts` header comment.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-02-SUMMARY.md` for the measured JS figures the ratio is computed against.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md` decisions D-11, D-12 and D-13, and the "Rationale for JS-first" block, which states why the ratio is expected to be near parity and what would make that expectation wrong.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-RESEARCH.md` sections "Environment Availability" and "Package Legitimacy Audit". The audit records that the npm wrapper for the WASM build tool fetches a platform binary from a releases host during install, which is why the toolchain is installed through rustup and cargo and never enters `package.json`.
  </read_first>

  <files>spike/wasm-microbench/Cargo.toml, spike/wasm-microbench/src/lib.rs, spike/wasm-microbench/README.md</files>

  <action>
If the precondition is unmet, install the toolchain before proceeding: install rustup through the official rustup installer, then run `rustup target add wasm32-unknown-unknown` and `cargo install wasm-pack`. Never install the WASM build tool through npm and never add it to `package.json`, per the Package Legitimacy Audit in RESEARCH.md and D-13's no-permanent-toolchain-cost premise. If the toolchain cannot be installed in this environment, halt and surface the blocker rather than skipping the arm or substituting an estimate; D-11 is a locked decision and the ratio is the input to one of the two Key Decisions this phase owes.

Create `spike/wasm-microbench/` as a standalone Cargo crate. `Cargo.toml` declares `crate-type = ["cdylib"]` and depends on `wasm-bindgen`. `src/lib.rs` ports two things from the JS arm: `make_seeded_gbm_series`, reproducing `makeSeededGbmSeries` exactly per its documented algorithm, and `run_spike_backtest`, reproducing `runSpikeBacktest` exactly including the branchy parts, per D-12. The branchy parts are the contribution schedule, the ruin clamp with its absorbing state, calendar-day financing accrual and expense accrual. Do not port a stripped arithmetic loop: a stripped loop is the case where WASM looks best and would flatter it relative to the real kernel.

Expose `bench_full_series` through `wasm-bindgen` as the timed entry point, taking the same parameter set as `SpikeKernelParams` and returning the final value and ruin flag so the result can be compared against the JS arm.

Build with `wasm-pack build --target web --release`. Drive the built module from a small standalone HTML or module harness run under the same headless Chromium the bench suite uses, timing it with the same minimum-of-5 methodology and the same calibration reference loop as `bench/calibration.ts`, so the ratio is not contaminated by a different timing statistic. Time both a single 25,000-bar backtest and, if the harness permits it cheaply, a full 10,000-cell sweep on a single thread.

Before recording any ratio, assert the Rust arm's final value and ruin flag are element-wise equal to the JS arm's for the same seed and the same parameters, within floating-point tolerance. State the tolerance used. A ratio computed against a Rust port that produces different numbers measures nothing.

Write `spike/wasm-microbench/README.md` recording the exact commands used to install the toolchain, build, and run, so Task 2 can transcribe reproduction steps into the permanent spike-results document before the crate is deleted.
  </action>

  <verify>
    <automated>cd spike/wasm-microbench &amp;&amp; cargo build --target wasm32-unknown-unknown --release &amp;&amp; wasm-pack build --target web --release</automated>
  </verify>

  <acceptance_criteria>
    - `cargo --version`, `rustc --version` and `wasm-pack --version` all exit 0.
    - `wasm-pack build --target web --release` in `spike/wasm-microbench/` exits 0 and produces a `pkg/` directory containing a `.wasm` file.
    - Running the harness prints the Rust arm's final value and ruin flag for a named seed and parameter set, and those match the JS arm's values for the same inputs within the stated tolerance.
    - Running the harness prints a single-backtest timing in milliseconds using minimum of 5 repeats and the same calibration normalization as the JS arm.
    - `node -e "const p=require('./package.json');const d=Object.keys({...p.dependencies,...p.devDependencies});if(d.some(k=>k.startsWith('wasm')))process.exit(1)"` exits 0, proving no WASM tooling entered the npm dependency tree.
    - `spike/wasm-microbench/README.md` records the toolchain install, build and run commands verbatim.
    - `spike/wasm-microbench/src/lib.rs` contains the contribution schedule, the ruin clamp with its absorbing state, the calendar-day financing term and the expense-ratio term, matching `bench/kernel.ts`.
  </acceptance_criteria>

  <done>A throwaway Rust WASM microbenchmark of the same branchy recurrence over the same seeded series produces numbers proven equal to the JS arm's and a calibration-normalized timing, giving a JS-versus-WASM ratio that predicts what escalating to Rust would buy.</done>
</task>

<task type="auto">
  <name>Task 2: Record the spike results permanently and delete the Rust crate</name>

  <read_first>
    - `spike/wasm-microbench/README.md` from Task 1, for the reproduction steps that must survive the crate's deletion.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-02-SUMMARY.md` and `01-03-SUMMARY.md`, for the measured PERF-02, PERF-03 and both canvas arm figures.
    - `.bench/bench-results.json` from the most recent bench run, which is the source of the numbers transcribed into the document. Transcribe from the artifact rather than retyping remembered values.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-CONTEXT.md` decisions D-13, D-17, D-18 and D-20.
    - `perf-budgets.ts`, specifically `ESCALATION_TRIGGER_RATIO`, for the 70 percent evaluation.
  </read_first>

  <files>.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md, spike/wasm-microbench/Cargo.toml, spike/wasm-microbench/src/lib.rs, spike/wasm-microbench/README.md</files>

  <action>
Write `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` as the permanent record D-13 requires. It carries five sections.

**Method.** How each figure was produced: the seeded generator and its parameters, the branchy recurrence and its day-count convention, the Worker pool size and chunk count, the calibration reference loop and its scaling, minimum of 5 repeats, and the equivalence assertions that preceded each timing.

**Raw numbers.** A table of every measured figure, raw and calibration-normalized, for the single backtest over 25,000 bars, the 10,000-cell Worker-pool sweep, both canvas arms, and the Rust WASM arm. Every figure carries the machine, its core count, the browser version, the OS, the calibration score, and whether it came from the authoritative `ubuntu-latest` baseline or an informational dev-machine run, per D-17 and D-18. An unlabelled figure in this table is a defect.

**JS versus WASM ratio.** The measured ratio, the tolerance at which the two arms' outputs were proven equal, and whether the result supports or contradicts the "Rationale for JS-first" block in CONTEXT.md. If the ratio contradicts it, say so plainly rather than reconciling it away; that block was written as a prediction and this is its test.

**Escalation evaluation (D-20).** For each measured figure, its value as a percentage of its budget, and whether it crosses `ESCALATION_TRIGGER_RATIO`. For every figure that crosses, state the deliberate escalation chosen (Worker pool tuning, the WASM ratio from this document, a coarser default grid) and note that it is recorded as a Key Decision in Task 3. Do not relax any budget: PERF-01a makes an unreachable target an architecture escalation, never an automatic relaxation.

**Reproduction steps.** The verbatim commands from `spike/wasm-microbench/README.md` for installing the toolchain, building and running the WASM arm, plus the `npm run bench` invocation for the JS and canvas arms. Someone reading this document after the crate is deleted must be able to rebuild the arm from scratch.

Then delete the entire `spike/` directory, per D-13. Confirm nothing outside `spike/` referenced it, that no npm dependency was added, and that no CI step invokes it. The toolchain leaves with the code and there is no permanent CI cost.
  </action>

  <verify>
    <automated>test ! -e spike &amp;&amp; test -f .planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md &amp;&amp; npm run typecheck &amp;&amp; npx vitest run --project unit</automated>
  </verify>

  <acceptance_criteria>
    - `test ! -e spike` exits 0, proving the crate is gone.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` exists and contains all five section headings: Method, Raw numbers, JS versus WASM ratio, Escalation evaluation, Reproduction steps.
    - Every row of the Raw numbers table names a machine and a core count. Confirm by reading the table; a row with an empty machine or core-count cell fails this criterion.
    - The Escalation evaluation section states a percentage of budget for each of the measured figures and an explicit crossed or not-crossed verdict against the 70 percent trigger.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'cargo'` prints `0`.
    - `node -e "const p=require('./package.json');const d=Object.keys({...p.dependencies,...p.devDependencies});if(d.some(k=>k.startsWith('wasm')))process.exit(1)"` exits 0.
    - `npm run typecheck` and `npx vitest run --project unit` both exit 0, proving deleting the crate broke nothing.
  </acceptance_criteria>

  <done>The spike's method, raw labelled numbers, JS-versus-WASM ratio, escalation evaluation and reproduction steps are recorded permanently, and no Rust, no Cargo file and no WASM toolchain reference remains in the repository or in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Record the two architecture Key Decisions in PROJECT.md, each citing its measured figure</name>

  <reversibility rating="costly">Records the D-19 budget lock and the two architecture decisions that every later phase inherits. Undoing the plain-JS choice means building a second compute implementation and re-deriving every figure denominated against it; undoing the hand-rolled-canvas choice means replacing the renderer Phase 6 designs against and Phase 7 builds. Both were chosen by the developer during discuss-phase, so no checkpoint re-asks them.</reversibility>

  <read_first>
    - `.planning/PROJECT.md`, specifically the existing `## Key Decisions` table and its three-column format (Decision, Rationale, Outcome), so the new rows match the established format exactly rather than inventing a fourth column.
    - `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` from Task 2, the source of every number cited.
    - `.planning/STATE.md`, specifically the `## Accumulated Context` section's Decisions list and its Blockers/Concerns entry for Phase 1, which restates the PERF-01a locking rule and must be updated now that the budgets are locked.
    - `.claude/CLAUDE.md` section "Q2 - Charting", the documented grounds on which each charting library was rejected, which the canvas Key Decision cites alongside the measured hand-rolled figure per D-14.
    - `.planning/REQUIREMENTS.md` PERF-01a, for the exact wording of the locking rule the decisions are recorded under.
  </read_first>

  <files>.planning/PROJECT.md, .planning/STATE.md</files>

  <action>
Append two rows to the `## Key Decisions` table in `.planning/PROJECT.md`, using the existing three-column format and a scoped edit that leaves every existing row untouched.

The first row records plain JS with a Worker pool over WASM. Its Rationale cell names the measured 10,000-cell sweep wall-clock figure, the machine and core count it was measured on, the figure as a percentage of the 1000 millisecond budget, and the measured JS-versus-WASM ratio from the microbenchmark. Its Outcome cell links to `01-SPIKE-RESULTS.md`. The decision is settled by the number, not by the rationale block in CONTEXT.md; if the number contradicts that block, the row says so.

The second row records the hand-rolled Canvas 2D renderer over a charting library. Its Rationale cell names the measured 10,000-cell repaint figure for the winning arm, the machine and core count, the figure as a percentage of the 16 millisecond budget, and cites the prior findings in `.claude/CLAUDE.md` section Q2 for why no charting library was benchmarked, per D-14. Its Outcome cell links to `01-SPIKE-RESULTS.md`.

If the Escalation evaluation in `01-SPIKE-RESULTS.md` recorded any figure crossing the 70 percent trigger, append a third row recording the deliberate escalation chosen and why, per D-20 and PERF-01a. If nothing crossed, add no third row.

Then update `.planning/STATE.md` with scoped edits only. Add the new decisions to the `## Accumulated Context` Decisions list in the same bracketed-source style the existing entries use. Replace the Phase 1 entry under Blockers/Concerns, which currently says the PERF-02 through PERF-09 thresholds are provisional, with a statement that all eight budgets are now locked at their perception anchors per D-19, that every threshold equals its anchor so no relaxation reason is owed, and that any future relaxation requires a Key Decision under PERF-01a.

Do not rewrite either file wholesale. Use scoped replacements so no unrelated section is disturbed.
  </action>

  <verify>
    <automated>grep -c '01-SPIKE-RESULTS' .planning/PROJECT.md &amp;&amp; npx vitest run --project unit</automated>
    <human-check>
      1. Open `.planning/PROJECT.md` and read the two new Key Decisions rows. Each must name a specific measured number and the machine it was measured on, not a qualitative claim.
      2. Open `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` and confirm every number cited in PROJECT.md appears there with its machine and core count.
      3. Push a branch and open a pull request. Confirm the CI job runs and passes.
      4. On that branch, temporarily lower one threshold in `perf-budgets.ts` below its measured value, push, and confirm the CI check goes red on the budget assertion. Revert the change before merging. This is the one-time live demonstration of phase criterion 5; the permanent proof is the D-09 self-test that runs on every pull request forever.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c '01-SPIKE-RESULTS' .planning/PROJECT.md` prints a value of at least 2, one per Key Decision row.
    - The `## Key Decisions` table in `.planning/PROJECT.md` has exactly two more rows than before this task (or three, if an escalation row was owed), and every pre-existing row is byte-identical to its prior content.
    - Each new Rationale cell contains at least one numeric millisecond figure and a named machine.
    - `.planning/STATE.md` Blockers/Concerns no longer describes the PERF-02 through PERF-09 thresholds as provisional, and states that all eight are locked at their perception anchors.
    - `npx vitest run --project unit` exits 0.
    - `npm run bench` exits 0.
  </acceptance_criteria>

  <done>Both architecture decisions are recorded in PROJECT.md as Key Decisions citing the measured figures and machines that settled them, any escalation triggered by the 70 percent rule is recorded as its own Key Decision rather than absorbed into a relaxed budget, and STATE.md reflects that the budgets are locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io and GitHub releases to the developer machine | The Rust toolchain, wasm-pack and the crate's dependencies are fetched over the network during Task 1 |
| npm registry to local and CI `node_modules` | Unchanged by this plan, but this plan is where a WASM toolchain would leak into the dependency tree if handled carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering, Information Disclosure | WASM build tooling installation | medium | mitigate | The WASM build tool is installed through rustup and cargo, never through npm, so the npm wrapper's install-time binary fetch from a releases host never runs. Asserted by the Task 1 and Task 2 checks that no dependency key begins with the WASM tool prefix, and by the Task 2 check that no CI step invokes cargo |
| T-01-08 | Tampering | `spike/wasm-microbench` Rust dependency tree | medium | accept | The crate depends on `wasm-bindgen`, the canonical Rust-to-JS binding crate named in RESEARCH.md. The crate is local, never published, never shipped to a browser outside the local harness, and deleted in Task 2, so its dependency tree has a lifetime of one task and reaches no production artifact. Accepted on that basis |
| T-01-09 | Repudiation | `01-SPIKE-RESULTS.md` and the PROJECT.md Key Decisions rows | medium | mitigate | A figure without provenance cannot be challenged or reproduced later. Every row of the Raw numbers table carries the machine, core count, browser version, OS, calibration score and CI flag per D-18, numbers are transcribed from the bench JSON artifact rather than retyped, and reproduction steps survive the crate's deletion |
| T-01-SC | Tampering | npm installs | high | mitigate | This plan adds no npm package. The dependency set remains the four packages cleared by the `01-RESEARCH.md` Package Legitimacy Audit, all Approved, with no `[SLOP]` entry and no unresolved `[ASSUMED]` or `[SUS]` verdict requiring a blocking human checkpoint |
</threat_model>

<verification>
- The Rust arm built, ran, and produced numbers proven equal to the JS arm before its ratio was recorded.
- `test ! -e spike` exits 0.
- `01-SPIKE-RESULTS.md` carries all five sections, and every figure names its machine and core count.
- `.planning/PROJECT.md` carries two new Key Decisions rows, each citing a measured millisecond figure and a named machine, each linking to the spike results document.
- `.planning/STATE.md` records the budgets as locked rather than provisional.
- `npm run typecheck`, `npx vitest run --project unit` and `npm run bench` all exit 0 after the crate's deletion.
</verification>

<success_criteria>
The architecture is decided by measured numbers: the sweep figure and the JS-versus-WASM ratio settle plain JS over WASM, and the repaint figure settles hand-rolled Canvas over a charting library. Both are written into PROJECT.md citing those numbers and the machines they came from, the throwaway Rust is gone, and its finding survives in a document that can be reproduced from scratch.
</success_criteria>

<output>
Create `.planning/phases/01-performance-spike-and-budget-lock/01-04-SUMMARY.md` when done. Record the JS-versus-WASM ratio, whether it supported or contradicted the JS-first rationale in CONTEXT.md, every figure's percentage of its budget, whether any figure crossed the 70 percent escalation trigger, and what escalation was chosen if so.
</output>

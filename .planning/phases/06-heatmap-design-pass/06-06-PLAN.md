---
phase: 06-heatmap-design-pass
plan: 06
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - .planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md
  - .planning/PROJECT.md
autonomous: true
requirements: [VIZ-05, VIZ-07, VIZ-10]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "06-HEATMAP-SPEC.md carries the full implementable treatment, chosen form, palette stops, symlog domain, categorical cell rendering, legend structure, caveat copy and placement, and per-form geometry, as the contract Phase 7 is planned and verified against (D-26)."
    - "Every numeric value in the spec is copied from the code or from a recorded measurement, never re-derived from prose, and each names the module or the bench row it came from (D-27)."
    - "A PROJECT.md Key Decision records which form won, which three lost and why, in the same table as the two Phase 1 architecture decisions (D-26)."
    - "The spec states that PERF-03's 'coarser default grid' lever remains unspent, so Phase 7 does not inherit an escalation whose designated lever appears to have been used (finding F-04)."
    - "The spec points Phase 7 at D-19's incomplete-hold treatment as the solved precedent for VIZ-09's short-horizon strip, rather than leaving Phase 7 to start from PITFALLS E5 alone (finding F-01)."
    - "The spec records the fixture's measured ruin count, incomplete count and clipped-cell counts as facts, and states plainly what those counts mean for how far the design pass could judge the ruin hatch."
    - "The spec states that a cell hover readout remains an open Phase 7 decision and was excluded from the mockups only, so D-07 is not misread as declining it."
  artifacts:
    - .planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md
    - .planning/PROJECT.md
  key_links:
    - "The spec names src/colorscale/value-to-color.ts as the authority for the mapping, so Phase 7 imports the function rather than re-deriving the palette from the spec's prose (D-27)."
    - "The spec names the committed sweep fixture and its four mockups as the record the choice was made against, all still importing the live colour function (D-28, D-29)."
  prohibitions:
    - "The written rejection reasons must not be composed or softened at spec-writing time; they are copied from the recorded decision in 06-05-SUMMARY.md."
    - "No numeric value in the spec may be an estimate: each is copied from code or from .bench/bench-results.json and names its source."
    - "No copy in the spec may describe the grid as 10,000 independent backtests or as independent trials."
---

<objective>
Write the two artifacts that bind Phase 7 (D-26): `06-HEATMAP-SPEC.md`, the full implementable
treatment, and a PROJECT.md Key Decision carrying the one-paragraph account of which form won and
why the other three lost.

Purpose: each artifact does the job it is good at. A spec alone has no natural home for the
rejection reasoning; a Key Decision alone would force Phase 7 to re-derive palette stops and hatch
geometry from a paragraph, and a re-derived colour mapping is exactly how a spec silently drifts
from what shipped.

Output: the phase's two binding artifacts, plus three findings recorded so Phase 7 does not
inherit them as surprises.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-heatmap-design-pass/06-CONTEXT.md
@.planning/phases/06-heatmap-design-pass/06-UI-SPEC.md
@.planning/phases/06-heatmap-design-pass/06-01-SUMMARY.md
@.planning/phases/06-heatmap-design-pass/06-02-SUMMARY.md
@.planning/phases/06-heatmap-design-pass/06-03-SUMMARY.md
@.planning/phases/06-heatmap-design-pass/06-04-SUMMARY.md
@.planning/phases/06-heatmap-design-pass/06-05-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md` | new file | D-26, the contract Phase 7 is planned and verified against |
| A new row in `.planning/PROJECT.md` Key Decisions | modification | D-26, alongside the two Phase 1 architecture decisions |

<tasks>

<task type="auto">
  <name>Task 1: 06-HEATMAP-SPEC.md, the implementable treatment</name>
  <reversibility rating="costly">This document is what Phase 7's plan-checker and verifier test against, so changing the treatment after Phase 7 starts invalidates its verification. Undoing it costs a replan of Phase 7, not a data migration, which is why it is rated costly rather than one-way.</reversibility>
  <files>.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md</files>
  <read_first>
    - `.planning/phases/06-heatmap-design-pass/06-05-SUMMARY.md` (the winning form id and the three rejection reasons, to be copied verbatim)
    - `src/colorscale/value-to-color.ts` (the authoritative RAMP_STOPS, DOMAIN_LOG_MIN, DOMAIN_LOG_MAX, RUIN_BASE_RGBA, INCOMPLETE_RGBA, LEGEND_TICK_MULTIPLES values to copy)
    - `src/data/sweep-fixture-format.ts` (the fixture layout and its meta fields)
    - `.planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts` (VIZ10_CAVEAT_SENTENCES, the legend structure, the hatch pattern geometry)
    - the winning form's module under `.planning/phases/06-heatmap-design-pass/mockups/forms/` (its exact geometry constants)
    - `.bench/bench-results.json` (the four normalized repaint figures and the environment block's machine and core count)
    - `.planning/phases/06-heatmap-design-pass/06-01-SUMMARY.md` (the six printed fixture counts)
    - `.planning/PROJECT.md` section Key Decisions (the PERF-03 D-20 escalation and its three named levers, for finding F-04)
    - `.planning/research/PITFALLS.md` section E5 (the short-horizon edge artifact VIZ-09 owns, for finding F-01)
    - `.planning/phases/06-heatmap-design-pass/06-UI-SPEC.md` (the constraint table this spec resolves into concrete values)
  </read_first>
  <action>
Write `06-HEATMAP-SPEC.md` as the contract Phase 7 is planned and verified against. Every numeric
value is COPIED from the code or from `.bench/bench-results.json` and names its source file or
bench row inline; nothing in this document is re-derived from prose, because a re-derived mapping
is exactly how a spec drifts from what shipped (D-27).

Sections, in this order:

1. **The chosen form.** Name the winning form id and title. State that the full implementable
   record is this document plus the graduated modules it names, not this document alone.
2. **Why the other three lost.** Copy the three rejection reasons from `06-05-SUMMARY.md`
   VERBATIM, each with the observation it cites. Do not soften, expand or re-argue them.
3. **The value-to-colour mapping.** State that
   `src/colorscale/value-to-color.ts` is the AUTHORITY and that Phase 7 imports `valueToColor`
   rather than reimplementing it (D-27). Then record, as a table copied from that module: scale
   type diverging with the midpoint at exactly `t = 0.5` where `log10(1.0)` normalises (D-13);
   the transform as symmetric log about 1.0x (D-14); the fixed domain endpoints `DOMAIN_LOG_MIN`
   and `DOMAIN_LOG_MAX` with their multiple equivalents, clipped beyond and never fitted per sweep
   (D-16); every entry of `RAMP_STOPS` with its `t` and its hex value; the interpolation space; and
   the single background-neutral palette shared by both themes (D-15).
4. **Categorical cells.** `RUIN_BASE_RGBA` with the hatch geometry (angle, period, stroke width)
   and the fact that the hatch is drawn as a display-space `CanvasPattern` clipped to the union of
   ruined cells rather than per cell, which is what makes it legible independent of cell size and
   which resolves finding F-03 (D-18). `INCOMPLETE_RGBA` as a flat, textureless, out-of-scale
   neutral carrying no value (D-20). State that texture means exactly one thing and is reserved
   for ruin alone.
5. **Legend.** The continuous ramp with the five `LEGEND_TICK_MULTIPLES` at their true symlog
   positions, the emphasised 1.00x breakeven tick, the two end labels stating the fixed domain and
   its clipping, and the two DETACHED categorical swatches with their exact label strings and the
   `var(--space-sm)` gap that is itself the detached signal (D-24, D-25). Record that tick labels
   come from `src/metrics/format.ts` `formatMultiple` and therefore read `0.10x` through `10.00x`,
   and note explicitly that `06-UI-SPEC.md`'s bare-integer copy examples were illustrative of which
   values get ticks, so Phase 7 does not add a second formatter to chase them.
6. **The overlapping-windows caveat.** The exact two sentences from `VIZ10_CAVEAT_SENTENCES`, the
   placement directly under the grid and above the legend, the Body role at 14px and 1.5
   line-height, the requirement that it sit inside Phase 4's D-20 screenshot region, and the rule
   that it is never smaller or lower-contrast than the legend labels (D-21, D-22, D-23).
7. **Geometry.** The winning form's exact geometry constants, plus the other three forms' for the
   record. State that per D-12 the four criterion-4 figures were taken at four different geometries
   and are each individually gated against 16ms rather than being a ranking (finding F-02).
8. **Measured figures.** All four normalized repaint figures with the machine and core count from
   the bench environment block, each naming its `.bench/bench-results.json` row or info-line id.
9. **What the fixture could and could not show.** Copy the six measured counts from
   `06-01-SUMMARY.md`: `ruinedCount`, `incompleteCount`, `minMultiple`, `maxMultiple`,
   `clippedBelowCount`, `clippedAboveCount`. Then state plainly what they mean. In particular: D-09
   fixes the entry axis to SPX total return's strict tier, which the manifest records as
   1988-01-05 to 2026-08-14, and D-08 caps leverage at 5x. If `ruinedCount` is zero, say so and say
   why, that no daily return in that window reaches the roughly -20% a 5x position needs to be
   driven to zero, and record that D-18's hatch was therefore judged from the legend swatch and not
   from an in-field region. If `incompleteCount` is large, state the exact figure and the exact
   share of the 200 columns it represents, since a fixed 20-year hold against a 38-year strict span
   makes the incomplete-hold region roughly half the axis by construction rather than an edge case.
   These are recorded as facts about what the design pass could judge, not as defects.
10. **What Phase 7 inherits.** Three explicit carry-forwards:
    - **Finding F-04:** PROJECT.md's PERF-03 D-20 escalation named "a coarser default grid" as one
      of three remaining levers and assigned it to Phase 6. D-08 and D-09 kept the grid at 200 by
      50, so the lever is UNSPENT and still available to Phase 7. Say this outright, or Phase 7
      inherits an escalation whose designated lever appears to have been used.
    - **Finding F-01:** D-10 chose a fixed holding period, so VIZ-09's hold-to-today short-horizon
      strip has no mockup behind it. D-19's incomplete-hold treatment, an out-of-scale de-emphasised
      region at the recent edge excluded from the colour scale, is the solved precedent Phase 7
      should start from rather than starting from PITFALLS E5 alone.
    - **The open hover question:** a cell hover readout was excluded from the MOCKUPS by D-07 only
      and remains an open Phase 7 decision. State that it must not be read as declined, and record
      the argument in its favour: the dense grid's cells are below comfortable pointing precision.
11. **Out of scope for this document.** Name what Phase 7 still decides: the shipped leverage range
    and entry span (D-08 and D-09 narrowed both for the design pass only), the sweep mode toggle,
    the metric toggle, ruin rendering under hold-to-today, pan and zoom, and progressive paint.

Write in the project's own voice: terse, no em dashes, no marketing language.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md &amp;&amp; grep -c 'value-to-color.ts' .planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md</automated>
    <human-check>Read the spec beside the four mockups and confirm a Phase 7 planner could implement the winning treatment from it without opening the mockups, and that the three rejection reasons match what the comparison page actually shows.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md` exists and contains all eleven sections named above.
    - The spec names `src/colorscale/value-to-color.ts` as the authority for the mapping.
    - Every hex value in the spec's palette table appears verbatim in `src/colorscale/value-to-color.ts`: for each stop, `grep -c '<hex>' src/colorscale/value-to-color.ts` is at least 1.
    - The spec contains the literal strings `Ruined: position reached zero` and `Holding period incomplete`.
    - The spec contains the exact two caveat sentences from `VIZ10_CAVEAT_SENTENCES`.
    - The spec contains `ruinedCount`, `incompleteCount`, `clippedBelowCount` and `clippedAboveCount` with numeric values matching `06-01-SUMMARY.md`.
    - The spec contains the literal `coarser default grid` and the word `unspent` in the same section (finding F-04).
    - The spec contains `VIZ-09` and `D-19` in the same section (finding F-01).
    - The spec states the hover readout is an open Phase 7 decision: it contains `hover` and `open` in the same sentence.
    - `grep -v '^>' .planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md | grep -ci 'independent backtests\|independent trials'` counts only the occurrences inside the quoted caveat sentence.
    - The spec contains no em dash character.
    - Each of the four normalized repaint figures in the spec matches the corresponding value in `.bench/bench-results.json`.
  </acceptance_criteria>
  <done>Phase 7 has a single document carrying the chosen treatment at implementable precision, every number copied from its source, the rejection reasoning preserved verbatim, and three findings recorded so none of them arrives as a surprise.</done>
</task>

<task type="auto">
  <name>Task 2: The PROJECT.md Key Decision</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` section Key Decisions (the existing row format and the two Phase 1 architecture decisions this row sits beside)
    - `.planning/phases/06-heatmap-design-pass/06-05-SUMMARY.md` (the winner and the three rejection reasons)
    - `.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md` (as written by Task 1)
  </read_first>
  <action>
Add ONE row to `.planning/PROJECT.md`'s Key Decisions table, matching the existing row format
exactly. Use a scoped `Edit`, never a whole-file `Write`: the table carries every prior decision
and a full-file rewrite would put all of them at risk.

The row carries the one-paragraph account: which form won, which three lost, and the reason each
lost, in the same table as the two Phase 1 architecture decisions so the reasoning survives in the
project record rather than only in a phase directory (D-26). Cite the measured evidence the choice
rested on, the four normalized repaint figures with their machine and core count, and point at
`06-HEATMAP-SPEC.md` for the implementable detail. Do not restate the palette stops or the hatch
geometry here: the spec is where those live, and duplicating them creates two sources that can
drift.

Do not soften the rejection reasons and do not add reasons that were not recorded at decision time.
  </action>
  <verify>
    <automated>grep -c 'HEATMAP-SPEC' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` gains exactly one new row in the Key Decisions table: `git diff --stat .planning/PROJECT.md` shows a small addition and no deletions outside that table row.
    - The new row names the winning form and all three losing forms.
    - The new row references `06-HEATMAP-SPEC.md`.
    - The new row contains at least one normalized repaint figure with its machine and core count.
    - The new row contains no em dash character.
    - Every Key Decisions row that existed before this task still exists afterwards: `git diff .planning/PROJECT.md` shows no removed line.
  </acceptance_criteria>
  <done>The choice of heatmap treatment sits in the project record beside the two Phase 1 architecture decisions, with its evidence and a pointer to the implementable spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase summaries to the binding spec | Decision text and measured figures cross from per-plan summaries into the document Phase 7 is verified against |
| bench artifact to the spec | Normalized performance figures cross from `.bench/bench-results.json` into a human-readable record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-16 | Tampering | Numeric values copied into `06-HEATMAP-SPEC.md` | high | mitigate | Every palette hex, every repaint figure and every fixture count carries an acceptance criterion asserting it matches its source file or bench row, so a spec that drifts from the code fails verification rather than silently binding Phase 7 to a wrong number |
| T-06-17 | Repudiation | Rejection reasons at spec-writing time | high | mitigate | The reasons are copied verbatim from `06-05-SUMMARY.md`, which was written at decision time, and the four losing mockups stay committed importing the live colour function so any reader can re-render the comparison and check |
| T-06-18 | Tampering | `.planning/PROJECT.md` Key Decisions table | medium | mitigate | The row is added with a scoped `Edit`, never a whole-file `Write`, and an acceptance criterion asserts `git diff` shows no removed line, so a prior decision cannot be silently dropped |
| T-06-SC | Tampering | npm, pip and cargo installs | high | mitigate | No install task exists in this plan; this plan writes documentation only |
</threat_model>

<verification>
- `06-HEATMAP-SPEC.md` exists with all eleven sections and every numeric value matching its source.
- `.planning/PROJECT.md` carries exactly one new Key Decisions row and loses none.
- No em dash appears in either artifact.
- `npm run test && npm run bench && npm run typecheck` still pass, since this plan changes no code.
</verification>

<success_criteria>
- Phase 7 can be planned and verified against a single document without reading the mockups.
- The reasoning behind the choice survives in the project record, not only in a phase directory.
- The three findings that would otherwise have arrived as Phase 7 surprises, the unspent PERF-03 lever, VIZ-09's missing mockup and the still-open hover question, are recorded as carry-forwards.
</success_criteria>

<output>
Create `.planning/phases/06-heatmap-design-pass/06-06-SUMMARY.md` when done.
</output>

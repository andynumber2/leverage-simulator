---
phase: 03-simulation-kernel-and-the-upro-tqqq-gate
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/kernel/fixtures.ts
  - tests/kernel/pitfalls-a.test.ts
  - tests/kernel/ruin.test.ts
autonomous: true
requirements: [SIM-01, SIM-02, SIM-03, SIM-04, SIM-05, SIM-12]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every numbered item in PITFALLS.md section A (A1 through A12) is either covered by a named unit test in this phase or carries an explicit, asserted disposition stating why it is not a kernel-code concern (SIM-12)."
    - "A run at leverage 1 with expense ratio 0 and financing spread 0 reproduces the unlevered compounded bundled series to a maximum relative deviation of 1e-9 across every bar (SIM-04, D-06, D-07)."
    - "A leveraged run's final value differs from `initialInvestment * (1 + leverage * totalPeriodReturn)` on any volatile window, proving leverage is compounded daily and never applied to a cumulative return (SIM-02, PITFALLS A1)."
    - "A run at leverage 0.5 produces a financing term that increases the position's value rather than decreasing it, and the credit is not clamped to zero (D-08)."
    - "The expense ratio charged over a full calendar year of bars sums to the annual expense ratio regardless of whether that year carried 249 or 252 trading bars (D-02, PITFALLS A4)."
    - "A bar whose value would cross zero after the return and both cost terms writes exactly 0, sets the ruin flag, records its index in `ruinBarIndex`, and no subsequent return or contribution moves the value off 0 (SIM-05, D-22, D-23, D-24)."
    - "Contributions scheduled at or after the ruin bar are dropped and their total is reported in `droppedContributionsTotal` rather than silently discarded (D-21)."
    - "No element of `outValue` is ever negative on any input, including inputs that drive the position far below zero before the clamp (D-24)."
    - "A bar whose `calendarDaysElapsed` is at or above `LONG_GAP_FLAG_MIN_DAYS` sets `outLongGap` to 1 and is counted in `longGapBarCount`; an ordinary 3-day or 4-day holiday weekend does not (D-04)."
  prohibitions:
    - requirement_id: SIM-05
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT represent ruin as a numeric extreme that a downstream consumer has to infer from a value near zero. Ruin is a categorical outcome carried by the flag and the bar index, and any test that asserts ruin by comparing a value against a small epsilon is itself the defect (D-22)."
  artifacts:
    - path: "tests/kernel/fixtures.ts"
      provides: "Shared `makeKernelSeries` / `baseKernelParams` / `makeOutputs` builders for hand-built kernel inputs"
      exports: ["makeKernelSeries", "baseKernelParams", "makeOutputs"]
    - path: "tests/kernel/pitfalls-a.test.ts"
      provides: "SIM-12's correctness checklist: PITFALLS A1, A2, A4, A8, A10, A11 plus D-03, D-05, D-06, D-07, D-08"
      min_lines: 150
    - path: "tests/kernel/ruin.test.ts"
      provides: "PITFALLS A7 as tests: clamp, flag, absorbing state, dropped contributions, long-gap flag, and the A-checklist disposition table"
      min_lines: 90
  key_links:
    - from: "tests/kernel/pitfalls-a.test.ts"
      to: "src/kernel/backtest.ts"
      via: "direct import of runBacktest, the only kernel under test"
      pattern: "runBacktest"
    - from: "tests/kernel/pitfalls-a.test.ts"
      to: "src/data/kernel-inputs.ts"
      via: "loadBundleFromDisk for the SIM-04 real-series exactness assertion"
      pattern: "loadBundleFromDisk"
---

## Phase Goal (verbatim from ROADMAP.md)

> The simulation is demonstrably right against real leveraged-ETF history, before a single pixel
> of UI is built on top of it

The ROADMAP `**Goal:**` line is not in `As a … I want to … so that …` form, so no user story was
invented. Run `/gsd mvp-phase 03` if a user-story framing is wanted.

<objective>
Turn PITFALLS.md section A into the executable correctness checklist SIM-12 requires, passing
before any UI code exists.

Purpose: section A's own header states it is written to double as this kernel's correctness
checklist. This plan makes that literal. Every row becomes either a named test or an asserted
disposition, so a later reader can see at a glance which pitfalls the code defends against and
which are data-sourcing or documentation concerns owned elsewhere.

Output: `tests/kernel/fixtures.ts`, `tests/kernel/pitfalls-a.test.ts`, `tests/kernel/ruin.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simulation-kernel-and-the-upro-tqqq-gate/03-CONTEXT.md
@.planning/phases/03-simulation-kernel-and-the-upro-tqqq-gate/03-01-SUMMARY.md
@.planning/research/PITFALLS.md
@tests/kernel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PITFALLS A1, A2, A4, A8, A10 and A11 as unit tests, including the 1x exactness invariant</name>
  <files>tests/kernel/fixtures.ts, tests/kernel/pitfalls-a.test.ts</files>
  <read_first>
    - `.planning/research/PITFALLS.md` section A, the whole section — A1 through A12 including each row's own suggested detection test. This is the plan's primary source; read it before writing a single assertion.
    - `src/kernel/backtest.ts` and `src/kernel/backtest.types.ts` — the kernel under test as plan `03-01` actually built it, including `LONG_GAP_FLAG_MIN_DAYS`, `FINANCING_DAY_COUNT_BASIS` and `EXPENSE_DAY_COUNT_BASIS`.
    - `src/data/kernel-inputs.ts` — `loadBundleFromDisk` and `buildKernelInputs`, needed for the SIM-04 assertion against the real bundled series.
    - `tests/kernel.test.ts` — the existing spike-kernel tests. `makeFixedSeries`, `baseParams`, the 1x invariant case, the 3-day-gap case and the repeat-call case are the direct structural templates. Leave this file untouched: it still guards `bench/kernel.ts`, which PERF-03 depends on.
    - `.planning/phases/03-simulation-kernel-and-the-upro-tqqq-gate/03-CONTEXT.md` — D-01 through D-08 in full, especially D-06's derivation of the 1e-9 figure.
    - `package.json` — confirm `fast-check` 4.9.0 is an installed devDependency before importing it.
  </read_first>
  <behavior>
    - A1: a leveraged run over a volatile window and `initialInvestment * (1 + leverage * totalPeriodReturn)` differ materially, and the sign of the difference is negative for a symmetric up-then-down path (volatility drag).
    - A2: at leverage 3 with a nonzero short rate, the run's value is strictly lower than the same run with the rate set to 0, and the gap scales linearly with `(leverage - 1)`.
    - A4: a synthetic year of bars whose `calendarDaysElapsed` sums to 365 charges an expense drag of exactly the annual expense ratio, whether that year is built from 249 bars or 252 bars.
    - A8: a 3-calendar-day gap costs exactly 3x a 1-calendar-day gap's financing; a 12-day gap costs exactly 12x.
    - A10 / SIM-04: leverage 1, expense ratio 0, spread 0 against the real bundled SPX price-return series reproduces the directly compounded series with a maximum relative deviation at or below 1e-9 across every bar.
    - A11: the same 1x run's terminal value tracks a direct `level[last] / level[first]` ratio within the same 1e-9 relative bound.
    - D-03: `outValue[0]` equals `initialInvestment` bit-for-bit for every parameter combination tested.
    - D-05: a property over leverage values sampled from `[1, 20]` including values within 1e-9 of 1 shows the value curve is continuous — there is no step discontinuity at exactly 1.
    - D-08: at leverage 0.5 with a positive short rate and zero return, the value after one bar is strictly greater than `initialInvestment`.
  </behavior>
  <action>
Create `tests/kernel/fixtures.ts` exporting `makeKernelSeries(returns, shortRate,
calendarDaysElapsed, contributionFlags?)` returning a `KernelSeries` built from `Float64Array.from`
/ `Int32Array.from` / `Uint8Array.from`, `baseKernelParams(overrides?)` returning a `KernelParams`
defaulted to leverage 3, `initialInvestment` 10000, `contributionAmount` 0, `financingSpread` 0,
`expenseRatio` 0 and `longGapMinDays` set from `LONG_GAP_FLAG_MIN_DAYS`, and
`makeOutputs(length)` returning a `KernelOutputs` with all three arrays preallocated. This mirrors
`tests/kernel.test.ts`'s `makeFixedSeries` / `baseParams` pair, updated for the new contract. This
file is a helper, not a suite: it carries no `test()` call, and the `unit` project's
`tests/**/*.test.ts` glob will not collect it.

Create `tests/kernel/pitfalls-a.test.ts` with one `describe` block per PITFALLS row it covers,
each block's name carrying the row id so a failure names the pitfall. Cover A1, A2, A4, A8, A10
and A11 with the behaviors listed above, plus D-03, D-05, D-07 and D-08.

For A10 / SIM-04 use the real committed bundle, not a synthetic series: call `loadBundleFromDisk`
then `buildKernelInputs` for SPX with `dividendReinvest` false, leverage 1,
`expenseRatioPercent` 0, `financingSpreadPercent` 0 and `holdingPeriodBars` null, run
`runBacktest`, and compare each `outValue[k]` against a reference computed by compounding the same
derived `series.returns` directly. Assert the maximum of `Math.abs(actual - reference) /
Math.abs(reference)` across all bars is at or below 1e-9. Write the 1e-9 figure as a named
constant in the test file with a comment recording D-06's derivation: the accumulation floor over
24,773 bars is roughly 5e-12 relative, and one day of a 0.03% fee leaking in would be roughly
8e-7 relative, so the bound sits about 200x above the floor and about 800x below the smallest
possible real modelling error. Do not use `toBeCloseTo`, whose digit argument is an absolute
bound and cannot express a relative tolerance at this scale.

For D-05 use `fast-check` (installed, 4.9.0) rather than a hand-written random loop: assert a
property over `fc.double({ min: 1, max: 20, noNaN: true })` that a one-bar run's value is a
continuous function of leverage, by sampling a leverage `L` and asserting the value at `L` and the
value at `L + 1e-9` differ by less than a bound derived from the expense ratio, which is what a
`leverage === 1` special case would violate. Add a fixed case at leverage exactly 1 and one at
leverage 0.9999999999 asserting both charge the same expense ratio.

For A4 build two synthetic years: one with 249 bars and one with 252 bars, both with
`calendarDaysElapsed` summing to exactly 365 and all returns zero, and assert both charge the same
total expense drag to within 1e-12 relative, and that the drag equals `1 - (1 - er/365)^365`
computed independently in the test.

Do not assert on comment text or on source strings in this file: every assertion here is a
behavioral assertion against `runBacktest`'s outputs.
  </action>
  <verify>
    <automated>npm run test -- tests/kernel/pitfalls-a.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- tests/kernel/pitfalls-a.test.ts` passes.
    - The suite contains a `describe` block whose name contains each of `A1`, `A2`, `A4`, `A8`, `A10` and `A11`.
    - The SIM-04 assertion loads the real bundle via `loadBundleFromDisk` and reports the measured maximum relative deviation in its assertion message, so the actual figure is visible on failure.
    - The 1e-9 tolerance is a named constant whose adjacent comment records the 5e-12 accumulation floor and the 8e-7 smallest-real-bug figure.
    - At least one assertion uses `fast-check`'s `fc.assert` / `fc.property`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>PITFALLS A1, A2, A4, A8, A10 and A11 each fail this suite when their corresponding defect is introduced, and 1x reproduces the real bundled series inside the 1e-9 relative bound D-06 derives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PITFALLS A7 ruin semantics, the D-04 long-gap flag, and an asserted disposition for every remaining A-row</name>
  <files>tests/kernel/ruin.test.ts</files>
  <read_first>
    - `.planning/research/PITFALLS.md` section A — A3, A5, A6, A7, A9 and A12 specifically. A7 carries its own concrete detection test; A3, A5, A9 and A12 are the rows that are not kernel-code concerns and need dispositions rather than tests.
    - `src/kernel/backtest.ts` — the ruin branch order, `ruinBarIndex`, `droppedContributionsTotal`, `totalContributed`, `outLongGap` and `longGapBarCount` as plan `03-01` built them.
    - `tests/kernel/fixtures.ts` — created in Task 1; reuse its builders rather than redeclaring them.
    - `tests/kernel.test.ts` — the two existing ruin cases (`a single -40% day at leverage 3` and the absorbing-state case with contributions) are the direct templates; port both to the new contract.
    - `.planning/phases/03-simulation-kernel-and-the-upro-tqqq-gate/03-CONTEXT.md` — D-04, D-21, D-22, D-23, D-24, plus F-03 and F-04 for why the long-gap flag exists.
  </read_first>
  <behavior>
    - A single -40% bar at leverage 3 writes exactly 0 into `outValue[0]`... at index 1 given the entry bar is a cost-free anchor: build the series with a zero-return entry bar followed by the -40% bar, and assert the clamp lands on index 1 with `ruinBarIndex` equal to 1.
    - After the clamp, subsequent bars carrying returns of +50%, +100% and -90% all write exactly 0 and keep the ruin flag set.
    - With `contributionAmount` 500 and a contribution flag on every post-ruin bar, the position stays at exactly 0 and `droppedContributionsTotal` equals 500 times the number of post-ruin flagged bars.
    - `totalContributed` on a ruined run equals `initialInvestment` plus only the contributions applied before the ruin bar.
    - A run that never ruins reports `ruined` false and `ruinBarIndex` -1.
    - A ruin driven far below zero (a -90% bar at leverage 20) still writes exactly 0, never a negative number, on the ruin bar and every bar after it.
    - `outLongGap` is 1 exactly on bars whose `calendarDaysElapsed` is at or above `LONG_GAP_FLAG_MIN_DAYS`, is 0 on bars with gaps of 1, 3, 4 and 5, and `longGapBarCount` equals the number of 1s.
    - Over the real committed bundle across the full SPX window, `longGapBarCount` is exactly 2, corresponding to the 1933 bank holiday and the 2001 closure.
  </behavior>
  <action>
Create `tests/kernel/ruin.test.ts` covering every behavior above with table-driven fixed cases
built from `tests/kernel/fixtures.ts`, plus one case that runs the full SPX window from the real
committed bundle and asserts `longGapBarCount` is exactly 2 and that the two flagged bars' dates,
resolved back through the calendar, fall in March 1933 and September 2001. Assert ruin only
through the `ruined` flag, `ruinBarIndex` and exact equality against 0 — never by comparing a
value against an epsilon, which is the defect D-23 rejected.

In the same file add the SIM-12 checklist coverage assertion. Declare a table constant mapping
each of `A1` through `A12` to either the name of the test that covers it or a disposition object
carrying a `reason` string. The dispositions are: A3 (wholesale rate class) is a Phase 2
data-sourcing property inherited from the FRED splice, detectable only through D-20's diagnosis
table; A5 (era-appropriate reference rate) is Phase 2's spliced series with manifest seam records;
A6 (product spread) is detected by plan `03-06`'s rate-regime sub-window statistic, not by a
kernel unit test; A9 (overfitting the validation target) is enforced by plan `03-03`'s commit
ordering and pinning test, not by kernel behavior; A12 (swap and futures cost structure) is a
Phase 5 methodology-disclosure requirement. Then assert that the table has exactly twelve entries,
that every key `A1` through `A12` is present, and that every entry is either a covered-by name or
a disposition with a non-empty reason. This makes an added or renamed pitfall row a failing test
rather than a silent gap.
  </action>
  <verify>
    <automated>npm run test -- tests/kernel/ruin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- tests/kernel/ruin.test.ts` passes.
    - The checklist assertion fails if any of the twelve keys `A1` through `A12` is removed from the table or given an empty disposition reason.
    - A test case asserts `longGapBarCount` is exactly 2 over the full SPX window from the real committed bundle, and names the two resolved dates in its assertion message.
    - A test case asserts a -90% bar at leverage 20 produces exactly 0, not a negative value, at the ruin bar and at every bar after it.
    - A test case asserts `droppedContributionsTotal` equals the contribution amount times the number of post-ruin flagged bars.
    - `npm run test` (the whole unit project) is green, including the untouched `tests/kernel.test.ts`.
  </acceptance_criteria>
  <done>PITFALLS A7 is held by tests covering clamp, flag, absorbing state and dropped-contribution accounting, the D-04 long-gap flag is proven against the two real closures, and every remaining A-row carries an asserted disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → kernel | Hand-built typed arrays deliberately drive the kernel into its clamp and boundary paths |
| on-disk bundle → SIM-04 assertion | The real committed asset crosses into the exactness comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering (result integrity) | Ruin representation | high | mitigate | Every ruin assertion uses the categorical flag and exact-zero equality; the plan explicitly forbids epsilon-based ruin detection (D-23) so a fabricated threshold cannot enter through the test suite |
| T-03-07 | Denial of service | Negative value entering a downstream colour scale | medium | mitigate | An asserted case drives the position far below zero at leverage 20 and proves the clamp writes exactly 0 on that bar and all later bars (D-24) |
| T-03-08 | Repudiation | Silent checklist gap | medium | mitigate | The twelve-entry A-row disposition table is asserted, so removing or blanking a row fails the build rather than shrinking the checklist quietly |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; `fast-check` 4.9.0 is already an installed devDependency audited in Phase 1 |
</threat_model>

## Artifacts this phase produces

See the full phase-wide list in `03-01-PLAN.md` under the same heading. Symbols this plan adds:
`makeKernelSeries`, `baseKernelParams`, `makeOutputs` (all in `tests/kernel/fixtures.ts`), plus
the files `tests/kernel/fixtures.ts`, `tests/kernel/pitfalls-a.test.ts` and
`tests/kernel/ruin.test.ts`.

<flagged_assumptions>
Edge-probe items this plan carries as explicit unresolved assumptions rather than silently
dropping (fallback report `/home/claude/.claude/jobs/1f528056/tmp/edge-coverage.json`, category
`unclassified`, status `unresolved`):

1. **SIM-04 / unclassified** — the probe derived no edge shape for "1x reproduces the unlevered
   series exactly". D-06's 1e-9 relative bound is the acceptance criterion this plan writes, but it
   is derived analytically rather than probed: if the real accumulation drift over 24,773 bars
   exceeds the assumed 5e-12 relative floor, the bound is wrong in the tight direction and D-15's
   name-a-mechanism revision protocol applies.
2. **SIM-05 / unclassified** — no edge shape derived for the ruin boundary. The exact-zero and
   absorbing-state assertions here are taken from PITFALLS A7's own suggested test rather than from
   a probed edge taxonomy.
3. **SIM-12 / unclassified** — no edge shape derived for "the checklist exists as unit tests before
   any UI work begins". The twelve-entry disposition table is this plan's answer; whether PITFALLS
   section A itself is complete remains an assumption inherited from the research phase.
</flagged_assumptions>

<verification>
- `npm run test` is green across the whole unit project.
- `npm run typecheck` exits 0.
- No file under `src/` was modified by this plan; the kernel is under test, not under edit.
</verification>

<success_criteria>
- Every PITFALLS section A row is either a named passing test or an asserted disposition.
- 1x reproduces the real bundled series inside D-06's 1e-9 relative bound.
- Ruin is proven categorical, absorbing, non-negative and correctly accounted for dropped
  contributions.
- The D-04 long-gap flag fires on exactly the two real multi-day closures and nothing else.
</success_criteria>

<output>
Create `.planning/phases/03-simulation-kernel-and-the-upro-tqqq-gate/03-02-SUMMARY.md` when done
</output>

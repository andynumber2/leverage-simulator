---
phase: 07-sweep-engine-and-the-heatmap
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/sweep/sweep-grid.ts
  - src/sweep/sweep.worker.ts
  - src/metrics/irr.ts
  - tests/sweep/metrics-one-pass.test.ts
  - tests/metrics/irr.test.ts
  - bench/sweep.bench.test.ts
autonomous: true
requirements: [METR-06, PERF-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One sweep pass writes all three display metrics plus the flag byte for every cell, so changing the displayed metric never re-runs a backtest."
    - "The sweep's annualized metric mirrors METR-01 and METR-02 exactly: IRR when contributionAmount is non-zero, CAGR when it is zero, never both unqualified and never a re-derived formula."
    - "The official PERF-03 figure is recorded from the production pool over the real bundle, not from the Phase 1 spike pool over a synthetic series."
    - "PERF-03 is measured at the declared baseline pool width on every host, and its verdict is withheld when the recorded hardwareConcurrency differs from PERF_03_BASELINE_HARDWARE_CONCURRENCY."
    - "METR-06 adjacency: a cell whose value lands exactly on an interior band boundary resolves to a single band under bandIndexFor's existing upper-band tie rule, never to both and never to neither."
    - "METR-06 empty: a cell whose IRR bracket does not straddle zero stores a non-finite sentinel in annualized rather than 0, so a genuinely undefined annualized return is never rendered as a real 0%/yr, and a zero-bar or single-bar window is rejected by resolveColumnSeries's named error rather than producing a silent empty cell."
    - "METR-06 ordering: every metric array and the flag array are indexed row * cols + col; a cell's multiple, drawdown, annualized value and flag byte always come from the same runBacktest call, asserted by a test that checks all four against a serially recomputed reference at a stride sample."
  artifacts:
    - src/sweep/sweep-grid.ts
    - src/sweep/sweep.worker.ts
    - tests/sweep/metrics-one-pass.test.ts
    - bench/sweep.bench.test.ts
  key_links:
    - "src/sweep/sweep.worker.ts calls solveIrr or solveCagr per cell from src/metrics/, never a re-derived annualized formula."
    - "bench/sweep.bench.test.ts is the single recorder of the PERF-03 MeasurementRow; no second file records that budget id."
  prohibitions:
    - "A displayed metric value must never come from a different sweep generation than the field it is painted on."
    - "No PERF budget threshold, and no calibration reference constant, may be changed in response to a measurement produced by this phase; an over-budget or trigger-crossing figure is escalated and recorded as a Key Decision, never relaxed."
    - "A sweep timing figure must never be reported for a run that computed fewer cells than the declared grid, or whose cells were shortened relative to the declared window."
---

<objective>
Make the sweep compute everything the heatmap can display, in one pass, and put the phase's headline
performance number on the code that actually ships.

Purpose: METR-06 is what turns a metric change into a re-color rather than a re-run, which is the
whole premise of PERF-05. PERF-03 has been measured against the Phase 1 spike pool since Phase 1 and
is already escalated at 80.8% of budget with zero sweep code running; a figure measured against code
that does not ship is not a figure.
Output: a complete per-cell record, a measured PERF-03 against the production pool, and a measured
answer to F-06's unmeasured IRR hypothesis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-CONTEXT.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-RESEARCH.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-01-SUMMARY.md
</context>

## Artifacts this phase produces

New in this plan: the `annualized` array on `SweepGrid` becomes populated, `ANNUALIZED_UNDEFINED`
sentinel, `annualizedForCell`, and the retargeted PERF-03 recorder. The full phase-level artifact
list is in `07-01-PLAN.md` under the same heading; source-grounding drift verification must treat
every symbol there as new.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write all three metrics and the flag byte for every cell in one pass</name>
  <files>src/sweep/sweep.worker.ts, src/sweep/sweep-grid.ts, src/metrics/irr.ts, tests/sweep/metrics-one-pass.test.ts</files>
  <read_first>
    - `src/sweep/sweep.worker.ts` and `src/sweep/sweep-grid.ts` (post-plan-07-01)
    - `src/kernel/backtest.types.ts` (`KernelResult`: `finalValue`, `ruined`, `ruinBarIndex`, `totalContributed`, `maxDrawdown`, `barCount`)
    - `src/metrics/irr.ts` in full (`buildCashFlows`, `solveIrr`, and note `solveIrr`'s own doc comment stating that contributions land on trading days with irregular calendar gaps)
    - `src/metrics/cagr.ts` in full (`solveCagr(initialInvestment, finalValue, calendarDays)`)
    - `src/app/components/ResultColumn/MetricsPanel.tsx` and `src/app/state.ts`'s `computeDerivedMetrics` (the single-run rule METR-01 and METR-02 already implement, which D-24 requires the sweep to mirror exactly rather than re-derive)
    - `src/data/sweep-fixture-format.ts` (`CELL_FLAG_RUINED`, `CELL_FLAG_INCOMPLETE`, and the rule that an incomplete cell stores 0, never a partial value)
  </read_first>
  <behavior>
    - After one sweep, `multiples[i]`, `drawdowns[i]`, `annualized[i]` and `flags[i]` for the same `i` all derive from the same `runBacktest` call, checked against a serially recomputed reference at a 50-cell stride sample that spans both axes.
    - `multiples[i]` is `finalValue / totalContributed`; `drawdowns[i]` is `maxDrawdown`, both read straight off `KernelResult` at zero extra cost.
    - When `contributionAmount` is zero, `annualized[i]` is `solveCagr`'s result; when it is non-zero, `annualized[i]` is `solveIrr`'s result over `buildCashFlows`. The branch is decided once per sweep from the request, not per cell.
    - When `solveIrr` or `solveCagr` returns `null`, `annualized[i]` stores `ANNUALIZED_UNDEFINED` (a non-finite sentinel), never 0.
    - A ruined cell carries `CELL_FLAG_RUINED`; a fixed-period window running past the last supported bar carries `CELL_FLAG_INCOMPLETE` and stores 0 in all three metric arrays.
    - A cell carrying both conditions carries both bits, and the renderer's existing ruin-first branch order resolves the display.
    - No object is allocated inside the per-cell loop: the `KernelOutputs` scratch buffers and the cash-flow array are reused across cells within a chunk.
  </behavior>
  <action>
Extend the worker's inner row loop to write four values per cell instead of one. `multiples` and
`drawdowns` come free off `KernelResult`. `annualized` follows D-24's rule exactly: resolve
`contributionAmount !== 0` ONCE per sweep into the choice of solver, then call `solveIrr` over
`buildCashFlows(params, series, outputs, result)` or `solveCagr(initialInvestment, finalValue,
calendarDays)` per cell. Do not re-derive either formula and do not approximate either: METR-01 and
METR-02's exact semantics, including the ruined and undefined-bracket cases, are the contract.

Export `ANNUALIZED_UNDEFINED` from `sweep-grid.ts` as the non-finite sentinel stored when a solver
returns `null`. Storing 0 there would paint an undefined annualized return as a real breakeven cell,
which is precisely the failure the diverging scale's 0%/yr midpoint makes most misleading.

Honor SIM-11's allocation discipline in the new code: `buildCashFlows` currently returns a fresh
array per call. Inside the sweep's hot loop that is 10,000 array allocations per sweep at minimum.
Add a reuse path (a caller-supplied output array that `buildCashFlows` fills and returns its used
length, or an equivalent that keeps `buildCashFlows`'s existing signature working unchanged for the
single-run call site) so the sweep loop allocates once per chunk, not once per cell. Do not change
`buildCashFlows`'s existing exported behavior for its current caller.

The flag byte follows `06-HEATMAP-SPEC.md` section 4's branch order. An incomplete cell stores 0 in
all three metric arrays, matching `SweepFixture`'s stated D-20 rule that an incomplete cell never
carries a partial value.

Create `tests/sweep/metrics-one-pass.test.ts` in the fast Node `unit` project asserting every
behavior above. The stride sample uses a stride coprime with the cell count so it visits distinct
rows and columns, following `bench/sweep.bench.test.ts`'s existing
`SERIAL_REFERENCE_STRIDE` precedent.
  </action>
  <acceptance_criteria>
    - `grep -c "solveIrr\|solveCagr" src/sweep/sweep.worker.ts` is at least 2.
    - `grep -v '^ \*' src/sweep/sweep.worker.ts | grep -c "annualized"` is at least 1.
    - `grep -c "ANNUALIZED_UNDEFINED" src/sweep/sweep-grid.ts` is at least 1.
    - `tests/sweep/metrics-one-pass.test.ts` asserts, at a 50-cell stride sample, that all four of `multiples`, `drawdowns`, `annualized` and `flags` match a serially recomputed reference.
    - `tests/sweep/metrics-one-pass.test.ts` asserts a cell with an undefined annualized result stores a non-finite value and specifically not 0.
    - `tests/sweep/metrics-one-pass.test.ts` asserts an incomplete cell carries `CELL_FLAG_INCOMPLETE` and stores 0 in all three metric arrays.
    - `tests/sweep/metrics-one-pass.test.ts` asserts a value exactly on an interior band boundary resolves to exactly one band index.
    - `npm run test` exits 0 including the existing `tests/metrics/` suite unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test -- --run tests/sweep tests/metrics</automated>
  </verify>
  <done>One sweep produces every number the heatmap can display, proven cell-for-cell against a serial reference.</done>
</task>

<task type="auto">
  <name>Task 2: Point the official PERF-03 measurement at the production pool</name>
  <files>bench/sweep.bench.test.ts</files>
  <read_first>
    - `bench/sweep.bench.test.ts` in full (the correctness-before-timing discipline in its header, `computeSerialReferenceSample`, `SERIAL_REFERENCE_SAMPLE_COUNT`, `SERIAL_REFERENCE_STRIDE`)
    - `bench/calibration.ts` (`measureMinOfN`, `measureBatchedMinOfN`, `normalize`, `REPEAT_COUNT`, and the worker-width guard quick-260818-v2d added)
    - `bench/report.ts` (`MeasurementRow`, `checkBudget`, `assertWithinBudget`, `escalationTriggered`, `hostMatchesPerf03Baseline`, `buildFullRowSet`, `assertRunInvariants`)
    - `bench/canonical-calibration.ts`, `bench/environment-block.ts`
    - `perf-budgets.ts` (`PERF_BUDGETS['PERF-03']`, `PERF_03_BASELINE_HARDWARE_CONCURRENCY`, `ESCALATION_TRIGGER_RATIO`, `BENCH_TOTAL_RUNTIME_CAP_MS`, and the header's PERF-01a rule)
    - `bench/sweep-pool.ts` (`BASELINE_WORKER_COUNT`, `resolveWorkerCount`) and `src/sweep/sweep-pool.ts`
    - `bench/perf-08.bench.test.ts` and `bench/decode-time.bench.test.ts` (how a bench test reaches the real compiled bundle from inside the browser context)
    - `.planning/WINDOWS.md` entry 2 (the parallel-width and elasticity findings)
    - `.planning/PROJECT.md` Key Decisions (the recorded D-20 PERF-03 escalation)
  </read_first>
  <action>
Retarget `bench/sweep.bench.test.ts` from `runSpikeSweep` over the synthetic GBM series to the
production pool over the real compiled bundle, closing Finding F-05 for PERF-03. Keep the file's
existing discipline exactly:

- The correctness assertions run BEFORE any timing figure is trusted. A sweep that is fast because
  it computed nothing must not be able to produce a passing figure. Replace the spike-kernel serial
  reference with a serial reference computed through `runBacktest` and `resolveColumnSeries` on the
  calling thread at the same 50-cell coprime stride, and assert the pool grid matches it.
- Assert the full grid has 10,000 cells with no holes and that the declared window per cell is the
  declared window, not a shortened one. 01-02's own decision recorded that every cell's backtest
  must stay at 99.2% or more of the full bar count; carry an equivalent assertion so a shortened
  cell cannot flatter the figure.
- Measure with `measureMinOfN` and `REPEAT_COUNT` as the file already does. Worker construction cost
  stays inside every repeat only if the production pool is constructed per repeat; the production
  pool is persistent, so state explicitly in the file header which cost the figure includes and keep
  that choice constant across repeats.
- Measure at `PERF_03_BASELINE_HARDWARE_CONCURRENCY` pool width on every host, and withhold the
  verdict when the recorded `hardwareConcurrency` differs, exactly as quick-260818-v2d established.
- Normalize through the run's canonical calibration score and record exactly one PERF-03
  `MeasurementRow` via `commands.recordMeasurement`. No second file may record this budget id.

The sweep measured for the headline figure uses the app's default request shape (zero contributions,
so the annualized metric resolves through `solveCagr`), because D-03 requires the headline figure to
describe the default view. Record a SECOND figure as an info line via `commands.recordInfoLine` for
the same sweep with a monthly contribution schedule, so the `solveIrr` path's real cost is on the
record rather than estimated. That info line is the measurement F-06 asked for.

If the normalized figure exceeds the 1000ms budget, or crosses `ESCALATION_TRIGGER_RATIO`, do NOT
relax the budget and do NOT retune `NOMINAL_REFERENCE_MS`. Record the figure, let the gate report
what it reports, and write the escalation into the summary as a Key Decision candidate under D-20,
exactly as PERF-03's prior crossing was handled.

Keep the whole bench file inside `BENCH_TOTAL_RUNTIME_CAP_MS`; if the real-bundle sweep pushes the
suite past it, reduce repeat counts on the info-line arm before touching the headline arm.
  </action>
  <acceptance_criteria>
    - `grep -c "runSpikeSweep" bench/sweep.bench.test.ts` prints `0`.
    - `grep -c "src/sweep/sweep-pool" bench/sweep.bench.test.ts` is at least 1.
    - `grep -c "recordMeasurement" bench/sweep.bench.test.ts` is at least 1 and `grep -rlc "budgetId: 'PERF-03'" bench/*.bench.test.ts` lists exactly one file.
    - `grep -c "PERF_03_BASELINE_HARDWARE_CONCURRENCY" bench/sweep.bench.test.ts` is at least 1.
    - `grep -c "recordInfoLine" bench/sweep.bench.test.ts` is at least 1.
    - The file's correctness assertions appear before its measurement call, verified by reading the file top to bottom.
    - `npm run bench` exits 0, `.bench/bench-results.json` contains a PERF-03 row whose `source` names this file, and the run's info lines contain the contribution-schedule figure.
    - No line of `perf-budgets.ts` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>npm run bench -- --run bench/sweep.bench.test.ts</automated>
  </verify>
  <done>PERF-03's headline number comes from the pool that ships, over the bundle that ships, with correctness proven before the stopwatch and the contribution-path cost on the record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Act on the measured annualized cost, and correct F-06's hypothesis in the record</name>
  <files>src/metrics/irr.ts, tests/metrics/irr.test.ts</files>
  <read_first>
    - `src/metrics/irr.ts` in full, especially `npv`'s `Math.pow(1 + rate, flow.daysSinceEntry / 365)` and `solveIrr`'s doc comment stating that contributions land on trading days with IRREGULAR calendar gaps
    - `tests/metrics/irr.test.ts` in full
    - The info-line figure Task 2 recorded for the contribution-schedule sweep
    - `.planning/phases/07-sweep-engine-and-the-heatmap/07-CONTEXT.md` D-24 and Finding F-06
  </read_first>
  <behavior>
    - The reduced `npv` agrees with the current `Math.pow`-based `npv` to within 1e-12 relative error, over property-generated flow sequences (fast-check is already a dev dependency): random flow counts from 2 to 400, random ascending irregular day offsets, random amounts, and rates sampled across the full `[-0.9999, 10.0]` bracket.
    - `solveIrr`'s returned rate is unchanged to within the existing 1e-9 tolerance for every case in the existing `tests/metrics/irr.test.ts`, including the ruined terminal-zero case and the no-sign-change null case.
  </behavior>
  <action>
Read Task 2's recorded contribution-schedule info line first. Two branches, and which one you take
is decided by that number, not by preference:

If the contribution-schedule sweep is comfortably inside the PERF-03 budget, do not change `npv`.
Record the measured figure and the decision not to optimize in the summary, and stop. F-06 asked for
a measurement before relying on the hypothesis; a measurement that says no work is needed is a
complete answer.

If it is over budget or crosses the escalation trigger, apply the reduction and gate it on exactness.
**Correct the reduction to the one the code actually admits.** D-24's lead proposes a Horner-style
loop on the grounds that a regular monthly schedule makes the discount factors a geometric
progression. That premise does not hold here: `solveIrr`'s own doc comment states that contributions
land on trading days with irregular calendar gaps rather than a uniform cadence, and `buildCashFlows`
accumulates `series.calendarDaysElapsed` per bar, so consecutive `daysSinceEntry` gaps are not
constant and the factors are not a geometric progression. The valid reduction for irregular offsets
is to hoist the logarithm out of the flow loop: compute `k = Math.log(1 + rate) / 365` once per `npv`
call and discount each flow by `Math.exp(-k * flow.daysSinceEntry)`, replacing one `Math.pow` per
flow (internally a log plus an exp) with one `Math.exp` per flow plus one `Math.log` per call.
Combine it with the per-chunk cash-flow array reuse Task 1 added.

Whichever branch you take, record in `src/metrics/irr.ts`'s header and in the plan summary that
D-24's geometric-progression premise was checked against the code and did not hold, with the reason.
This is the correction F-06 exists to force; leaving the record saying the hypothesis was adopted
when a different reduction shipped would make the next reader repeat the analysis.

If the reduction ships, extend `tests/metrics/irr.test.ts` with the property test in `<behavior>`
using fast-check, keeping the existing `Math.pow` formulation in the test file as the oracle.
  </action>
  <acceptance_criteria>
    - `src/metrics/irr.ts`'s module header contains a sentence stating whether D-24's geometric-progression premise held, and why.
    - If `npv` changed: `grep -c "Math.exp" src/metrics/irr.ts` is at least 1, `grep -c "fc.assert\|fc.property" tests/metrics/irr.test.ts` is at least 1, and the property test compares the shipped `npv` against an independent exponentiation-based oracle defined in the test file, over at least 200 generated cases.
    - If `npv` did not change: the summary records the measured contribution-schedule figure and the decision, and `git diff src/metrics/irr.ts` shows header-only changes.
    - Every pre-existing assertion in `tests/metrics/irr.test.ts` passes unchanged.
    - `npm run test` exits 0 and `npm run bench` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run tests/metrics/irr.test.ts &amp;&amp; npm run bench -- --run bench/sweep.bench.test.ts</automated>
  </verify>
  <done>The annualized metric's real per-cell cost is measured, acted on only if the measurement demanded it, and F-06's hypothesis is corrected in the record rather than silently adopted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main thread to Worker (Comlink postMessage) | Chunk requests and result buffers cross here; a stale-generation result arriving after a parameter change is the tampering risk |
| Measured figure to CI gate | A measurement that describes different work than the budget names would silently pass the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | `src/sweep/sweep-pool.ts` result merge | high | mitigate | Every chunk carries a generation token; the main thread compares it before merging. This plan carries the token through the record; plan 07-05 enforces the check and tests it |
| T-07-09 | Repudiation | `bench/sweep.bench.test.ts` | high | mitigate | Correctness assertions run before the stopwatch, the grid is asserted complete at 10,000 cells with the declared window per cell, and the pool width is pinned to the declared baseline with the verdict withheld off-baseline, so a fast-because-it-computed-nothing figure cannot pass |
| T-07-02 | Denial of Service | `src/sweep/sweep.worker.ts` | medium | mitigate | Per-chunk timeout carried from plan 07-01; the cash-flow reuse buffer is sized from the declared bar count, never from an untrusted length |
| T-07-04 | Information Disclosure | whole phase | low | accept | Client-side, backend-less tool: no credentials, accounts, or PII |
| T-07-SC | Tampering | npm installs | low | accept | This plan installs no packages; fast-check is already a pinned dev dependency and `07-RESEARCH.md`'s Package Legitimacy Audit records zero new dependencies for the phase |
</threat_model>

<planner_assumptions>
Flagged, unresolved edge-probe item carried forward for this plan (spec-less fallback; `unclassified`
rows are never auto-resolved and never auto-dismissed):

- **PERF-03 (unclassified):** the probe could not classify this requirement's edge family. Planner
  assumption: PERF-03's only edges are measurement-integrity edges, and they are covered by the
  correctness-before-timing rule, the full-grid assertion, and the baseline-width verdict
  withholding, all carried from Phase 1's existing discipline. Review manually.
</planner_assumptions>

<verification>
- `npm run typecheck` exits 0.
- `npm run test` is green.
- `npm run bench` is green and records exactly one PERF-03 row, sourced from the production pool.
- No value in `perf-budgets.ts` moved.
</verification>

<success_criteria>
- All three display metrics plus the flag byte are written per cell in one pass and proven cell-for-cell against a serial reference.
- PERF-03's headline figure describes the shipped pool over the shipped bundle at the declared baseline width.
- F-06's IRR hypothesis is answered by a measurement and corrected in the record.
</success_criteria>

<output>
Create `.planning/phases/07-sweep-engine-and-the-heatmap/07-03-SUMMARY.md` when done
</output>

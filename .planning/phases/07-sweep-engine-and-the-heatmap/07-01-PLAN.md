---
phase: 07-sweep-engine-and-the-heatmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/heatmap/iso-lines.ts
  - src/heatmap/field-sampler.ts
  - src/heatmap/hatch-pattern.ts
  - src/heatmap/sweep-copy.ts
  - src/heatmap/paint-contour.ts
  - src/sweep/sweep-grid.ts
  - src/sweep/resolve-column-series.ts
  - src/sweep/sweep.worker.ts
  - src/sweep/sweep-pool.ts
  - src/app/state.ts
  - src/app/App.tsx
  - src/app/components/ResultColumn/HeatmapPanel.tsx
  - .planning/phases/06-heatmap-design-pass/mockups/shared/iso-lines.ts
  - .planning/phases/06-heatmap-design-pass/mockups/shared/field-sampler.ts
  - .planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts
  - .planning/phases/06-heatmap-design-pass/mockups/forms/form-1-dense-grid.ts
  - .planning/phases/06-heatmap-design-pass/mockups/forms/form-2-filled-contour.ts
  - .planning/phases/06-heatmap-design-pass/mockups/forms/form-3-small-multiples.ts
  - .planning/phases/06-heatmap-design-pass/mockups/forms/form-4-grid-with-contour.ts
  - tests/iso-lines.test.ts
  - tests/field-sampler.test.ts
  - bench/heatmap-form-2.bench.test.ts
  - tests/sweep/column-series.test.ts
  - tests/app/sweep-tracer.browser.test.ts
autonomous: true
requirements: [VIZ-03, VIZ-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching the result column to Sweep runs a real sweep over the real compiled bundle through src/kernel/backtest.ts and paints a filled-contour field on a canvas, with no synthetic series and no committed fixture anywhere in the path."
    - "Every cell of the painted field comes from a runBacktest call whose params differ from its row neighbours only in leverage, and whose KernelSeries was resolved once for its entry-date column."
    - "resolveColumnSeries produces a KernelSeries byte-identical to buildKernelInputs's series for the same symbol, dividendReinvest, entryDate, holdingPeriodBars and contribution schedule."
    - "A cell whose holding period runs past the last supported bar carries CELL_FLAG_INCOMPLETE and a 0 value, never a partial value."
    - "A cell whose position reached zero carries CELL_FLAG_RUINED and renders through the hatched destructive fill, never as a value on the continuous ramp."
    - "src/heatmap/paint-contour.ts accepts an arbitrary cols by rows field and never reads a hardcoded 200 or 50 grid dimension."
    - "UI consideration E3 populated: the field renders the full 200 by 50 cell grid at 06-HEATMAP-SPEC.md section 7 Form 2 geometry."
    - statement: "UI consideration E3 overflow: at the narrowest supported viewport the heatmap panel neither forces horizontal scroll nor clips, checked against the same narrow-viewport test class tests/app/narrow-viewport.browser.test.ts established."
      verification: backstop
  artifacts:
    - src/heatmap/iso-lines.ts
    - src/heatmap/field-sampler.ts
    - src/heatmap/hatch-pattern.ts
    - src/heatmap/sweep-copy.ts
    - src/heatmap/paint-contour.ts
    - src/sweep/sweep-grid.ts
    - src/sweep/resolve-column-series.ts
    - src/sweep/sweep.worker.ts
    - src/sweep/sweep-pool.ts
    - src/app/components/ResultColumn/HeatmapPanel.tsx
    - tests/sweep/column-series.test.ts
    - tests/app/sweep-tracer.browser.test.ts
  key_links:
    - "src/sweep/sweep.worker.ts calls src/kernel/backtest.ts's runBacktest directly; the recurrence is never reimplemented."
    - "src/heatmap/field-sampler.ts and iso-lines.ts are the graduated originals; the four Phase 6 mockups import them from src/heatmap/ so a palette or band change still re-renders all four."
    - "src/app/App.tsx's result column reads resultMode() and mounts HeatmapPanel in place of the single-run stack."
    - "src/sweep/sweep-pool.ts transfers every chunk buffer in both directions via Comlink.transfer, never structured clone."
  prohibitions:
    - "The sweep engine must never reimplement, approximate, or tune any part of the simulation recurrence, the cost model, or the data bundle; it may only call runBacktest."
---

<objective>
Prove the whole Phase 7 architecture end to end on one thin path: a real sweep over the real bundle,
through the real kernel, into a live grid, painted as a filled contour field on screen.

This is the tracer slice. It is production-quality, not a prototype: the per-pixel `resampleField`
renderer it paints through is D-09's documented fallback and D-08's permanent test oracle, so
nothing built here is thrown away when plan 07-04 attempts the polygon fill behind the same seam.

Purpose: catch an architectural dead end (worker boundary, series resolution cost, grid shape,
renderer seam) after one commit rather than after ten.
Output: `src/heatmap/` and `src/sweep/` exist with a working sweep-to-canvas path, and Phase 6's
geometry modules are graduated so both the mockups and production consume one copy.

**ROADMAP goal, verbatim** (MVP mode is active but the ROADMAP `**Goal:**` line for Phase 7 is not
in `As a / I want to / so that` user-story format, so it is carried through unchanged rather than
invented): "A user sees how the answer changes across every entry date and every leverage level at
once, without the interface ever stalling."
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-CONTEXT.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-PATTERNS.md
@.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 7 and does not exist before it. Source-grounding drift
verification must treat these as new, not as unacknowledged references.

**New files:** `src/heatmap/iso-lines.ts`, `src/heatmap/field-sampler.ts`,
`src/heatmap/hatch-pattern.ts`, `src/heatmap/sweep-copy.ts`, `src/heatmap/paint-contour.ts`,
`src/heatmap/polygon-fill.ts`, `src/heatmap/short-horizon.ts`, `src/heatmap/crosshair.ts`,
`src/heatmap/viewport.ts`, `src/heatmap/curve-label.ts`, `src/sweep/sweep-grid.ts`, `src/sweep/resolve-column-series.ts`,
`src/sweep/sweep.worker.ts`, `src/sweep/sweep-pool.ts`,
`src/app/components/ResultColumn/HeatmapPanel.tsx`,
`src/app/components/ResultColumn/SliceChart.tsx`,
`src/app/components/ResultColumn/SweepModeToggle.tsx`,
`src/app/components/ResultColumn/MetricToggle.tsx`,
`src/app/components/ResultColumn/SweepLegend.tsx`,
`src/app/components/ResultColumn/SweepCaption.tsx`,
`src/app/components/ResultColumn/HoverReadout.tsx`, `tests/sweep/column-series.test.ts`,
`tests/sweep/cancellation.test.ts`, `tests/sweep/metrics-one-pass.test.ts`,
`tests/heatmap/polygon-fill.test.ts`, `tests/app/sweep-tracer.browser.test.ts`,
`tests/app/sweep-controls.browser.test.ts`, `tests/app/slice-charts.browser.test.ts`,
`tests/app/sweep-caption.browser.test.ts`, `tests/app/crosshair.browser.test.ts`,
`tests/app/ruin-and-horizon.browser.test.ts`, `bench/sweep-progressive.bench.test.ts`,
`bench/heatmap-panzoom.bench.test.ts`.

**New exported symbols:** `SweepGrid`, `SweepGridMeta`, `SWEEP_COLS`, `SWEEP_ROWS`,
`LEVERAGE_MIN`, `LEVERAGE_MAX`, `leverageForRow`, `rowForLeverage`, `cellIndex`,
`createSweepGrid`, `resolveColumnSeries`, `ColumnSeriesRequest`, `SweepWorkerApi`,
`SweepChunkRequest`, `SweepPool`, `createSweepPool`, `SweepRunHandle`, `isStaleGeneration`,
`paintSweepField`, `SweepPaintOptions`, `FillPath`, `stitchBandRings`, `BandRing`,
`buildBandPolygons`, `makeHatchPattern`, `integerLeverageTicks`, `VIZ10_CAVEAT_SENTENCES`,
`buildRampInterpolator`, `SEQUENTIAL_RAMP_STOPS`, `interpolateSequentialRamp`,
`DRAWDOWN_DOMAIN_MIN`, `DRAWDOWN_DOMAIN_MAX`, `rampPositionForMetric`, `scaleTypeForMetric`,
`SweepMetric`, `SHORT_HORIZON_BARS`, `shortHorizonColumn`, `paintShortHorizonRule`,
`crosshairCellFor`, `clampLeverageToGrid`, `SweepViewport`, `applyViewportTransform`,
`paintCurveLabel`, `labelAnchorFor`,
`ResultMode`, `resultMode`, `setResultMode`, `displayedMetric`, `setDisplayedMetric`,
`sweepGrid`, `sweepGeneration`, `scheduleSweep`, `crosshairCell`, `setCrosshairCell`.

**New permalink keys:** `mode`, `metric`.

**New config keys / CLI flags:** none.

<tasks>

<task type="auto">
  <name>Task 1: Graduate the Phase 6 geometry and the pure mockup-runtime helpers into src/heatmap/</name>
  <files>src/heatmap/iso-lines.ts, src/heatmap/field-sampler.ts, src/heatmap/hatch-pattern.ts, src/heatmap/sweep-copy.ts, .planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts, .planning/phases/06-heatmap-design-pass/mockups/forms/form-1-dense-grid.ts, .planning/phases/06-heatmap-design-pass/mockups/forms/form-2-filled-contour.ts, .planning/phases/06-heatmap-design-pass/mockups/forms/form-3-small-multiples.ts, .planning/phases/06-heatmap-design-pass/mockups/forms/form-4-grid-with-contour.ts, tests/iso-lines.test.ts, tests/field-sampler.test.ts, bench/heatmap-form-2.bench.test.ts</files>
  <read_first>
    - `.planning/phases/06-heatmap-design-pass/mockups/shared/iso-lines.ts` (read the header in full: it states that segments are UNSTITCHED and why)
    - `.planning/phases/06-heatmap-design-pass/mockups/shared/field-sampler.ts`
    - `.planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts` (only `VIZ10_CAVEAT_SENTENCES`, `makeHatchPattern`, `integerLeverageTicks` graduate; `loadSweepFixture`, `renderCaveat`, `renderLegend`, `mountMockup`, `fixtureRowForLeverage` stay)
    - `.planning/phases/06-heatmap-design-pass/mockups/forms/form-2-filled-contour.ts`
    - `src/colorscale/value-to-color.ts` (the import target the graduated modules already point at)
    - `tests/iso-lines.test.ts`, `tests/field-sampler.test.ts`
    - `bench/heatmap-form-2.bench.test.ts`
    - `.planning/phases/07-sweep-engine-and-the-heatmap/07-PATTERNS.md` section on the D-11 graduation
  </read_first>
  <action>
Move `mockups/shared/iso-lines.ts` to `src/heatmap/iso-lines.ts` and
`mockups/shared/field-sampler.ts` to `src/heatmap/field-sampler.ts`, deleting the originals. This
is D-11's graduation and it is near-verbatim: the only edits are import-path shortening. In
`field-sampler.ts` the two five-level relative imports (`../../../../../src/data/sweep-fixture-format.ts`
and `../../../../../src/colorscale/value-to-color.ts`) become `../data/sweep-fixture-format.ts`
and `../colorscale/value-to-color.ts`. `iso-lines.ts` has zero imports and needs none. Do not
change any algorithm, constant, tie rule, or exported name: `marchingSquaresSegments`, `IsoSegment`,
`resampleField`, `sampleField`, `bandIndexFor`, `BAND_LEVELS`, `Metric`, `CategoricalMask`,
`FieldSample`, `ResampleGeometry` all keep their current signatures and semantics.

Close Pitfall 7 by graduating the three PURE helpers D-11 does not name but production needs:
create `src/heatmap/hatch-pattern.ts` carrying `makeHatchPattern(ctx, rgba)` and
`integerLeverageTicks(...)` moved verbatim from `mockup-runtime.ts`, and
`src/heatmap/sweep-copy.ts` carrying `VIZ10_CAVEAT_SENTENCES` moved verbatim. Delete all three from
`mockup-runtime.ts` and have it re-import them from `src/heatmap/`, so the sentence pair and the
hatch geometry have exactly one definition. Do NOT graduate `renderLegend`, `renderCaveat`,
`mountMockup`, `loadSweepFixture` or `fixtureRowForLeverage`: `renderLegend` builds DOM strings and
is reimplemented as a Solid component in plan 07-07, and the others are mockup-page scaffolding.

Repoint every consumer: the four `mockups/forms/form-*.ts` files, `mockups/shared/mockup-runtime.ts`,
`tests/iso-lines.test.ts`, `tests/field-sampler.test.ts`, and `bench/heatmap-form-2.bench.test.ts`
now import `iso-lines.ts`, `field-sampler.ts`, `makeHatchPattern` and `integerLeverageTicks` from
`src/heatmap/`. D-11's stated reason for keeping the four losing forms committed is that a palette
or band-level change still re-renders all four, so all four must still compile and still paint.

Each graduated file keeps its existing module header and gains one line naming this plan and D-11
as the reason it moved. Follow the project rule that every module header states its plan, its
decision IDs, and its reasoning. No em dash characters anywhere.
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/06-heatmap-design-pass/mockups/shared/` lists neither `iso-lines.ts` nor `field-sampler.ts`.
    - `ls src/heatmap/` lists `iso-lines.ts`, `field-sampler.ts`, `hatch-pattern.ts`, `sweep-copy.ts`.
    - The import lines of every consumer resolve to `src/heatmap/`: `grep -h "^import\|^} from" .planning/phases/06-heatmap-design-pass/mockups/forms/*.ts .planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts tests/iso-lines.test.ts tests/field-sampler.test.ts bench/heatmap-form-2.bench.test.ts | grep -c "mockups/shared"` prints `0`.
    - `grep -c "export function makeHatchPattern" src/heatmap/hatch-pattern.ts` prints `1` and the same grep against `mockups/shared/mockup-runtime.ts` prints `0`.
    - `grep -c "VIZ10_CAVEAT_SENTENCES" src/heatmap/sweep-copy.ts` is at least 1 and `grep -c "export const VIZ10_CAVEAT_SENTENCES" .planning/phases/06-heatmap-design-pass/mockups/shared/mockup-runtime.ts` prints `0`.
    - `npm run typecheck` exits 0.
    - `npm run test` passes with `tests/iso-lines.test.ts` and `tests/field-sampler.test.ts` green and unchanged in assertion content.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test</automated>
  </verify>
  <done>The geometry, hatch and caveat text live under `src/heatmap/`, every consumer imports them from there, and the existing unit suite is green with no assertion changed.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "a user sees a real swept field" tracer, one path only</name>
  <files>src/sweep/sweep-grid.ts, src/sweep/resolve-column-series.ts, src/sweep/sweep.worker.ts, src/sweep/sweep-pool.ts, src/heatmap/paint-contour.ts, src/app/state.ts, src/app/components/ResultColumn/HeatmapPanel.tsx, src/app/App.tsx, tests/app/sweep-tracer.browser.test.ts</files>
  <read_first>
    - `src/kernel/backtest.ts` and `src/kernel/backtest.types.ts` (the `runBacktest(params, series, outputs)` contract, SIM-10's zero runtime imports, SIM-11's allocation-free discipline)
    - `src/data/kernel-inputs.ts` in full (`buildKernelInputs`, lines 133-262: the per-bar series loop reads no leverage, which is the whole basis for per-column resolution)
    - `src/data/sweep-fixture-format.ts` (`SweepFixture`, `SweepFixtureMeta`, `CELL_FLAG_RUINED`, `CELL_FLAG_INCOMPLETE`)
    - `bench/sweep-pool.ts` and `bench/sweep.worker.ts` in full (structure precedent only: `workerCountForCores`, `CHUNKS_PER_WORKER`, `watchWorkerFailure`, `Comlink.transfer`, `DEFAULT_CHUNK_TIMEOUT_MS`, and the `finally { terminate() }` teardown that must NOT be copied)
    - `src/heatmap/field-sampler.ts` and `src/heatmap/iso-lines.ts` (post-Task-1)
    - `.planning/phases/06-heatmap-design-pass/mockups/forms/form-2-filled-contour.ts` in full (`FORM_2_GEOMETRY`, `fieldRect`, `paintFilledContour`'s two-pass render and its ruin-hatch clip)
    - `src/app/state.ts` (the store shape, `scheduleRun`'s requestAnimationFrame coalescing, `activeTier`, `loadedBundle`)
    - `src/app/App.tsx` (the single result-column slot D-21 reserved)
    - `src/app/bounds.ts` (`Tier`, `resolveEntryDateBounds`)
    - `src/app/components/ResultColumn/EquityCurveChart.tsx` (the Solid-wraps-canvas and `onThemeChange` pattern)
  </read_first>
  <action>
Wire ONE path from a click to a painted field. Every layer this phase will touch is exercised
exactly once, with no second call site, no batching, no cancellation, no coarse pass, no toggles,
no crosshair, no slice charts. Real error handling on this one path only.

`src/sweep/sweep-grid.ts`: declare the live grid. `SWEEP_COLS = 200`, `SWEEP_ROWS = 50`,
`LEVERAGE_MIN = 1`, `LEVERAGE_MAX = 5` (D-01, D-03), `leverageForRow(row)` returning
`LEVERAGE_MIN + row * (LEVERAGE_MAX - LEVERAGE_MIN) / (SWEEP_ROWS - 1)`, `rowForLeverage` as its
inverse, `cellIndex(col, row)` as `row * cols + col`. Declare `SweepGrid` structurally extending
`SweepFixture` (same `cols`, `rows`, `meta`, `multiples`, `drawdowns`, `flags` field names and the
same row-major indexing) plus `annualized: Float32Array` and `generation: number`. Declare
`SweepGridMeta` extending `SweepFixtureMeta` with `holdMode: 'fixed' | 'end-of-data'` and
`endOfDataDate: string`. State and enforce the contract that in `end-of-data` mode `holdingYears`
is 0 and consumers read `holdMode` first, so a 0 can never be rendered as a holding period.
`createSweepGrid(cols, rows, meta)` preallocates the four typed arrays. `SWEEP_COLS`/`SWEEP_ROWS`
are module constants and are never derived from a URL, a viewport, or a permalink value.

`src/sweep/resolve-column-series.ts`: export `resolveColumnSeries(bundle, request)` returning the
same `KernelSeries` shape `buildKernelInputs` returns, for one entry date, with `leverage` absent
from its input type entirely (`ColumnSeriesRequest` carries `symbol`, `dividendReinvest`,
`entryDate`, `holdingPeriodBars`, `contributionAmount`, `contributionFrequency`). Reuse
`buildKernelInputs`'s per-bar loop logic and its fail-loud validation discipline verbatim: an
out-of-range entry date or an overrun holding period throws naming both the offending value and the
supported range, never silently truncates. A right-edge overrun in fixed-period mode is NOT an
error: it is D-28's categorical case and the caller flags those cells `CELL_FLAG_INCOMPLETE`, so
`resolveColumnSeries` must distinguish the two and expose which one it hit rather than collapsing
them into one throw.

`src/sweep/sweep.worker.ts`: a Comlink-exposed worker. `SweepChunkRequest` carries `generation`,
the shared `BacktestRequest`-derived params, an explicit list of entry-date column indices, and an
explicit list of leverage row indices, so a coarse strided pass in plan 07-05 needs no worker
change. `runChunk(request, buffer)` loops columns on the outside and rows on the inside: resolve
the `KernelSeries` ONCE per column via `resolveColumnSeries`, then call `runBacktest` once per row
against that same series varying only `params.leverage`. This ordering is load-bearing for PERF-03,
not polish. Preallocate the `KernelOutputs` scratch arrays once per worker and reuse them for every
call, mirroring `bench/sweep.worker.ts`'s `scratchValue`/`scratchRuined` pattern; never allocate
per cell or build a per-cell intermediate object. Write results into the transferred buffer and
return it via `Comlink.transfer`.

`src/sweep/sweep-pool.ts`: `createSweepPool()` constructs workers ONCE and keeps them alive.
Reuse `workerCountForCores(cores)` and the `hardwareConcurrency - 1` floor-at-1 rule, the
`CHUNKS_PER_WORKER` queue-draining shape so a slow worker cannot strand the tail, `watchWorkerFailure`
attached before any dispatch, and a per-chunk timeout. Do NOT copy the
`finally { worker.terminate() }` teardown: the production pool outlives a sweep. Every chunk buffer
uses `Comlink.transfer` in both directions. This task's pool runs one sweep at a time and merges
every result; the generation check is plan 07-05's work, but `SweepChunkRequest.generation` is
carried from here so that plan is a check, not a signature change.

`src/heatmap/paint-contour.ts`: export `paintSweepField(ctx, grid, options)` adapted from
`paintFilledContour`. Keep the two-pass render (base fill, then `marchingSquaresSegments` strokes
over `BAND_LEVELS` with breakeven at 2px `var(--color-text)` and the rest at 1px
`var(--color-border)`) and keep the ruin-hatch clip-and-fill exactly as the mockup does it, now
importing `makeHatchPattern` from `src/heatmap/hatch-pattern.ts`. Two adaptations: it takes a
`SweepGrid` rather than a static `SweepFixture`, and it reads `grid.cols`/`grid.rows` for every
dimension so an arbitrary N by M field renders (F-07). Route the fill through a `FillPath` seam
(a named union or function parameter) whose only member in this task is the per-pixel
`resampleField` path, so plan 07-04's polygon path is a swap at this seam rather than a rewrite.
Read chrome colours from CSS custom properties via `getComputedStyle` at render time, following
`EquityCurveChart.tsx`'s header rule; the heatmap palette itself does not swap by theme.

`src/app/state.ts`: add `ResultMode = 'single' | 'sweep'`, a `resultMode()` accessor and
`setResultMode`, a `sweepGrid()` signal, and `scheduleSweep()` that coalesces writes through
`requestAnimationFrame` exactly the way the existing `scheduleRun()` does. A fresh visit seeds
`resultMode` to `'single'` (D-18). `scheduleSweep()` builds the sweep request from the EXISTING
`backtestRequest()` fields plus `activeTier()`, resolving the entry-date axis as 200 evenly-spaced
trading-calendar positions across `resolveEntryDateBounds(activeTier())` (D-02), and the leverage
axis from `leverageForRow` (D-01). Do not add a parallel request shape.

`src/app/components/ResultColumn/HeatmapPanel.tsx`: a Solid component owning one canvas at
`FORM_2_GEOMETRY`'s 800 by 240 size, calling `paintSweepField` in a `createEffect` that tracks
`sweepGrid()` and the theme version, and subscribing to `onThemeChange` the way
`EquityCurveChart.tsx` does. Render the multiple-of-contributed metric only; the metric toggle is
plan 07-06.

`src/app/App.tsx`: inside the existing `data-testid="result-slot"` main element, branch on
`resultMode()`: `'single'` renders the current Phase 4 and 5 stack unchanged, `'sweep'` renders
`HeatmapPanel`. Add a minimal `data-testid`-carrying control to reach sweep mode so the tracer is
reachable from the page; the styled segmented control is plan 07-06's job and replaces it there.

New modules carry the project's standard header stating plan, decision IDs and reasoning. No em
dash characters. Minimize ternaries and never nest them.
  </action>
  <acceptance_criteria>
    - `tests/app/sweep-tracer.browser.test.ts` drives the real app, switches the result column to sweep mode, waits for the sweep to resolve, and asserts: the canvas exists at the declared geometry; `sweepGrid()` reports `cols === 200` and `rows === 50`; at least 9,500 of the 10,000 `multiples` entries are finite and strictly positive OR carry a categorical flag; and the painted canvas contains at least 8 distinct RGBA values sampled across the field (a uniform canvas fails).
    - The sweep result does not come from the committed design-pass fixture: `grep -c "sweep-fixture.bin\|decodeSweepFixture\|loadSweepFixture" src/sweep/sweep-pool.ts src/sweep/sweep.worker.ts src/app/components/ResultColumn/HeatmapPanel.tsx` prints `0` for each file.
    - `grep -c "runBacktest" src/sweep/sweep.worker.ts` is at least 1 and `grep -v '^ \*' src/sweep/sweep.worker.ts | grep -c "Math.exp\|Math.pow"` prints `0`, proving the recurrence is called and not reimplemented.
    - `grep -c "Comlink.transfer" src/sweep/sweep-pool.ts` is at least 2 (call side and return side).
    - The pool is persistent: with comment lines stripped (`grep -v '^\s*\(\*\|//\|/\*\)'`), `src/sweep/sweep-pool.ts` has no line containing both a teardown call and an enclosing cleanup block, and a browser test asserts two consecutive sweeps report the same worker count.
    - The renderer is grid-size agnostic (F-07): `grep -c "grid.cols\|grid.rows" src/heatmap/paint-contour.ts` is at least 2, and a browser assertion paints a deliberately non-default 51 by 14 grid through `paintSweepField` without error, producing a canvas with at least 4 distinct sampled RGBA values.
    - `npm run typecheck` exits 0; `npm run test` passes; `npm run test:app` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test &amp;&amp; npm run test:app -- --run tests/app/sweep-tracer.browser.test.ts</automated>
  </verify>
  <done>Clicking into sweep mode paints a real filled-contour field computed from the real bundle through the real kernel, verified by an automated browser test, and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the per-column series reuse is correct, not just fast</name>
  <files>tests/sweep/column-series.test.ts</files>
  <read_first>
    - `src/sweep/resolve-column-series.ts` (post-Task-2)
    - `src/data/kernel-inputs.ts` (`buildKernelInputs`, the reference implementation this must agree with)
    - `tests/data/kernel-inputs.test.ts` (the existing assertion style for this module)
    - `src/kernel/backtest.types.ts` (`KernelSeries` field names and array types)
    - `.planning/phases/07-sweep-engine-and-the-heatmap/07-VALIDATION.md` (Wave 0 requirements)
  </read_first>
  <behavior>
    - For a fixed symbol, dividendReinvest, entryDate, holdingPeriodBars and contribution schedule, `resolveColumnSeries` returns `returns`, `shortRate`, `calendarDaysElapsed` and `contributionFlags` element-for-element equal to `buildKernelInputs`'s series for the same inputs, at three separate entry dates spanning the strict tier.
    - Running `runBacktest` 50 times against one `resolveColumnSeries` result, varying only `params.leverage` across `leverageForRow(0)` through `leverageForRow(49)`, produces the same 50 `finalValue`, `maxDrawdown` and `ruined` results as calling `buildKernelInputs` freshly for each of the 50 leverages.
    - An entry date outside the resolvable bounds throws an error whose message contains both the offending date and the supported range.
    - A fixed-period holding window that runs past the last supported bar is reported as an overrun condition the caller can flag `CELL_FLAG_INCOMPLETE` on, and is distinguishable from the out-of-bounds throw above.
  </behavior>
  <action>
Create `tests/sweep/column-series.test.ts` in the fast Node `unit` project (it is matched by the
existing `tests/**/*.test.ts` include, so no vitest config change is needed). Load the bundle
through the Node loader `tests/data/kernel-inputs.test.ts` already uses. Assert the four behaviors
above with exact array equality for the series comparison (these are deterministic typed arrays;
use element-wise strict equality, not a tolerance) and exact scalar equality for the 50-row
comparison.

This closes the first `07-VALIDATION.md` Wave 0 requirement and is what makes the per-column reuse
in Task 2 a correctness claim rather than only a performance one.
  </action>
  <acceptance_criteria>
    - `tests/sweep/column-series.test.ts` exists and runs under `npm run test` (the Node `unit` project), not under `bench` or `app`.
    - The file contains at least one assertion comparing a full `Float64Array` from `resolveColumnSeries` against the corresponding array from `buildKernelInputs`.
    - The file contains a loop over all 50 leverage rows asserting equality of `finalValue` against per-leverage `buildKernelInputs` calls.
    - The file contains an assertion that an out-of-bounds entry date throws with a message containing both the date and the range.
    - `npm run test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run tests/sweep/column-series.test.ts</automated>
  </verify>
  <done>Per-column series reuse is proven equal to per-cell resolution across all 50 leverage rows, and the failure modes throw with named values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string to app state | Hand-editable untrusted input crosses into `BacktestRequest` and, from this phase on, into the sweep pool |
| Main thread to Worker (Comlink postMessage) | Same-origin, but a malformed chunk request can hang a worker or drive an oversized allocation |
| Compiled binary bundle to typed arrays | Build-time trusted, content-hashed, no runtime fetch beyond the static asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `src/sweep/resolve-column-series.ts` | high | mitigate | Reuse `buildKernelInputs`'s fail-loud range validation before any value reaches a worker; an out-of-range entry date or leverage throws naming the value and the supported range, never silently truncates |
| T-07-03 | Denial of Service | `src/sweep/sweep-grid.ts` | high | mitigate | `SWEEP_COLS` and `SWEEP_ROWS` are module constants; grid dimensions are never read from a URL, viewport, or permalink, so no input can drive an unbounded typed-array allocation |
| T-07-02 | Denial of Service | `src/sweep/sweep-pool.ts` | medium | mitigate | Pool width bounded by `workerCountForCores(hardwareConcurrency)` floored at 1; every chunk carries the `DEFAULT_CHUNK_TIMEOUT_MS`-style per-chunk timeout so a pathological input cannot hang a worker indefinitely |
| T-07-04 | Information Disclosure | whole phase | low | accept | Client-side, backend-less, single-user analysis tool (APP-03): no credentials, no accounts, no PII, no server-side state to disclose |
| T-07-SC | Tampering | npm installs | low | accept | This phase installs no packages. `07-RESEARCH.md`'s Package Legitimacy Audit records zero new runtime or dev dependencies, and every capability is already installed (comlink, solid-js, uplot) or hand-rolled in repo. If a later plan proposes a dependency, the Package Legitimacy Gate must run against that specific package |
</threat_model>

<planner_assumptions>
Flagged, unresolved edge-probe items carried forward for this plan (spec-less fallback, `unclassified`
rows are never auto-resolved and never auto-dismissed):

- **VIZ-03 (unclassified):** the probe could not classify this requirement's edge family. Planner
  assumption: the field's edge behavior is fully governed by `06-HEATMAP-SPEC.md` sections 3, 4 and 7
  plus D-05/D-09's fill fork, and no additional edge category applies. Review manually.
- **VIZ-06 (unclassified):** the probe could not classify this requirement's edge family. Planner
  assumption: ruin's only edge is the branch-order tie (ruin wins over incomplete), already asserted
  in `tests/value-to-color.test.ts`. Review manually.
</planner_assumptions>

<verification>
- `npm run typecheck` exits 0.
- `npm run test` is green with no pre-existing assertion weakened (Phase 6 baseline: 697 unit tests pass).
- `npm run test:app` is green including the new tracer browser test.
- The four Phase 6 mockup pages still build and paint from their graduated imports.
</verification>

<success_criteria>
- A real sweep over the real bundle paints a real filled-contour field on screen, proven by an automated browser test.
- `src/heatmap/` and `src/sweep/` exist with the module boundaries this phase's remaining nine plans expand from.
- Per-column series reuse is proven equal to per-cell resolution.
- No hardcoded grid dimension exists in the renderer.
</success_criteria>

<output>
Create `.planning/phases/07-sweep-engine-and-the-heatmap/07-01-SUMMARY.md` when done
</output>

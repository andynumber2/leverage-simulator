---
phase: 07-sweep-engine-and-the-heatmap
plan: 11
type: execute
wave: 6
depends_on: ["07-03", "07-10"]
files_modified:
  - .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md
  - bench/heatmap-form-2.bench.test.ts
  - bench/sweep.bench.test.ts
autonomous: false
gap_closure: true
requirements: [PERF-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Measured on a 4-core baseline machine and reported by the benchmark command: a full 10,000-cell sweep completes in under 1000ms wall clock from user action to final cell (PERF-03)."
    - "A GitHub Actions run of `npm run bench` on `ubuntu-latest` records `hardwareConcurrency: 4` in its environment block and a PERF-03 row whose `verdict` is not `unmeasured` and whose `source` is `production`, and that run id, its head SHA and its figures are transcribed into 07-PERF-03-BASELINE.md from the downloaded artifact."
    - "`npm run bench` exits 0 on a host where every measured budget row passes, so a run-level invariant cannot swallow the PERF-03 verdict before the run reports it."
    - "Every PERF-03 figure written into a planning document states its host `hardwareConcurrency`, its `source` field and whether it is the D-17 baseline, so a dev-sandbox figure can never be read as a baseline figure."
  artifacts:
    - .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md
  key_links:
    - ".github/workflows/ci.yml's `npm run bench` step is the only producer of a D-17 baseline PERF-03 verdict; bench/sweep.bench.test.ts remains its single recorder."
    - "bench/global-setup.ts's teardown writes .bench/bench-results.json BEFORE assertRunInvariants throws, and ci.yml uploads that artifact with `if: always()`, so the figure survives a failing run and is recoverable by run id."
  prohibitions:
    - statement: "No PERF budget threshold, no calibration reference constant, and no run-level cap may be changed in response to any measurement produced by this work. An over-budget or trigger-crossing figure is escalated and recorded as a Key Decision, never relaxed."
      status: unverified
      verification: flagged
    - statement: "The PERF-03 headline arm's workload, repeat count, grid size, entry-date axis and correctness gates must not be reduced to buy suite runtime. Only informational arms that record no MeasurementRow may have their sampling cost reduced."
      status: unverified
      verification: flagged
    - statement: "A figure that was not measured on the D-17 baseline must never be written into a planning document without being labelled non-baseline or informational, and a row carrying verdict `unmeasured` must never be transcribed as though it carried a verdict."
      status: unverified
      verification: flagged
---

<objective>
Produce the thing this phase has never had: a real, non-withheld PERF-03 verdict for the shipped
sweep, measured on the declared D-17 baseline, with the evidence recorded somewhere that is not
gitignored.

Purpose: roadmap success criterion 3 is the only one of five that 07-VERIFICATION.md could not
verify. It is not failing because the sweep is slow. It is failing because the retargeted
`bench/sweep.bench.test.ts` (plan 07-03) has never been run on the baseline machine, so its verdict
has been withheld on every host it has touched. D-03 keeps the grid at 10,000 cells and leaves the
"coarser default grid" lever unspent precisely so this figure stays comparable to Phase 1's locked
measurement, which means the figure has to actually exist.
Output: `07-PERF-03-BASELINE.md`, carrying the harness-readiness evidence, the CI run id, and the
transcribed baseline PERF-03 row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-VERIFICATION.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-03-SUMMARY.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-10-SUMMARY.md
</context>

## Artifacts this phase produces

New in this plan: one planning artifact,
`.planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md`, with three sections
(`## 1. Harness readiness`, `## 2. The D-17 baseline run`, `## 3. The contribution-schedule
figure`). No new production code symbol, no new exported function, no new module. The only source
changes permitted are reductions to the sampling cost of bench arms that record no
`MeasurementRow`. The full phase-level artifact list is in `07-01-PLAN.md` under the same heading;
this plan adds nothing to it.

## UI Considerations

`07-UI-SPEC.md`'s `## UI Considerations` section covers 8 elements and 54 applicable
considerations. This plan touches the bench harness and one planning document. It renders nothing,
adds no DOM node, and changes no component. No consideration from that section bears on this work,
and none is dropped: all 54 remain covered by plans 07-01 through 07-10, whose evidence
07-VERIFICATION.md verified.

## Out of scope, deliberately

- **WR-01 through WR-04 from `07-REVIEW.md`** are WARNING-tier advisories. None of them blocks a
  roadmap truth or a requirement id, and 07-VERIFICATION.md records them outside the `gaps:` block.
  They are not planned here.
- **The two `human_verification` items in 07-VERIFICATION.md** are eyes-on visual judgments
  (perceptual legibility of the ruin hatch; observing the WR-01 caption race during a live drag).
  Neither is automatable and neither is a gap. They are not planned here.
- **`workflow.human_verify_mode` is `end-of-phase`**, so this plan contains no
  `checkpoint:human-verify`. Its one blocking pause is a `checkpoint:human-action`, because the
  action it needs (pushing this branch so GitHub Actions runs) is one the project's own
  instructions forbid the agent from taking unasked.

<assumption_delta_decision>
The `assumption-delta scan` detector fires `detected: true` on this phase scope, with three
signals: `pluralization` on the word "multiple" (the metric name `multiple-of-contributed`, not a
second instance of anything), and `chosen` on "chosen treatment" and "stops-parameterized" (both
Phase 6 vocabulary carried into the roadmap text). All three are prose collisions with the trigger
vocabulary, not identity-model changes.

- **Primary noun:** unchanged. `SweepGrid` remains keyed by `row * cols + col` over one grid.
- **Decision:** `no-change`. This plan introduces no second case of anything: one bench file
  remains the single PERF-03 recorder, one baseline machine remains declared, one budget row
  remains.
- No accepted debt, so nothing would force a later promote.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: Prove end to end that a bench run can land a PERF-03 verdict, and clear whatever actually stops it</name>
  <reversibility rating="reversible">Reducing an informational arm's sample count is a one-line change in one bench file, revertible with no migration and no contract broken.</reversibility>
  <files>bench/heatmap-form-2.bench.test.ts, bench/sweep.bench.test.ts, .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md</files>
  <read_first>
    - `bench/report.ts` in full, `assertRunInvariants` especially: the ordered list of run-level checks, the `PERF_08_COVERAGE_PHASE` coverage check, the `environment.ci` branch of the `hostMatchesPerf03Baseline` guard, and the `BENCH_TOTAL_RUNTIME_CAP_MS` check that runs last
    - `bench/global-setup.ts` in full, specifically that `totalRuntimeMs` is `Date.now() - runStart` measured from before the browser starts, and that `.bench/bench-results.json` is written BEFORE `assertRunInvariants` is called
    - `perf-budgets.ts`, the `BENCH_TOTAL_RUNTIME_CAP_MS` doc comment and the `PERF_03_BASELINE_HARDWARE_CONCURRENCY` doc comment
    - `bench/sweep.bench.test.ts` in full, both measured arms and the withheld-verdict branch
    - `.github/workflows/ci.yml` in full, and `tests/ci-workflow.test.ts` (it positionally pins `npm run build` before `npm run bench`; any step reordering breaks it)
    - `07-10-SUMMARY.md`'s "Issues Encountered" and "Next Phase Readiness" sections, which record both carried-forward harness conditions
    - `07-03-SUMMARY.md`'s `key-decisions` block, the PERF-03 escalation-candidate entry
    - `.planning/WINDOWS.md` entry 2, for the recorded runner-variance band on normalized figures
  </read_first>
  <action>
Establish the real blocker from evidence before changing anything. Three facts are already on
record from the gap analysis and must be re-confirmed, not assumed:

(a) The CI bench job genuinely IS the D-17 baseline. Download the most recent successful CI bench
artifact with `gh run download <run-id> -R andynumber2/leverage-simulator --name bench-results` and
read its `environment.hardwareConcurrency`. Use `gh run list -R andynumber2/leverage-simulator
--workflow CI` to pick the run. If that value is 4, the baseline machine is real and no CI
configuration needs changing.

(b) That same artifact's PERF-03 row and its `totalRuntimeMs`. Record both. A prior run rendering a
non-withheld PERF-03 verdict proves the withheld-verdict machinery is not what blocks a verdict;
its `source` field tells you whether that figure predates 07-03's retarget.

(c) Why no verdict exists for the retargeted bench: check whether this branch has an upstream and
whether a pull request exists for it, with `git rev-parse --abbrev-ref --symbolic-full-name @{u}`
and `gh pr list -R andynumber2/leverage-simulator --state all --head
gsd/phase-07-sweep-engine-and-the-heatmap`. `.github/workflows/ci.yml` triggers only on
`pull_request` and on `push` to `main`, so a branch with neither has never run the job.

Then run the full suite locally, `npm run bench`, and read its exit code and its `Total bench
runtime` line against `BENCH_TOTAL_RUNTIME_CAP_MS`. Two conditions 07-10-SUMMARY.md carried forward
are now in scope because they are the mechanism by which a run-level invariant can discard a real
verdict:

- The isolated-single-file PERF-08 coverage failure does NOT apply to a full-suite run. The full
  suite measures every `PERF-08` sub-budget, so `assertRunInvariants`'s coverage check is satisfied.
  Confirm this from the run's own output and record it; do not "fix" a condition that only exists
  when a single file is run alone.
- The total-runtime cap DOES apply. If `npm run bench` exits non-zero solely on the cap, that is a
  real defect against this plan, because the same run-level invariant runs in CI and would fail the
  job that has to publish the verdict.

Clear the cap breach by removing avoidable measurement cost, in this order, and stop as soon as the
suite exits 0:

1. Measure where the time actually goes before cutting anything. Run the suite with a per-file
   reporter (`npm run bench -- --reporter=verbose`, or per-file invocations timed individually,
   reading each file's figures from stdout rather than exit code, since a single-file run trips the
   unrelated PERF-08 coverage check at teardown). Write the per-file costs into
   `07-PERF-03-BASELINE.md` section 1. Cut the largest cost that meets the next rule.
2. Only arms that record NO `MeasurementRow` are eligible. In `bench/heatmap-form-2.bench.test.ts`
   the `PERF-05-heatmap-form-2-polygon-rejected` arm re-measures a path plan 07-04 already rejected
   and whose figure is already recorded in that file's header, in 07-04-SUMMARY.md and in
   PROJECT.md; its `PERF-05-heatmap-form-2-phase6-geometry` arm is likewise informational. Reducing
   an eligible arm's batch size or repeat count preserves its disclosure (the figure is still
   printed, with its batch size still disclosed) while cutting wall clock. State the reduction and
   the reason inline in the file.
3. `bench/sweep.bench.test.ts`'s contribution-schedule info arm is ALREADY at `sampleCount=1` and
   is the sole evidence plan 07-12's Key Decision rests on. Do not reduce it and do not remove it.
   The PERF-03 headline arm is off limits entirely: its `REPEAT_COUNT`, its 10,000-cell grid, its
   extended-tier entry-date axis and its four correctness gates all stay exactly as they are.

Do not raise `BENCH_TOTAL_RUNTIME_CAP_MS`. Do not retune `NOMINAL_REFERENCE_MS`. Do not move a
`thresholdMs`. Do not change what `totalRuntimeMs` measures. If the cap cannot be cleared without
breaking one of those rules, HALT and report: raising the cap is a recorded decision this plan does
not authorize, and sharding the run across two vitest invocations would split the accumulator and
re-trip the PERF-08 coverage check per shard, which is a larger harness change than this gap
warrants.

Note for the record, do not act on it: under `CI=true` this dev sandbox correctly exits non-zero on
the baseline-host guard, because it reports 9 logical cores and is not the baseline machine. That
is the guard working. The acceptance criterion below is the plain `npm run bench` invocation.

Create `07-PERF-03-BASELINE.md` with section 1, `## 1. Harness readiness`, recording: the CI run id
and head SHA inspected, its `environment.hardwareConcurrency`, its PERF-03 row verbatim (including
`source` and `verdict`), its `totalRuntimeMs` and the headroom that leaves against the cap, the
branch/PR finding from (c), the per-file cost table, and exactly what was reduced and why. Label
every figure measured on this sandbox as non-baseline, informational, with its core count, exactly
as PROJECT.md's existing rows do.
  </action>
  <acceptance_criteria>
    - `npm run bench` exits 0.
    - The run prints a `Total bench runtime:` line whose value is below the cap printed on the same line.
    - `node -e "const j=require('./.bench/bench-results.json'); const p=j.rows.filter(r=>r.requirementId==='PERF-08'); if(p.some(r=>r.verdict==='unmeasured')) process.exit(1)"` exits 0, proving the full-suite run satisfies the PERF-08 coverage invariant.
    - `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/report.ts` exits 0, proving no threshold, no cap and no calibration constant moved.
    - `git diff -- bench/sweep.bench.test.ts` shows no change to the PERF-03 headline test's repeat count, grid dimensions, entry-date axis construction or correctness assertions, and no change to the contribution-schedule arm's `sampleCount`.
    - `.planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md` exists and contains a `## 1. Harness readiness` heading.
    - Section 1 states the inspected CI run id, that run's `environment.hardwareConcurrency` value, and that run's PERF-03 `source` and `verdict` values.
    - Section 1 states whether `gsd/phase-07-sweep-engine-and-the-heatmap` has an upstream and an open or merged pull request, with the command output that established it.
    - Every millisecond figure in section 1 that was measured on this development sandbox is written with its core count and the word informational or non-baseline beside it.
    - `npm run test -- --run tests/ci-workflow.test.ts tests/report.test.ts tests/perf-budgets.selftest.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run bench &amp;&amp; git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/report.ts &amp;&amp; npm run test -- --run tests/ci-workflow.test.ts tests/report.test.ts tests/perf-budgets.selftest.test.ts</automated>
  </verify>
  <done>The full bench suite exits 0 on its own run-level invariants, the real reason no baseline verdict exists is written down with the commands that established it, and no budget, cap or calibration constant moved.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The owner runs the D-17 baseline job</name>
  <reversibility rating="costly">Pushing this branch publishes phase-07 history to a public repository; withdrawing it means a force-push, which the project's instructions forbid without a fresh explicit request.</reversibility>
  <read_first>
    - `.github/workflows/ci.yml`, specifically its `on:` block, to confirm which events start the job
    - `07-PERF-03-BASELINE.md` section 1 as written by Task 1
    - `CLAUDE.md` (repository root) and `~/.claude/CLAUDE.md`, the standing rule that the agent commits freely but never pushes without an explicit request
  </read_first>
  <action>
Pause here. Everything up to the push is done and committed; the trigger is what is missing.

Task 1 established that `.github/workflows/ci.yml`'s bench job runs on `ubuntu-latest`, that
`ubuntu-latest` genuinely reports 4 logical cores (the declared
`PERF_03_BASELINE_HARDWARE_CONCURRENCY`), and that the job has produced a real non-withheld PERF-03
verdict before. It also cleared whatever run-level invariant would have failed the job after the
measurement already succeeded. What remains is that the workflow starts only on a `pull_request` or
a `push` to `main`, and this branch has had neither, so the `bench/sweep.bench.test.ts` plan 07-03
retargeted has never executed on the baseline machine.

The agent must NOT push. The project's standing instructions authorize commits freely and forbid
`git push` without an explicit request, and a push to a public repository is not reversible on the
agent's own authority. Present the two commands below, wait, and do not run them yourself.
  </action>
  <instructions>
1. Review what would be pushed: `git log --oneline main..HEAD` and `git diff main...HEAD --stat`.
2. Push the branch and open the pull request, which starts the CI bench job:
   `git push -u origin gsd/phase-07-sweep-engine-and-the-heatmap`
   then `gh pr create -R andynumber2/leverage-simulator --fill`
3. Watch it with `gh run watch -R andynumber2/leverage-simulator`.
4. When it finishes, read the run id from `gh run list -R andynumber2/leverage-simulator --limit 1`
   and reply with it.

If you would rather not push this branch, reply "declined". Task 3 then records that no D-17
baseline figure was obtainable inside this phase, and plan 07-12 decides against the best honest
evidence available, which is the dev-sandbox figure labelled non-baseline. Nothing is fabricated
either way.
  </instructions>
  <verification>
The `bench-results` artifact uploads on `if: always()`, so the PERF-03 figure is recoverable by run
id whether the job goes green or red. Confirm the run is the right one before replying:
`gh run view <id> -R andynumber2/leverage-simulator --json headBranch,headSha,conclusion` should
report `headBranch` `gsd/phase-07-sweep-engine-and-the-heatmap` and a `headSha` equal to
`git rev-parse HEAD`.

A red job is still usable evidence and is not a reason to re-run before replying: whatever made it
red is itself the finding, and Task 3 records it.
  </verification>
  <acceptance_criteria>
    - The resume signal is either a numeric GitHub Actions run id for a CI run whose head branch is `gsd/phase-07-sweep-engine-and-the-heatmap`, or an explicit decline.
    - If a run id is given, `gh run view <id> -R andynumber2/leverage-simulator --json headBranch,headSha,conclusion` returns a `headSha` that matches `git rev-parse HEAD` at the time of the push.
    - If a run id is given, `gh run download <id> -R andynumber2/leverage-simulator --name bench-results` succeeds and yields a `bench-results.json`.
  </acceptance_criteria>
  <resume-signal>Reply with the CI run id (for example "run 32551360160"), or "declined" to record that no baseline run is available.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Transcribe the baseline verdict from the artifact, verbatim</name>
  <precondition>Task 2 returned either a CI run id whose artifact `gh run download` can fetch, or an explicit decline.</precondition>
  <files>.planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md</files>
  <read_first>
    - `07-PERF-03-BASELINE.md` section 1 as written by Task 1
    - `bench/report.ts`'s `MeasurementRow` interface and `MeasurementSource` type, so every transcribed field is named the way the artifact names it
    - `.planning/PROJECT.md`'s two existing D-17 baseline rows (the JS-over-WASM row and the PERF-05 hand-rolled-canvas row), for the exact form a baseline figure is written in: measured value, normalized value, worker and chunk counts, calibration score, run id, and the separate clearly-labelled dev-sandbox figure
    - `.planning/WINDOWS.md` entry 2, and the `PERF-07 measurement band` info line the bench run prints, for the confidence band a single baseline run supports
  </read_first>
  <action>
Fetch the artifact for the run id Task 2 returned with `gh run download <id> -R
andynumber2/leverage-simulator --name bench-results`, and write `## 2. The D-17 baseline run` into
`07-PERF-03-BASELINE.md` from the downloaded `bench-results.json` only. Do not retype a number from
the job's stdout, from memory, or from any earlier document.

Transcribe: the run id, the run's `headSha` and `conclusion`; the full `environment` block
(`hardwareConcurrency`, `os`, `calibrationScore`, `ci`, `timestamp`); the PERF-03 row's
`measuredMs`, `normalizedMs`, `budgetMs`, `source` and `verdict` exactly as the JSON carries them;
the run's `totalRuntimeMs`; and the two PERF-03 info lines verbatim, the `PERF-03 sweep:` line
(which carries `workerCount`, `hardwareConcurrency` and the withheld-or-rendered disclosure) and
the `PERF-03 with a monthly contribution schedule` line.

State the verdict plainly in prose beneath the transcription: whether the row is non-withheld, what
percentage of the 1000ms budget the normalized figure is, and whether it crossed
`ESCALATION_TRIGGER_RATIO`. Cite the confidence band the run's own `measurement band` info line
declares, so a single-run figure is not read as tighter than it is.

Add `## 3. The contribution-schedule figure` recording the `solveIrr`-branch figure from the same
run alongside the dev-sandbox figure 07-03-SUMMARY.md and 07-VERIFICATION.md already carry, each
labelled with its host core count and whether it is the baseline. This section is the input plan
07-12's decision reads; it states the numbers and takes no position on them.

If Task 2 was declined, write both sections as an explicit negative record instead: no D-17
baseline run was obtained during this phase, the reason, and the best available evidence, which is
the dev-sandbox figure labelled non-baseline with its 9-core host stated. Do not write a baseline
figure that does not exist, do not interpolate one, and do not present a sandbox figure as a
baseline. Say in one line what would produce the real figure later.
  </action>
  <acceptance_criteria>
    - `07-PERF-03-BASELINE.md` contains a `## 2. The D-17 baseline run` heading and a `## 3. The contribution-schedule figure` heading.
    - Section 2 states a run id, a head SHA, and an `environment.hardwareConcurrency` value, or states in its first sentence that no baseline run was obtained and why.
    - When a run was obtained: section 2 quotes the PERF-03 row's `source` and `verdict` values, and `verdict` is not `unmeasured`.
    - When a run was obtained: `node -e "const j=require('<downloaded>/bench-results.json'); const r=j.rows.find(x=>x.budgetId==='PERF-03'); if(r.verdict==='unmeasured'||r.source!=='production') process.exit(1)"` exits 0.
    - Every figure in sections 2 and 3 is followed by its host core count, and every figure not from the baseline run carries the word informational or non-baseline.
    - `grep -c "hardwareConcurrency" .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md` is at least 3.
    - `git diff --exit-code -- perf-budgets.ts bench/calibration.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md &amp;&amp; grep -q "## 2. The D-17 baseline run" .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md &amp;&amp; grep -q "## 3. The contribution-schedule figure" .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md &amp;&amp; git diff --exit-code -- perf-budgets.ts bench/calibration.ts</automated>
  </verify>
  <done>The phase has a non-gitignored record of a real baseline PERF-03 verdict copied field for field from the CI artifact, or an explicit, honest record that no baseline run was obtainable and what the best available evidence is instead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measured figure to recorded claim | A number crossing from `.bench/bench-results.json` (gitignored, per-run) into a planning document becomes a durable claim later phases and later readers rely on |
| CI artifact to local working tree | A `bench-results.json` fetched by run id could come from a different commit, a different branch, or a different machine than the one being claimed |
| Bench harness to CI gate | A run-level invariant that fails for a reason unrelated to a budget can discard a genuine verdict before it is reported |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-25 | Repudiation | `07-PERF-03-BASELINE.md` | high | mitigate | Every figure is transcribed field for field from the downloaded `bench-results.json`, carrying its `source`, `verdict` and host `hardwareConcurrency`; a row whose `verdict` is `unmeasured` may not be written as a verdict, and every non-baseline figure carries its core count and the word informational or non-baseline |
| T-07-26 | Tampering | run-level invariants and the budget table | high | mitigate | Only arms recording no `MeasurementRow` may have sampling cost reduced; `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/report.ts` is an acceptance criterion on Task 1, so no threshold, cap or calibration constant can move under cover of this work |
| T-07-27 | Spoofing | CI artifact provenance | medium | mitigate | The artifact is fetched by explicit run id with `gh run download -R andynumber2/leverage-simulator`, and the run's `headSha` is recorded and checked against the pushed commit, so a figure from another commit cannot be presented as this phase's baseline |
| T-07-28 | Denial of Service | `.github/workflows/ci.yml` bench step | medium | mitigate | Task 1 clears the run-level cap breach before the job is triggered, so the job that has to publish the verdict does not die at teardown after the measurement already succeeded |
| T-07-02 | Elevation of Privilege | fork pull-request trigger surface | low | accept | Already mitigated by D-01 and pinned by `tests/ci-workflow.test.ts`: `pull_request` only, workflow-level `contents: read`, no secrets referenced. This plan changes no workflow file, and that test is in its verify command |
| T-07-04 | Information Disclosure | whole phase | low | accept | Client-side, backend-less tool. No credentials, accounts or PII exist to disclose; the bench artifact carries only timings, a user-agent string and a core count |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `07-RESEARCH.md`'s Package Legitimacy Audit records "Not applicable this phase, no new external packages are introduced", zero `[SLOP]`, zero `[SUS]`, zero `[ASSUMED]` |
</threat_model>

<verification>
- `npm run bench` exits 0 on this host, with its printed total runtime under the printed cap.
- `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/report.ts` exits 0.
- `npm run test -- --run tests/ci-workflow.test.ts tests/report.test.ts tests/perf-budgets.selftest.test.ts` exits 0.
- `npm run typecheck` exits 0.
- `07-PERF-03-BASELINE.md` exists with all three section headings, and section 2 either transcribes a non-withheld PERF-03 row from a named CI run or records explicitly that none was obtainable.
</verification>

<success_criteria>
- The reason PERF-03 has no baseline verdict is established from repository and CI evidence, with the commands that established it written down, not inferred.
- The full bench suite no longer fails on a run-level invariant unrelated to any budget.
- No threshold, no cap, no calibration constant, and no headline measurement arm changed.
- A non-gitignored document carries either the real baseline verdict or an explicit statement that it could not be obtained, with no fabricated or unlabelled figure anywhere in it.
</success_criteria>

<output>
Create `.planning/phases/07-sweep-engine-and-the-heatmap/07-11-SUMMARY.md` when done
</output>

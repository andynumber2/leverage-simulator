---
phase: 07.1-sweep-pool-tuning-inserted
verified: 2026-08-27T00:00:00Z
status: gaps_found
score: 4/6 truths verified (3 cleanly met, 1 met via its own built-in escalation clause); 2 failed
behavior_unverified: 0
overrides_applied: 0
re_verification: null
gaps:
  - truth: "The zero-contribution headline PERF-03 row measures at or under 1000ms on the declared D-17 baseline, source=production, verdict=pass, covering the fixed 200x50 default grid."
    status: failed
    reason: >
      Measured FAILED on every run this phase produced. 07.1-PERF-03-BASELINE.md section 2
      transcribes five hardwareConcurrency=4, source=production CI runs, all verdict=fail:
      1120.86ms, 1208.38ms, 1115.92ms normalized at worker width 3 (three runs); 1191.34ms at
      worker width 4 (the authoritative run, CI 32676218114); 1411.05ms at width 3 again
      post-revert (CI 32677103132). Width 4 landed inside the width-3 spread rather than below
      it, refuting 07.1-PERF-03-PROFILE.md section 5's own 25.00% linear-scaling projection, and
      workerCountForCores was reverted to cores - 1 as a direct result.

      Subsequent corroborating context, postdating this phase and NOT performed by it (cited here
      because it settles whether the gap is closeable, not because 07.1 did this work): three
      quick tasks on 2026-08-24 (quick-260824-46s, quick-260824-52h, quick-260824-r5d) measured
      and refuted the two compute-side levers 07.1-06-SUMMARY.md had left named as unspent, the
      kernel's write-only per-bar output arrays (1-2% gain only) and pool/dispatch overhead
      (0.14ms across the whole grid, most of the earlier 273.98ms residual was a narrow-arm
      extrapolation artifact, not a tunable cost), and a third task ruled out hand-optimizing
      per-cell kernel compute itself (six of eight ablation arms measured slower than shipped).
      Combined with WASM (ruled out in Phase 1, ~1.20x slower) and worker width 4 (this phase,
      no improvement), PROJECT.md's "PERF-03 merge bar released (2026-08-25)" row records that
      five levers are now measured and refuted, not reasoned about, leaving only D-03's coarser
      default grid, a last resort that changes what is measured rather than closing the
      measured figure, explicitly reserved against exactly this situation. On 2026-08-25 the
      project released the two-consecutive-pass MERGE BAR (a process gate) so the milestone stops
      blocking behind a budget no available lever reaches; the PERF-03 budget itself
      (thresholdMs=1000) was not relaxed and the criterion is not claimed met.
    artifacts:
      - path: "bench/sweep.bench.test.ts"
        issue: "Sole PERF-03 recorder. Produces real, non-withheld verdicts on the declared baseline; every one is FAIL for the headline row."
      - path: ".planning/PROJECT.md"
        issue: "Carries the 'PERF-03 merge bar released' Key Decision (row ~197) recording all five refuted levers and the process-only release. PERF-03.thresholdMs remains 1000; nothing was relaxed."
    missing:
      - "A sixth, genuinely new lever, distinct from the five already measured and refuted, or an accepted change to what the criterion measures (e.g. a Key-Decision-gated coarser default grid). Per PROJECT.md's own closing line, reopening PERF-03 requires a measurement, not a projection."
  - truth: "The bench suite's own total runtime is back inside BENCH_TOTAL_RUNTIME_CAP_MS (30,000ms) on the baseline host."
    status: failed
    reason: >
      Not met. 07.1-05 spent every lever available within its own authorized file list (standing
      cost budgets on the phase's two new diagnostic arms, removal of an already-rejected
      re-measurement in heatmap-form-2.bench.test.ts) and cut what it could touch by roughly half,
      but the two largest costs (bench/sweep.bench.test.ts and bench/perf-07.bench.test.ts) are
      both fully gated files outside this plan's authority. The authoritative run (CI 32676218114,
      width 4) measured totalRuntimeMs=56141 against the 30000ms cap, 87.1% over. The three
      width-3 D-17 runs ranged 45709-66527ms, all also over cap. The independently-verifiable half
      of this criterion (assertRunInvariants reports a cap breach independently of a verdict-fail)
      IS met, 07.1-02 landed that clamp and it is confirmed in the codebase, but the criterion's
      conjunctive text requires both halves, and the runtime itself remains far over its cap.
    artifacts:
      - path: "bench/report.ts"
        issue: "assertRunInvariants correctly collects and reports a runtime-cap breach independently of a verdict-fail gate (07.1-02); this half is verified working, but does not by itself satisfy the criterion."
    missing:
      - "Either explicit authority (a new Key Decision, since the no-relaxation discipline applies) to reduce the two gated files' own measured workload, or a restructuring separating a fully-gated CI job from a non-gating diagnostic job. PROJECT.md's 'BENCH_TOTAL_RUNTIME_CAP_MS shortfall' Key Decision names this remaining path and states no future plan has yet been authorized to take it."
deferred: []
human_verification: []
---

# Phase 07.1: Sweep Pool Tuning (INSERTED) Verification Report

**Phase Goal:** The 10,000-cell sweep completes inside its 1000ms budget on the declared 4-core baseline, closing Phase 7's third success criterion by making the number true rather than by moving the number
**Verified:** 2026-08-27
**Status:** gaps_found
**Re-verification:** No, initial verification (this artifact did not previously exist; all 6 plans executed 2026-08-24/25 with no prior VERIFICATION.md)

## Why this report exists

Phase 07.1 executed all 6 of its plans and shipped, but no `07.1-VERIFICATION.md` was ever
produced. `gsd-tools query init.progress` reported `completion_status: "executed"` rather than
`complete`, while ROADMAP.md's progress table hand-annotated the phase as "Complete (3/6 criteria
met, 3 escalated; PERF-03 merge bar released 2026-08-25)." This report closes that gap by
independently verifying the roadmap annotation against the phase's own artifacts, rather than
merely restating it.

**The phase did not achieve its stated goal.** The 10,000-cell sweep does not complete inside its
1000ms budget on the declared 4-core baseline. Criterion 2 is a measured FAIL, recorded as such
below. What the phase did achieve: it spent the lever Phase 7 named (pool tuning), measured it
honestly on the real baseline rather than projecting it, found it did not work, reverted it, and
escalated with named remaining levers rather than silently declaring victory or quietly loosening
a budget. That is a successful piece of engineering discipline and a failed roadmap criterion at
the same time, both are true, and this report states both.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

Primary source for this table is `07.1-PERF-03-BASELINE.md` section 7 (the phase's own
six-criterion disposition), cross-checked against the six plan SUMMARY files, PROJECT.md's Key
Decision rows, and this session's own re-run of the PERF-01a diff check rather than copied
wholesale.

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Where the 4-core baseline spends its wall clock is a measured breakdown, not an inference | ✓ MET | `07.1-PERF-03-PROFILE.md` sections 2-5: attributed figures for worker startup (224.80ms), chunk dispatch/transfer (zero-copy, ruled out as a cost), per-cell kernel time (idealParallelMs splits for both branches), and idle/residual time, all at hardwareConcurrency=4. Landed by 07.1-01, confirmed present and substantive in this session's read |
| 2 | Zero-contribution headline row at or under 1000ms, `source=production`, `verdict=pass` | ✗ FAILED | `07.1-PERF-03-BASELINE.md` section 2: 5 of 5 D-17 production runs FAIL, 1120.86, 1208.38, 1115.92ms (width 3), 1191.34ms (width 4, authoritative), 1411.05ms (width 3, post-revert). See Gaps below |
| 3 | Nothing was relaxed to get there (`PERF-03.thresholdMs`, `NOMINAL_REFERENCE_MS`, D-19's lock, D-03's grid) | ✓ MET | Independently re-run this session: `git diff --exit-code 7c21f0bc2f5fe7637ea7e898320b84f5824199eb..HEAD -- perf-budgets.ts bench/calibration.ts bench/canonical-calibration.ts` exits 0, zero diff, confirmed against current HEAD (`8a4ad03`, well past this phase, Phase 8 and its follow-ups merged). See PERF-01a Compliance below |
| 4 | `solveIrr` branch re-measured at a real sample count, inside budget or escalated with a named remaining lever | ✓ MET (via the criterion's own escalation clause) | 07.1-03 shipped a rate-space bracket-width termination test (`RATE_TOLERANCE`, confirmed present in `src/metrics/irr.ts`) and a `CashFlows` typed-array container, cutting `npvEvaluationsPerSolve` from 105.01 to ~36.3 and measuring a real 2.27-2.30x improvement on two D-17 production runs (3888.66ms, 3952.70ms normalized). Still ~3.9x over budget, but PROJECT.md's row 193 records the required Key Decision naming Newton-with-bisection-fallback as the specific remaining lever and explaining why pool tuning alone could not reach it, exactly what this criterion's text asks for as an alternative to a raw pass |
| 5 | Bench suite total runtime back inside `BENCH_TOTAL_RUNTIME_CAP_MS` (30,000ms), and `assertRunInvariants` reports a cap breach independently of verdict-fail | ✗ FAILED | Independently confirmed in `bench/report.ts`: `assertRunInvariants` now collects and reports a runtime-cap violation whether or not a verdict-fail also fires (07.1-02), that half is real. But total runtime remains far over cap: 56141ms (authoritative run) against 30000ms, 87.1% over; three width-3 runs ranged 45709-66527ms, also all over. The criterion is conjunctive and the runtime half is not met. See Gaps below |
| 6 | Sweep still computes what it computed before (per-cell one-pass metrics, cancellation, pool-versus-serial equality at the tuned shape, ruin/short-horizon rules) | ✓ MET | `07.1-PERF-03-BASELINE.md` section 5: every named correctness gate green in CI job 32676218114 at width 4 (`tests/sweep/metrics-one-pass.test.ts`, `cancellation.test.ts`, `column-series.test.ts`, `partition.test.ts` at every worker count 1-8, `upro-tqqq-gate.test.ts`, `irr.test.ts`, and `bench/sweep.bench.test.ts`'s pool-versus-serial equality gates asserted at the width-4 partition shape actually shipped). Re-verified locally at width 3 after the revert. `tests/sweep-pool.test.ts` expectations updated to assert width 3, confirmed present in current codebase |

**Score:** 4/6 truths verified under a strict reading of each criterion's own text (criteria 1, 3,
4, 6); 2/6 failed (criteria 2, 5). The roadmap's own hand-annotation ("3/6 criteria met, 3
escalated") groups criteria 2, 4, and 5 together as "escalated" because all three carry a PROJECT.md
Key Decision naming a remaining lever or a released process gate. This report keeps that grouping
visible but scores criterion 4 separately as MET, because criterion 4's own text explicitly
accepts "recorded as a...Key Decision naming the specific remaining lever" as a satisfying
condition, not just an escalation of a failure, unlike criteria 2 and 5, whose text describes
only a pass/fail measurement with no such alternative. Both framings agree on the substance that
matters: the headline PERF-03 figure (criterion 2) is unambiguously failed, and the phase's goal
sentence, closing Phase 7's third success criterion, is not achieved.

### PERF-01a Compliance (independently verified, not taken from the summaries)

```
git diff --exit-code 7c21f0bc2f5fe7637ea7e898320b84f5824199eb..HEAD -- perf-budgets.ts bench/calibration.ts bench/canonical-calibration.ts
exit code: 0
```

Zero diff. `bench/canonical-calibration.ts` exists in the current tree and is also unchanged
against the phase base commit. `git log --oneline` over the same range and path list returns no
commits, confirming HEAD's advancement past this phase (Phase 8 merged, current HEAD `8a4ad03`)
carried no calibration or budget-constant change of any kind. This independently confirms
criterion 3 and corroborates PROJECT.md's repeated "no budget value, calibration constant, run-level
cap, or grid dimension moved" claims across every Key Decision row this phase and its follow-up
quick tasks wrote.

### Required Artifacts

| Artifact | Expected | Status |
|----------|----------|--------|
| `bench/sweep-pool-profile.bench.test.ts` | Diagnostic-only breakdown for both branches (07.1-01) | ✓ VERIFIED: exists, 374 lines, substantive |
| `07.1-PERF-03-PROFILE.md` | Attributed breakdown document, section 5 corrected in place after the revert | ✓ VERIFIED: exists, correction present verbatim alongside preserved original text |
| `bench/report.ts` (`assertRunInvariants`) | Collects verdict-fail and runtime-cap violations together (07.1-02) | ✓ VERIFIED: confirmed present |
| `tests/sweep/partition.test.ts` | Exactly-once column-coverage proof for `partitionColumns` | ✓ VERIFIED: exists, 118 lines |
| `src/metrics/irr.ts` | `RATE_TOLERANCE` bracket-width test alongside dollar-denominated `TOLERANCE`; `CashFlows` typed-array container | ✓ VERIFIED: both present, confirmed by direct grep of the shipped file |
| `bench/sweep-contention.bench.test.ts` | Main-thread responsiveness evidence at width 3 and width 4 (07.1-04) | ✓ VERIFIED: exists, 409 lines |
| `07.1-PERF-03-BASELINE.md` | Authoritative D-17 gate run transcription with six-criterion disposition (07.1-06) | ✓ VERIFIED: exists, all figures independently re-checked against the artifact per its own section 1 methodology |
| `src/sweep/sweep-pool.ts`, `bench/sweep-pool.ts` (`workerCountForCores`) | Reverted to `cores - 1` after the abort condition fired | ✓ VERIFIED: confirmed in current codebase: `return Math.max(1, cores - 1)` |
| `tests/sweep-pool.test.ts` | Updated to assert width 3 (not the reverted-away width 4) | ✓ VERIFIED: exists, 38 lines |
| `.planning/PROJECT.md` | Key Decision rows for worker-count revert, `solveIrr` escalation, bench-runtime shortfall, PERF-03 headline outcome | ✓ VERIFIED: all four rows present (rows ~192-195), plus the subsequent "PERF-03 merge bar released" row (~197) recording the process-level release |
| `.planning/REQUIREMENTS.md` | PERF-03 mapping updated to reflect escalation status | ✓ VERIFIED: line 299: "Escalated, NOT MET, and no longer blocking. Five levers measured and refuted" |

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) found in any of the eight
source/test files this phase modified (`src/sweep/sweep-pool.ts`, `bench/sweep-pool.ts`,
`tests/sweep-pool.test.ts`, `src/metrics/irr.ts`, `src/sweep/sweep.worker.ts`,
`bench/sweep.bench.test.ts`, `bench/report.ts`, `tests/sweep/partition.test.ts`).

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `bench/sweep.bench.test.ts` | `src/sweep/sweep-pool.ts` | production-pool PERF-03 measurement, now at the reverted `cores - 1` width | ✓ WIRED, verdict FAIL |
| `src/sweep.worker.ts` (`solveIrr` branch) | `src/metrics/irr.ts` | `RATE_TOLERANCE` bracket-width test now checked in the same loop as the dollar-denominated `TOLERANCE` test | ✓ WIRED, confirmed via `grep -n "RATE_TOLERANCE" src/metrics/irr.ts` |
| `bench/report.ts` (`assertRunInvariants`) | CI job output | collects verdict-fail + runtime-cap violations into one thrown message | ✓ WIRED (07.1-02) |
| `07.1-PERF-03-BASELINE.md` | `.planning/PROJECT.md` | Phase's own baseline transcription feeds the Key Decision rows this session cross-checked | ✓ WIRED, figures match across both documents |

### Requirements Coverage

Per ROADMAP.md's own Coverage section, Phase 7.1 adds no new requirement mapping, it re-measures
PERF-03 (primary mapping stays Phase 7) and works under PERF-01a (primary mapping stays Phase 1).
No orphaned requirements.

| Requirement | Status | Evidence |
|---|---|---|
| PERF-03 (re-measurement) | ESCALATED, NOT SATISFIED | `.planning/REQUIREMENTS.md` line 299 and line 30 (`[ ]`, unchecked); the criterion this phase existed to close remains open |
| PERF-01a (no-relaxation constraint) | SATISFIED | Independently re-verified `git diff --exit-code` above; zero diff across the full phase and its follow-up work to date |

### Anti-Patterns Found

None. No blocker or warning-tier debt markers in any file this phase touched.

### Behavioral Spot-Checks

Not re-run. This phase's evidence is exclusively CI-artifact-based (GitHub Actions
`hardwareConcurrency=4` runs, downloaded and independently re-parsed by the phase's own agents
per `07.1-PERF-03-BASELINE.md` section 1's stated methodology). Re-running the benches locally
would produce a different-host, non-baseline figure and was explicitly out of scope per this
task's own instructions ("Do not re-run the benches and do not re-derive these"). The correctness
regression suite's presence and pass status was instead confirmed by direct inspection of the CI
job log transcription in `07.1-PERF-03-BASELINE.md` section 5, which names each test file and its
pass/fail outcome for the authoritative run.

### Human Verification Required

None. Every truth in this report resolves against a CI-artifact-based measurement or a direct
codebase check (file existence, git diff, grep for the shipped mechanism). Nothing here depends
on visual judgment or real-time interaction.

### Gaps Summary

Two of six roadmap success criteria are unambiguously failed: the headline PERF-03 measurement
(criterion 2) and the bench-suite runtime cap (criterion 5). Neither is a case of missing or stub
code, every artifact this phase built exists, is substantive, and is wired; the gap is a real,
measured performance number that the built code does not close. The phase spent the one lever
Phase 7 named (worker-pool tuning: worker count 4, plus the `solveIrr` convergence and
representation fixes), measured it honestly against the real D-17 4-core baseline on five separate
CI runs, found the worker-count lever bought nothing on the headline figure, and reverted it. That
is real, disciplined engineering work; it did not produce a passing criterion.

Three quick tasks dated 2026-08-24, after this phase's own six plans completed, measured and
refuted the two levers 07.1-06-SUMMARY.md had left named as still unspent (the kernel's
write-only per-bar output arrays: 1-2% only; pool/dispatch overhead: 0.14ms across the whole
grid) plus per-cell kernel compute itself (six of eight hand-optimization arms measured slower
than shipped). These are cited here as subsequent corroborating context that the gap is not
easily closeable by further pool-side work, they are not part of this phase's own six plans and
are not claimed as this phase's work. Combined with WASM (ruled out in Phase 1) and worker width
4 (this phase), PROJECT.md records five levers now measured and refuted, leaving only D-03's
coarser default grid, a last resort that changes what PERF-03 measures rather than closing it,
as the sole remaining lever. On 2026-08-25 the project released the two-consecutive-pass MERGE
BAR (a process decision to stop blocking the milestone) without relaxing the 1000ms budget itself;
PERF-03 remains recorded as FAILED, not met, in both PROJECT.md and REQUIREMENTS.md.

This report's `gaps_found` status reflects that a must-have roadmap truth is FAILED, per the
verification decision tree, regardless of the project's own separate, already-recorded decision
to stop blocking on it. The two are not in tension: the project chose to proceed with an honestly
documented, unresolved performance gap rather than either hiding the failure or spending further
phases chasing a lever that measurement has already refuted five times over. This VERIFICATION.md
makes the artifact record agree with that already-public decision, rather than leaving a phase
marked "executed" with no report at all.

---

_Verified: 2026-08-27_
_Verifier: Claude (gsd-verifier)_

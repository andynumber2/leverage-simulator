---
phase: 07.1-sweep-pool-tuning-inserted
plan: 01
subsystem: testing
tags: [bench, vitest, performance-profiling, sweep-pool]

# Dependency graph
requires:
  - phase: 07-sweep-engine-and-the-heatmap
    provides: the real production Worker pool sweep, sweep.worker.ts's exported computeChunkMetrics, and bench/sweep.bench.test.ts's gated PERF-03 measurement this file explains
provides:
  - "bench/sweep-pool-profile.bench.test.ts: a diagnostic-only, info-line-only bench arm attributing the PERF-03 headline sweep's wall clock to pool construction, worker startup, serial kernel time, and pool overhead, plus the same breakdown for the D-24 solveIrr branch (cash-flow count and npv-evaluations-per-solve)"
  - "07.1-PERF-03-PROFILE.md: the attributed breakdown for both branches, read back from a real hardwareConcurrency=4, source=production CI verdict (run 32669644628), replacing PROJECT.md's inference with measured figures and disposing RESEARCH assumptions A1, A2, A4"
affects: [07.1 later plans that read this plan's PROFILE.md attribution before choosing a pool-tuning lever]

# Actuals (#2632)
actuals:
  tokens: 11500
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Diagnostic-only bench file: recordInfoLine exclusively, never the gated measurement-row channel, kept in its own module (own beforeAll, own pool) so instrumentation never perturbs the figure it explains (07.1-RESEARCH.md Pattern 2)"

key-files:
  created:
    - bench/sweep-pool-profile.bench.test.ts
    - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md
  modified: []

key-decisions:
  - "Task 1's own <precondition> and the orchestrator's explicit instruction both applied: the first executor's worktree branch (worktree-agent-a4185acfd0f39e3e2) could not push or open a PR. The bench file was created, locally verified, and committed (0b7a8ee); the push-and-CI-readback half of Task 1, and all of Task 2, were deferred to the orchestrator per 07-PERF-03-BASELINE.md section 2's own precedent. The orchestrator merged 0b7a8ee onto gsd/phase-07.1-sweep-pool-tuning (merge commit 4534f877), pushed it, and PR #8's synchronize event fired CI run 32669644628."
  - "cashFlowCount and the npvEvaluationsPerSolve batch were measured against the IRR_PROFILE_COLS-wide axis (the same axis serialKernelMs uses for the solveIrr branch), not the wider PROFILE_COLS axis, so all three figures describe the same tested column range and Open Question 3 gets one internally consistent answer."
  - "The solveIrr branch's warmSweepMs (24 cols) and serialKernelMs (8 cols) were measured at different cell counts, so the bench file's own recorded idealParallelMs/poolOverheadMs split for that branch is not apples-to-apples. 07.1-PERF-03-PROFILE.md Section 3 corrects this by scaling serialKernelMs to the 24-column width before dividing, rather than presenting the file's own uncorrected figures as the settled split."
  - "07.1-PERF-03-PROFILE.md traces the worker-startup question to bench/sweep.bench.test.ts's own test order (two correctness tests warm the shared pool before PERF-03's measureMinOfN begins), concluding the 224.80ms workerStartupMs figure is already excluded from the gated window and is not a lever with headroom left to spend against PERF-03 specifically."
  - "npvEvaluationsPerSolve measured 105.01 against RESEARCH's naive ~33.4 estimate; the root cause identified by reading src/metrics/irr.ts is that solveIrr's convergence test compares |npv(mid)| (a dollar-denominated NPV magnitude) against TOLERANCE, not the bracket width in rate-space, so the loop almost always exhausts MAX_ITERATIONS=100 rather than converging early. This is recorded as the corrected lever for a later plan, not implemented here (tracer discipline: no tuning behavior changed)."

requirements-completed: [PERF-03]

coverage:
  - id: D1
    description: "bench/sweep-pool-profile.bench.test.ts exists, records only via recordInfoLine, and both branches' info lines land in .bench/bench-results.json's infoLines"
    requirement: "PERF-03"
    verification:
      - kind: other
        ref: "npm run typecheck (exit 0); grep -c 'recordMeasurement' bench/sweep-pool-profile.bench.test.ts (returns 0); node -e checks against .bench/bench-results.json (both pass, this dev sandbox, hardwareConcurrency=9, informational)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A named CI run on gsd/phase-07.1-sweep-pool-tuning publishes a bench-results artifact with environment.hardwareConcurrency=4, a PERF-03 row at source=production with a rendered verdict, and this file's profile info lines for both branches"
    requirement: "PERF-03"
    verification:
      - kind: other
        ref: "gh run view 32669644628 -R andynumber2/leverage-simulator --json headBranch,headSha,conclusion,event (headBranch=gsd/phase-07.1-sweep-pool-tuning, conclusion=failure as expected, event=pull_request); gh run download 32669644628 --name bench-results; node -e checks against the downloaded bench-results.json confirm hardwareConcurrency=4, PERF-03 row source=production verdict=fail, and both sweep-pool-profile info lines present"
        status: pass
    human_judgment: false
  - id: D3
    description: "07.1-PERF-03-PROFILE.md records the D-17 baseline attribution for both branches, the per-file bench cost table, a named lever and reachable margin per branch, and an explicit disposition for research assumptions A1, A2 and A4"
    requirement: "PERF-03"
    verification:
      - kind: other
        ref: "node -e check against 07.1-PERF-03-PROFILE.md confirms all required tokens (hardwareConcurrency, npvEvaluationsPerSolve, cashFlowCount, poolOverheadMs, A1, A2, A4) present; grep -c hardwareConcurrency returns 17 (>= 4 required); grep for em dash character returns 0"
        status: pass
    human_judgment: false

duration: 55min
completed: 2026-08-23
status: complete
---

# Phase 07.1 Plan 01: Sweep-Pool Profile Bench Arm and Attributed Baseline Summary

**Diagnostic-only `bench/sweep-pool-profile.bench.test.ts` created, pushed by the orchestrator onto `gsd/phase-07.1-sweep-pool-tuning`, and read back from a real CI run (32669644628, `hardwareConcurrency=4`, `source=production`, PERF-03 `verdict=fail` at `normalizedMs=1120.86`). `07.1-PERF-03-PROFILE.md` transcribes and reasons over that readback, replacing PROJECT.md's inference-only root-cause statement with measured figures for both sweep branches.**

## Explicit answer to this plan's success criterion

**A real `hardwareConcurrency=4`, `source=production` PERF-03 verdict WAS read back for this plan's code, in this session.** CI run **32669644628** (workflow: CI, `pull_request` event, PR #8 `synchronize`, `headBranch=gsd/phase-07.1-sweep-pool-tuning`, `headSha=4534f877ede822417a90d224d788d3fd10ddb8e3`, `conclusion=failure` as expected) published a `bench-results` artifact whose environment block reads `hardwareConcurrency: 4` and whose PERF-03 row reads `source: "production"`, `verdict: "fail"`, `measuredMs: 1179.70`, `normalizedMs: 1120.86` (112.1% of the 1000ms budget). The same artifact's `infoLines` carry both of this plan's own profile lines (`sweep-pool-profile-cagr`, `sweep-pool-profile-irr`) plus the gated file's `solveIrr` contribution-schedule info line (`measuredMs=8769.00`, `normalizedMs=8331.59`, `sampleCount=1`). Every figure was verified directly against the downloaded artifact with `gh run view`/`gh run download`, not assumed from the orchestrator's supplied context.

`07.1-PERF-03-PROFILE.md` transcribes this readback (Section 1) and reasons over it (Sections 2-6): the zero-contribution branch splits into 72.2% kernel-bound compute (`idealParallelMs=851.39ms` of `1179.70ms`, extrapolated from the 24-column profile arm) and a 27.8% overhead-and-residual share, of which only 54.33ms is explained by the profile's own measured pool overhead at constant chunk count; the remaining ~274ms is an explicitly unresolved residual. The `solveIrr` branch's own recorded `idealParallelMs`/`poolOverheadMs` split was found to be apples-to-oranges (cell-count mismatch between `warmSweepMs` at 24 columns and `serialKernelMs` at 8 columns) and is corrected in the document rather than transcribed uncritically. `npvEvaluationsPerSolve` measured 105.01 against RESEARCH's naive ~33.4 estimate; reading `src/metrics/irr.ts` traces the gap to `solveIrr`'s convergence test comparing a dollar-denominated NPV magnitude against `TOLERANCE`, not a rate-space bracket width, so the loop almost always exhausts `MAX_ITERATIONS=100`. RESEARCH assumptions A1 (confirmed directionally, with a quantified residual), A2 (confirmed), and A4 (confirmed exactly, by construction: the "~25%" figure is the linear-scaling arithmetic itself) are each disposed by name, and Open Question 3 (`cashFlowCountRatio=1.00`) is answered.

## Performance

- **Duration:** ~55 min total across two sessions (local half ~25min, orchestrator push + CI wait + this continuation's readback and Task 2 ~30min)
- **Started:** 2026-08-23 (session start)
- **Completed:** 2026-08-23 (this continuation)
- **Tasks:** 2 of 2
- **Files modified:** 2 (1 created by Task 1, 1 created by Task 2)

## Accomplishments

- `bench/sweep-pool-profile.bench.test.ts` created (Task 1): two `recordInfoLine`-only tests, zero `recordMeasurement` calls, own `beforeAll` (own bundle, own entry-date axis, own pool). Zero-contribution (CAGR) branch measures `poolConstructionMs`, `coldSweepMs`, `warmSweepMs`, `serialKernelMs`, and the derived `workerStartupMs`/`idealParallelMs`/`poolOverheadMs`/`perCellKernelUs`/`extrapolatedFullGridSerialMs`. D-24 `solveIrr` branch measures `warmSweepMs`, `serialKernelMs`, `cashFlowCount` (first and last column plus ratio), and `npvEvaluationsPerSolve` (measured via a real timed `npv`/`solveIrr` batch).
- Merged onto `gsd/phase-07.1-sweep-pool-tuning` by the orchestrator (merge commit `4534f877`), pushed, and PR #8's `synchronize` event fired CI run **32669644628**.
- Read back that run's `bench-results` artifact directly: `environment.hardwareConcurrency === 4`; PERF-03 row `source: "production"`, `verdict: "fail"`, `measuredMs: 1179.70`, `normalizedMs: 1120.86`; both profile info lines present with the full attributed breakdown for both branches.
- `07.1-PERF-03-PROFILE.md` created (Task 2): six numbered sections transcribing the baseline run, the attributed breakdown for each branch (with the `solveIrr` branch's cell-count mismatch corrected rather than transcribed uncritically), a per-file bench cost table from the CI log, a stated lever and reachable margin per branch, and an explicit disposition of RESEARCH assumptions A1/A2/A4 and Open Question 3.
- `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/sweep.bench.test.ts src/sweep/sweep-grid.ts` exits 0: no budget, calibration, gated-file, or grid-shape constant touched by either task.

## Task Commits

1. **Task 1 (local portion): sweep-pool-profile bench arm** - `0b7a8ee` (feat), landed on `gsd/phase-07.1-sweep-pool-tuning` via orchestrator merge commit `4534f877`
2. **Task 2: PERF-03 attributed breakdown from CI readback** - `a89e546` (docs)

## Files Created/Modified

- `bench/sweep-pool-profile.bench.test.ts` - diagnostic-only bench arm, info-line-only, attributed breakdown for both sweep branches (Task 1)
- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md` - the attributed baseline document, transcribed and reasoned from CI run 32669644628 (Task 2)

## Decisions Made

- Followed Task 1's own `<precondition>`, the plan's `orchestrator_context`, and 07-PERF-03-BASELINE.md section 2's precedent for the local-half executor: no `git push`, PR creation, or PR edit attempted from that worktree.
- Measured `cashFlowCount` and `npvEvaluationsPerSolve` against the `IRR_PROFILE_COLS` (8-column) axis, matching `serialKernelMs`'s own axis for that branch, so the three figures describe one consistent tested range.
- Chose `NPV_SOLVE_BATCH_SIZE = 5000` for the `npv`/`solveIrr` timing batch; both arms cleared `MIN_MEASUREMENT_MS` by a wide margin in the real CI run (`npvMs=74.00`, `solveIrrMs=7770.90`).
- Corrected the `solveIrr` branch's cell-count mismatch (`warmSweepMs` at 24 columns vs `serialKernelMs` at 8 columns) rather than presenting the bench file's own uncorrected `idealParallelMs`/`poolOverheadMs` split as the settled figure -- 07.1-PERF-03-PROFILE.md Section 3 shows both the as-recorded and the corrected split, and states which one is defensible.
- Traced the worker-startup question to `bench/sweep.bench.test.ts`'s own test order rather than relying on the `measurementExcludesWorkerConstruction` flag alone: the shared pool's two correctness tests warm every worker's bundle fetch before PERF-03's own `measureMinOfN` loop begins, so `workerStartupMs` (224.80ms) is already excluded from the gated window, correctly, per RESEARCH's Pitfall 2 amortization test.
- Did not implement any tuning fix (bracket-width convergence, worker count 4, batch-size reduction) -- this plan is a tracer; every named lever is recorded for a later plan to spend, per this plan's own scope and prohibitions.

## Deviations from Plan

None. Both tasks executed per their own `<action>`/`<acceptance_criteria>`. The gap between the halted session's SUMMARY and this one is not a deviation: it is the precondition the plan itself named (this worktree branch cannot push or open a PR), resolved by the orchestrator exactly as 07-PERF-03-BASELINE.md section 2 records for the analogous case.

## Issues Encountered

- **CI runtime rose to 66,527ms** (against a 30,000ms cap), up from Phase 7's D-17 baseline run's 43,259ms -- `bench/sweep-pool-profile.bench.test.ts` itself (this plan's own new file) accounts for roughly 50.9% of that rise (11,835ms). Noted plainly in `07.1-PERF-03-PROFILE.md` Section 1 and Section 4, per this plan's own instruction not to fix it here; plan 05 owns the reduction. The CI job's reported failure text names only the PERF-03 budget breach, not the runtime-cap breach, because `assertRunInvariants` checks the verdict-fail condition first -- the same masking issue 07.1-RESEARCH.md's Code Examples section already documents from Phase 7's baseline run.
- **The `solveIrr` branch's bench-file-recorded `poolOverheadMs` (815.40ms) is not apples-to-apples**, because `warmSweepMs` and `serialKernelMs` for that branch were measured at different column counts (24 vs 8) by Task 1's own design (cost control for a markedly more expensive serial arm). `07.1-PERF-03-PROFILE.md` corrects this with a cell-count-scaled comparison (corrected `poolOverheadMs=320.80ms`) rather than transcribing the uncorrected figure as the settled split. This is not a defect in Task 1's bench file -- both figures are exactly what the plan asked it to measure -- it is a reasoning step Task 2 owns.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Ready.** ROADMAP success criterion 1 is met: the 4-core baseline sweep's wall clock is a measured, attributed breakdown for both the zero-contribution and `solveIrr` branches, recorded in `07.1-PERF-03-PROFILE.md`, on a real `hardwareConcurrency=4`, `source=production` CI verdict. Later plans in this phase (worker-count tuning, `solveIrr` convergence-criterion fix, runtime-cap reduction) have concrete, measured figures to aim at rather than the inference PROJECT.md previously recorded:

1. **Worker count (`workerCount=4`) is the lever with real headroom** for the zero-contribution branch (theoretical `idealParallelMs=638.54ms` at 4 workers, 231ms of margin under the 870ms target) -- but 07.1-RESEARCH.md's Pitfall 3 (no concurrent-drag-during-sweep evidence at any worker count) remains untested by this plan and must be addressed before adoption.
2. **The `solveIrr` branch needs a convergence-criterion fix** (bracket-width test instead of dollar-NPV-magnitude test) as a first lever, but this profile's own arithmetic shows that fix alone, even combined with a fourth worker, still lands roughly 1.7x over the 870ms target -- a further reduction (display-precision-verified tolerance loosening, or a scoped Newton-with-bisection-fallback solver) remains necessary.
3. **The runtime-cap breach (66,527ms vs 30,000ms) is unresolved** and this plan's own new bench file is roughly half its recent growth; plan 05 should evaluate a smaller `NPV_SOLVE_BATCH_SIZE` first (concrete batch-cost breakdown in `07.1-PERF-03-PROFILE.md` Section 4).

Nothing in `perf-budgets.ts`, `bench/calibration.ts`, `bench/sweep.bench.test.ts`, or `src/sweep/sweep-grid.ts` was touched by either task -- verified via `git diff --exit-code` before each commit.

## Self-Check: PASSED

- `bench/sweep-pool-profile.bench.test.ts` exists: FOUND (confirmed present, unmodified by this continuation).
- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md` exists: FOUND.
- Commit `0b7a8ee`: FOUND in `git log --oneline --all`.
- Commit `a89e546`: FOUND in `git log --oneline -3` (HEAD at commit time).
- CI run `32669644628`: FOUND, confirmed via `gh run view` and `gh run download`, artifact contents verified directly against every figure transcribed into `07.1-PERF-03-PROFILE.md`.

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed: 2026-08-23*

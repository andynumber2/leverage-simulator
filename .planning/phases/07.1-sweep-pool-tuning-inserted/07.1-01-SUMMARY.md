---
phase: 07.1-sweep-pool-tuning-inserted
plan: 01
subsystem: testing
tags: [bench, vitest, performance-profiling, sweep-pool]

# Dependency graph
requires:
  - phase: 07-sweep-engine-and-the-heatmap
    provides: the real production Worker pool sweep, sweep.worker.ts's exported computeChunkMetrics, and bench/sweep.bench.test.ts's gated PERF-03 measurement this file explains
provides:
  - "bench/sweep-pool-profile.bench.test.ts: a diagnostic-only, info-line-only bench arm attributing the PERF-03 headline sweep's wall clock to pool construction, worker startup, serial kernel time, and pool overhead, plus the same breakdown for the D-24 solveIrr branch (cash-flow count and npv-evaluations-per-solve)"
  - "a locally-proven measurement mechanism (both info lines confirmed present in .bench/bench-results.json), not yet exercised on the D-17 baseline (hardwareConcurrency=4) because the commit has not reached gsd/phase-07.1-sweep-pool-tuning"
affects: [07.1 later plans that read this plan's PROFILE.md attribution before choosing a pool-tuning lever]

# Actuals (#2632)
actuals:
  tokens: 4572
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Diagnostic-only bench file: recordInfoLine exclusively, never the gated measurement-row channel, kept in its own module (own beforeAll, own pool) so instrumentation never perturbs the figure it explains (07.1-RESEARCH.md Pattern 2)"

key-files:
  created:
    - bench/sweep-pool-profile.bench.test.ts
  modified: []

key-decisions:
  - "Task 1's own <precondition> and the orchestrator's explicit instruction both apply here: this worktree branch (worktree-agent-a4185acfd0f39e3e2) cannot push or open a PR. The bench file is created, locally verified, and committed (0b7a8ee); the push-and-CI-readback half of Task 1, and all of Task 2, are deferred to the orchestrator per 07-PERF-03-BASELINE.md section 2's own precedent (Task 1 commits merged onto the phase branch, pushed, PR opened by the orchestrator, not the worktree agent)."
  - "cashFlowCount and the npvEvaluationsPerSolve batch are measured against the IRR_PROFILE_COLS-wide axis (the same axis serialKernelMs uses for the solveIrr branch), not the wider PROFILE_COLS axis, so all three figures describe the same tested column range and Open Question 3 gets one internally consistent answer."

requirements-completed: []  # PERF-03 is not complete: Task 2 (the PROFILE.md that closes ROADMAP success criterion 1) has not run yet.

coverage:
  - id: D1
    description: "bench/sweep-pool-profile.bench.test.ts exists, records only via recordInfoLine, and both branches' info lines (cols/rows/cells, poolConstructionMs, coldSweepMs, warmSweepMs, workerStartupMs, serialKernelMs, idealParallelMs, poolOverheadMs, perCellKernelUs, extrapolatedFullGridSerialMs for the CAGR branch; warmSweepMs, serialKernelMs, cashFlowCount x2 with ratio, npvEvaluationsPerSolve with batch size for the solveIrr branch) land in .bench/bench-results.json's infoLines"
    requirement: "PERF-03"
    verification:
      - kind: other
        ref: "npm run typecheck (exit 0); grep -c 'recordMeasurement' bench/sweep-pool-profile.bench.test.ts (returns 0); node -e checks against .bench/bench-results.json (both pass, this dev sandbox, hardwareConcurrency=9, informational)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A named CI run on gsd/phase-07.1-sweep-pool-tuning publishes a bench-results artifact with environment.hardwareConcurrency=4, a PERF-03 row at source=production with a rendered verdict, and this file's profile info lines for both branches"
    requirement: "PERF-03"
    verification: []
    human_judgment: true
    rationale: "Requires pushing this worktree's commit to gsd/phase-07.1-sweep-pool-tuning and downloading the resulting CI run's artifact -- both are actions this worktree-agent branch cannot perform (per Task 1's own <precondition> and the orchestrator's standing instruction). Not yet done; the orchestrator must push, then a continuation agent reads the artifact back and completes Task 2."

duration: 25min
completed: 2026-08-23
status: halted
---

# Phase 07.1 Plan 01: Sweep-Pool Profile Bench Arm (Local Half) Summary

**Diagnostic-only `bench/sweep-pool-profile.bench.test.ts` created, typechecked, and locally proven to record its attributed breakdown for both the CAGR and solveIrr branches; the push-and-CI-readback half of Task 1 and all of Task 2 are blocked on the orchestrator, per this task's own precondition.**

## Explicit answer to this plan's success criterion

**No real `hardwareConcurrency=4`, `source=production` PERF-03 verdict has been read back for this plan's changes in this session.** The only CI evidence that exists right now is:

- Run **32654061079** (Phase 07, a different branch/commit, `gsd/phase-07-sweep-engine-and-the-heatmap`): the pre-tuning D-17 baseline this whole phase is escalated from (`normalizedMs=1168.59`, `verdict=fail`). Not this plan's code.
- Run **32668937235** (this phase's branch `gsd/phase-07.1-sweep-pool-tuning`, PR #8): a confirmation baseline at commit `983e6c5b`, i.e. Phase 7's shipped code plus planning docs only -- **before** this plan's `bench/sweep-pool-profile.bench.test.ts` existed. It does not, and cannot, carry this file's info lines.

Commit `0b7a8ee` (this plan's Task 1 file) sits on the worktree-agent branch only. It has not reached `gsd/phase-07.1-sweep-pool-tuning`, so no CI run has ever exercised it. Task 1's `<precondition>` states exactly this case: a `worktree-agent-*` branch "cannot push or open the PR itself: halt and report that the orchestrator must merge this plan's commits onto `gsd/phase-07.1-sweep-pool-tuning`, push it to `origin`, and open a pull request." PR #8 is already open (opened before this session, per orchestrator-supplied context), so what remains is narrower than the precondition's general case: **the orchestrator needs to push commit `0b7a8ee` (or a merge of it) onto `gsd/phase-07.1-sweep-pool-tuning`** -- the already-open PR's `pull_request` `synchronize` event will then fire CI automatically, no new PR needed.

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-23 (session start)
- **Completed:** 2026-08-23T22:05:54Z (local half only)
- **Tasks:** 1 of 2 (Task 1's local portion; Task 1's CI-readback portion and Task 2 are not started)
- **Files modified:** 1

## Accomplishments
- `bench/sweep-pool-profile.bench.test.ts` created: two `recordInfoLine`-only tests, zero `recordMeasurement` calls (`grep -c 'recordMeasurement'` returns 0), own `beforeAll` (own bundle, own entry-date axis, own pool -- no import of `bench/sweep.bench.test.ts`'s module-level state).
- Zero-contribution (CAGR) branch arm measures and records: `poolConstructionMs`, `coldSweepMs`, `warmSweepMs`, `workerStartupMs` (derived), `serialKernelMs` (a direct, synchronous `computeChunkMetrics` call, zero pool involvement), `idealParallelMs`/`poolOverheadMs`/`perCellKernelUs`/`extrapolatedFullGridSerialMs` (all derived, labelled as such).
- D-24 `solveIrr` contribution-schedule branch arm measures and records: `warmSweepMs`, `serialKernelMs` (at the narrower `IRR_PROFILE_COLS` width), `cashFlowCount` for both the first and last column of the tested axis plus their ratio (answering 07.1-RESEARCH.md Open Question 3 -- this dev-sandbox run shows `cashFlowCountRatio=1.00`, both figures 1185, informational only), and `npvEvaluationsPerSolve` measured via a real timed batch (`npv` vs `solveIrr` over the same cash-flow list), not counted from source.
- `npm run typecheck` exits 0. Local `npm run bench` (full suite, this dev sandbox) writes `.bench/bench-results.json` with both new info lines present, confirmed with the plan's own `node -e` checks.
- `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/sweep.bench.test.ts src/sweep/sweep-grid.ts` exits 0: no budget, calibration, or grid-shape constant touched.

## Task Commits

1. **Task 1 (local portion): sweep-pool-profile bench arm** - `0b7a8ee` (feat)

No plan-metadata commit yet: this SUMMARY documents a halted plan, not a completed one, per the frontmatter guidance's `status: halted` contract.

## Files Created/Modified
- `bench/sweep-pool-profile.bench.test.ts` - diagnostic-only bench arm, info-line-only, attributed breakdown for both sweep branches

## Decisions Made
- Followed Task 1's own `<precondition>`, the plan's `orchestrator_context`, and 07-PERF-03-BASELINE.md section 2's precedent (Task 1's commits merged/pushed/PR-opened by the orchestrator, not the worktree executor) rather than attempting any `git push`, PR creation, or PR edit from this worktree.
- Measured `cashFlowCount` and `npvEvaluationsPerSolve` against the `IRR_PROFILE_COLS` (8-column) axis, matching `serialKernelMs`'s own axis for that branch, so the three figures describe one consistent tested range rather than two different slices of the entry-date axis.
- Chose `NPV_SOLVE_BATCH_SIZE = 5000` for the `npv`/`solveIrr` timing batch; this dev-sandbox run cleared `MIN_MEASUREMENT_MS` (10ms) by a wide margin on both arms (`npvMs=20.60`, `solveIrrMs=2170.70` in the full-suite run), so no batch-size increase is needed.

## Deviations from Plan

None - Task 1's implementable-by-this-agent scope executed exactly as written. The gap between what this SUMMARY reports and the plan's full `<done>` criteria is not a deviation: it is the precondition the plan itself names, not a discretionary choice this agent made.

## Issues Encountered

- **Acceptance-criteria self-correction:** the file's header comment originally used the literal phrase "never recordMeasurement" to explain the recording discipline, which itself matched the `grep -c 'recordMeasurement'` acceptance check and returned 1, not 0. Reworded the comment to describe the same discipline without repeating the banned string; `grep -c 'recordMeasurement' bench/sweep-pool-profile.bench.test.ts` now returns 0, verified before committing.
- **Local full-suite exit code:** `npm run bench` (full suite, this 9-core dev sandbox, after `npm run build`) exits non-zero on `PERF-08c` (a warm-load-ceiling budget unrelated to this plan's change -- this sandbox's own host variance, not caused by `bench/sweep-pool-profile.bench.test.ts`). This matches the plan's own stated expectation ("Expect the local run to exit non-zero... That is the starting state this phase exists to change, not a defect in this file") for the PERF-03/runtime-cap case; the specific tripped check here (`PERF-08c`) is a different pre-existing budget, not PERF-03 itself, but the same "local dev-sandbox exit code is not the acceptance gate" principle applies -- both of this file's own info lines recorded correctly regardless.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness (for the orchestrator / next agent)

**Not ready to proceed to Task 2 yet.** What is needed, exactly:

1. Merge or push commit `0b7a8ee` from this worktree branch (`worktree-agent-a4185acfd0f39e3e2`) onto `gsd/phase-07.1-sweep-pool-tuning`, then push that branch to `origin`. PR #8 (https://github.com/andynumber2/leverage-simulator/pull/8) is already open and tracking this branch, so no new PR is needed -- the push alone fires `.github/workflows/ci.yml`'s `pull_request` `synchronize` trigger.
2. Wait for the resulting `CI` workflow run to complete (it is expected to fail at `npm run bench`, on the same pre-existing PERF-03 budget breach and runtime-cap issue this phase exists to fix -- `actions/upload-artifact@v4`'s `if: always()` still publishes the artifact, per T-07-28's mitigation, exactly as 07-PERF-03-BASELINE.md section 2 recorded for the analogous PR #7 run).
3. `gh run download <new-run-id> -R andynumber2/leverage-simulator --name bench-results`, confirm `environment.hardwareConcurrency === 4` and the PERF-03 row's `source: "production"`/non-`unmeasured` verdict, per Task 1's own acceptance criteria.
4. Hand the run id and downloaded artifact to a continuation agent (or resume this same plan) to complete Task 2: transcribe the attribution into `07.1-PERF-03-PROFILE.md` (sections 1-6), closing ROADMAP success criterion 1.

Nothing in `perf-budgets.ts`, `bench/calibration.ts`, `bench/sweep.bench.test.ts`, or `src/sweep/sweep-grid.ts` was touched -- verified via `git diff --exit-code` before this commit.

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed (local half only): 2026-08-23*

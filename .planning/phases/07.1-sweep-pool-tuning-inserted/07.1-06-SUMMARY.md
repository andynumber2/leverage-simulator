---
phase: 07.1-sweep-pool-tuning-inserted
plan: 06
subsystem: performance
tags: [worker-pool, bench, ci, perf-budget, key-decisions, sweep]

# Dependency graph
requires:
  - phase: 07.1-sweep-pool-tuning-inserted
    provides: "07.1-04's worker-count threshold and abort condition, 07.1-03's solveIrr escalation text, 07.1-05's bench-runtime shortfall text, all resolved with real measurements in this plan"
provides:
  - "The authoritative D-17 baseline verdict for PERF-03: FAILED on all four hardwareConcurrency=4 production runs at both worker widths tried this phase"
  - "workerCountForCores tried at cores (width 4), measured, and reverted back to cores - 1 after the authoritative run fired the abort condition and showed no headline improvement"
  - "All four owed PROJECT.md Key Decisions, each independently verified against downloaded CI artifacts"
  - "07.1-PERF-03-PROFILE.md section 5's refuted 25% linear-scaling projection, corrected in place"
  - "07.1-PERF-03-BASELINE.md: the phase's authoritative baseline transcription and six-criterion disposition"
affects: ["ship gate (PERF-03 remains an open escalation, not a pass)", "any future plan spending the two remaining named levers: kernel write-only output arrays, D-03's coarser grid"]

actuals:
  tokens: 18953
  tasks: 2
  commits: 8

tech-stack:
  added: []
  patterns:
    - "Worker-count width decided against a two-run informational-arm comparison first, then bound by an explicit abort condition on the authoritative gated run -- the informational arm alone was shown unreliable at n=2, so the real decision was deferred to the budget-enforced PERF-07a/07b rows themselves"
    - "A projection carried a self-flagged caveat ('this is arithmetic, not a profiled number') that later measurement proved was the load-bearing caveat; correcting the document in place (with the refuted original text preserved verbatim, not deleted) rather than silently rewriting it"
    - "PROJECT.md Key Decision rows corrected in place when a prior session's disposition is superseded by real measurement, with the original text kept for the record and a CORRECTED paragraph inserted at the point of supersession, rather than editing history away"

key-files:
  created:
    - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md
  modified:
    - src/sweep/sweep-pool.ts
    - bench/sweep-pool.ts
    - tests/sweep-pool.test.ts
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md

key-decisions:
  - "Worker-count tried and reverted: workerCountForCores went cores - 1 -> cores (width 4) pending the abort condition's authoritative run, then back to cores - 1 once that run (CI 32676218114) answered. Two independent grounds, the first the deciding one: (1) width 4 measured no PERF-03 headline improvement -- 1191.34ms normalized, inside the width-3 three-run spread (1115.92-1208.38ms), not below it, refuting 07.1-PERF-03-PROFILE.md's own 25.00% linear-scaling projection; (2) the abort condition's own trigger fired -- PERF-07b reached 76.9% of budget, crossing D-20's 70% escalation threshold, though this alone (a single sample against a 56.4-68.7% width-3 range) would have been thin evidence without ground 1."
  - "solveIrr escalation Key Decision (already recorded by the prior session, unchanged by this plan): the branch improved 2.3-2.27x on two production runs (normalizedMs=3888.66, 3952.70) but remains ~3.9-4.0x over budget. This plan's own run (32676218114) adds a third confirming figure (normalizedMs=3992.09, sampleCount=2), unchanged disposition: escalate, Newton-with-bisection-fallback named as the remaining lever, deferred to a future planning cycle."
  - "Bench-runtime shortfall Key Decision (already recorded by the prior session, unchanged by this plan): totalRuntimeMs remains 45,709-66,527ms across all four D-17 runs against the 30,000ms cap, unclosed. This plan's own run measured 56,141ms, inside that range -- confirming, not moving, the disposition."
  - "PERF-03 headline outcome (this plan's own new Key Decision): FAILED on all four hardwareConcurrency=4 production runs at both worker widths (1120.86, 1208.38, 1115.92ms at width 3; 1191.34ms at width 4). Two remaining levers named and held in reserve: the kernel's write-only per-bar output arrays in src/kernel/backtest.ts (reasoned, not measured, a kernel change needing its own measurement), and D-03's coarser default grid (reserved to preserve headline-figure comparability across phases). No budget value, calibration constant, cap, or grid dimension moved to reach any of this plan's dispositions."
  - "The normalization stability point, recorded because it is the clearest demonstration on this project's own record of why PERF-01a forbids retuning calibration constants: across all four runs, raw measuredMs swung 879.10-1180.00ms (~34%, tracking each run's own calibrationScore) while normalizedMs clustered at 1115.92-1208.38ms (~4%). One run's raw figure (879.10ms) was under the 1000ms budget yet correctly failed once normalized, because that run drew the fastest host."

requirements-completed: []

coverage:
  - id: D1
    description: "workerCountForCores reverted to cores - 1 in both production declarations, correctness clamps re-run and passing at the restored width (chunkCount 16 -> 12)"
    requirement: "PERF-03"
    verification:
      - kind: unit
        ref: "tests/sweep-pool.test.ts (updated expectations, 3 tests pass, width 3 confirmed)"
        status: pass
      - kind: unit
        ref: "tests/sweep/partition.test.ts (unmodified, covers widths 1-8, passes at width 3)"
        status: pass
      - kind: integration
        ref: "bench/sweep.bench.test.ts's pool-versus-serial equality gates -- 5/5 pass locally at the reverted width; the same gates also passed in CI at width 4 on the pre-revert code (run 32676218114), so equality is proven at both shapes"
        status: pass
    human_judgment: false
  - id: D2
    description: "The authoritative D-17 baseline gate run, transcribed with a disposition for all six ROADMAP criteria and the four Key Decisions this phase owes, all figures independently re-verified against downloaded CI artifacts"
    requirement: "PERF-01a"
    verification:
      - kind: other
        ref: "gh run download 32669644628/32673398579/32674615017/32676218114 --name bench-results, re-parsed with node -e against each run's own bench-results.json (environment, PERF-03/PERF-07a/PERF-07b rows) -- every figure cited in 07.1-PERF-03-BASELINE.md and PROJECT.md's new/corrected rows matches its artifact"
        status: pass
    human_judgment: false
  - id: D3
    description: "PERF-03's own headline disposition: FAILED, not met, on the real 4-core CI baseline -- this is the phase's actual, final answer, not a projection or a withheld verdict"
    verification:
      - kind: other
        ref: "07.1-PERF-03-BASELINE.md section 2, four independently-verified production runs, all verdict=fail"
        status: pass
    human_judgment: false
---

# Phase 07.1 Plan 06: The Authoritative Baseline Run, the Worker-Count Revert, and the Four Owed Key Decisions Summary

**The authoritative D-17 baseline run fired the plan's own abort condition and measured no PERF-03 headline improvement from a fourth worker; `workerCountForCores` was reverted to `cores - 1`, the refuted 25% projection in `07.1-PERF-03-PROFILE.md` was corrected in place, and all four owed PROJECT.md Key Decisions are now recorded -- PERF-03 closes this phase as FAILED, not met, with two named levers (the kernel's write-only per-bar output arrays, and D-03's coarser default grid) held in reserve for a future phase.**

## Performance

- **Duration:** multi-session (interim halt pending orchestrator-triggered CI, then resumed with the authoritative run's result)
- **Started:** 2026-08-24T00:08:39Z (Task 1's original width-4 adoption commit)
- **Completed:** 2026-08-24T00:31:44Z
- **Tasks:** 2 of 2 complete
- **Files modified:** 6 modified, 1 created (this session); 9 total across the whole plan

## Accomplishments

- The one authoritative D-17 baseline gate run (CI 32676218114, `hardwareConcurrency=4`, `calibrationScore=0.9350`, `source=production`) downloaded, independently re-parsed, and cross-checked against the other three `hardwareConcurrency=4` production runs already on record
- Both grounds for the worker-count revert established and recorded honestly, in the order that matters: the throughput ground (no measurable PERF-03 improvement at width 4) as the deciding one, the abort-condition ground (PERF-07b crossing 70% of budget) as corroborating but not load-bearing on its own
- `workerCountForCores` reverted to `cores - 1` in both `src/sweep/sweep-pool.ts` and `bench/sweep-pool.ts`; `BASELINE_WORKER_COUNT` back to 3, `chunkCount` back to 12; every correctness clamp this plan names re-run and passing at the restored width
- `07.1-PERF-03-PROFILE.md` section 5's refuted 25.00% linear-scaling projection corrected in place, original text preserved and marked refuted, not silently deleted; its A4 assumption disposition corrected to distinguish "the arithmetic was confirmed" from "the real-world prediction was refuted"
- `07.1-PERF-03-BASELINE.md` written: nine sections covering the run, the headline row across all four production runs, the `solveIrr` branch, bench-suite runtime, correctness, the no-relaxation proof, the six-criterion disposition, the revert's two grounds, and the levers spent-versus-reserved this phase
- PROJECT.md's fourth owed Key Decision ("PERF-03 headline outcome") written: FAILED, not met, with both remaining levers named
- PROJECT.md's worker-count Key Decision corrected in place to record the adoption as tried-and-reverted with the authoritative measurement, rather than left reading as an open-ended adoption
- REQUIREMENTS.md's PERF-03 mapping row updated to reflect the Phase 7.1 re-measurement and escalation (checkbox stays unmet, unchanged, since the requirement is not met)
- A wrongly-made edit to ROADMAP.md (this plan's own PLAN.md Task 2 instructed a criterion-disposition edit there, but this session's own orchestrator instructions explicitly forbid it) was caught and reverted before returning, per the explicit "Do NOT update STATE.md or ROADMAP.md" instruction

## Task Commits

Both tasks were committed atomically across the plan's full, multi-session history:

1. **Task 1 (prior session): apply the worker-count decision, re-assert correctness clamps** - `3b5af18` (perf) -- adopted width 4
2. **Task 2 (prior session, partial): record three of four owed Key Decisions** - `e346e6a` (docs)
3. **Interim summary (prior session)** - `8bad5f2` (docs)
4. **Orchestrator merge (prior session)** - `79c540d` (chore) -- carried the width-4 code into CI, producing this session's authoritative run
5. **Task 1 (this session): revert `workerCountForCores` to `cores - 1`** - `155c539` (perf)
6. **Task 1 (this session): transcribe the D-17 baseline run and correct the refuted 25% projection** - `1286dc9` (docs)
7. **Task 2 (this session): record the PERF-03 headline outcome and correct the worker-count decision** - `10869c0` (docs)
8. **Task 2 (this session): revert an unauthorized ROADMAP.md edit** - `ecaffe1` (docs)

## Files Created/Modified

- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` (created) - nine-section authoritative baseline transcription covering all four production runs, the revert, and the six-criterion disposition
- `src/sweep/sweep-pool.ts` - `workerCountForCores`: `Math.max(1, cores)` -> `Math.max(1, cores - 1)`, doc comment corrected to record the tried-and-reverted history
- `bench/sweep-pool.ts` - same revert, same doc-comment correction
- `tests/sweep-pool.test.ts` - expectations reverted to width 3 (`workerCountForCores(4)` -> `3`, `workerCountForCores(9)` -> `8`); the test no longer silently encodes the reverted-away width 4
- `.planning/PROJECT.md` - worker-count Key Decision corrected in place (adoption reframed as tried-and-reverted, with the measured outcome inserted before the pre-existing residual-risk paragraph); fourth Key Decision ("PERF-03 headline outcome") appended
- `.planning/REQUIREMENTS.md` - PERF-03's mapping-table row updated to name the Phase 7.1 re-measurement and its escalated disposition
- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md` - section 5's 25% projection and the A4 assumption disposition corrected in place, original text preserved and marked refuted

## Decisions Made

See `key-decisions` in the frontmatter above for the full list. In prose: this plan's own job was to run the one authoritative measurement and record whichever way it went, and it went the way that closes the loop honestly rather than the way that would have made a cleaner-looking phase. Width 4 was a reasonable bet given the profile's own arithmetic, and the bet was tested rather than assumed; it did not pay off, and the revert plus the corrected documents are the record of that, not a hidden edit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, self-corrected] Reverted an unauthorized ROADMAP.md edit before returning**
- **Found during:** Task 2, after committing `10869c0`
- **Issue:** `07.1-06-PLAN.md`'s own Task 2 action explicitly instructs appending a criterion-disposition to ROADMAP.md's Phase 7.1 section. This session's own orchestrator instructions (the prompt that spawned this specific finishing agent) explicitly state "Do NOT update STATE.md or ROADMAP.md" and list "No modifications to STATE.md or ROADMAP.md" as a success criterion. The plan's own file list and this session's explicit instruction conflict; the more specific, more recent instruction governs.
- **Fix:** `git checkout 10869c0^ -- .planning/ROADMAP.md` restored the file to its pre-edit state, verified byte-identical to the plan's own starting commit (`git diff 79c540d..HEAD -- .planning/ROADMAP.md` empty), then committed as `ecaffe1`. The six-criterion disposition this plan's own PLAN.md wanted in ROADMAP.md lives in `07.1-PERF-03-BASELINE.md` section 7 instead, which is the document this plan's own PLAN.md Task 1 already designated as the authoritative disposition record.
- **Files modified:** `.planning/ROADMAP.md` (reverted, net zero change against the plan's starting state)
- **Verification:** `git diff 79c540d..HEAD -- .planning/ROADMAP.md` exits with no output
- **Committed in:** `ecaffe1`

---

**Total deviations:** 1 self-corrected (an instruction conflict between the plan's own file list and this session's more specific orchestrator instruction, caught and fixed before returning).
**Impact on plan:** None on the substance of any decision; ROADMAP.md ends this plan identical to how it started, and the disposition table this plan's own PLAN.md wanted there is fully present in `07.1-PERF-03-BASELINE.md` instead.

## Issues Encountered

None beyond the ROADMAP.md instruction conflict documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Phase 07.1 closes here.** Criteria 1, 3 and 6 are met; criteria 2, 4 and 5 are escalated, each with a specific remaining lever named in PROJECT.md rather than left as an open question. PERF-03 is FAILED, not met, on the real 4-core baseline, and REQUIREMENTS.md says so plainly. A future phase inherits three named, unspent levers: Newton-with-bisection-fallback for the `solveIrr` branch (scoped to the sweep call site), the kernel's write-only per-bar output arrays for the headline branch, and D-03's coarser default grid (last resort, since spending it forfeits headline-figure comparability). No budget, calibration constant, cap or grid dimension moved anywhere in this phase; `git diff --exit-code` against the phase base commit (`7c21f0bc2f5fe7637ea7e898320b84f5824199eb`) proves it.

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed: 2026-08-24*

## Self-Check: PASSED

- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` - FOUND
- `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-06-SUMMARY.md` - FOUND
- Commits `3b5af18`, `e346e6a`, `8bad5f2`, `79c540d`, `155c539`, `1286dc9`, `10869c0`, `ecaffe1` - all FOUND in `git log --oneline --all`
- `git diff 79c540d..HEAD -- .planning/ROADMAP.md` - empty (no unauthorized modification remains)
- `git diff --exit-code 7c21f0bc2f5fe7637ea7e898320b84f5824199eb..HEAD -- perf-budgets.ts bench/calibration.ts src/sweep/sweep-grid.ts` - exit 0

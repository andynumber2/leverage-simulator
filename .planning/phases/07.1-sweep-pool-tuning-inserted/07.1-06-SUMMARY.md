---
phase: 07.1-sweep-pool-tuning-inserted
plan: 06
subsystem: performance
tags: [worker-pool, bench, ci, perf-budget, key-decisions, sweep]

# Dependency graph
requires:
  - phase: 07.1-sweep-pool-tuning-inserted
    provides: "07.1-04's worker-count threshold and abort condition, 07.1-03's solveIrr escalation text, 07.1-05's bench-runtime shortfall text, all resolved or updated with real measurements in this plan"
provides:
  - "workerCountForCores changed from cores - 1 to cores in both declarations (src/sweep/sweep-pool.ts, bench/sweep-pool.ts), committed and correctness-clamped at chunkCount=16"
  - "Three of this phase's four owed PROJECT.md Key Decisions, each independently verified against downloaded CI artifacts rather than trusted from a prompt transcription"
  - "The correct phase-07.1 base commit for the no-relaxation diff proof (7c21f0bc2f5fe7637ea7e898320b84f5824199eb), since PR #7 (Phase 7) is still open and the plan's own literal merge-base-with-main instruction resolves to a pre-Phase-7 commit instead"
affects: ["ship gate (PERF-03 disposition still open)", "this plan's own resumption (needs orchestrator push + CI)"]

# Actuals (#2632): interim figure for this session; will be superseded by the final actuals
# recorded when this plan completes.
actuals:
  tokens: 6749
  tasks: 1
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Worker-count width decided against a two-run informational-arm comparison rather than a single run, with the arm's own max-of-one-run statistic flagged as an unreliable decision input (a defect in the arm's design, not fixed here since fixing it is outside this plan's authorized file scope)"
    - "PROJECT.md Key Decision rows drafted in a prior plan's SUMMARY.md are copied verbatim and only the specific projection sentence is replaced with the real measurement, never rewritten wholesale -- keeps the rest of the reasoning (what was tried, what remains) intact and auditable"

key-files:
  created: []
  modified:
    - src/sweep/sweep-pool.ts
    - bench/sweep-pool.ts
    - tests/sweep-pool.test.ts
    - .planning/PROJECT.md

key-decisions:
  - "Worker-count adoption: workerCountForCores changed from cores - 1 to cores, width 4 on the 4-core baseline. Plan 07.1-04's own stated threshold is recorded as NOT satisfied by the evidence (maxRecomputeMs swung 1.2ms to 10.0ms across two runs, an unusable signal); what the evidence does support (11-15% throughput gain, maxFrameGapMs unmoved at 16.8ms both runs) is what the adoption is actually decided on, with an explicit abort condition binding it."
  - "solveIrr escalation Key Decision updated with the real measured figure (normalizedMs=3888.66/3952.70, hardwareConcurrency=4, source=production) replacing plan 03's own ~4872ms projection: a 2.3x improvement over the pre-phase 8961.84ms, beating the projection, still ~3.9x over budget. Decision unchanged from plan 03's own checkpoint: escalate."
  - "Bench-runtime shortfall Key Decision updated with the real measured totalRuntimeMs=57717 (down 13.2% from 66527) replacing plan 05's own 52,958-60,791ms projection. Still ~92% over the 30,000ms cap; not closed by this phase's two runtime-reduction tasks."
  - "The plan's own literal instruction for identifying the phase base commit (merge-base of gsd/phase-07.1-sweep-pool-tuning and main) resolves to 15b53b3, a PRE-Phase-7 commit, because PR #7 (Phase 7) is still open and main has not absorbed Phase 7's work. Using that commit for the no-relaxation diff proof spuriously flags src/sweep/sweep-grid.ts as new (Phase 7 created it, not this plan). The correct phase-07.1 base is 7c21f0bc2f5fe7637ea7e898320b84f5824199eb (the parent of this phase's first commit, a969703); diffing against it is clean."

requirements-completed: []

coverage:
  - id: D1
    description: "workerCountForCores changed from cores - 1 to cores in both production declarations, with correctness clamps re-run and passing at the tuned width (chunkCount 12 -> 16)"
    requirement: "PERF-03"
    verification:
      - kind: unit
        ref: "tests/sweep-pool.test.ts (updated expectations, 3 tests pass)"
        status: pass
      - kind: unit
        ref: "tests/sweep/partition.test.ts (unmodified, already covers widths 1-8, passes at width 4)"
        status: pass
      - kind: integration
        ref: "bench/sweep.bench.test.ts's pool-versus-serial equality gates (axis-tightness, no-holes, 50-cell coprime-stride equality) -- 5/5 pass, workerCount=4 confirmed in the run's own PERF-03 info line"
        status: pass
    human_judgment: false
  - id: D2
    description: "Three of this phase's four owed PROJECT.md Key Decisions written, each carrying measured figures with hardwareConcurrency and run ids, independently re-verified against downloaded CI artifacts"
    requirement: "PERF-01a"
    verification:
      - kind: other
        ref: "gh run download 32669644628/32673398579/32674615017 --name bench-results, re-parsed with node -e against bench-results.json (environment, PERF-03 row, infoLines) -- every figure cited in the three rows matches the artifact"
        status: pass
    human_judgment: false
  - id: D3
    description: "PERF-03's own headline disposition (met on two consecutive production CI runs, or escalated) and the 07.1-PERF-03-BASELINE.md transcription of that disposition"
    verification: []
    human_judgment: true
    rationale: "Requires a fresh CI run of this plan's own worker-count change (workerCountForCores=cores, just committed), which only the orchestrator can trigger by pushing -- this agent cannot push per this plan's own <how_ci_runs_happen> constraint. Not yet knowable from any existing artifact, since every prior run measured the old cores - 1 code."
  - id: D4
    description: "PROJECT.md's fourth Key Decision (PERF-03 headline outcome: met with margin, met thinly, or escalated with named remaining levers) and REQUIREMENTS.md's PERF-03 checkbox/mapping-row update to match"
    verification: []
    human_judgment: true
    rationale: "Depends on D3's disposition, which is not yet known. Writing this decision before the real figure exists would be exactly the kind of restated-against-a-different-measurement error this plan's own prohibition forbids."

duration: "in progress (interim halt, resumable)"
completed: "2026-08-24 (interim)"
status: halted
---

# Phase 07.1 Plan 06: Worker-Count Adoption and Three of Four Key Decisions Summary

**Adopted worker-count width 4 (`workerCountForCores`: `cores - 1` -> `cores`), re-asserted the correctness clamps at the tuned chunk shape, and wrote three of this phase's four owed PROJECT.md Key Decisions with every figure independently re-verified against downloaded CI artifacts -- the fourth (PERF-03's own headline disposition) is pending the two consecutive production CI runs only the orchestrator can trigger by pushing this plan's own code change.**

## Performance

- **Duration:** interim halt (multi-session by design; see `<how_ci_runs_happen>` in this plan's own prompt)
- **Started:** 2026-08-24T00:07:00Z (approx)
- **Tasks:** 1 of 2 fully complete (Task 1: worker-count change + correctness clamps); Task 2 partially complete (3 of 4 Key Decisions written; the 4th and the baseline transcription are blocked on a pending CI run)
- **Files modified:** 4

## Why this halts here, not a failure

This plan's own instructions are explicit that it is normal to be resumed more than once: applying the worker-count code change requires a fresh production CI run to know PERF-03's real disposition under the new code, and this agent cannot push. Everything that did NOT require that pending run has been completed and committed in this session.

## Accomplishments

- `workerCountForCores` adopted `cores` (was `cores - 1`) in both declarations, per plan 07.1-04's deferred decision and the two already-existing `hardwareConcurrency=4` contention-arm CI runs that answer it
- `BASELINE_WORKER_COUNT` moves 3 -> 4 (`chunkCount` 12 -> 16); every correctness clamp this plan names re-run at the tuned shape and passes: `tests/sweep-pool.test.ts` (updated), `tests/sweep/partition.test.ts` (unmodified), and `bench/sweep.bench.test.ts`'s pool-versus-serial equality gates (5/5 pass, `workerCount=4` confirmed in the run's own info line)
- Three PROJECT.md Key Decision rows written: worker-count adoption (new text), solveIrr escalation (07.1-03's drafted text with its projection replaced by the real measurement), bench-runtime shortfall (07.1-05's drafted text with its projection replaced by the real measurement)
- Every figure in all three rows independently re-verified by downloading and parsing the actual `bench-results.json` artifacts for runs 32669644628, 32673398579, and 32674615017 -- not trusted from this plan's own prompt transcription, per `<how_ci_runs_happen>`'s own instruction
- Found and documented that the plan's own literal "phase base commit" instruction (merge-base of `gsd/phase-07.1-sweep-pool-tuning` and `main`) resolves to a pre-Phase-7 commit (PR #7 is still open), and used the correct phase-07.1 base instead for the no-relaxation diff proof

## Task Commits

1. **Task 1: apply the worker-count decision, re-assert correctness clamps** - `3b5af18` (perf)
2. **Task 2 (partial): record three of four owed Key Decisions** - `e346e6a` (docs)

Both commits pushed onto `worktree-agent-a06b9f53d1a3ff010`; neither has reached `origin` yet (this agent cannot push).

## Files Created/Modified

- `src/sweep/sweep-pool.ts` - `workerCountForCores`: `Math.max(1, cores - 1)` -> `Math.max(1, cores)`, doc comment updated to cite the Phase 7.1 worker-count Key Decision
- `bench/sweep-pool.ts` - same change, same doc-comment update
- `tests/sweep-pool.test.ts` - numeric expectations updated for the new rule (`workerCountForCores(4)` now `4`, not `3`; `workerCountForCores(9)` now `9`, not `8`; the floor-at-1 test for 0/1 cores is unchanged)
- `.planning/PROJECT.md` - three new Key Decisions rows appended to the Key Decisions table

## Decisions Made

See `key-decisions` in the frontmatter above for the four findings; the full reasoning for each of the three written Key Decisions is in PROJECT.md itself (not restated here, per this project's own "don't compress a decision into a summary sentence" convention for that table).

In prose: the worker-count adoption is decided on throughput and frame-gap evidence, explicitly NOT on plan 07.1-04's originally stated `maxRecomputeMs`-clears-with-margin threshold, because that specific statistic proved unmeasurable at n=2 (a max-of-one-run figure that swung by more than half its own budget across two runs of identical code). The adoption carries an explicit abort condition: if PERF-07a or PERF-07b turns red, or crosses D-20's 70%-of-budget trigger, on either of the two authoritative runs this plan still needs, `workerCountForCores` reverts to `cores - 1` and PERF-03 is recorded as an escalation, not a pass.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected the phase base commit for the no-relaxation diff proof**
- **Found during:** Task 1, verifying `git diff --exit-code <phase-base-commit>..HEAD -- perf-budgets.ts bench/calibration.ts src/sweep/sweep-grid.ts`
- **Issue:** The plan's own literal instruction ("the merge base of `gsd/phase-07.1-sweep-pool-tuning` and `main`") resolves to `15b53b3d6a2983f45158c763bbaf20b52270eb7c`, which predates Phase 7 entirely -- because PR #7 (Phase 7) is still open (`gh pr view 7` confirms `state: OPEN`, `mergedAt: null`) and `main` has never absorbed Phase 7's work. Diffing against that commit spuriously flags `src/sweep/sweep-grid.ts` as a brand-new file (it genuinely is new relative to `main`, but it is Phase 7's own file, created before Phase 7.1 began, not touched by this plan).
- **Fix:** Identified the true phase-07.1 base commit as the parent of this phase's first commit (`a969703`'s parent, `7c21f0bc2f5fe7637ea7e898320b84f5824199eb`, itself Phase 7's own final commit) and used that commit for the diff proof instead. `git diff --exit-code 7c21f0bc2f5fe7637ea7e898320b84f5824199eb..HEAD -- perf-budgets.ts bench/calibration.ts src/sweep/sweep-grid.ts` exits 0.
- **Files modified:** None (verification-only finding, documented in the Task 2 commit message and here)
- **Verification:** Both diff commands run and their outputs compared; the true base commit's diff is clean while the literal-instruction commit's diff falsely shows a whole new file
- **Committed in:** `e346e6a` (documented in the commit message; no code change required)

---

**Total deviations:** 1 auto-fixed (Rule 3, blocking-adjacent verification correction). No production code beyond the two authorized `workerCountForCores` declarations was touched.
**Impact on plan:** None on scope; this only affects which commit the no-relaxation diff proof is run against, and using the wrong one would have produced a false-positive "relaxation" finding on a file this plan never touched.

## Issues Encountered

- `tests/app/static-build.test.ts` failed on first full-suite run in this sandbox (`dist/` did not exist yet, a pre-existing environment-only condition also noted in 07.1-03-SUMMARY.md and 07.1-05-SUMMARY.md, not something this plan introduced). Resolved locally by running `npm run build` once (gitignored output, not committed); confirmed pre-existing and unrelated.
- This agent cannot push and must not per this plan's own `<how_ci_runs_happen>` constraint -- see "What the orchestrator must do next" below.

## User Setup Required

None - no external service configuration required.

## What the orchestrator must push next

1. **Push this worktree's two new commits** (`3b5af18`, `e346e6a`, on top of `5d25895`) onto `gsd/phase-07.1-sweep-pool-tuning` (the branch PR #8 tracks). If PR #8 is still open, pushing retriggers CI; if it has been closed, reopen it or open a new PR against `main`.
2. **Wait for the `CI` workflow run to complete.** Note its run id. The job's own `conclusion` may read `failure` even on a genuine pass of every figure this plan cares about -- `assertRunInvariants` also gates on `BENCH_TOTAL_RUNTIME_CAP_MS` (already a known, disclosed shortfall per this phase's own bench-runtime Key Decision, ~57,717ms against the 30,000ms cap on the pre-this-plan code), and that gate can fire and mark the job red even when PERF-03 itself passed. Read the artifact's own rows regardless of job conclusion; `actions/upload-artifact@v4` runs `if: always()`, so a red job still publishes the real figures.
3. **Download the artifact:** `gh run download <run-id> -R andynumber2/leverage-simulator --name bench-results`.
4. **Confirm `environment.hardwareConcurrency === 4`.** If it is not 4, this is not a D-17 baseline run; rerun, do not read the figures.
5. **Read the PERF-03 row** (`budgetId: "PERF-03"`): `measuredMs`, `normalizedMs`, `verdict`, `source`. Also read the `sweep-contention` info lines (if present -- they now measure widths 4 and 5, since `BASELINE_WORKER_COUNT` moved) and, most importantly, **the PERF-07a and PERF-07b rows** (`budgetId: "PERF-07a"` / `"PERF-07b"`): these are the abort-condition gates.
6. **Repeat steps 1-5 for a second, later run.** The merge bar this phase's own `07.1-VALIDATION.md` sets is **two consecutive** production runs at `hardwareConcurrency=4` with the PERF-03 headline row at or under 1000ms normalized, `verdict=pass`, `source=production`. One pass is not enough.
7. **Hand back to this plan, resumed, with:** both run ids, both full `bench-results.json` artifacts (or at minimum the PERF-03, PERF-07a, and PERF-07b rows plus the environment block from each), and each run's `totalRuntimeMs`.

**What I will do when resumed, depending on what those two runs say:**

- **If both runs pass the merge bar AND neither PERF-07a nor PERF-07b turns red or crosses D-20's 70% trigger on either run:** write `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` (the full 7-section transcription this plan's own PLAN.md names), write the fourth PROJECT.md Key Decision recording PERF-03 as met (with margin, or thinly, per the actual figures), update `.planning/REQUIREMENTS.md`'s PERF-03 checkbox and mapping row, and complete this plan's SUMMARY with final actuals.
- **If either run misses the 1000ms budget, or PERF-07a/07b turns red, or crosses the 70% trigger, on either run:** revert `workerCountForCores` to `Math.max(1, cores - 1)` in both declarations (a third commit), re-run the correctness clamps at the reverted width, write the baseline transcription recording PERF-03 as an escalation (not a pass) with the two remaining named levers (the kernel's write-only per-bar output arrays, and D-03's coarser default grid, both still unspent and still in reserve), and write the fourth Key Decision accordingly.

Either way, the transcription and the fourth Key Decision will state the disposition plainly, backed by the two runs' real figures -- not asserted in advance of them.

## Next Phase Readiness

**Blocked on the orchestrator's push, not on missing work.** Everything this agent can do without a real CI run of the new code is done and committed. Resume this plan once the two consecutive runs described above are available.

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed: 2026-08-24 (interim; plan halted pending CI, not finished)*

---
phase: 7.1
slug: sweep-pool-tuning-inserted
# status lifecycle: draft (seeded by plan-phase) -> validated (set by validate-phase)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-23
---

# Phase 7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `07.1-RESEARCH.md` § Validation Architecture. The planner fills the
> per-task map; execution keeps it current.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.10. `bench` project (browser mode, Playwright/Chromium) for timing; `unit` project (Node) for correctness |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm run bench -- --reporter=verbose` |
| **Full suite command** | `npm run test && npm run bench` |
| **Estimated runtime** | bench ~43s currently (over the 30,000ms cap this phase must restore); unit suite ~1-2 min |

**Baseline discipline:** local sandbox figures are informational only and are never
presented as the D-17 baseline (PERF-11). The phase gate is a real GitHub Actions
`ubuntu-latest` run with `hardwareConcurrency=4`.

---

## Sampling Rate

- **After every task commit:** `npm run bench -- --reporter=verbose`
- **After every plan wave:** `npm run test && npm run bench`
- **Before `/gsd-verify-work`:** full suite green locally, and a real D-17 baseline CI run
  showing `verdict=pass` for the headline PERF-03 row
- **Max feedback latency:** ~60s local; a CI baseline run is minutes, not seconds, so it is a
  wave-boundary gate rather than a per-task one

---

## Per-Task Verification Map

*Populated by the planner. Every task carries an automated verify or a Wave 0 dependency.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] A diagnostic-only profiling bench arm (`recordInfoLine` only, no `MeasurementRow`) for
      success criterion 1's attributed breakdown: worker startup, chunk dispatch and transfer,
      per-cell kernel time, idle time at `workerCount=3`
- [ ] New `assertRunInvariants` test cases asserting a verdict-fail and a runtime-cap breach are
      both reported when both are true (success criterion 5)
- [ ] Only if `workerCount=4` is pursued: a concurrent-drag-during-sweep bench arm, since no
      existing bench exercises interaction against an in-flight sweep

*No framework install. This phase adds no dependency.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Sweep still feels responsive during a leverage drag if `workerCount` rises to 4 | PERF-04, PERF-06, PERF-07 | The automated arm measures task duration; whether the UI reads as stalled to a person is a perceptual judgment the numbers bound but do not settle | Start a full sweep, immediately scrub the leverage slider through its range, watch for visible stutter or a frozen control |

---

## Validation Sign-Off

- [ ] All tasks have an `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency under 60s locally
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

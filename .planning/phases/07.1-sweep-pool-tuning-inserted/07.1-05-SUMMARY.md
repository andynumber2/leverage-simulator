---
phase: 07.1-sweep-pool-tuning-inserted
plan: 05
subsystem: testing
tags: [bench, vitest, performance-budget, runtime-cap, brotli, worker-pool]

# Dependency graph
requires:
  - phase: 07.1-sweep-pool-tuning-inserted
    provides: "07.1-PERF-03-PROFILE.md section 4's per-file bench cost table (the aiming device this plan's Task 2 worked down); 07.1-03-SUMMARY.md's sampleCount=2 ceiling-overage disclosure; 07.1-04-SUMMARY.md's new sweep-contention.bench.test.ts file"
provides:
  - "STANDING_COST_BUDGET_MS constants on bench/sweep-pool-profile.bench.test.ts (600ms) and bench/sweep-contention.bench.test.ts (1500ms), both hardwareConcurrency=4, each declared and shrunk toward but not reaching its own budget"
  - "A host-to-baseline ratio (RATIO ~= 2.49, derived from bench/sweep.bench.test.ts's own untouched PERF-03 gated row) and a hybrid per-file projection method for converting this dev sandbox's local bench figures into a D-17 baseline CI estimate"
  - "A projected current D-17 baseline totalRuntimeMs (~52,958ms, hybrid method; ~60,791ms, whole-suite method) against BENCH_TOTAL_RUNTIME_CAP_MS=30000, both far over cap even after every lever this plan could spend"
  - "The exact PROJECT.md Key Decision text plan 07.1-06 must write, recording the runtime-cap shortfall and the remaining named lever"
affects: ["07.1-06 (writes this plan's Key Decision text into PROJECT.md verbatim, and replaces this plan's projection with the real CI run's totalRuntimeMs)"]

# Actuals (#2632)
actuals:
  tokens: 6200
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Standing-cost-budget constant on a diagnostic-only bench file: a named STANDING_COST_BUDGET_MS with a doc comment stating the budget, why the arm may now run smaller than its first-run size, and where the full-size figures live (07.1-PERF-03-PROFILE.md, 07.1-04-SUMMARY.md) -- the arm's job shifts from answering a one-time question to catching a standing structural regression."
    - "Per-call batch-size split for a ratio measurement: when one side of a ratio (npv) is too cheap per call to share a batch size with the other side (solveIrr) without either falling under MIN_MEASUREMENT_MS or blowing the file's budget, measure each side at its own batch size and compute the ratio per-call (mathematically identical to a shared-batch ratio, since the batch size cancels algebraically)."
    - "Hybrid CI-projection method: for files with byte-identical code to a known CI run, use that run's own figure directly; for files whose code changed since, project this session's local re-measurement by a host-to-baseline ratio derived from an untouched gated row. More defensible than applying one ratio uniformly to the whole suite total, given per-file CPU-vs-IO sensitivity to core count varies by roughly 1.1x-2.6x across this suite's own unchanged files."

key-files:
  created: []
  modified:
    - bench/sweep-pool-profile.bench.test.ts
    - bench/sweep-contention.bench.test.ts
    - bench/heatmap-form-2.bench.test.ts

key-decisions:
  - "Both of this phase's new diagnostic arms were shrunk (PROFILE_COLS 24->12, IRR_PROFILE_COLS 8->4, the shared npv/solveIrr batch split into separate NPV_BATCH_SIZE=4000/SOLVE_IRR_BATCH_SIZE=100 batches; sweep-contention's warm-baseline and in-flight sweeps both narrowed to CONTENTION_COLS=135, bounded by MIN_FRAME_COUNT) but neither reaches its own declared STANDING_COST_BUDGET_MS. Recorded as a disclosed shortfall per each task's own escape clause (worker-startup and per-test harness IPC overhead are largely irreducible via these levers), not forced by tripping a floor or a frame-count guard."
  - "heatmap-panzoom.bench.test.ts, bundle-size.bench.test.ts, sweep-progressive.bench.test.ts, and perf-07.bench.test.ts were examined and found to have NO step-1/step-2/step-3 reduction candidate: each file's cost is either entirely gated (PAN and ZOOM both feed PERF-09's single MeasurementRow via Math.max; PERF-04 and PERF-06 each record their own row directly) or the measured quantity IS the cost (bundle-size's brotliCompressSync output size at default quality). Only heatmap-form-2.bench.test.ts had a genuine step-1 candidate."
  - "INTERACTION_DRAG_STEP_COUNT (vitest.config.ts, perf-07.bench.test.ts's only lever, this plan's own named step-4 last resort) was considered and NOT spent. Reasoning: even halving it saves at most a few thousand ms of this file's own 8863ms CI-projected cost, against a remaining gap of roughly 23,000-31,000ms -- spending it would weaken D-03's coalescing-sample-size evidence and PERF-07a/07b's own maximum-over-a-smaller-sample for a saving too small to change the qualitative outcome (shortfall either way). Recorded as a considered-and-declined lever, not an oversight."
  - "The suite does not fit inside BENCH_TOTAL_RUNTIME_CAP_MS=30000 even after every lever available to this plan's two tasks. This is a disclosed shortfall per Task 2's own shortfall rule, escalated to plan 07.1-06 with the exact Key Decision text below, naming the remaining lever (a further, out-of-scope reduction to the two gated files with the largest remaining cost -- bench/sweep.bench.test.ts and bench/perf-07.bench.test.ts -- or a restructuring of what runs in the standing CI suite versus a separate, non-gating job)."

requirements-completed: []

coverage:
  - id: D1
    description: "Both of this phase's new diagnostic bench arms (sweep-pool-profile, sweep-contention) declare a standing cost budget and are measurably shrunk, with every reduction disclosed in its own recorded info line"
    requirement: "PERF-01a"
    verification:
      - kind: other
        ref: "npm run bench -- --reporter=verbose (local, hardwareConcurrency=9): sweep-pool-profile.bench.test.ts 1674ms -> 529ms; sweep-contention.bench.test.ts 2038ms -> 1344ms, both measured in full-suite context; .bench/bench-results.json infoLines confirm both arms still report every figure they reported before, with reduced sizes named (cols, batchSize, contentionCols)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The pre-existing bench files the baseline attribution named are reduced where a genuine informational-arm candidate exists, with no gated MeasurementRow's measured workload altered"
    requirement: "PERF-03"
    verification:
      - kind: other
        ref: "git diff on bench/heatmap-form-2.bench.test.ts (the only file with a step-1 candidate) confined to the removed informational test and its own doc comments; git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/sweep.bench.test.ts src/sweep/sweep-grid.ts src/sweep/sweep-pool.ts exits 0; npm run typecheck and npm run test both exit 0 (819 tests passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The suite's projected D-17 baseline total is stated with its derivation, and whether it fits inside 30,000ms with margin -- it does not, and the shortfall is recorded with the required escalation text for plan 07.1-06"
    requirement: "PERF-01a"
    verification: []
    human_judgment: true
    rationale: "Whether a ~23,000-31,000ms shortfall against a hard runtime cap should be closed by a further scoped reduction plan, a CI-job restructuring, or another approach entirely is a scope decision for plan 07.1-06 and the project owner, not something this plan's own local projection can settle -- the real CI run plan 07.1-06 produces is the decisive figure per this plan's own hard rule."

# Metrics
duration: 62min
completed: 2026-08-23
status: complete
---

# Phase 07.1 Plan 05: Bench Runtime Reduction Summary

**Shrunk both of this phase's own new diagnostic bench arms (sweep-pool-profile, sweep-contention) toward declared standing-cost budgets and removed one pre-existing informational arm that re-measured an already-REJECTED path, cutting this dev sandbox's full-suite bench runtime from 27,963ms to 24,398ms (12.7%) -- but the projected D-17 baseline total (~52,958ms to ~60,791ms depending on projection method) remains far over `BENCH_TOTAL_RUNTIME_CAP_MS=30000` even after every lever available to this plan, a disclosed shortfall escalated to plan 07.1-06 with the exact Key Decision text below.**

## Performance

- **Duration:** 62 min
- **Started:** 2026-08-23T23:24:10Z
- **Completed:** 2026-08-24T00:26:00Z
- **Tasks:** 2 of 2
- **Files modified:** 3

## The problem, restated with the real figures

This plan's own PLAN.md was written against a stale 43,259ms starting figure. The measured D-17 baseline (CI run 32669644628, `hardwareConcurrency=4`, `source=production`, transcribed in `07.1-PERF-03-PROFILE.md`) is **66,527ms** against the 30,000ms cap -- a 23,268ms rise since Phase 7's own baseline, of which this phase's own `bench/sweep-pool-profile.bench.test.ts` (07.1-01's new file) accounted for 11,835ms (50.9% of the rise) at its original, un-shrunk size.

Since that baseline run, three more plans landed real changes that also move the suite's runtime:

- **07.1-03** applied two levers to `solveIrr` (a rate-space bracket-width termination test, a typed-array `CashFlows` container), cutting that branch's own wall-clock cost by a measured 1.83x, but also raised the contribution-schedule info arm's `sampleCount` from 1 to 2 -- disclosed in WINDOWS.md entry 7 as projecting roughly 9.2% over that arm's own stated ceiling, and explicitly flagged for this plan.
- **07.1-04** added `bench/sweep-contention.bench.test.ts`, a new full-grid-shaped informational arm answering the worker-count interactivity question, at two widths.
- Neither plan's own net effect on the suite's total runtime was measured against the real D-17 baseline (both plans' own full-suite figures are dev-sandbox, non-baseline, per PERF-11) -- so this plan is the first to face the combined effect of all three prior plans' changes at once.

## Task 1: standing budgets for this phase's two new diagnostic arms

Both `bench/sweep-pool-profile.bench.test.ts` and `bench/sweep-contention.bench.test.ts` now declare a named `STANDING_COST_BUDGET_MS` constant with a doc comment stating the budget, why the arm may run smaller than its first-run size, and where the full-size figures live (`07.1-PERF-03-PROFILE.md`, `07.1-04-SUMMARY.md`).

**sweep-pool-profile.bench.test.ts (budget: 600ms, `hardwareConcurrency=4`):**

- `PROFILE_COLS`: 24 -> 12 (keeps `partitionColumns`'s chunk count at exactly 12, `BASELINE_WORKER_COUNT * CHUNKS_PER_WORKER`, unchanged from the original -- only per-chunk kernel work halves, not the dispatch-overhead shape being profiled).
- `IRR_PROFILE_COLS`: 8 -> 4.
- The shared `NPV_SOLVE_BATCH_SIZE=5000` batch (used for both `npv()` and `solveIrr()` timing) split into separate `NPV_BATCH_SIZE=4000` and `SOLVE_IRR_BATCH_SIZE=100` batches. No single batch size could clear `MIN_MEASUREMENT_MS` for the cheap `npv()` call while also keeping the expensive `solveIrr()` call's batch small enough to fit the budget (measured pre-reduction: batch of 5000 cost 74.00ms for npv but 7770.90ms for solveIrr on the D-17 baseline, a ~105x ratio). `npvEvaluationsPerSolve` is now computed as `(solveIrrMs / SOLVE_IRR_BATCH_SIZE) / (npvMs / NPV_BATCH_SIZE)`, a per-call ratio mathematically identical to the old same-batch ratio (the batch size cancels algebraically) -- the derived figure is unchanged, only how each side is measured. Both batch sizes are disclosed in the recorded info line.

Local (`hardwareConcurrency=9`, this dev sandbox, full-suite context) file duration: **1674ms -> 529ms** (68.4% local reduction). Every remaining timed segment cleared `MIN_MEASUREMENT_MS` (10ms) with real margin (smallest: `solveIrrMs`=14.40-16.15ms at `SOLVE_IRR_BATCH_SIZE=100`, roughly 1.4-1.6x the floor; `npvMs`=15.70-16.10ms at `NPV_BATCH_SIZE=4000`).

**sweep-contention.bench.test.ts (budget: 1500ms, `hardwareConcurrency=4`):**

- Both the warm-baseline sweep AND the in-flight sweep now share a narrowed `CONTENTION_COLS=135` axis (not just the in-flight one, as a literal first reading of the plan's action text might suggest). A single un-repeated full-`SWEEP_COLS` (200) sweep alone costs on the order of 900-1180ms at the D-17 baseline (`07.1-PERF-03-PROFILE.md` section 2's own `idealParallelFullGridMs`/measured PERF-03 figures); two widths times two full-grid measurements (warm, in-flight) each would alone exceed this arm's entire 1500ms budget several times over. Narrowing only the in-flight sweep, as the plan's action text's literal wording could be read to require, would not have made the budget reachable even in principle -- this is recorded here as the interpretation applied, with the reasoning, not silently substituted.
- The narrowing is bounded by `MIN_FRAME_COUNT=10`, exactly as instructed: `CONTENTION_COLS=60` tripped the guard at both tested worker counts; `CONTENTION_COLS=120` passed but with only 14 frames at `workerCount=4` (thin margin); `CONTENTION_COLS=135` (final) measured 15-20 frames across repeat runs at both widths -- comfortable margin above the floor, verified stable across two consecutive local runs.
- The cold sweep (already a tiny, grid-size-independent 1x1 grid) is unchanged.

Local (`hardwareConcurrency=9`, full-suite context) file duration: **2038ms -> 1344ms** (34.1% local reduction).

**Neither arm reaches its own declared budget.** Projected to the D-17 baseline (RATIO ~= 2.49, derivation below): sweep-pool-profile projects to roughly 1318ms against its 600ms budget (2.20x over); sweep-contention projects to roughly 3349ms against its 1500ms budget (2.23x over). Worker-startup cost (a fixed, grid-size-independent cost per test, measured at 75-90ms locally / roughly 190-225ms CI-equivalent) and per-test harness IPC overhead (`resolveRunCalibration`, `recordEnvironment`, `recordInfoLine` -- every bench test in this suite pays these, not something specific to these two arms) are largely irreducible via `PROFILE_COLS`/`IRR_PROFILE_COLS`/`CONTENTION_COLS`. Recorded as a disclosed shortfall per each task's own escape clause, not forced by tripping `MIN_MEASUREMENT_MS` or `MIN_FRAME_COUNT`.

## Task 2: the pre-existing files the baseline attribution named

Worked down `07.1-PERF-03-PROFILE.md` section 4's per-file cost table, most expensive first, among the five files this task may touch: `perf-07.bench.test.ts` (8863ms, rank 1 of the five), `heatmap-form-2.bench.test.ts` (6354ms, rank 2), `heatmap-panzoom.bench.test.ts` (6118ms, rank 3), `sweep-progressive.bench.test.ts` (5367ms, rank 4), `bundle-size.bench.test.ts` (4431ms, rank 5).

**Classified every arm in every file** (gated = calls `commands.recordMeasurement`; informational = `recordInfoLine` only) before touching anything:

| File | Arms | Classification | Reduction candidate? |
|---|---|---|---|
| `perf-07.bench.test.ts` | PERF-07a, PERF-07b (single drag produces both) | Both gated | None (steps 1-3); only lever is `INTERACTION_DRAG_STEP_COUNT` (step 4, see below) |
| `heatmap-form-2.bench.test.ts` | `resampleField` oracle test; equivalence (`test.fails`); informational polygon-repaint; PERF-05 (gated) | Informational polygon-repaint arm re-measures an already-REJECTED path | **Yes -- removed (step 1)** |
| `heatmap-panzoom.bench.test.ts` | PAN, ZOOM (both feed the single PERF-09 row via `Math.max`) | Both gated (no separate informational re-measurement exists) | None |
| `bundle-size.bench.test.ts` | `readBundleBytes` -> `brotliCompressSync` | Gated; the measured quantity IS the compressed byte count | None -- lowering compression quality would change what's measured, not just its cost |
| `sweep-progressive.bench.test.ts` | PERF-04, PERF-06 (each records its own row directly) | Both gated | None |

**heatmap-form-2.bench.test.ts (the only file with a genuine step-1 candidate):** removed the "informational: form 2 (filled contour, polygon FillPath, REJECTED) repaint on a metric change, at the declared shipped-panel geometry" test and its dedicated `REJECTED_POLYGON_BATCH_SIZE` constant. This arm re-measured the REJECTED `'polygon'` fill path's own repaint cost on every CI run; the decision it informed (D-09's offscreen-cache `'resample'` path over `'polygon'`, 07-04-PLAN.md Task 3) is closed, and the historical figure that decision was made against (149.71ms) is already transcribed in 07-04-PLAN.md's Task 2 commit message and `07-PERF-03-BASELINE.md`. The equivalence test (`test.fails`) that proves the polygon path's OUTPUT still disagrees with the shipped path stays live, untouched, exactly as 07-04-PLAN.md Task 3 requires -- only the wall-clock re-measurement of the rejected path's own cost was removed, not the correctness regression detector.

Local file duration: **2518ms -> 1434ms** (43.0% local reduction). `git diff` on this file is confined to the removed test, its dedicated constant, and doc-comment updates; the gated PERF-05 test body (line-for-line) and the `resampleField`/equivalence tests are byte-for-byte unchanged (confirmed by re-reading the diff before committing).

**INTERACTION_DRAG_STEP_COUNT (vitest.config.ts, `perf-07.bench.test.ts`'s only lever, this plan's own step-4 last resort): considered, not spent.** Steps 1-3 are exhausted across all five files (only one candidate existed, already spent above). The plan's own text permits touching this constant once steps 1-3 don't close the gap -- they do not, so the condition for considering it is met, but spending it is not obligatory. Even halving it (300 -> 150) would save at most a few thousand ms of this file's own ~8863ms CI-projected cost, against a remaining gap measured in the tens of thousands of ms (below). Spending it would weaken D-03's coalescing-sample-size evidence and PERF-07a/07b's own maximum-over-a-smaller-sample, for a saving too small to change the qualitative outcome (shortfall either way, before or after). Declined as a deliberate, documented choice, not an oversight.

## The projection: does the suite fit?

**No.** Both a hybrid per-file method and a whole-suite method project the current D-17 baseline total far over `BENCH_TOTAL_RUNTIME_CAP_MS=30000`, even after this plan's own two tasks.

**Ratio derivation.** `bench/sweep.bench.test.ts`'s own PERF-03 gated row is untouched by any of 07.1-01 through 07.1-05 (07.1-04's `defer-to-baseline` decision changed no worker-count code; 07.1-03's `irr.ts` levers only affect the separate `solveIrr` contribution-schedule branch, not the zero-contribution CAGR branch PERF-03 gates) -- so it is the cleanest apples-to-apples, same-code comparison across the two hosts available.

- D-17 baseline (CI run 32669644628, `hardwareConcurrency=4`): `measuredMs=1179.70`.
- This dev sandbox (`hardwareConcurrency=9`), averaged across three runs in this session (475.90, 473.20, 471.70): `measuredMs~=473.60`.
- **RATIO = 1179.70 / 473.60 ~= 2.49.**

This single ratio is a rough approximation, not a precise prediction: cross-checked against five entirely unchanged files (identical source since the original CI baseline, so any deviation is pure host effect, not a code effect), the implied per-file ratio ranges from 1.08x (`decode-time.bench.test.ts`, I/O-bound) to 2.56x (`canvas-repaint.bench.test.ts`, CPU-bound) -- a real spread this single-ratio method cannot capture precisely. RATIO=2.49 is a CPU/worker-pool-bound anchor, likely reasonable for the sweep-related files and `bundle-size` (also CPU-bound, brotli compression) but possibly an overestimate for I/O-lighter files.

**Hybrid method (primary, more grounded):** for files with byte-identical code to the original CI baseline (`heatmap-panzoom`, `sweep-progressive`, `bundle-size`, `perf-07`, `canvas-repaint`, `kernel`, `perf-08`, `decode-time`, `playwright-context-probe`), use that run's own real CI figure directly -- no projection needed. For files whose code changed since (`heatmap-form-2` via this plan's own edit; `sweep.bench.test.ts` via 07.1-03's `irr.ts`/`sampleCount` changes though the file itself is untouched by this plan; `sweep-pool-profile`, `sweep-contention` via this plan's Task 1), project this session's local re-measurement by RATIO.

| Component | Basis | Value |
|---|---|---|
| Unchanged files (real CI figures) | `heatmap-panzoom`(6118) + `sweep-progressive`(5367) + `bundle-size`(4431) + `perf-07`(8863) + `canvas-repaint`(1421) + `kernel`(710) + `perf-08`(455) + `decode-time`(184) + `playwright-probe`(50) | 27,599ms |
| `heatmap-form-2` (changed) | local 1434ms x RATIO | ~3,573ms |
| `sweep.bench.test.ts` (changed via 07.1-03, untouched by this plan) | local 5836ms x RATIO | ~14,541ms |
| `sweep-pool-profile` (changed) | local 529ms x RATIO | ~1,318ms |
| `sweep-contention` (changed) | local 1344ms x RATIO | ~3,349ms |
| Fixed overhead (browser start/transform/import) | from original CI run: 66527 - 63949 | 2,578ms |
| **Projected total** | | **~52,958ms** |

**Whole-suite method (cross-check, upper bound):** this session's own current full-suite local total (24,398ms) x RATIO = **~60,791ms**.

**Both methods land far over the 30,000ms cap** (a gap of roughly 22,958ms under the hybrid method, or 30,791ms under the whole-suite method) -- neither number fits with the required 3,000ms margin, or fits at all. This is a projection, not a measurement (PERF-11): plan 07.1-06's real CI run at `hardwareConcurrency=4` replaces both figures with the actual `totalRuntimeMs`.

## Shortfall rule: the exact PROJECT.md Key Decision text (plan 07.1-06 writes this verbatim)

> **BENCH_TOTAL_RUNTIME_CAP_MS shortfall (Phase 7.1 continuation): the bench suite's runtime remains far over its 30,000ms cap even after both of this phase's own runtime-reduction tasks.** The D-17 baseline (CI run 32669644628, `hardwareConcurrency=4`) measured `totalRuntimeMs=66527` against the cap, a rise of 23,268ms since Phase 7's own baseline (43,259ms), of which 07.1-01's new `bench/sweep-pool-profile.bench.test.ts` accounted for 11,835ms (50.9%) at its original size. 07.1-05 spent every lever available to it: Task 1 declared standing cost budgets for this phase's two new diagnostic arms (`sweep-pool-profile`: 600ms; `sweep-contention`: 1500ms, both `hardwareConcurrency=4`) and shrunk both toward those budgets (`PROFILE_COLS` 24->12, `IRR_PROFILE_COLS` 8->4, a shared npv/solveIrr batch split into separate smaller batches measured per-call; `sweep-contention`'s warm-baseline and in-flight sweeps both narrowed from the full 200-column grid to a shared 135-column axis, bounded by its own 10-frame minimum guard), cutting those two files' combined local (dev-sandbox) cost by 55.3% (3712ms to 1873ms). Task 2 worked down `07.1-PERF-03-PROFILE.md` section 4's per-file cost table among the five pre-existing files it could touch and found exactly one genuine reduction candidate: `heatmap-form-2.bench.test.ts`'s informational polygon-repaint arm, which re-measured an already-REJECTED path (D-9's `'resample'` over `'polygon'`, 07-04-PLAN.md Task 3) whose historical figure (149.71ms) was already transcribed in committed documents -- removed, cutting that file's local cost by 43.0% (2518ms to 1434ms). The other four files (`heatmap-panzoom`, `bundle-size`, `sweep-progressive`, `perf-07`) were found to have no reduction candidate under the same no-gated-workload-touched discipline: each file's cost is either entirely gated (two arms feeding one `MeasurementRow` via a `Math.max`, or each arm recording its own row directly) or the measured quantity IS the cost itself (`bundle-size`'s brotli-compressed byte count, where lowering compression quality would change what is measured, not merely its cost). `perf-07.bench.test.ts`'s only remaining lever, `INTERACTION_DRAG_STEP_COUNT` (this plan's own named step-4 last resort), was considered and declined: even halving it saves at most a few thousand ms against a remaining gap measured in the tens of thousands, for a real cost (a weaker sample size behind D-03's coalescing evidence and PERF-07a/07b's own maximum-over-a-smaller-sample). Projected against the D-17 baseline by a host-to-baseline ratio (RATIO~=2.49, derived from `bench/sweep.bench.test.ts`'s own untouched PERF-03 gated row, cross-checked against five entirely unchanged files whose own implied ratios span 1.08x-2.56x, so this is a rough approximation not a precise prediction), the current suite projects to roughly 52,958ms (hybrid per-file method) to 60,791ms (whole-suite method) against the 30,000ms cap -- a shortfall of roughly 22,958ms to 30,791ms, over 75% above the cap under even the more optimistic method. This is a projection; plan 07.1-06's own CI run at `hardwareConcurrency=4` replaces it with the measured figure. **No budget value, threshold, anchor, calibration constant, run-level cap, or grid dimension moved to reach this disposition**: `PERF-03.thresholdMs` stays 1000, `NOMINAL_REFERENCE_MS` stays 40, `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000, the default grid stays 200x50. **The named remaining lever, for whichever future plan takes this up:** the two largest remaining costs are both fully gated files this plan had no authority to touch -- `bench/sweep.bench.test.ts` (18161ms at the original CI baseline, now likely lower after 07.1-03's `irr.ts` levers but still the single largest file) and `bench/perf-07.bench.test.ts` (8863ms, un-reducible without weakening its own gate's sample size). Closing this gap requires either a scoped plan with explicit authority to touch one or both of those gated files' own measured workload (a Key Decision in itself, since T-07.1-12's no-relaxation discipline would need a different, explicitly-approved tradeoff), or a restructuring of what runs inside the `BENCH_TOTAL_RUNTIME_CAP_MS`-gated CI job versus a separate, non-gating diagnostic job -- neither of which this plan's own scope or file list authorized it to decide. See `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-05-SUMMARY.md` for the full per-file breakdown, the ratio derivation and its cross-check, and the two-method projection.

## Accomplishments
- `bench/sweep-pool-profile.bench.test.ts` and `bench/sweep-contention.bench.test.ts` each declare a standing cost budget (600ms, 1500ms, `hardwareConcurrency=4`) and are measurably shrunk (68.4% and 34.1% local reduction respectively), with every reduction disclosed in its own recorded info line -- neither reaches its budget, disclosed as a shortfall rather than forced
- `npvEvaluationsPerSolve` is now measured via separate per-call batch sizes for `npv()` and `solveIrr()`, the same derived figure computed a different, floor-respecting way
- `bench/heatmap-form-2.bench.test.ts`'s informational polygon-repaint arm (re-measuring an already-REJECTED, already-documented path) removed, 43.0% local file reduction
- The other four Task-2-eligible files examined and confirmed to have no legitimate reduction candidate, each with its own stated reason, so a future reader does not re-spend time re-checking them
- A host-to-baseline projection ratio derived and cross-checked against five untouched files' own implied ratios, exposing the method's real uncertainty rather than presenting a single number as precise
- The suite's projected D-17 baseline total (52,958ms-60,791ms) stated with its derivation, against the 30,000ms cap, with the full escalation text for plan 07.1-06 recorded verbatim

## Task Commits

Each task was committed atomically:

1. **Task 1: give this phase's two diagnostic arms a standing cost budget and shrink them to fit** - `1eb48bb` (perf)
2. **Task 2: reduce the pre-existing arms the baseline attribution named, and stop when the suite fits** - `c74d822` (perf)

## Files Created/Modified
- `bench/sweep-pool-profile.bench.test.ts` - `STANDING_COST_BUDGET_MS=600` declared; `PROFILE_COLS` 24->12, `IRR_PROFILE_COLS` 8->4; shared npv/solveIrr batch split into `NPV_BATCH_SIZE=4000`/`SOLVE_IRR_BATCH_SIZE=100`, ratio computed per-call
- `bench/sweep-contention.bench.test.ts` - `STANDING_COST_BUDGET_MS=1500` declared; both warm-baseline and in-flight sweeps narrowed to shared `CONTENTION_COLS=135`, bounded by `MIN_FRAME_COUNT`
- `bench/heatmap-form-2.bench.test.ts` - removed the informational polygon-repaint arm and its `REJECTED_POLYGON_BATCH_SIZE` constant; gated PERF-05 test and the equivalence regression detector untouched

## Decisions Made
See `key-decisions` in the frontmatter above, and the "Shortfall rule" section for the full escalation text. In prose: both new diagnostic arms were shrunk but neither reaches its declared budget (worker-startup and per-test harness overhead are largely fixed costs); only one of the five Task-2-eligible pre-existing files had a genuine reduction candidate, and it was spent; `INTERACTION_DRAG_STEP_COUNT` was considered as the explicit step-4 last resort and declined as disproportionate to the remaining gap; the suite does not fit inside the cap even after every lever, escalated to plan 07.1-06 with the exact required text.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4-adjacent, disclosed rather than silently applied] Narrowed the warm-baseline sweep, not only the in-flight sweep, in sweep-contention.bench.test.ts**
- **Found during:** Task 1
- **Issue:** The plan's action text reads "keep the cold-versus-warm pair, but narrow the in-flight sweep's entry-date axis," which could be read as narrowing only the in-flight (third) grid. A single un-repeated full-`SWEEP_COLS` sweep alone costs on the order of 900-1180ms at the D-17 baseline; narrowing only the in-flight sweep while leaving the warm-baseline sweep at the full 200 columns would leave the arm's cost several times over its 1500ms budget from the warm-baseline measurement alone, regardless of how far the in-flight sweep narrowed.
- **Fix:** Read "keep the cold-versus-warm pair" as keeping the STRUCTURE (both a cold and a warm measurement, neither removed), not the warm sweep's exact SIZE, and narrowed both the warm-baseline and in-flight sweeps to a shared `CONTENTION_COLS` axis, disclosed as `contentionCols` in every info line and explained in the file's own header.
- **Files modified:** `bench/sweep-contention.bench.test.ts`
- **Verification:** `MIN_FRAME_COUNT` guard still passes with real margin (15-20 frames, stable across two runs); `npm run typecheck` exits 0; local file duration measurably lower than before.
- **Committed in:** `1eb48bb` (Task 1 commit)

---

**Total deviations:** 1 disclosed interpretation (not a Rule 1-3 bug fix; a reading choice made necessary by the physics of the budget, disclosed rather than silently substituted).
**Impact on plan:** Without this reading, Task 1's own stated budget for the contention arm would have been unreachable even in principle, regardless of how the in-flight sweep alone was narrowed. No scope creep -- the cold sweep and both worker-count widths are unchanged, exactly as the plan required.

## Issues Encountered
- The plan's starting figure (43,259ms) was explicitly flagged as stale before this plan began (see the orchestrator's own `<the_problem_is_bigger_than_your_plan_assumed>` context); the real gap (66,527ms against 30,000ms, before this plan's own reductions) was roughly 2.8x the gap the plan was originally scoped against. This plan's own two tasks close a real but partial share of that gap; the remainder is escalated per the shortfall rule above, not forced.
- No CI access from this environment: the projection above relies on a derived host-to-baseline ratio rather than a direct re-measurement at `hardwareConcurrency=4`. This is disclosed as a projection at every use, per PERF-11, and plan 07.1-06's own CI run is the decisive figure.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

**Blocked on a scope decision, not on missing work.** Plan 07.1-06 should:
1. Run its own CI job at `hardwareConcurrency=4` and read the real `totalRuntimeMs` (replacing this plan's 52,958ms-60,791ms projection with the measured figure).
2. If the real figure still exceeds 30,000ms (this plan's projection says it will, with high confidence given the size of the gap), copy the "Shortfall rule" Key Decision text above verbatim into PROJECT.md, substituting the real measured `totalRuntimeMs` for the projected range.
3. Decide, as a project-owner-level scope call, whether to authorize a further plan with explicit permission to touch one of the two named large gated files (`bench/sweep.bench.test.ts`, `bench/perf-07.bench.test.ts`), or to restructure the CI job so `BENCH_TOTAL_RUNTIME_CAP_MS` gates a smaller, faster subset while the rest runs elsewhere -- this plan's own scope and file list authorized neither.

## Known Stubs

None. Every change is a real reduction to a real bench arm's own measured workload or a real removal of a real, no-longer-needed measurement; no synthetic or placeholder data path was introduced.

## Threat Flags

None beyond what this plan's own threat model already covers (T-07.1-12, T-07.1-13, T-07.1-02): no production source file was modified, only bench arms, and every reduction is disclosed per the threat model's own mitigation plan.

## Self-Check

- `bench/sweep-pool-profile.bench.test.ts` exists and typechecks: FOUND.
- `bench/sweep-contention.bench.test.ts` exists and typechecks: FOUND.
- `bench/heatmap-form-2.bench.test.ts` exists and typechecks: FOUND.
- Commit `1eb48bb` exists: FOUND (`git log --oneline --all`).
- Commit `c74d822` exists: FOUND (`git log --oneline --all`).
- `git diff --diff-filter=D --name-only` on both commits: empty (no accidental file deletions).
- `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/sweep.bench.test.ts src/sweep/sweep-grid.ts src/sweep/sweep-pool.ts`: exits 0 (confirmed after both commits).
- `npm run typecheck`: exits 0.
- `npm run test`: exits 0 (819 tests passed).
- `npm run bench -- --reporter=verbose`: all 13 files pass, 26 tests passed, 1 expected `test.fails`; `.bench/bench-results.json` has 13 rows and 27 info lines.

## Self-Check: PASSED

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed: 2026-08-23*

---
phase: 07.1-sweep-pool-tuning-inserted
plan: 03
subsystem: performance
tags: [irr, bisection, typed-array, bench, vitest, solveIrr]

# Dependency graph
requires:
  - phase: 07.1-sweep-pool-tuning-inserted
    provides: "07.1-PERF-03-PROFILE.md's measured npvEvaluationsPerSolve=105.01 and cashFlowCount=1185 figures, which aimed both levers this plan applies"
provides:
  - "src/metrics/irr.ts: a rate-space bracket-width termination test (Lever A) alongside the existing dollar-denominated NPV test, and a CashFlows typed-array container (Lever B) replacing the CashFlow[] object-array representation"
  - "A precision-proof test corpus in tests/metrics/irr.test.ts proving Lever A moves no formatSignedPercent output by more than a raw-rate difference of ~3.20e-10 across nine representative cases"
  - "bench/sweep.bench.test.ts's contribution-schedule info line: sampleCount raised 1 -> 2, extended with budgetMs/ratioToBudget/disposition/runtimeCeilingMs/projectedBaselineTotalMs/runtimeCeilingDisposition fields"
  - "A measured, not assumed, local reduction factor for Lever A+B combined (~1.83x, min-of-3 samples, hardwareConcurrency=9) to correct 07.1-PERF-03-PROFILE.md's iteration-count-derived 3.14x projection"
affects: ["07.1-06 (must read this plan's Task 3 checkpoint outcome before writing PROJECT.md's PERF-03 Key Decision)", "07.1-05 (owns the overall bench-runtime reduction; this plan's Task 2 finding narrows its remaining budget)"]

# Actuals (#2632)
actuals:
  tokens: 10560
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Dual-criterion bisection termination: a dollar-denominated NPV tolerance and a rate-space bracket-width tolerance checked in the same loop, the reachable one firing first, rather than retuning an unreachable single criterion."
    - "Parallel-typed-array container with a reuse/grow-in-place helper (CashFlows: daysSinceEntry, amount, count) replacing an array-of-objects representation in a hot loop, following the same reuse discipline sweep.worker.ts's scratch-array pattern already established."
    - "Pre-change reference implementation embedded directly in a test file (not imported) to isolate one specific algorithmic change's effect from a concurrent representation change, proven via display-formatter string equality rather than raw-float tolerance alone."

key-files:
  created: []
  modified:
    - src/metrics/irr.ts
    - src/sweep/sweep.worker.ts
    - tests/metrics/irr.test.ts
    - bench/sweep.bench.test.ts
    - bench/sweep-pool-profile.bench.test.ts

key-decisions:
  - "Both levers (A: rate-space bracket-width termination; B: CashFlows typed-array container) were applied: 07.1-PERF-03-PROFILE.md measured npvEvaluationsPerSolve=105.01 (>40, Lever A's stated precondition) and cashFlowCount=1185 (>100, Lever B's), so neither was skipped."
  - "The precision-proof corpus deliberately excludes an EXACT-zero-true-IRR test case (terminal inflow bit-for-bit equal to the initial investment): bisection near an exact root is architecturally sign-ambiguous under ANY tolerance-based termination criterion, a pre-existing property of the method this plan did not introduce. The corpus uses a near-zero-but-genuinely-nonzero case instead, which is what a real sweep cell can actually produce (an exact double-precision tie against real market data has probability zero)."
  - "bench/sweep-pool-profile.bench.test.ts (owned by 07.1-01, not in this plan's declared files_modified) was adapted to the new CashFlows shape as a Rule 3 blocking-issue fix -- it imports buildCashFlows/CashFlow and would otherwise fail typecheck. Recorded in .planning/WINDOWS.md entry 6."
  - "Task 2's sampleCount was raised to 2 (satisfying the plan's own acceptance criteria) despite this task's own ceiling derivation projecting N=2 at ~9.2% OVER the arm's stated 7,303.90ms bench-runtime ceiling. N=3 and N=5 (the plan's other candidates) project 64% and 173% over respectively, so N=2 is the closest fit among {2,3,5}, not a clean pass. Disclosed in the arm's own info line and .planning/WINDOWS.md entry 7 rather than silently choosing N=1 (which would satisfy the ceiling but fail the acceptance criteria) or silently accepting N=2 as if it cleanly satisfied both."

requirements-completed: []

coverage:
  - id: D1
    description: "solveIrr's convergence cost is reduced via a rate-space bracket-width termination test (Lever A) and a typed-array cash-flow container (Lever B), with every displayed annualized figure proven unchanged at UI precision"
    requirement: "PERF-03"
    verification:
      - kind: unit
        ref: "tests/metrics/irr.test.ts (26 tests, all passing, including the 'display-precision equality' describe block)"
        status: pass
      - kind: unit
        ref: "npm run typecheck (exit 0), npm run test (815 passed, 1 pre-existing unrelated failure -- see Issues Encountered)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The contribution-schedule arm reports a minimum-of-N figure with N>=2, states its own N, the 1000ms budget, the ratio, and an explicit inside/outside statement"
    requirement: "PERF-01a"
    verification:
      - kind: other
        ref: "bench/sweep.bench.test.ts's own recorded verify script against .bench/bench-results.json's infoLines (sampleCount=2 present); local npm run bench full-suite run confirms the info line renders with all required fields"
        status: pass
    human_judgment: false
  - id: D3
    description: "The solveIrr branch's disposition against the measured result (close-it / escalate / adopt-newton)"
    requirement: "PERF-03"
    verification: []
    human_judgment: true
    rationale: "Task 3 is a checkpoint:decision (gate=blocking) requiring an explicit human choice among three options with materially different consequences (one requires reopening D-08, a recorded Key Decision). Not auto-selectable: auto-chain/auto-advance were both confirmed false this session."

duration: 23min
completed: 2026-08-23
status: halted
---

# Phase 07.1 Plan 03: solveIrr Convergence-Cost Reduction Summary

**Two levers applied to `solveIrr` (a rate-space bracket-width termination test and a `Float64Array`-backed cash-flow container), measured at ~1.83x wall-clock reduction locally (not the 3.14x the research profile projected), with a precision proof showing no displayed IRR moves by more than 3.20e-10 -- Task 3's disposition checkpoint is reached with the branch improved but the 1000ms budget nowhere near closed.**

## Performance

- **Duration:** 23 min
- **Started:** 2026-08-23T22:23:31Z
- **Completed:** 2026-08-23T22:46:57Z (Task 3 checkpoint reached, awaiting human decision)
- **Tasks:** 2 of 3 completed (Task 3 is the checkpoint this plan stops at)
- **Files modified:** 5

## Explicit answer to this plan's must-have truths

**The `solveIrr` contribution-schedule branch costs measurably less per sweep, and the reduction is real work eliminated, not less work measured.** Local min-of-3 samples on this executor's own dev sandbox (`hardwareConcurrency=9`, informational, non-baseline per PERF-11):

| | Raw ms (min-of-3) | Normalized ms |
|---|---|---|
| Before this plan (Task 1's pre-change baseline) | 2003.00 | -- |
| After Lever A + Lever B | 1097.60 | 1934.10 |
| **Reduction factor** | **1.83x** | -- |

`bench/sweep-pool-profile.bench.test.ts`'s own diagnostic arm confirms Lever A worked as designed at the iteration-count level: `npvEvaluationsPerSolve` dropped from the profile's measured 105.01 to a freshly re-measured **36.17-36.37** (two runs), landing almost exactly on 07.1-RESEARCH.md's naive `~33-34` estimate. The wall-clock reduction (1.83x) is smaller than the iteration-count reduction (105.01/36.17 ~= 2.90x) because `buildCashFlows`, `runBacktest` and pool/dispatch overhead do not shrink with fewer `npv` evaluations -- this is the plan's own stated risk (07.1-PERF-03-PROFILE.md section 5's arithmetic assumed nearly all cost was `npv` evaluations) materializing as measured, not assumed.

**Every displayed annualized figure is unchanged at UI precision.** A dedicated precision-proof corpus (ruined run, no-sign-change, single-cash-flow degenerate case, ~98-year long hold, 9x-in-one-year high-leverage-shaped case, near-zero-but-nonzero IRR, negative IRR, ruin-adjacent case, and a ~1,176-flow long monthly-contribution sequence matching the profile's own measured `cashFlowCount`) asserts `formatSignedPercent` is byte-identical between a pre-Lever-A reference implementation (embedded directly in the test file, not imported, so it cannot silently drift with the shipped code) and the shipped `solveIrr`. **Maximum raw-rate difference measured across the corpus: ~3.20e-10** (the long monthly-contribution case), five orders of magnitude under `formatPercent`'s two-decimal display precision.

**The contribution-schedule arm reports a minimum-of-N figure with a real verdict, not an anecdote.** `sampleCount` raised from 1 to 2; the info line now states `budgetMs=1000`, `ratioToBudget=1.92`, `disposition=outside` (the branch remains at ~1.92x the 1000ms budget at this host's normalized figure -- non-baseline, informational), plus the new ceiling-tension fields described below.

**Whether the branch ends inside or outside budget is not yet a recorded decision** -- that is Task 3's own job, and Task 3 is the checkpoint this plan halts at. See "Checkpoint Reached" below.

## Accomplishments
- `solveIrr`'s bisection loop gained a rate-space bracket-width termination test (`hi - lo <= 1e-9`) alongside its existing dollar-denominated NPV tolerance test, cutting measured `npvEvaluationsPerSolve` from 105.01 to ~36
- `buildCashFlows`/`npv`/`solveIrr` now operate on `CashFlows` (two parallel `Float64Array` buffers plus a `count`) instead of `CashFlow[]` (an array of per-flow objects), eliminating millions of short-lived object allocations per sweep at the measured ~1,185 flows/cell
- A precision-proof test corpus (9 representative cases) proves the combined change moves no displayed IRR by more than ~3.20e-10 raw, formatted identically in every case
- The contribution-schedule bench arm's `sampleCount` raised 1 -> 2 with a fully disclosed budget/ceiling verdict, rather than left as an unresolved informational row a second time
- A genuine tension between this task's own acceptance criteria (N>=2) and its own stated bench-runtime ceiling was measured, disclosed in-code and in `.planning/WINDOWS.md`, and carried forward to the Task 3 checkpoint rather than silently resolved either way

## Task Commits

Each task was committed atomically:

1. **Task 1: cut the per-solve cost of the existing bisection, aimed by the profile** - `9bcdd1a` (perf)
2. **Task 2: re-measure the contribution-schedule arm at a sample count that fits the runtime ceiling** - `d89376c` (perf)

Task 3 (checkpoint:decision, gate=blocking) is NOT committed -- no code change is made by that task under any option, per its own acceptance criteria. This SUMMARY and `.planning/WINDOWS.md` are committed as this plan's halt-point metadata (see `## CHECKPOINT REACHED` in the executor's return message).

## Files Created/Modified
- `src/metrics/irr.ts` - Lever A (RATE_TOLERANCE bracket-width termination) and Lever B (CashFlows typed-array container) applied to `buildCashFlows`, `npv`, `terminalFlow`, `solveIrr`; header comment extended with both levers' reasoning
- `src/sweep/sweep.worker.ts` - `scratchCashFlows` changed from `CashFlow[]` to a reusable `CashFlows` container (grown in place, not reallocated per cell)
- `tests/metrics/irr.test.ts` - all fixtures converted to `CashFlows` via a `makeCashFlows` helper; new tests for reuse/grow-in-place behavior and the display-precision-equality corpus
- `bench/sweep.bench.test.ts` - contribution-schedule arm's `sampleCount` raised 1 -> 2; info line extended with budget/ratio/disposition/ceiling fields; two new named constants documenting the ceiling and the projection derivation
- `bench/sweep-pool-profile.bench.test.ts` - adapted to the `CashFlows` shape (Rule 3 fix, not in this plan's declared scope -- see Deviations)

## Decisions Made
See `key-decisions` in the frontmatter above. In prose: both levers were applied (both preconditions measured true); the precision-proof corpus deliberately excludes an exact-zero-true-IRR pathological case (a property of bisection itself, not of this plan's changes, and a probability-zero event against real market data); `bench/sweep-pool-profile.bench.test.ts` was fixed under Rule 3 to keep the repo compiling; Task 2's sample count was raised to 2 despite projecting a modest (~9.2%) ceiling overage, disclosed rather than hidden or unilaterally reverted to 1.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adapted `bench/sweep-pool-profile.bench.test.ts` to the new `CashFlows` shape**
- **Found during:** Task 1 (typecheck after the Lever B representation change)
- **Issue:** This 07.1-01-owned diagnostic file imports `CashFlow`/`buildCashFlows`/`npv`/`solveIrr` and constructs cash flows as `CashFlow[]`; it is not in this plan's declared `files_modified`, but Lever B's representation change broke its typecheck.
- **Fix:** Updated its `CashFlow` import to `CashFlows`, changed `buildFlowsFor`'s return type, and changed two `.length` reads to `.count`. No behavior or figure this file reports changed.
- **Files modified:** `bench/sweep-pool-profile.bench.test.ts`
- **Verification:** `npm run typecheck` exits 0; `npm run bench` re-ran this file's own arms and their `npvEvaluationsPerSolve`/`cashFlowCount` figures still print correctly (confirmed 36.17-36.37, consistent with Lever A's effect).
- **Committed in:** `9bcdd1a` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 3, blocking). Plus two disclosed findings that are not "auto-fixes" but are recorded here and in `.planning/WINDOWS.md` because they bear directly on Task 3's decision:

1. **[Finding] Measured reduction (1.83x) fell short of the profile's projected reduction (3.14x).** Both are real: the iteration-count reduction is confirmed near-exact (105.01 -> ~36), but overhead outside the `npv` loop (cash-flow construction, `runBacktest`, pool dispatch) doesn't shrink with it, diluting the wall-clock effect. This directly informs Task 3: the branch remains far over the 1000ms budget even after both levers.
2. **[Finding] Task 2's `sampleCount=2` projects ~9.2% over its own stated bench-runtime ceiling**, per this task's own local-derived projection. N=2 was kept (satisfying the acceptance criteria) with the overage fully disclosed in the arm's own info line and `.planning/WINDOWS.md` entry 7, rather than silently reverted to N=1 (which would satisfy the ceiling but fail the acceptance criteria) or silently accepted as if it cleanly passed. Plan 07.1-06's real CI run is what settles this for the actual D-17 baseline.

**Impact on plan:** The Rule 3 fix was necessary for the repo to typecheck and is behavior-neutral. The two disclosed findings are exactly the kind of measurement-driven correction this plan was written to surface -- they change what Task 3 has to decide, not this plan's own completeness.

## Issues Encountered
- `tests/app/static-build.test.ts` and `tests/app/offline.browser.test.ts` fail in this sandbox with "`dist` does not exist -- run `npm run build` first," confirmed via `git stash`-equivalent (temporary revert/restore of only this plan's files, never `git stash` itself per this project's prohibition) to be a pre-existing, environment-only condition unrelated to this plan's changes -- this sandbox had no prior production build. Running `npm run build` (gitignored `dist/` output) resolved it for local bench/verification purposes; not committed, not part of this plan's scope.
- Two of my own precision-proof test fixtures had arithmetic errors on first write (a `buildCashFlows` reuse test expecting the wrong flow count, and an exact-zero-true-IRR test case that is architecturally sign-ambiguous under any bisection tolerance). Both fixed before committing; see the corrected test file.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

**BLOCKED on Task 3's checkpoint decision.** This plan halts here (`status: halted`) rather than completing. The executor is not auto-resumable for this checkpoint (`workflow._auto_chain_active` and `workflow.auto_advance` both confirmed `false` this session) -- a human must select one of Task 3's three options (`close-it`, `escalate`, `adopt-newton`) before this plan (and the phase's dependent plan 07.1-06, which writes the resulting PROJECT.md Key Decision) can proceed. See `## CHECKPOINT REACHED` in the executor's return message for the full decision context, including the projected D-17 baseline normalized figure and its band.

---
*Phase: 07.1-sweep-pool-tuning-inserted*
*Completed: 2026-08-23 (halted at Task 3 checkpoint)*

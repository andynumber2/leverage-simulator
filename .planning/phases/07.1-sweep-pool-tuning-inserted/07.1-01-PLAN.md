---
phase: 07.1-sweep-pool-tuning-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/sweep-pool-profile.bench.test.ts
  - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md
autonomous: false
requirements: [PERF-03]
assumption_delta_checkpoint: "Not applicable. The detector fired on a single `pluralization` signal matching the word `second` inside the phrase `a second time`; no core assumption loses a monopoly in this phase, so no identity-model question is raised."

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A real GitHub Actions `ubuntu-latest` CI run exists for branch `gsd/phase-07.1-sweep-pool-tuning`, its `bench-results` artifact carries `environment.hardwareConcurrency === 4`, and its PERF-03 row carries `source: production` with a verdict other than `unmeasured`."
    - "The artifact's `infoLines` carry an attributed breakdown of where the baseline sweep spends wall clock: pool construction, cold-versus-warm first sweep, per-cell kernel time measured with zero pool involvement, and the residual that is dispatch, transfer and idle."
    - "The same attribution exists for the D-24 `solveIrr` contribution-schedule branch, including its measured cash-flow count per cell and its measured npv-evaluations-per-solve ratio."
    - "`07.1-PERF-03-PROFILE.md` states, per branch, which lever the measured split points at and what margin that lever can reach, replacing PROJECT.md's recorded inference with a measurement."
    - "A per-bench-file cost table for the D-17 baseline host is on the record, so criterion 5's runtime reduction is aimed rather than guessed."
  artifacts:
    - bench/sweep-pool-profile.bench.test.ts
    - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md
  key_links:
    - "`commands.recordInfoLine` -> `bench/accumulator-store.ts` -> `bench/global-setup.ts` teardown -> `.bench/bench-results.json` `infoLines` -> `actions/upload-artifact@v4` (`if: always()`) -> `gh run download`. This is the only channel that survives the browser-to-Node module boundary and a red job."
    - "`bench/global-setup.ts` writes the JSON artifact BEFORE calling `assertRunInvariants`, so a verdict-fail or a runtime-cap breach still publishes a complete measurement."
    - "`computeChunkMetrics` is exported, synchronous and Comlink-free, so a zero-pool-overhead per-chunk kernel figure needs no Worker."
  prohibitions:
    - statement: "No budget value, threshold, anchor or run-level cap moves in response to any figure this plan records. PERF-03.thresholdMs stays 1000 and BENCH_TOTAL_RUNTIME_CAP_MS stays 30000."
      status: enforced
      verification: "git diff --exit-code -- perf-budgets.ts"
    - statement: "No calibration constant moves. NOMINAL_REFERENCE_MS stays 40."
      status: enforced
      verification: "git diff --exit-code -- bench/calibration.ts"
    - statement: "The default grid stays 200 x 50. D-03's coarser default grid is not spent by this plan."
      status: enforced
      verification: "git diff --exit-code -- src/sweep/sweep-grid.ts"
    - statement: "The gated PERF-03 measurement path is not instrumented. No timing call this plan adds may run inside `bench/sweep.bench.test.ts`'s `measureMinOfN` closure, and `bench/sweep.bench.test.ts` is not modified by this plan at all."
      status: enforced
      verification: "git diff --exit-code -- bench/sweep.bench.test.ts"
    - statement: "The new diagnostic file records no `MeasurementRow`. It never calls `commands.recordMeasurement`."
      status: enforced
      verification: "grep -c 'recordMeasurement' bench/sweep-pool-profile.bench.test.ts returns 0"
    - statement: "No local dev-sandbox figure is presented as a D-17 baseline figure. Every figure in the profile document states its own `hardwareConcurrency` (PERF-11)."
      status: enforced
      verification: "Every figure line in 07.1-PERF-03-PROFILE.md carries a hardwareConcurrency value; the sandbox figures are labelled informational and non-baseline."
---

<objective>
Prove, end to end, that a change on branch `gsd/phase-07.1-sweep-pool-tuning` produces a real
`hardwareConcurrency=4`, `source=production` D-17 baseline verdict that can be read back, and land
ROADMAP success criterion 1's attributed breakdown through that same loop.

07-PERF-03-BASELINE.md section 1 records the reason this comes first: the predecessor branch never
rendered a verdict at all, not because of a harness defect, but because the branch had no upstream
and no PR, so `.github/workflows/ci.yml`'s `pull_request` trigger never fired. **A plan that cannot
be measured cannot close this phase.** Every later plan in this phase reports against a figure that
only this loop can produce.

The second half of the objective is criterion 1 itself. PROJECT.md's "PERF-03 D-17 baseline
escalation (Phase 7)" row states outright that the shared-worker-pool root cause for the two
overruns is "an inference from the pool configuration, not a profiled finding". 07.1-RESEARCH.md's
assumption A1 restates that as a hypothesis, not a conclusion, and says so explicitly. This plan
replaces both with measured figures.

**The margin this phase targets, stated once here and inherited by every later plan:** the headline
row's goal is `normalizedMs <= 870` on the D-17 baseline, not `<= 1000`. WINDOWS.md entry 2 puts
roughly plus or minus 13% (2 standard deviations) on any single normalized figure, from a 6.36%
relative residual over 13 recorded D-17 runs. A figure that lands at 995ms has not closed this
phase in any durable sense: the next run could read 1124ms on identical code. 870ms is the point at
which a 2-sigma excursion still lands inside budget. A figure between 870 and 1000 is a pass on the
letter of criterion 2 but a thin one, and this phase treats it as requiring an explicit,
disclosed Key Decision rather than a quiet close.

Purpose: turn an inference into a measurement, and turn an unmeasurable branch into a measurable one.
Output: `bench/sweep-pool-profile.bench.test.ts`, `07.1-PERF-03-PROFILE.md`, and a named CI run id
carrying a real baseline verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-RESEARCH.md
@.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PATTERNS.md
@.planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md
</context>

<!-- planner-discipline-allow: recordMeasurement -->

<tasks>

<task type="tracer">
  <name>Task 1: One measured D-17 baseline verdict, read back from CI, carrying the attributed breakdown</name>
  <precondition>The commits this task produces must reach branch `gsd/phase-07.1-sweep-pool-tuning` before CI can build them. If `git rev-parse --abbrev-ref HEAD` reports a `worktree-agent-*` branch, this task cannot push or open the PR itself: halt and report that the orchestrator must merge this plan's commits onto `gsd/phase-07.1-sweep-pool-tuning`, push it to `origin`, and open a pull request, exactly as 07-PERF-03-BASELINE.md section 2 records for PR #7. Do not attempt to push a `worktree-agent-*` branch.</precondition>
  <files>bench/sweep-pool-profile.bench.test.ts</files>
  <read_first>
    - bench/playwright-context-probe.bench.test.ts (the info-line-only bench file analog: header convention, calibration and environment boilerplate, `recordInfoLine` shape, and the explicit absence of `recordMeasurement`)
    - bench/sweep.bench.test.ts (the pool and bundle construction in `beforeAll`, `buildEarlyEntryDates`, `baseParams`, `makeMeta`, `BASELINE_WORKER_COUNT` usage; this file is read for its shapes and is NOT modified)
    - src/sweep/sweep.worker.ts (`computeChunkMetrics`'s exported signature and its `SweepChunkRequest` input shape)
    - src/sweep/sweep-pool.ts (`createSweepPool`, `SweepRunRequest`, `CHUNKS_PER_WORKER`, `workerCountForCores`)
    - src/metrics/irr.ts (the exported `buildCashFlows`, `npv` and `solveIrr` this task times directly)
    - bench/calibration.ts (`normalize`, `REPEAT_COUNT`, `MIN_MEASUREMENT_MS` and why the floor applies to `measureMinOfN` only)
    - bench/global-setup.ts (the artifact is written before `assertRunInvariants` throws, which is why a red job still publishes a complete measurement)
    - .github/workflows/ci.yml (the `on:` block, confirming `pull_request` is the only trigger available to this branch, and the `if: always()` artifact upload)
  </read_first>
  <action>
Create `bench/sweep-pool-profile.bench.test.ts`, a diagnostic-only bench file that records via
`commands.recordInfoLine` and never calls `commands.recordMeasurement`. Follow
`bench/playwright-context-probe.bench.test.ts`'s header convention: state what mechanism this file
pins, and state that it is not a throwaway. Stamp a full environment block via
`resolveRunCalibration` plus `commands.recordEnvironment(captureEnvironment(score))`, per D-18,
even though the file records no duration row.

Construct the file's own `bundle`, entry-date axis and pool in its own `beforeAll`. Do not import
module-level state from `bench/sweep.bench.test.ts`; each bench file is its own module. Reuse
`bench/sweep.bench.test.ts`'s own entry-date construction logic (extended tier, `resolveEntryDateBounds`,
`indexOfDate`, `fromDaysSinceEpoch`, first N real trading sessions from the extended tier's earliest
date) so this file's columns are the same shape as the gated file's, and pin the pool width to
`BASELINE_WORKER_COUNT` imported from `bench/sweep-pool.ts`.

Size the arms so this file's standing cost stays small against `BENCH_TOTAL_RUNTIME_CAP_MS`.
Declare two named constants with doc comments explaining the choice: `PROFILE_COLS = 24` for the
zero-contribution (CAGR) arms and the pool-level IRR arm, and `IRR_PROFILE_COLS = 8` for the
serial IRR arm. Rows stay the full `SWEEP_ROWS` on every arm, so no arm is a narrower slice of the
leverage axis than the real grid. Every recorded figure states its own column count and its
implied cell count, so nothing in this file can be read as a full-grid figure.

Measure and record, for the zero-contribution branch (`baseParams()` with `contributionAmount: 0`):

  - `poolConstructionMs`: a `performance.now()` delta across a fresh `createSweepPool({ workerCount: BASELINE_WORKER_COUNT })`.
  - `coldSweepMs`: the first `runSweep` against that fresh pool. This carries each worker's module
    evaluation and its one-time `loadBundleFromFetch` decode.
  - `warmSweepMs`: a second `runSweep` at a later generation against the same pool, with no other
    change. `coldSweepMs - warmSweepMs` is the worker-startup figure criterion 1 asks for.
  - `serialKernelMs`: a direct, synchronous call to `computeChunkMetrics(bundle, request)` on the
    calling thread, with `columnIndices`/`entryDates` covering all `PROFILE_COLS` columns and
    `rowIndices` covering every row. This is the pure per-cell kernel cost with zero pool, zero
    dispatch and zero transfer.
  - Derived and recorded alongside, each labelled as derived rather than measured:
    `idealParallelMs = serialKernelMs / pool.workerCount`,
    `poolOverheadMs = warmSweepMs - idealParallelMs` (the dispatch, transfer and idle residual),
    `perCellKernelUs = serialKernelMs * 1000 / (PROFILE_COLS * SWEEP_ROWS)`, and
    `extrapolatedFullGridSerialMs = perCellKernelUs * SWEEP_COLS * SWEEP_ROWS / 1000`.

Measure and record, for the D-24 `solveIrr` contribution-schedule branch
(`baseParams({ contributionAmount: 250, contributionFrequency: 'monthly' })`), the same
`warmSweepMs` at `PROFILE_COLS` and the same `serialKernelMs` at `IRR_PROFILE_COLS`, plus the two
figures 07.1-RESEARCH.md's assumption A2 needs tested rather than assumed:

  - `cashFlowCount`: the cash-flow count for a representative leverage row, resolved through
    `resolveColumnSeries` and `runBacktest` exactly the way `computeChunkMetrics` resolves it,
    measured for BOTH the first and the last column of the profile axis and reported as two figures
    plus their ratio. Two columns rather than one because 07.1-RESEARCH.md's Open Question 3 asks
    specifically whether per-cell `solveIrr` cost is uniform across the tested axis or varies near
    calendar-gap edges, and a single column cannot answer that. Record the entry date and leverage
    each figure came from.
  - `npvEvaluationsPerSolve`: measured, not counted from source. Time a batch of `npv(rate, flows)`
    calls against those same flows to get `npvMs`, time a batch of `solveIrr(flows)` calls to get
    `solveIrrMs`, and record `solveIrrMs / npvMs` as the implied evaluation count. Use a batch size
    large enough that each timed total clears 10ms, and record the batch size beside the ratio.
    This needs no change to `src/metrics/irr.ts`: `npv`, `solveIrr` and `buildCashFlows` are all
    already exported.

Every figure goes out through `commands.recordInfoLine` with a distinct id per line, formatted the
way `bench/sweep.bench.test.ts`'s own PERF-03 info line is: `key=value` pairs on one line, always
including `hardwareConcurrency` and `workerCount`, and always including both the raw
`performance.now()` figure and the `normalize(raw, score)` figure so the numbers are comparable
with the gated row's `normalizedMs`.

Run `npm run bench` locally and confirm the new info lines land in `.bench/bench-results.json`'s
`infoLines`. Local figures are informational and non-baseline (this sandbox reports
`hardwareConcurrency=9`); they prove the mechanism, not the number. Expect the local run to exit
non-zero: the PERF-03 verdict is failing and the suite is over its runtime cap. That is the
starting state this phase exists to change, not a defect in this file.

Commit, then get the change onto `gsd/phase-07.1-sweep-pool-tuning` and open a pull request against
`main` so `.github/workflows/ci.yml`'s `pull_request` trigger fires. Wait for the `CI` workflow run
to complete, note its run id, and download its artifact with
`gh run download <run-id> -R andynumber2/leverage-simulator --name bench-results`.

**The CI job is expected to be red.** It will fail at `npm run bench` on the PERF-03 budget breach
and, behind that, on the total-runtime cap. `actions/upload-artifact@v4` carries `if: always()` and
`bench/global-setup.ts` writes the JSON before `assertRunInvariants` throws, so the measurement is
complete and recoverable regardless. This task's acceptance is on the artifact's contents and on a
named run id, never on a green job.

Record the run id in the commit message so it is recoverable later.
  </action>
  <acceptance_criteria>
    - `bench/sweep-pool-profile.bench.test.ts` exists and `grep -c 'recordMeasurement' bench/sweep-pool-profile.bench.test.ts` returns 0.
    - `npm run typecheck` exits 0.
    - A local `npm run bench` writes `.bench/bench-results.json` whose `infoLines` array contains at least one line matching `sweep-pool-profile` and at least one line containing `serialKernelMs`, verified with `node -e "const j=require('./.bench/bench-results.json'); const s=j.infoLines.join('\n'); if(!/sweep-pool-profile/.test(s)||!/serialKernelMs/.test(s)) process.exit(1)"`.
    - A named `CI` workflow run id exists for `headBranch` `gsd/phase-07.1-sweep-pool-tuning`, confirmed with `gh run view <run-id> -R andynumber2/leverage-simulator --json headBranch,headSha,conclusion`.
    - The downloaded `bench-results.json` from that run reports `environment.hardwareConcurrency === 4`, verified with `node -e "const j=require('./bench-results.json'); if(j.environment.hardwareConcurrency!==4) process.exit(1)"`.
    - The same artifact's PERF-03 row carries `source: "production"` and a `verdict` that is not `"unmeasured"`, verified with `node -e "const j=require('./bench-results.json'); const r=j.rows.find(x=>x.budgetId==='PERF-03'); if(!r||r.source!=='production'||r.verdict==='unmeasured') process.exit(1)"`.
    - The same artifact's `infoLines` contain the profile lines for BOTH branches, verified with `node -e "const j=require('./bench-results.json'); const s=j.infoLines.join('\n'); if(!/serialKernelMs/.test(s)||!/npvEvaluationsPerSolve/.test(s)||!/cashFlowCount/.test(s)) process.exit(1)"`.
    - The recorded `cashFlowCount` figures cover the first and the last column of the profile axis and are reported with their ratio, so 07.1-RESEARCH.md's Open Question 3 (whether per-cell `solveIrr` cost is uniform across the tested axis) has a measured answer.
    - `git diff --exit-code -- perf-budgets.ts bench/calibration.ts bench/sweep.bench.test.ts src/sweep/sweep-grid.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run bench -- --reporter=verbose; node -e "const j=require('./.bench/bench-results.json'); const s=j.infoLines.join('\n'); if(!/sweep-pool-profile/.test(s)||!/serialKernelMs/.test(s)||!/npvEvaluationsPerSolve/.test(s)) { console.error('profile info lines missing'); process.exit(1) } console.log('profile info lines present')"</automated>
  </verify>
  <done>A named CI run id on `gsd/phase-07.1-sweep-pool-tuning` has published a `bench-results` artifact whose environment block reads `hardwareConcurrency: 4`, whose PERF-03 row reads `source: production` with a rendered verdict, and whose `infoLines` carry the attributed breakdown for both the zero-contribution and the `solveIrr` branch.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe the baseline attribution and state what it aims at</name>
  <files>.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md</files>
  <read_first>
    - The `bench-results.json` downloaded in Task 1 (the authoritative figures; transcribe verbatim, do not restate from memory)
    - .planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md (the transcription convention this document follows: verbatim JSON blocks, named run ids, explicit hardwareConcurrency on every figure, informational figures labelled as such)
    - .planning/WINDOWS.md (entry 2: the plus-or-minus 13% band any single normalized figure carries)
    - .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-RESEARCH.md (assumptions A1, A2 and A4, which this document either confirms or falsifies with measured figures)
  </read_first>
  <action>
Create `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md`.

Section 1, "The baseline run": the CI run id, `headBranch`, `headSha`, `conclusion`, and the
environment block transcribed verbatim from the downloaded artifact. State plainly that the job
was red and why that does not weaken the measurement.

Section 2, "Attributed breakdown, zero-contribution branch": a table of the measured and derived
figures Task 1 recorded, each labelled measured or derived, each carrying its column count and
implied cell count. Add a row stating what fraction of `warmSweepMs` is `idealParallelMs` and what
fraction is `poolOverheadMs`. This is the row that settles criterion 1: it is the split PROJECT.md
currently records as an inference.

Section 3, "Attributed breakdown, `solveIrr` branch": the same table for the contribution-schedule
branch, plus `cashFlowCount` and `npvEvaluationsPerSolve` with the entry date, leverage and batch
sizes those figures came from.

Section 4, "Per-file bench cost on the baseline host": retrieve the CI job's own stdout with
`gh run view <run-id> -R andynumber2/leverage-simulator --log`, extract the per-file lines the
Vitest reporter prints for the `bench` project (each names a `bench/*.bench.test.ts` path and its
duration), and transcribe them into a table sorted by cost descending, with the run's own
`totalRuntimeMs` beside `BENCH_TOTAL_RUNTIME_CAP_MS`. Note explicitly which files are gated
`MeasurementRow` recorders and which are informational-only arms, because the informational arms
are the only legitimate reduction candidates (the precedent is 07-PERF-03-BASELINE.md section 1's
`REJECTED_POLYGON_BATCH_SIZE` cut, which left every gated measurement byte-for-byte unchanged).
If the reporter's per-file lines are not present in the log, say so plainly and record the
per-test durations that are present instead; do not invent an attribution the log does not carry.

Section 5, "What the profile says, and what it aims at". For each of the two branches separately:
state the measured split, name the single lever the split points at, and state the margin that
lever can reach against the 870ms target this phase declared. Confirm or falsify, by name, each of
07.1-RESEARCH.md's assumptions A1 (headline is kernel-bound rather than pool-overhead-bound), A2
(the `solveIrr` branch's cost is dominated by npv evaluations times cash flows), and A4 (a fourth
worker is worth roughly 25%), and state the measured answer to its Open Question 3 (whether the
`solveIrr` branch's per-cell cost is uniform across the tested entry-date axis). Where a figure falsifies an assumption, say so rather than restating
the assumption. Where the measurement cannot settle a question, say that too.

Section 6, "Levers this profile rules out". Name, with the measured reason, anything the profile
shows is not worth spending: transfer discipline (already zero-copy in both directions),
per-column resolution (already once per column), and chunk-shape rebalancing on the bench's own
near-uniform entry-date axis. Note that the production live sweep's axis
(`resolveSweepEntryDates` in `src/app/state.ts`, 200 positions spread across the whole history) is
genuinely non-uniform and is a different axis from the one PERF-03 gates, so a partition-shape
judgement made against one axis does not transfer to the other.

Every figure in this document states its own `hardwareConcurrency`. No dev-sandbox figure is
presented as a baseline figure (PERF-11).
  </action>
  <acceptance_criteria>
    - `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md` exists and contains sections numbered 1 through 6 with the headings named above.
    - The document names a CI run id and a `headSha`, and contains a verbatim environment block reporting `hardwareConcurrency` 4.
    - The document contains an explicit numeric split of `warmSweepMs` into an ideal-parallel component and a pool-overhead residual, for both the zero-contribution and the `solveIrr` branch.
    - The document contains `cashFlowCount` figures for the first and last column of the profile axis with their ratio, and an `npvEvaluationsPerSolve` figure with the batch sizes it was measured at.
    - The document contains a per-file bench cost table for the baseline host, or an explicit statement that the CI log did not carry per-file lines together with whatever attribution the log did carry.
    - The document names, for each branch, one lever and the margin it can reach against a 870ms target.
    - The document states a disposition for each of assumptions A1, A2 and A4 by name.
    - `grep -c 'hardwareConcurrency' .planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md` returns at least 4.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-PROFILE.md';const t=fs.readFileSync(p,'utf8');const need=['hardwareConcurrency','npvEvaluationsPerSolve','cashFlowCount','poolOverheadMs','A1','A2','A4'];const miss=need.filter(k=>!t.includes(k));if(miss.length){console.error('missing: '+miss.join(', '));process.exit(1)}console.log('profile document complete')"</automated>
  </verify>
  <done>`07.1-PERF-03-PROFILE.md` records the D-17 baseline attribution for both branches, the per-file bench cost table, a named lever and reachable margin per branch, and an explicit disposition for research assumptions A1, A2 and A4. ROADMAP success criterion 1 is met by this document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread to sweep Worker | `SweepChunkRequest` crosses `postMessage`. Fully typed, internal, never user-supplied. |
| bench browser context to Node host | `commands.recordInfoLine` / `recordEnvironment` cross the Vitest custom-command bridge into `.bench/.raw/` on local disk. |
| CI job to uploaded artifact | Read-only publication of `.bench/bench-results.json`. Workflow token is `contents: read`; no secrets are referenced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-01 | Denial of Service | `src/sweep/sweep-pool.ts` `drainQueue`, chunk timeout path | medium | mitigate | A failed or hung chunk degrades to `CELL_FLAG_INCOMPLETE` through `watchWorkerFailure` plus `chunkTimeoutMs`, routed through the same generation-gated `mergeChunkResult`. This plan adds no code to that path and changes neither worker count nor chunk shape, so the mitigation is untouched. Verified by `git diff --exit-code -- src/sweep/sweep-pool.ts`. |
| T-07.1-02 | Tampering | `perf-budgets.ts`, `bench/calibration.ts` | high | mitigate | PERF-01a's no-relaxation rule. Every task in this plan carries `git diff --exit-code` over both files in its acceptance criteria. |
| T-07.1-03 | Spoofing | a figure from an off-baseline host presented as the D-17 baseline | high | mitigate | `hostMatchesPerf03Baseline` withholds the PERF-03 verdict off the declared width, and this plan's own acceptance criteria assert `environment.hardwareConcurrency === 4` on the artifact before any figure is transcribed. Every recorded line carries its own `hardwareConcurrency`. |
| T-07.1-04 | Information Disclosure | `recordInfoLine` diagnostic channel | low | accept | Writes to local stdout and the CI artifact only, never to an external origin. APP-03's no-runtime-external-calls constraint is unaffected: the emitted app bundle is not touched by this plan. |

No package-manager install task exists in this plan or anywhere in this phase
(07.1-RESEARCH.md's Package Legitimacy Audit: "Not applicable"), so no supply-chain threat row and
no package-legitimacy checkpoint applies.
</threat_model>

<artifacts_this_phase_produces>
Created by this plan:

| Symbol | Kind | Location |
|--------|------|----------|
| `bench/sweep-pool-profile.bench.test.ts` | new file, diagnostic-only bench arm | `bench/` |
| `PROFILE_COLS` | new constant (24) | `bench/sweep-pool-profile.bench.test.ts` |
| `IRR_PROFILE_COLS` | new constant (8) | `bench/sweep-pool-profile.bench.test.ts` |
| `sweep-pool-profile` info-line id family | new `recordInfoLine` ids | `.bench/bench-results.json` `infoLines` |
| `07.1-PERF-03-PROFILE.md` | new document | `.planning/phases/07.1-sweep-pool-tuning-inserted/` |

No new function, constant, CLI flag or file is added to `src/`.
</artifacts_this_phase_produces>

<verification>
- `npm run typecheck` exits 0.
- `npm run test` exits 0 (this plan changes no production code, so the unit suite must be unmoved).
- A named CI run on `gsd/phase-07.1-sweep-pool-tuning` published a `bench-results` artifact with `environment.hardwareConcurrency === 4`, a PERF-03 row at `source: production` with a rendered verdict, and profile info lines for both branches.
- `git diff --exit-code -- perf-budgets.ts bench/calibration.ts src/sweep/sweep-grid.ts bench/sweep.bench.test.ts` exits 0.
</verification>

<success_criteria>
ROADMAP success criterion 1 is met: where the 4-core baseline sweep spends its wall clock is a
measured breakdown covering worker startup, chunk dispatch and transfer, per-cell kernel time and
idle time at `workerCount=3`, recorded in a committed document, for both the zero-contribution and
the `solveIrr` branch. The measurement loop that produces a readable D-17 baseline verdict for this
branch is proven to work.
</success_criteria>

<output>
Create `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-01-SUMMARY.md` when done.
</output>


---
phase: 05
slug: attribution-and-the-credibility-surface
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-20
---

# Phase 05 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

This phase extends `.planning/phases/04-first-defensible-backtest-in-the-browser/04-UI-SPEC.md`
(APPROVED 2026-08-19). It does not restate or re-decide anything Phase 4 already settled — it
declares only what Phase 5's new surfaces (attribution panel, ghost curve, provenance strip, tier
selector + extended-tier warning, validation section, methodology overlay, generalized
default/reset) add on top of the existing, unchanged tokens. Where a value below matches Phase 4
exactly, that is deliberate: one design system, one phase 4 palette, extended not replaced.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (unchanged from Phase 4) |
| Preset | not applicable |
| Component library | none — hand-rolled Solid.js components, same as Phase 4 |
| Icon library | none — hand-authored inline SVG. Phase 5 adds exactly one new glyph: a warning triangle for `ExtendedTierWarning`, drawn in the same hand-authored inline-SVG style as Phase 4's log/linear toggle, theme override and copy-link icons (no icon font, no icon package) |
| Font | unchanged: system-ui sans stack for UI text, `ui-monospace` stack for all numerics |

**Why no shadcn (carried forward):** the project is Solid.js, not React; shadcn's registry, CLI
and component output target React/Radix and have no official Solid.js support. Nothing about
Phase 5's new surfaces changes this — the methodology overlay, provenance strip and attribution
panel are DOM chrome of the same kind Phase 4 already built by hand.

---

## Spacing Scale

Declared values (must be multiples of 4) — **identical to Phase 4, no new tokens**:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, gap between a label and its value |
| sm | 8px | Compact element spacing — between a control and its citation, between metric/attribution rows, between provenance-strip fields |
| md | 16px | Default element spacing — between distinct controls, chart-to-metrics gap, chart-to-attribution gap |
| lg | 24px | Section padding — inside a column's outer padding, inside the methodology overlay's content column |
| xl | 32px | Layout gaps — between the parameter column and result column at desktop widths |
| 2xl | 48px | **Newly used in this phase**: between distinct sections of the methodology overlay (cost model / day-count / sources / limitations), and between the two-column run layout and the standalone validation section below it |
| 3xl | 64px | Not used in this phase |

Exceptions (carried forward, plus one addition):
- Icon-only touch targets remain 44×44px minimum hit area — this now also covers the tier
  selector's toggle control and the methodology overlay's close button.
- The parameter column's dense "instrument-panel voice" (xs/sm within a group, md/lg between
  groups, never xl+) is unchanged and now also governs the tier control and the generalized
  default/reset affordance added to every control.
- The provenance strip and attribution panel live inside the D-20 screenshot region and follow
  the same dense discipline as the parameter column (xs/sm internally) — they are informational
  chrome, not a second focal area, and must not visually compete with the chart for space.

---

## Typography

**Identical size set to Phase 4 — no new sizes added.** Four distinct sizes remain 12, 14, 16,
28px; two weights remain 400 (regular) and 600 (semibold).

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Body / prose | 14px | 400 | 1.5 | Unchanged. Also used for methodology-page narrative prose and validation-section body text |
| Label / inline annotation | 12px | 400 | 1.4 | Unchanged. Also the size for provenance-strip fields, attribution row labels, the tier control's meaning-on-screen copy, and the default/reset badge |
| Heading / section title | 14px | 600 | 1.2 | Unchanged. Also used for the attribution panel's own heading, the validation section's heading, and every methodology-overlay section heading (cost model, day-count conventions, sources, limitations) |
| Metric value (secondary) | 16px | 600 | 1.2 | Unchanged. Also used for each attribution component's dollar figure and for the tracking-error/return-drift figures in the validation section |
| Metric value (headline) | 28px | 600 | 1.15 | Unchanged reservation on the single-run screen (the one IRR slot). **New, non-conflicting reuse**: the methodology overlay's own page title ("Methodology") renders at this size — in the UI sans stack, not monospace, since it is a title, not a numeric value. This does not violate Phase 4's "no other element on this screen uses that size" rule because the overlay is a distinct full-screen surface (D-17) that never renders simultaneously with the single-run screen |

- All attribution dollar/percent figures, provenance-strip dates/versions, and validation figures
  render in the monospace numeric stack, matching D-18's existing rule that every number on
  screen is monospace.
- The methodology overlay's narrative prose (the "why" connecting the generated figures, per
  D-18) renders in the UI sans stack at Body size — the page must read as receipts with
  commentary, not as a wall of numbers.

---

## Color

**Identical palette to Phase 4 for every existing role — same hex values, light and dark.** One
new semantic role is added, scoped tightly to a single use.

| Role | Light | Dark | Usage |
|------|-------|------|-------|
| Dominant (60%) | `#F5F6F7` | `#14161A` | Page background — unchanged |
| Secondary (30%) | `#FFFFFF` / border `#E1E4E8` | `#1D2024` / border `#2B2F35` | Parameter column, result column, metric/attribution panel surfaces, provenance strip surface, validation-section card, methodology overlay surface — unchanged |
| Text primary | `#1A1D21` | `#E7E9EC` | Body text, labels, metric values, attribution component values — unchanged |
| Text muted | `#5B6169` | `#9AA0A8` | Inline citations, secondary/qualified metric text, provenance-strip field text, attribution row labels, the ghost-curve legend entry — unchanged |
| Accent (10%) | `#2E6BD6` | `#5B9BF0` | Equity-curve stroke, active log/linear toggle, focus outlines, Copy Link button — **unchanged set, no additions**. The "View methodology" link and the tier control's active state are explicitly NOT accent-colored (see reservation note below) |
| Destructive / ruin | `#C4341F` | `#E2604B` | Ruin banner/marker, invalid-control borders, validation-error text — **unchanged set, no additions**. Attribution's cost components are explicitly NOT destructive-colored even when they represent a cost (see D-04 note below) |
| **Warning (new, this phase)** | `#9A5B00` | `#D99A3E` | **Reserved for exactly one thing: the CRED-02 extended-tier bias banner** (its icon, border and heading text) |

**Accent reserved for (unchanged from Phase 4):** the equity-curve stroke, the active log-scale
toggle state, keyboard-focus outlines, and the Copy Link button. Nothing new in Phase 5 is
accent-colored — the chart remains the interface's only saturated element, per Phase 4's own
stated rule that this is what keeps it the visual center of an otherwise muted layout.

**Destructive reserved for (unchanged from Phase 4):** the ruin-state banner/marker and
validation-error/invalid-control copy. **Attribution's signed components (D-04) deliberately do
not use this color for cost components**, even though a positive `financing cost` row is, in
plain language, a cost. Reusing destructive color there would visually conflate "a mechanism
that consumed money" with "your input is invalid" or "your position is ruined" — three distinct
categories of information Phase 4 built destructive specifically to keep separate. Attribution
rows render in **text-primary** color regardless of sign, distinguished only by their `+`/`-`
prefix and their explanatory suffix text (D-04: "compounding helped" appears only when the sign
is a gain) — never by hue. This is a deliberate extension of D-04's own instruction not to give
one mechanism two names: it must not get two colors either.

**Warning reserved for:** the `ExtendedTierWarning` banner only (its background tint, icon
stroke and heading). Not reused for anything else in this phase — not the naive ghost curve, not
attribution's cost rows, not validation-section sub-window rows that "look worst" (D-12 explicitly
wants those read as ordinary data, not flagged as alarming — see Copywriting Contract). Warning is
introduced as its own role rather than reusing Destructive because CRED-02's bias disclosure is a
*caveat about interpreting a result correctly*, not an error or a ruined outcome — conflating the
two would blunt Destructive's existing, carefully-scoped meaning (Phase 4: "so it reads as a
categorically different outcome from the continuous chart color scale"). Chosen as an amber/ochre
distinct from both Accent's blue and Destructive's red-orange in both themes, so all three remain
visually distinguishable to a reader with the common forms of color-vision deficiency (a full
colorblind-safe check against the chart's continuous scale is Phase 6's VIZ-07 concern, not this
phase's — this is a solid banner fill/text pair, not a data-encoding scale).

**Ghost naive-curve stroke:** uses **text-muted**, not a new color — `#5B6169` (light) /
`#9AA0A8` (dark), dashed (4px dash, 3px gap), 1.5px stroke weight, no fill, rendered *behind* the
real (accent-stroked) series in z-order. This keeps the interface's only saturated stroke on the
real result, per D-07's requirement that the ghost curve be "unmistakably subordinate" — subordinate
by both color (muted vs. accent) and dash pattern (dashed vs. solid), not by opacity alone (a
purely opacity-based treatment risks becoming illegible in the dense 25,000-bar populated state
noted in Phase 4's E6).

---

## Copywriting Contract

Additive to Phase 4's table — Phase 4's existing rows (Primary CTA "Copy link", loading state,
single-field/cross-field error copy, ruin banner, IRR/CAGR notes, bundle-mismatch banner) are
unchanged and not repeated here.

| Element | Copy |
|---------|------|
| Secondary action — methodology | "View methodology" — a persistent text link (Label size, text-muted, underline on hover/focus) placed inside the provenance strip, reachable from any result in one click (CRED-04), opening the D-17 full-screen overlay. Never accent-colored (accent stays reserved for Copy Link). |
| Secondary action — reset a parameter to default | "Reset" — a small text button (Label size) appearing beside a control's citation only when that control is **not** currently at its default (D-22). Same word for every control, no per-control variants, so a screenshot of any parameter column reads consistently. |
| Default-value badge | "default" — appended inline after a control's value/citation whenever it is at its shipped default (extends Phase 4's D-18 pattern from 2 parameters to all 8 named in D-22: leverage, entry date, holding mode, initial investment, contribution amount, contribution frequency, tier, dividend mode). |
| Attribution panel heading | "Where the money went" — Heading style, directly under the metrics panel, always visible (D-08). |
| Attribution row — naive baseline | "Naive result (leverage × return, no costs)" |
| Attribution row — actual result | "Actual result" |
| Attribution row — volatility drag (cost case) | "Volatility drag: {signedCurrency} ({signedPercent} of gap)" |
| Attribution row — volatility drag (gain case, D-04) | "Volatility drag: {signedCurrency} ({signedPercent} of gap) — compounding helped" — the literal D-04 pattern; the suffix appears **only** when the row's sign represents a gain, never reworded, never a differently-named row. |
| Attribution row — financing cost | "Financing cost: {signedCurrency} ({signedPercent} of gap)" — same signed-suffix rule as volatility drag applies if financing is ever a net gain under some parameter combination; the copy pattern is identical across all three rows, no row gets special-cased wording. |
| Attribution row — expense ratio | "Expense ratio: {signedCurrency} ({signedPercent} of gap)" |
| Attribution reconciliation row | "Total: {sum of the three % shares}" — always rendered, always reads 100% by construction (Shapley's efficiency property, D-01/D-03), regardless of any individual row's sign or magnitude exceeding 100%. This row is what makes ATTR-02's "reconciles without an unexplained residual" self-evident on screen rather than merely asserted. |
| Zero-cost comparison mode label | "Naive (no cost) comparison" — labels the permanent ghost series in the chart legend (D-07; never a toggle label, since the mode is always on). |
| Ghost-curve legend entry | "Naive: leverage × return, no costs (dashed)" — Label size, text-muted, positioned beside the existing series legend so a reader knows what the dashed line means without hovering. |
| Provenance strip — tier field | "Tier: {Strict / Extended}" |
| Provenance strip — date range field | "Data: {firstDate}–{lastDate}" |
| Provenance strip — sources field | "Sources: {source name}, {source name}, …" (names link out; see Registry/CRED note below — no licence text ever renders here, D-15) |
| Provenance strip — seams-crossed field (D-14) | "{N} seam{s} in this run: {seam summary}" when the active window crosses at least one seam; omitted entirely when it crosses none — never "0 seams," which would add noise with no information. |
| Provenance strip — seam detail affordance | "Show all seams for {symbol}" — one click away, per D-14, revealing the series' full seam list beyond the ones the current window crosses. |
| Provenance strip — bundle version field | "Bundle v{version}" — unchanged from Phase 4's existing bundle-version display, now folded into the same strip rather than a separate line. |
| Tier control label | "History tier" |
| Tier control option — strict | "Strict — every input genuinely daily" |
| Tier control option — extended | "Extended — deep history, monthly rate/dividend data interpolated to daily" — states the tier's meaning inline, per APP-02, not in a tooltip. |
| Extended-tier warning heading | "Extended tier understates volatility drag" — states the direction of the bias in the heading itself, not buried in body text (CRED-02: "prominent," "names the direction"). |
| Extended-tier warning body | "Interpolating monthly data to daily smooths out day-to-day volatility, so leverage looks better in this era than it actually was. Measured effect: {CRED-03 magnitude}/yr of understated drag, from downsampling a known-good daily era to monthly and re-measuring the gap." — the {CRED-03 magnitude} placeholder is D-21's committed, generated figure; the sentence never renders without it. |
| Validation section heading | "Does the model match reality?" |
| Validation section subheading | "Synthetic 3x {index} vs. real {fund}" |
| Validation fund selector label | "Compare against" with options "UPRO (3x S&P 500)" / "TQQQ (3x Nasdaq-100)". |
| Validation tracking-error figure | "Tracking error: {value}" — headline figure inside the validation section, Metric-secondary style. |
| Validation return-drift figure | "Return drift: {value}" — same style, adjacent. |
| Validation sub-window table heading | "By rate regime" — introduces the full table including the post-2022 high-rate rows (D-12); no separate heading or framing singles out the unflattering rows, which are presented as ordinary data, not confessions. |
| Validation cost-parameter note | "Expense ratio and financing spread above use your current values from the parameter column — edit them there to see the effect on tracking error." (D-10's live pass-through, stated once, plainly, where a reader will connect the two columns.) |
| Methodology overlay page title | "Methodology" |
| Methodology section headings | "Cost model", "Day-count conventions", "Data sources", "Known limitations" — fixed, in this order, generated from the registries named in D-18. |
| Methodology limitation — extended tier (D-19a) | "The extended tier's interpolated monthly data understates volatility drag, making leverage look better than it was in the pre-1954 era it exists to reach." |
| Methodology limitation — financing spread (D-19b) | "The financing spread is an assumed value, not a cited one — five retrieval attempts, including two full fund filings, found no fund that itemizes its swap financing spread." |
| Methodology limitation — gate tolerance (D-19c) | "The validation tolerance is weaker than its headline number: roughly {3.2–3.5%} of the {3.955%} tolerance is bid/ask noise from using market closing prices rather than fund NAV, and TQQQ's margin against the tolerance is a thin {~11%}." |
| Methodology limitation — high-rate drift (D-19d) | "Since 2022, the model tracks slightly worse in the high-rate regime — a {+0.94%/+1.12%} drift that hints the financing spread may be a little under-priced. Left alone deliberately: this project does not adjust a cost parameter to close a measured gap." |
| Methodology overlay close affordance | "Close" — a 44×44px icon-only button (× glyph), top-right of the overlay, returns to the exact run underneath (D-17: the run is untouched by opening or closing). |
| Destructive confirmation | None in this phase (unchanged from Phase 4) — resetting a single parameter to its default is not destructive: it is fully reversible by editing the field again, and D-22 explicitly rejected a global reset-everything control specifically because *that* would be destructive enough to warrant confirmation. A per-field reset needs none. |

---

## UI Considerations

State coverage for Phase 5's new surfaces, produced by the shape-rooted UI-consideration probe
(`ui-consideration-probe.cjs`: empty / loading / error / populated / partial / overflow /
zero-one-many / long-text). Element kinds below are the confirmed classification (heuristic
detection reviewed and corrected per element), and the categories raised are exactly those the
taxonomy's relevance filter admits for those kinds. Phase 4's own ten elements (E1-E10) are
unchanged and not repeated. Empty-state and error-state **copy** lives in `## Copywriting Contract`
above; this section covers state *coverage* and references that copy rather than restating it.

**Coverage: 55 applicable, 55 resolved (47 explicit, 8 backstop), 0 unresolved.**

### F1 — AttributionPanel

_Kinds: list-collection, static-content_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Never empty when a result exists: the rows (naive, actual, three components, reconciliation) always compute together from the same result, so attribution has no independent empty state. When D-11 clears the result column entirely, the attribution panel disappears with it, same rule as MetricsPanel. |
| loading | explicit | No independent loading state: attribution's extra kernel-scale evaluations ride inside the same coalesced rAF pass as the main result (PERF-07b), so the panel updates synchronously with the chart and metrics and never shows a stale value while the rest of the screen has updated. |
| error | explicit | If a counterfactual arm cannot produce a finite value (a pathological parameter combination), the affected row prints "n/a" (the existing format.ts undefined placeholder) rather than a stale or zero figure, and the reconciliation row is suppressed for that run rather than shown falsely balancing. |
| populated | explicit | Fixed five-row layout: naive, actual, three signed components, reconciliation. Row identity and order never vary with the result, matching MetricsPanel's fixed-row discipline. |
| partial | explicit | With contributions on (D-05) the naive/actual rows still render as single figures (the per-cash-flow sum), not one row per contribution: the panel never grows with the number of contributions in the schedule. |
| overflow | 🧪 backstop | The longest signed-component string ("Financing cost: -$1,234,567 (140.00% of gap) - compounding helped" in an extreme case) must wrap under its label rather than overflow the fixed-width result column at the narrowest supported viewport. |
| zero-one-many | explicit | The row count is fixed at five and never varies with data volume, so there is no singular/plural copy or spacing variance to design for. Row labels are fixed strings; the only variable content is the numeric interpolation inside each row. |
| long-text | 🧪 backstop | The D-04 gain-case suffix ("- compounding helped") is the one variable-length addition to an otherwise fixed-format row; verify it wraps onto its own line rather than forcing horizontal scroll on narrow viewports. |

### F2 — NaiveGhostSeries (EquityCurveChart extension)

_Kinds: list-collection, media_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Inherits E6's existing rule: a run with zero plottable bars never reaches the chart at all (D-11 clear-and-explain), so the ghost series has no independent empty case. |
| loading | explicit | No independent loading state: the ghost series is computed in the same coalesced rAF pass as the real series and handed to uPlot in the same setData call, so the chart never renders the real curve with a stale or absent ghost beside it. |
| error | explicit | F-02's negative-value case on a log axis: the ghost series uses null at every bar where the naive value is <= 0 (the existing buildTerminatorData null-gap idiom, reused for a different fill pattern), producing a visible gap in the dashed line rather than a dropped series, a clipped render or a renderer crash. The real (accent) series is unaffected and continues to render normally through the same window. |
| populated | explicit | Typical volume matches the real series: ~25,000 daily bars, dashed muted stroke behind the solid accent stroke, always on (never behind a toggle, D-07). |
| partial | explicit | A partially-defined ghost series (some bars null per the error row above) renders as visible dashed gaps with the surrounding segments intact: uPlot must not interpolate a straight line across the gap, and the legend entry stays present rather than disappearing for the partial run. |
| overflow | 🧪 backstop | Two concerns a rendered pixel must answer: (a) at ~25,000 bars compressed into the chart width, the 4px/3px dash pattern must stay visually distinguishable from the solid accent stroke rather than degrading into a near-solid line; (b) the legend now carries two entries instead of one, which must not crowd the log/linear toggle or the theme control at the narrowest supported viewport (both already 44x44px touch targets that must not shrink). |
| zero-one-many | explicit | The series count is fixed at design time (real, ghost, plus the conditional ruin terminator) and never varies with data, so legend layout and its copy are authored once rather than pluralized. There is no zero-series or many-series case. |

### F3 — ProvenanceStrip

_Kinds: form, list-collection, static-content_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Never empty while a bundle is loaded: tier, date range and bundle version are always known the moment a result exists. The seams-crossed field is the one genuinely optional field, omitted rather than blanked when the window crosses zero seams. |
| loading | explicit | Hidden during cold load, same as the rest of the result column's LoadingNotice state (E6/E9): the strip has nothing true to say about a bundle that has not decoded yet. |
| error | explicit | If the manifest fails to decode, the whole result column already routes to Phase 4's load-failure state (E1); the strip does not attempt a partial render from a partially-loaded manifest. |
| populated | explicit | A single dense horizontal row of 4-5 fields (tier, date range, sources, seams-crossed when present, bundle version), Label size, monospace for dates/versions, sans for field names, matching the existing .result-summary-header visual weight extended with two new fields rather than replaced. |
| partial | explicit | Every field D-16's test requires traces to a manifest field; there is no "some fields present, others missing" partial state by design. A missing manifest field is a build-time data bug caught by D-16's test, never a rendering decision. |
| overflow | 🧪 backstop | The sources field (multiple names, each a link) is the field most likely to overflow a narrow viewport; verify it wraps to a second line within the strip rather than pushing the strip's height unpredictably or clipping a source name. |
| zero-one-many | explicit | Two variable-length fields, both with authored zero/one/many copy. Sources: zero is impossible (a bundled series always cites at least one source, enforced by D-16's manifest test), one renders bare, many render comma-separated. Seams: zero omits the field entirely rather than rendering "0 seams", one renders singular "1 seam in this run", many render the plural form (Copywriting Contract). |
| long-text | explicit | The seams-crossed summary is bounded by D-14's own scope rule (only seams the active window crosses, not every seam for the series), so its length is bounded by how many seams a single run window can realistically cross rather than by the series' total seam count. The full list lives behind the "Show all seams" affordance. |

### F4 — TierControl

_Kinds: form, interactive-control, list-collection_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Never empty: tier defaults to Strict (D-23's landing-run rule pins the default landing run to strict regardless of what a user later selects) and always holds one of exactly two values. |
| loading | explicit | Disabled with the rest of the parameter column until the manifest decodes: its options and their meaning-on-screen copy come from Manifest.series[].tiers, unavailable before decode. |
| error | explicit | Selecting Extended for a symbol whose manifest carries no extended-tier range (tiers.extended === null) disables that option with the reason stated inline (matching E1's existing disabled-with-reason pattern for a symbol missing a series), rather than allowing a selection that resolves to nothing. |
| populated | explicit | Exactly two radio-style options, both always visible with their meaning stated inline (APP-02), never a dropdown that hides the second option's text until opened. |
| partial | explicit | No partially-entered state exists: a radio selection is either Strict or Extended and applies immediately. Unlike the numeric controls (E2-E5) there is no unparseable intermediate value to guard against, so the control never suppresses recompute waiting for an edit to resolve. |
| overflow | 🧪 backstop | The Extended option's meaning-on-screen copy ("Extended - deep history, monthly rate/dividend data interpolated to daily") is the longest label in the parameter column, and APP-02 forbids relocating it to a tooltip; verify it wraps within the column at the narrowest supported viewport without pushing the column wider or clipping. |
| zero-one-many | explicit | Always exactly two options, never a variable count. A symbol lacking an extended range still renders both options with Extended disabled and its reason stated, rather than collapsing to a single-option control that would read as no choice at all. |
| long-text | explicit | Both option labels are fixed strings authored in the Copywriting Contract; no variable-length or data-derived content renders in this control. |

### F5 — ExtendedTierWarning

_Kinds: media, interactive-control, static-content_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Absent by default (Strict tier, the landing state): no placeholder and no reserved empty slot, matching Phase 4's E9 rule that conditional chrome has no residual footprint on the happy path. |
| loading | explicit | Never renders during load: the banner is conditional on a decoded manifest's active tier, and the result column shows LoadingNotice until decode completes. It appears in the same frame as the first extended-tier result, never before, so a reader never sees a bias warning attached to nothing. |
| error | explicit | This element is itself a standing disclosure, not an error state: it renders identically on every Extended-tier result (D-20: no gate, every extended result carries the warning) and is never conditionally suppressed by a dismiss/acknowledge gesture, explicitly rejected by D-20 since acknowledgment state is not in the URL. |
| populated | explicit | Single fixed composition at every volume: warning triangle icon, heading, one body paragraph carrying the interpolated D-21 magnitude figure. The banner looks identical on every extended-tier run apart from that one numeric interpolation, so there is no typical-volume variance to design for. |
| overflow | 🧪 backstop | The CRED-03 magnitude sentence is the longest single sentence introduced by this phase; verify it wraps within the warning banner without truncation at the narrowest supported viewport, and that the banner does not push the chart out of the D-20 screenshot region (E9's existing overflow concern, now with one more contributor). |
| long-text | explicit | Body copy is a fixed template with one bounded numeric interpolation (the D-21 magnitude), not open-ended; length is known and bounded at design time. |

### F6 — ValidationSection (fund selector, comparison, sub-window table)

_Kinds: form, list-collection, interactive-control, media_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Never empty: the section renders with a fixed default fund (UPRO) the moment the bundle is loaded, independent of whatever run is currently on screen (D-09). There is no "select a fund to begin" empty state. |
| loading | explicit | Section shows the same LoadingNotice treatment as the main result until the bundle decodes; its own computation (computeTrackingError over the pinned canonical config) has no additional network dependency once the bundle is resident. |
| error | explicit | If the resolved overlap window between synthetic and real is too short to compute a meaningful figure for some future symbol pairing, the section states that explicitly ("insufficient overlap to compare") rather than rendering a misleadingly small-sample tracking-error number. |
| populated | explicit | Fixed structure: fund selector (2 options), headline tracking-error and return-drift figures, then the full rate-regime sub-window table (D-12) including the post-2022 high-rate rows, rendered with no visual distinction singling them out as worse (D-12: they read as ordinary rows in an ordinary table). |
| partial | explicit | Switching the fund selector recomputes and replaces the whole section's figures together (window, tracking error, drift, sub-window table): never a state where some figures reflect UPRO and others still reflect TQQQ. |
| overflow | 🧪 backstop | The sub-window table is the widest tabular content in the app so far (regime label, tracking error, return drift, per fund); verify it degrades to a stacked or horizontally-scrollable layout rather than clipping columns at the narrowest supported viewport. |
| zero-one-many | explicit | The fund selector is a fixed 2-item set (UPRO, TQQQ) and the rate-regime table's row set is fixed by the regime registry, so neither is a variable-length list and no plural-copy or spacing variance arises. |
| long-text | explicit | All table cells are bounded-format numbers (percentages, dates) routed through format.ts; no open-ended text renders in this table. |

### F7 — MethodologyOverlay

_Kinds: nav, interactive-control, static-content_

| Category | Verification | Resolution |
|----------|--------------|------------|
| loading | explicit | Every value the overlay renders (cost parameters, day-count constants, manifest sources/seams, D-21's generated bias figure) is already resident in memory or a build-time constant by the time the overlay can be opened (it requires an existing result to link from), so there is no independent loading state for the overlay itself. |
| error | explicit | Opening ?methodology=1 alongside a run that fails permalink decode (Pitfall 2, RESEARCH.md) must not evict the overlay along with the run: the overlay flag is read and stripped from the URL before decodeParams runs, so a malformed run still shows its D-11 error state with the methodology link functional beside it. |
| overflow | explicit | Full-screen overlay with its own internal scroll (the underlying single-run screen does not scroll behind it); each of the four sections is a normal scrolling block with no fixed-height clipping. |
| long-text | explicit | Section content is generated from registries with bounded per-entry text (a citation string, a confidence tier, a numeric constant); only the four limitation paragraphs (D-19) are free-standing prose, and each is a fixed pre-written sentence in the Copywriting Contract, not user- or data-length-variable. |

### F8 — DefaultBadge + ResetButton (generalized across every parameter control)

_Kinds: form, interactive-control, static-content_

| Category | Verification | Resolution |
|----------|--------------|------------|
| empty | explicit | Every parameter named in D-22 always has a value (never a genuinely empty field, the existing rule from Phase 4's E1-E5 now applied uniformly), so the affordance always resolves to either the "default" badge or a Reset button, never a third "no value" state. |
| loading | explicit | Disabled with the rest of the parameter column until the manifest decodes, same as every other control: one rule stated once for all eight controls rather than per-control. |
| error | explicit | Reset cannot itself fail: it writes the shipped default constant through the control's existing validated setter, the same path an edit-to-default takes, so it can never produce an invalid value. When a control is currently in an invalid state, Reset stays enabled and clears it, acting as the recovery affordance rather than another way to trigger an error. |
| partial | explicit | A control mid-edit (not yet a valid parseable value) does not yet know whether it will land on-default or off-default; the badge/reset affordance updates only once the edit resolves to a valid value, matching each control's existing partial-input rule (E2-E5) of not recomputing on unparseable input. |
| overflow | 🧪 backstop | Adding a Reset affordance beside six more controls than Phase 4 shipped increases the parameter column's vertical density; verify at the narrowest supported viewport that no control's label, value and reset affordance collide or wrap unpredictably against each other. |
| long-text | explicit | The badge and reset copy are two fixed short strings ("default", "Reset") reused identically across all eight controls: no per-control text-length variance to account for. |

Rows marked 🧪 backstop are, as in Phase 4, questions only a rendered pixel can answer (wrapping,
collision, dash-pattern legibility, density at extremes). Each lifts into the plan's must-have truths
and is discharged by a held-out visual UI-state check during execution, not asserted in prose: at
verify time a backstop row with no wired evidence routes to `insufficient_spec → human_needed`
rather than passing silently.

---

## Layout & Component Reference

Supplementary to the template's required sections, extending Phase 4's D-17/D-21 layout exactly
as this phase's decisions require. Not subject to checker sign-off (that covers the six dimensions
above); still binding.

### Visual hierarchy (extends Phase 4's, does not replace it)

Phase 4 declared the equity-curve chart the sole focal point, with everything else subordinate.
Phase 5 adds new elements without disturbing that ranking:

1. Equity-curve chart, now including the ghost naive series — still the only accent-saturated
   stroke; the ghost series is muted/dashed specifically so it does not compete with rank 1.
2. IRR headline metric (unchanged, 28px monospace).
3. Ruin banner when present (unchanged — outranks the headline only in the ruin state).
4. **Extended-tier warning when present** — ranks here, below ruin (a categorical, absorbing
   outcome) but above ordinary metrics, because CRED-02 demands prominence. It is the only new
   element in this phase permitted to use a color other than muted/text-primary for this reason.
5. Remaining metrics and the attribution panel, together — attribution is new but reads at the
   same visual weight as MetricsPanel, directly beneath it, never styled to compete with the
   chart or the headline metric.
6. Provenance strip — dense, muted, informational, at the same recessive weight as the parameter
   column.
7. Validation section and methodology overlay — reachable, not omnipresent; neither renders
   inside the primary screenshot region's visual weight class (see Screen structure below).

No element in this phase may be styled to outrank the chart or the IRR headline. The one
exception already stated (rank 4) is scoped to the extended-tier warning's own banner only.

### Screen structure

```
Desktop (>= ~900px), single-run screen — same two-column shell as Phase 4:
┌─────────────────────┬──────────────────────────────────────────┐
│ Parameter column     │ Result column (D-20 screenshot region)    │
│ (unchanged controls, │ ┌────────────────────────────────────┐   │
│  now each carrying   │ │ Provenance strip (D-13) --          │   │
│  a default badge or  │ │   tier / dates / sources / seams /  │   │
│  Reset affordance,    │ │   bundle version -- + "View          │   │
│  D-22)                │ │   methodology" link                  │   │
│                       │ ├────────────────────────────────────┤   │
│ + Tier control (F4,   │ │ Extended-tier warning (D-20),        │   │
│   APP-02)             │ │   shown only when tier = extended     │   │
│                       │ ├────────────────────────────────────┤   │
│ [existing symbol,     │ │ Equity curve: real (accent, solid)  │   │
│  leverage, entry      │ │   + naive ghost (muted, dashed)     │   │
│  date, holding,       │ ├────────────────────────────────────┤   │
│  contributions,       │ │ Metrics panel (unchanged)            │   │
│  costs controls]      │ ├────────────────────────────────────┤   │
│                       │ │ Attribution panel (D-08) --          │   │
│ [Copy link]           │ │   naive/actual + 3 signed rows +    │   │
│                       │ │   reconciliation                     │   │
│                       │ │ (Ruin banner, when present, sits     │   │
│                       │ │  above the metrics panel, unchanged  │   │
│                       │ │  from Phase 4)                       │   │
│                       │ └────────────────────────────────────┘   │
└─────────────────────┴──────────────────────────────────────────┘

Below the two-column shell, full width, its own always-reachable section (D-09):
┌───────────────────────────────────────────────────────────────┐
│ ValidationSection -- fund selector, tracking-error/drift        │
│   headline figures, rate-regime sub-window table                │
└───────────────────────────────────────────────────────────────┘

Overlay (D-17), full screen, opened by the provenance strip's "View methodology" link:
┌───────────────────────────────────────────────────────────────┐
│ MethodologyOverlay -- Cost model / Day-count conventions /       │
│   Data sources / Known limitations, generated from registries    │
└───────────────────────────────────────────────────────────────┘

Narrow viewport: the two-column shell stacks to one column exactly as Phase 4 specified
(parameter group above result group); the validation section stacks below both, full width,
unchanged in field order; the methodology overlay is already full-screen at every width.
```

**Why the validation section sits below the screenshot region, not inside it:** D-09 pins it to
its own canonical parameters, independent of whatever run is currently on screen — it is not a
property of the current run the way the chart, metrics and attribution are, so bundling it into
the D-20 screenshot rectangle would misrepresent it as part of the pasted result. It stays "one
click / one scroll away," per D-09's own framing, always reachable, never buried behind a
disclosure.

### Component inventory (Solid.js, hand-rolled) — additions to Phase 4's list

- `ParameterColumn` (existing container, extended)
  - `TierControl` — new, radio-style, meaning-on-screen copy inline (APP-02)
  - Every existing control (`SymbolControl`, `LeverageControl`, `EntryDateControl`,
    `HoldingModeControl`, `ContributionControl`, `CostControls`) gains the generalized
    `DefaultBadge` / `ResetButton` pair (D-22), extending `SourceCitation`'s existing
    default/citation `<Show>` pattern rather than introducing a second mechanism.
- `ResultColumn` (existing D-21 slot, extended)
  - `ProvenanceStrip` — new, extends/replaces `ResultSummaryHeader`'s existing symbol/date/
    bundle-version fields with sources and seams-crossed (CRED-01), plus the "View methodology"
    link.
  - `ExtendedTierWarning` — new, conditional on `tier === 'extended'` (CRED-02/CRED-03).
  - `EquityCurveChart` (existing, extended) — adds the naive ghost series as a second uPlot
    series with a `null`-gap fill pattern (ATTR-03, F-02).
  - `AttributionPanel` — new, directly under `MetricsPanel` (ATTR-01/02, D-08).
- `ValidationSection` — new, top-level, not nested inside `ResultColumn`'s screenshot region
  (VALID-04, D-09): `FundSelector`, `TrackingErrorSummary`, `SubWindowTable`.
- `MethodologyOverlay` — new, top-level, rendered conditionally on the stripped `methodology` URL
  flag (CRED-04, D-17/D-18): four fixed sections generated from `COST_PARAMETERS`,
  `TOLERANCE_MECHANISMS`, the kernel's day-count constants, and the manifest.

### Theme mechanics

Unchanged from Phase 4 — every new surface in this phase reads the same CSS custom properties
(`--color-bg`, `--color-surface`, `--color-text`, `--color-text-muted`, `--color-accent`,
`--color-destructive`, plus border variants) and adds exactly one new pair: `--color-warning`
(light `#9A5B00`, dark `#D99A3E`), swapped by the same `data-theme` attribute. The naive ghost
series repaints explicitly on theme change, following the same rule Phase 4 established for the
real series and the ruin terminator — canvas gets no free `prefers-color-scheme` styling.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — not applicable, project does not use shadcn | not applicable |
| third-party | none | not applicable |

Unchanged from Phase 4. No component registry is in use; all Phase 5 UI is hand-rolled Solid.js +
CSS custom properties, consistent with the project's stated minimal-dependency stance.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED by gsd-ui-checker 2026-08-20 — 6/6 dimensions PASS, 0 BLOCK, 0 FLAG

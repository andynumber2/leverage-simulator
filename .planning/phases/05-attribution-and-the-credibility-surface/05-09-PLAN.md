---
phase: 05-attribution-and-the-credibility-surface
plan: 09
type: execute
wave: 8
depends_on: ["05-08"]
files_modified:
  - bench/perf-07.bench.test.ts
  - .planning/WINDOWS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [ATTR-01, ATTR-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A single-run parameter change stays inside the 16ms coalesced-recompute budget with attribution's extra evaluations live, measured on the declared baseline rather than assumed."
    - "The measurement runs through the existing PERF-07b bench path and its normalize-and-verdict pipeline, so it lands in the bench results artifact with a verdict like every other performance figure."
    - "The measured figure is recorded against the same measurement band the project already documents, so a single run is not read as a tighter headroom claim than it supports."
    - "If the measured figure crosses the escalation trigger, the escalation is recorded as a decision rather than the budget being relaxed."
    - "The consequence for the sweep phases is written down in this phase's output: attribution figures computed per heatmap cell would multiply onto the performance budget with the least headroom in the project."
  artifacts:
    - .planning/WINDOWS.md
    - .planning/PROJECT.md
  key_links:
    - "Attribution runs inside scheduleRun's existing performance.mark boundaries, which is what makes the existing PERF-07b bench test measure it without a new bench file."
    - "The escalation trigger and the no-relaxation rule are the project's existing performance protocol; this plan applies them, it does not restate or amend them."
---

<objective>
Close the phase's measured-not-assumed obligation: re-measure the coalesced recompute with
attribution live, and hand Phase 6 and 7 the consequence explicitly rather than letting them
rediscover it.

Purpose: ROADMAP criterion 5 requires attribution's extra kernel calls to be measured against the
16ms budget, not assumed. CONTEXT.md's F-04 additionally warns that the same evaluations, if ever
computed per heatmap cell, land on the budget with the least headroom in the project.
Output: a measured PERF-07b figure with attribution live, and the recorded consequence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-attribution-and-the-credibility-surface/05-CONTEXT.md
@.planning/phases/05-attribution-and-the-credibility-surface/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure PERF-07b with attribution live</name>
  <precondition>The attribution computation runs inside scheduleRun's existing recompute-start/recompute-end performance marks (plan 05-01), so the existing bench test's measurement window covers it.</precondition>
  <files>bench/perf-07.bench.test.ts</files>
  <read_first>
    - bench/perf-07.bench.test.ts (the whole file: how it drives the leverage slider, how it selects the maximum `app-recompute` duration, and how it records its info lines)
    - perf-budgets.ts (the PERF-07b entry: its budget, threshold, anchor and requirement id, and the escalation-trigger rule stated alongside)
    - bench/calibration.ts and bench/report.ts (`normalize`, the verdict pipeline, and how figures reach the bench results artifact)
    - src/app/state.ts (`scheduleRun`'s `performance.mark`/`performance.measure` calls and where the attribution computation sits relative to them)
    - .planning/WINDOWS.md entry 2 (the measurement band and why a single run supports a headroom claim only within it)
    - .planning/PROJECT.md § Key Decisions (the existing escalation rows and the no-relaxation rule)
  </read_first>
  <action>
    Run the bench project and read the PERF-07b figure with attribution live. Do not add a new bench
    file: attribution is inside `scheduleRun`'s existing mark boundaries, so the existing test's
    measurement window already covers it. First confirm that is actually true by reading
    `src/app/state.ts` and checking the attribution call sits between the `recompute-start` and
    `recompute-end` marks; if it does not, that is a defect in plan 05-01's wiring and must be fixed
    there rather than worked around with a second bench file.

    Extend `bench/perf-07.bench.test.ts`'s recorded info line for PERF-07b to state that the
    measurement includes attribution's extra evaluations, and to name the number of counterfactual
    kernel arms the measured recompute performed. Change no budget value, no threshold, no anchor
    and no normalization constant.

    Record the measured figure and its percentage of budget in the plan summary. If it crosses the
    project's escalation trigger, follow the existing protocol: record the escalation as a decision
    row in `.planning/PROJECT.md` with the measured figure and the baseline it came from. Do not
    relax the budget, do not retune the calibration reference, and do not reduce the number of
    counterfactual arms to make the figure fit.
  </action>
  <verify>
    <automated>npm run bench</automated>
  </verify>
  <acceptance_criteria>
    - `npm run bench` exits 0 with the PERF-07b entry present and carrying a verdict.
    - The PERF-07b entry in `.bench/bench-results.json` shows a normalized figure and its budget percentage.
    - `git diff perf-budgets.ts` shows no change to any budget, threshold, anchor or normalization constant.
    - `bench/perf-07.bench.test.ts`'s PERF-07b info line names that attribution is live in the measurement and states the counterfactual arm count.
    - The measured figure and its percentage of budget appear in the plan summary.
    - If the figure crosses the escalation trigger, a new decision row exists in `.planning/PROJECT.md` naming the measured figure and its baseline.
  </acceptance_criteria>
  <done>PERF-07b is measured with attribution live on the declared baseline, with no budget touched and any escalation recorded as a decision.</done>
</task>

<task type="auto">
  <name>Task 2: Hand the sweep phases the attribution cost consequence</name>
  <files>.planning/WINDOWS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/WINDOWS.md (the whole file: its entry format and the existing entry 2's measurement-band statement)
    - .planning/PROJECT.md § Key Decisions (the existing escalation rows and their format)
    - .planning/ROADMAP.md § Phase 6 and § Phase 7 (the heatmap design and implementation phases that inherit this)
    - .planning/phases/05-attribution-and-the-credibility-surface/05-CONTEXT.md § Findings F-04
    - perf-budgets.ts (the sweep budget entry and its current headroom, to state the multiplier concretely)
  </read_first>
  <action>
    Add a WINDOWS.md entry recording the attribution cost consequence for the sweep phases, in the
    file's existing entry format. It must state concretely: the measured per-recompute cost of
    attribution's counterfactual arms from Task 1; that a single-run parameter change has ample
    headroom against its 16ms budget; and that the same evaluations computed per heatmap cell would
    multiply onto the sweep budget, which already sits at the highest percentage of budget in the
    project and carries a recorded escalation. Name the multiplier explicitly as the number of
    counterfactual arms, so Phase 6 and 7 can do the arithmetic without rediscovering the finding.

    State what Phase 6 and 7 must therefore decide rather than assume: whether the heatmap needs
    per-cell attribution at all, and if it does, whether that computation belongs in the sweep
    worker pool rather than on the recompute path. Do not decide it here; this phase has no
    heatmap and no authority over its design.

    If Task 1 produced an escalation, ensure its PROJECT.md decision row cross-references this
    WINDOWS.md entry so the two are readable together.
  </action>
  <verify>
    <automated>grep -c "attribution" .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` contains a new entry naming attribution, the measured per-recompute cost, the counterfactual arm count as the multiplier, and the sweep budget it would land on.
    - The entry states the open question for Phase 6 and 7 without deciding it.
    - `grep -c "attribution" .planning/WINDOWS.md` returns at least 1.
    - No file under `src/`, `tests/` or `bench/` is modified by this task.
  </acceptance_criteria>
  <done>Phase 6 and 7 inherit the attribution cost multiplier as a written finding against the budget with the least headroom, rather than rediscovering it mid-implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measured figure -> recorded performance claim | A performance number that flatters itself through a changed budget or a reduced workload is a false claim about the product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Repudiation | PERF-07b budget or workload adjustment | high | mitigate | An acceptance criterion asserts `perf-budgets.ts` is unchanged by this plan, the action forbids retuning the calibration reference or reducing the counterfactual arm count, and any escalation is recorded as a decision row rather than absorbed. This is the project's existing performance protocol, applied. |
| T-05-25 | Repudiation | Single-run headroom overclaim | medium | mitigate | The measurement is recorded against the documented measurement band, so a single run is not read as supporting a tighter headroom claim than it does. |
| T-05-SC | Tampering | npm installs | low | accept | No package-manager install in this plan; no source file is added. |
</threat_model>

<verification>
- `npm run bench` green with PERF-07b carrying a verdict and attribution live.
- `git diff perf-budgets.ts` empty.
- `.planning/WINDOWS.md` carries the attribution cost consequence entry.
</verification>

<success_criteria>
- ROADMAP criterion 5's measurement half: attribution's extra kernel calls keep a single-run parameter change inside the 16ms budget, measured, not assumed.
- Phase 6 and 7 inherit F-04's consequence explicitly.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

- `.planning/WINDOWS.md` — new entry recording the attribution cost multiplier against the sweep budget.
- `.planning/PROJECT.md` — a new Key Decisions row only if the measurement crosses the escalation trigger.
- `bench/perf-07.bench.test.ts` — its PERF-07b info line now states that attribution is live and names the counterfactual arm count.

No new source symbols, no new files under `src/`, no new URL query params, no new CSS.

<output>
Create `.planning/phases/05-attribution-and-the-credibility-surface/05-09-SUMMARY.md` when done
</output>

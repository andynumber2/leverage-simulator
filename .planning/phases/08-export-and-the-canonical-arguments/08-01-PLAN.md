---
phase: 08-export-and-the-canonical-arguments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/app/static-build.test.ts
  - src/export/download.ts
  - src/export/png-export.ts
  - src/app/components/ResultColumn/ExportRow.tsx
  - src/app/components/ParameterColumn/ParameterColumn.tsx
  - src/app/components/ResultColumn/HoverReadout.tsx
  - src/app/App.tsx
  - src/app/styles.css
  - tests/app/export-png.browser.test.ts
autonomous: true
requirements: [SHARE-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user with a completed single-run result on screen clicks Export PNG and receives a PNG image of the whole .screenshot-region subtree on the clipboard, with a download fallback when the Clipboard API is unavailable (D-01, D-23)."
    - "The exported PNG carries every receipt the region contains: the provenance strip, the metrics panel, the attribution panel, the extended-tier warning when present, and the bundle version (D-01)."
    - "Two exports of the same run from two different viewport widths produce the same pixel dimensions, because the region is laid out at one fixed export width before rasterization (D-03)."
    - "The exported PNG has a fully opaque background matching the theme currently on screen, with a visible frame around the captured region (D-02)."
    - "The export row renders as a sibling immediately after the .screenshot-region element, never as a child of it, so no export control can appear inside an exported image (D-21)."
    - "Copy link is reachable from the export row in the result column and is no longer rendered by the parameter column (D-22)."
    - "Copy link's flushPermalinkUrl-before-read discipline survives the relocation: a Copy link issued immediately after a slider drag still yields the settled URL, not a stale one (D-22)."
    - "In sweep mode the transient hover readout is excluded from the capture while the committed crosshair stays in it (F-02)."
    - "Before any result exists, all export-row buttons render disabled with no note and no placeholder text, and the row is never hidden or collapsed, so the landing state and the result state have the same layout height (UI-SPEC E1 empty)."
    - "The export-row buttons have no separate in-flight state: they stay disabled while a run computes and enable on the completed result (UI-SPEC E1 loading)."
    - "PNG capture failure renders the shared failure copy inline beside the button in the destructive color at Label size (UI-SPEC E1 error)."
    - "The export row renders its buttons at .copy-link-button's exact visual treatment: accent text and border, transparent background, 4px border radius, 44px min-height (UI-SPEC E1 populated)."
    - "The export row always renders exactly three buttons and never conditionally removes one from the DOM, so the row's shape never changes (UI-SPEC E1 zero-one-many)."
    - statement: "At the narrowest supported viewport the three export-row buttons plus an occasional disabled-reason note wrap or stack within the result column rather than overflowing it, checked the same way 04-UI-SPEC.md E5's narrow-viewport check is."
      verification: backstop
  artifacts:
    - path: "src/export/download.ts"
      provides: "The one Blob-to-download primitive shared by the CSV export and the PNG clipboard-failure fallback"
      exports: ["triggerDownload"]
    - path: "src/export/png-export.ts"
      provides: "html-to-image call site: fixed export width, 2x pixel ratio, forced opaque background, transient-node filter"
      exports: ["exportRegionAsPng", "EXPORT_WIDTH_PX", "EXPORT_PIXEL_RATIO", "pngFilename"]
    - path: "src/app/components/ResultColumn/ExportRow.tsx"
      provides: "The D-21/D-22 export row holding Copy link, Export PNG and Export CSV"
      exports: ["ExportRow"]
    - path: "tests/app/export-png.browser.test.ts"
      provides: "Real-browser capture assertions for SHARE-04"
    - path: "tests/app/static-build.test.ts"
      provides: "F-01's inverted guard: capture code exists and is scoped to src/export/"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/app/components/ResultColumn/ExportRow.tsx"
      via: "rendered as a sibling immediately after the .screenshot-region div closes, never inside it"
      pattern: "<ExportRow"
    - from: "src/app/components/ResultColumn/ExportRow.tsx"
      to: "src/export/png-export.ts"
      via: "click handler calls exportRegionAsPng and passes the resulting promise straight into ClipboardItem"
      pattern: "exportRegionAsPng"
    - from: "src/export/png-export.ts"
      to: "src/app/styles.css"
      via: "reads the live theme background custom property off the document element for D-02's forced opaque background"
      pattern: "getPropertyValue"
    - from: "src/app/components/ResultColumn/ExportRow.tsx"
      to: "src/app/components/ParameterColumn/CopyLinkButton.tsx"
      via: "composes the existing component unchanged; ParameterColumn.tsx drops its own render call site"
      pattern: "CopyLinkButton"
  prohibitions:
    - "An exported artifact must not leave the app stripped of the receipts that make it checkable. A PNG that shows the chart and the headline number without the provenance strip, the bundle version, the tier and the source names is a picture that proves nothing, which is the exact failure D-01 rejected a canvas-only export to avoid."
    - "The export affordance must not acquire a promotional or celebratory voice. Phase 4 D-18 settled that this app is a measuring instrument, not an advocate; the three export controls name what they produce and nothing else."
  flagged_assumptions:
    - "EDGE PROBE, SHARE-04, category unclassified, status unresolved. The deterministic edge probe could not classify SHARE-04 into any of its eight shape categories and returned no probe question. Per the spec-less fallback rule this row is NOT auto-resolved with a backstop and is surfaced here instead. Planner reading: the requirement is a capability statement over a rendering surface, not a data-shape statement, so the shape taxonomy has nothing to bite on. The behavioural coverage SHARE-04 actually needs is carried by the explicit truths above plus 08-05's measured PERF-07a figure. The one open edge predicate a reviewer might want: what should the export do when .screenshot-region is present but zero-height (a run with no plottable bars)? No source artifact resolves it; App.tsx renders the empty-run notice instead of the region in that case, so the region is absent rather than zero-height, and the export button is already disabled with no result."
---

<objective>
Ship the PNG export path end to end and give the app the export row every later export lands in.

**User story (MVP mode):** **As a** person arguing about leveraged ETFs, **I want to** turn the
result currently on my screen into an image I can paste into a forum reply, **so that** the
argument travels with its own receipts instead of my summary of them.

Purpose: SHARE-04 and roadmap criterion 1. Phase 4's D-20 built `.screenshot-region` for exactly
this and deferred the capture code to this phase; `tests/app/static-build.test.ts` has been
guarding that deferral and must now be inverted rather than deleted (F-01).

Output: `html-to-image` as a runtime dependency, `src/export/download.ts`,
`src/export/png-export.ts`, `src/app/components/ResultColumn/ExportRow.tsx` mounted outside
`.screenshot-region`, Copy link relocated into it, and a real-browser capture test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export-and-the-canonical-arguments/08-CONTEXT.md
@.planning/phases/08-export-and-the-canonical-arguments/08-RESEARCH.md
@.planning/phases/08-export-and-the-canonical-arguments/08-PATTERNS.md
@.planning/phases/08-export-and-the-canonical-arguments/08-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New symbols and files created by THIS plan (excluded from drift verification as pre-existing):

| Kind | Symbol / path |
|---|---|
| file | `src/export/download.ts` |
| file | `src/export/png-export.ts` |
| file | `src/app/components/ResultColumn/ExportRow.tsx` |
| file | `tests/app/export-png.browser.test.ts` |
| function | `triggerDownload` |
| function | `exportRegionAsPng` |
| function | `pngFilename` |
| const | `EXPORT_WIDTH_PX` |
| const | `EXPORT_PIXEL_RATIO` |
| const | `EXPORT_FRAME_PX` |
| component | `ExportRow` |
| css class | `.export-row` |
| css class | `.export-button` |
| css class | `.export-failure-note` |
| test id | `export-row` |
| test id | `export-png-button` |
| test id | `export-csv-button` |
| data attribute | `data-export-exclude` |
| dependency | `html-to-image@1.11.13` |

Modified pre-existing symbols (NOT new artifacts):
- `tests/app/static-build.test.ts` — one test replaced with its inverted form (F-01).
- `src/app/components/ParameterColumn/ParameterColumn.tsx` — loses its `CopyLinkButton` render call site (D-22).
- `src/app/components/ResultColumn/HoverReadout.tsx` — gains one attribute on its root element (F-02).
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: CopyLinkButton -->
<!-- planner-discipline-allow: data-export-exclude -->

<task type="tracer">
  <name>Task 1: End-to-end "Export PNG" -- one path, click to clipboard</name>
  <reversibility rating="reversible">D-04: one dependency behind one export module; swapping html-to-image for a hand-rolled foreignObject pass later touches no call site outside src/export/png-export.ts.</reversibility>
  <files>package.json, package-lock.json, tests/app/static-build.test.ts, src/export/download.ts, src/export/png-export.ts, src/app/components/ResultColumn/ExportRow.tsx, src/app/App.tsx, src/app/styles.css, tests/app/export-png.browser.test.ts</files>
  <read_first>
    - `src/app/components/ParameterColumn/CopyLinkButton.tsx` — the full transient-state-machine and clipboard-failure-fallback template this row copies (`CopyState` union, `LABELS` record, `scheduleReset`, `data-copy-state` attribute, disabled condition).
    - `src/app/App.tsx` lines 140-200 — the exact `.screenshot-region` div, its comment block, and where it closes. The export row is inserted after that closing tag.
    - `tests/app/static-build.test.ts` lines 1-40 and 120-146 — the file header's stated purpose and the one test being inverted.
    - `src/app/styles.css` lines 11-45 (theme custom properties) and 667-700 (`.copy-link-row`, `.copy-link-button` and its disabled/failed variants).
    - `.planning/phases/08-export-and-the-canonical-arguments/08-RESEARCH.md` Pattern 1 (fixed-width capture), Pattern 2 (Safari-safe clipboard sequencing), Code Examples (download trigger), Pitfall 5 (how to invert the guard).
    - `tests/app/screenshot-region.browser.test.ts` — this repo's existing browser-test boilerplate for asserting on the screenshot region.
  </read_first>
  <action>
Install `html-to-image` at version `1.11.13` as a runtime dependency (D-04; cleared `OK` by 08-RESEARCH.md's Package Legitimacy Audit, so no legitimacy checkpoint is owed). Run `npm install html-to-image@1.11.13` so `package-lock.json` updates in the same commit.

Invert F-01's guard in `tests/app/static-build.test.ts` rather than deleting it. Replace the single test currently named for the Phase 8 deferral with one whose name states the new invariant, asserting four things: (a) over every `.ts`/`.tsx` file under `src/`, any file containing a `toDataURL` call or a canvas `toBlob` call sits under `src/export/`; (b) over the same tree, any file importing the rasterization package sits under `src/export/`; (c) `package.json` `dependencies` declares that package; (d) `package.json` declares none of the three sibling rasterization packages the original ban listed by name (keep that name array in the test file, now asserted against a set that must exclude only those three). Preserve the file's existing helper (`collectFiles`) and its reasoned-allow-list discipline; do not weaken the external-URL tests.

Create `src/export/download.ts` exporting `triggerDownload(blob: Blob, filename: string): void` exactly as 08-RESEARCH.md's Code Examples section specifies: `URL.createObjectURL`, a synthetic anchor with the `download` attribute appended to `document.body`, `.click()`, removal, then `URL.revokeObjectURL`. Revoke in a `finally` so an anchor-click throw cannot leak the object URL. This is the shared primitive plan 08-02 also consumes; keep it free of any PNG-specific or CSV-specific knowledge.

Create `src/export/png-export.ts` with:
  - `EXPORT_WIDTH_PX = 1160` (D-03's 1100-1200px band, Claude's discretion) and `EXPORT_PIXEL_RATIO = 2`, both exported so tests assert against the constant rather than a repeated literal.
  - `EXPORT_FRAME_PX = 24` — D-02's visible margin, matching the existing `--space-lg` token value.
  - `exportRegionAsPng(region: HTMLElement): Promise<Blob>` calling `toBlob` from `html-to-image` with `width: EXPORT_WIDTH_PX`, `pixelRatio: EXPORT_PIXEL_RATIO`, `backgroundColor` read at call time from `getComputedStyle(document.documentElement).getPropertyValue('--color-bg')` (D-02: the theme on screen, forced opaque — the custom property is a solid hex in both `:root` and `[data-theme='dark']`), `style` carrying the frame as padding of `EXPORT_FRAME_PX` plus a `width` of `EXPORT_WIDTH_PX` so the region lays out at the export width before rasterizing rather than at the caller's viewport width, and a `filter` predicate that returns false for any element carrying the `data-export-exclude` attribute. Throw a named error when `toBlob` resolves `null`.
  - `pngFilename(): string` producing `leverage-sim-<symbol>-<leverage>x-<entryDate>.png` from `backtestRequest()`, with leverage rendered to two decimals stripped of a trailing `.00`.

Create `src/app/components/ResultColumn/ExportRow.tsx` exporting `ExportRow`. This task ships the row with the Export PNG button plus a disabled Export CSV placeholder button (so UI-SPEC E1's zero-one-many invariant of exactly three buttons holds from the first commit; plan 08-02 wires the CSV handler behind it, plan 08-01 Task 2 composes Copy link). Structure per 08-PATTERNS.md's ExportRow assignment: a wrapping `div.export-row` with `data-testid="export-row"`; each button `type="button"`, class `export-button`, its own `data-testid`, its own `data-export-state` attribute carrying that button's transient state, and `disabled={currentKernelResult() === null}`. The Export PNG handler follows 08-RESEARCH.md Pattern 2 exactly and this is load-bearing: build the blob promise WITHOUT awaiting it, then pass that promise directly as the `ClipboardItem` value. Awaiting the blob first and calling `clipboard.write` afterward works in Chromium and is a documented WebKit user-activation failure. On an absent Clipboard API or `ClipboardItem` constructor, or on a rejected write, fall back to `triggerDownload` with `pngFilename()` (D-23: a PNG's destination is almost always a forum reply or a chat, so the clipboard removes the save-then-find-then-attach round trip, and the download is the fallback when the API is unavailable or the context is insecure); only when that fallback itself throws does the button enter its failed state. Confirmed state reverts to idle after 2000ms through the same clear-then-rearm timer shape `CopyLinkButton` uses. Labels come from 08-UI-SPEC.md's Copywriting Contract verbatim: idle `Export PNG`, confirmed `Copied!`, failed `Export failed`; the inline failure note reads `Export failed - try again.` and is the one shared failure string both export paths use.

Mount `<ExportRow />` in `src/app/App.tsx` as a sibling immediately AFTER the `.screenshot-region` div closes, inside the same `Show` guard that renders the single-run result, never as a child of the region (D-21: structural, not a rasterizer filter). Add the import alongside the other `ResultColumn` imports.

Add `.export-row`, `.export-button` and `.export-failure-note` to `src/app/styles.css`. `.export-row` is a flex row with `gap: var(--space-sm)`, `flex-wrap: wrap`, `align-items: center`, `margin-top: var(--space-md)`, and no background, border or card treatment (UI-SPEC Visual Hierarchy rank 2: an action strip, not a second panel). `.export-button` duplicates `.copy-link-button`'s declarations exactly (min-width 140px, min-height 44px, `var(--space-xs) var(--space-md)` padding, `--font-ui`, `--font-size-body`, weight 600, `--color-accent` text and border, transparent background, 4px radius, pointer cursor) with the same `:disabled` and failed-state variants keyed on `data-export-state`. `.export-failure-note` is `--font-size-label` in `--color-destructive`.

Create `tests/app/export-png.browser.test.ts` in the `app` project asserting: the export row exists and is a following sibling of `[data-testid="screenshot-region"]` rather than a descendant; the Export PNG button is disabled before a result and enabled after; clicking it produces a PNG Blob whose decoded intrinsic width equals `EXPORT_WIDTH_PX * EXPORT_PIXEL_RATIO`; the captured image is not blank (decode it and assert at least two distinct pixel values); and the captured DOM clone contains the provenance strip and the metrics panel. Intercept the blob by spying on the export module's returned promise or by asserting on the download fallback path, whichever this repo's existing browser tests make cleanest; do not assert on clipboard contents, which Playwright cannot read reliably without extra permissions.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project app tests/app/export-png.browser.test.ts</automated>
    <automated>npm run build &amp;&amp; npx vitest run --project unit tests/app/static-build.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project app tests/app/export-png.browser.test.ts` exits 0 with at least 5 passing assertions.
    - `npm run build &amp;&amp; npx vitest run --project unit tests/app/static-build.test.ts` exits 0, and the test file still contains its external-URL allow-list tests unchanged (`grep -c 'ALLOWED_EXTERNAL_URLS' tests/app/static-build.test.ts` is at least 2).
    - `node -e "const p=require('./package.json');process.exit(p.dependencies['html-to-image']?0:1)"` exits 0.
    - `grep -rl 'html-to-image' src --include=*.ts --include=*.tsx` prints exactly `src/export/png-export.ts` and nothing else.
    - `grep -c 'EXPORT_WIDTH_PX = 1160' src/export/png-export.ts` is 1 and `grep -c 'EXPORT_PIXEL_RATIO = 2' src/export/png-export.ts` is 1.
    - In `src/app/App.tsx`, `<ExportRow` appears after the line where the `.screenshot-region` div closes and is not nested inside it; the browser test's following-sibling assertion is the mechanical proof.
    - The Export PNG click handler passes a promise into `ClipboardItem` rather than an awaited Blob: `grep -c 'await ' src/app/components/ResultColumn/ExportRow.tsx` may be non-zero, but the line constructing `ClipboardItem` contains no `await` (assert by reading the constructed line; the browser test additionally asserts the handler resolves without a prior blob await by measuring that the clipboard call is issued in the same task as the click).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>A user with a completed single-run result clicks Export PNG and gets a 2320px-wide PNG of the whole screenshot region on the clipboard, or downloaded when the clipboard is unavailable. The static-build guard now asserts capture code exists and is scoped to `src/export/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move Copy link into the export row and make sweep-mode capture correct</name>
  <reversibility rating="costly">D-22 relocates a shipped, verified Phase 4 control. Undoing it means moving the control back plus re-editing its Phase 4 UI-SPEC E10 placement notes and both test suites that assert on its position.</reversibility>
  <files>src/app/components/ResultColumn/ExportRow.tsx, src/app/components/ParameterColumn/ParameterColumn.tsx, src/app/components/ResultColumn/HoverReadout.tsx, src/app/App.tsx, src/app/styles.css, tests/app/export-png.browser.test.ts</files>
  <read_first>
    - `src/app/components/ParameterColumn/ParameterColumn.tsx` — its current `CopyLinkButton` import and render call site, and the surrounding control order.
    - `src/app/components/ParameterColumn/CopyLinkButton.tsx` — its full header comment, which documents the `flushPermalinkUrl()`-before-read discipline the relocation must preserve untouched.
    - `src/app/components/ResultColumn/HoverReadout.tsx` line 139 — the `.hover-readout` root element that gains the exclusion attribute.
    - `src/app/components/ResultColumn/HeatmapPanel.tsx` lines 545-620 — the sweep `.screenshot-region`: the field canvas, the crosshair overlay canvas at identical geometry, the absolutely positioned axis-tick spans, the `HoverReadout` mount point.
    - `tests/app/permalink.browser.test.ts` and `tests/app/controls.browser.test.ts` — the existing assertions on Copy link's position that this move invalidates.
    - `.planning/phases/08-export-and-the-canonical-arguments/08-CONTEXT.md` F-02 and 08-RESEARCH.md Open Question 1.
  </read_first>
  <behavior>
    - Copy link renders inside `[data-testid="export-row"]` and is absent from the parameter column's DOM.
    - Copy link issued immediately after a leverage change still yields the settled URL (the flush discipline survives the move).
    - In sweep mode the export row is a following sibling of the sweep `.screenshot-region`, not a descendant.
    - A capture taken while the hover readout is visible produces an image whose DOM clone contains no `.hover-readout` node.
    - A capture taken after a crosshair cell has been committed still contains the crosshair overlay canvas.
  </behavior>
  <action>
Compose the existing `CopyLinkButton` component inside `ExportRow.tsx` as the first of the three buttons. Do NOT duplicate its internals or rewrite it: import it from `../ParameterColumn/CopyLinkButton.tsx` and render it. Its own `flushPermalinkUrl()`-before-read call and its `.copy-link-row` wrapper stay exactly as they are; `.export-row`'s flex layout absorbs that wrapper without needing it restyled. Remove the import and the render call site from `src/app/components/ParameterColumn/ParameterColumn.tsx` (D-22: mirroring it in both places was explicitly rejected). Update `.copy-link-row`'s `margin-top` handling so the row does not double the export row's own top margin.

Update every existing test that asserts Copy link's position in the parameter column to assert its new position in the export row instead. Search for `copy-link-button` and `copy-link-row` across `tests/` and fix each assertion; do not delete a test to make it pass.

Add `data-export-exclude="true"` to `HoverReadout.tsx`'s root `.hover-readout` element (F-02, resolving 08-RESEARCH.md Open Question 1 in the direction that research recommends): the hover readout names transient pointer state, not the argument, so it is excluded from the capture. The committed crosshair overlay canvas gets NO such attribute and stays in the image, because a click-committed crosshair names the cell being argued about (Phase 7 D-22). This is the only element in the app that carries the exclusion attribute; the export controls stay out of the region structurally rather than through this filter (D-21).

Mount `<ExportRow />` in `src/app/App.tsx` for sweep mode too, as a sibling immediately after `<HeatmapPanel />` (whose own root IS the sweep `.screenshot-region`, per `HeatmapPanel.tsx` line 552), so both result modes get the same row in the same relative position.

Extend `tests/app/export-png.browser.test.ts` with the sweep-mode cases: switch to sweep mode, wait for a grid, assert the export row is a following sibling of the sweep screenshot region; hover the heatmap to raise the readout, capture, and assert the capture's node filter rejected the readout; click a cell to commit the crosshair, capture, and assert the crosshair overlay canvas survived. Add the Copy-link-after-a-parameter-change assertion that proves the flush discipline survived relocation.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project app tests/app/export-png.browser.test.ts tests/app/permalink.browser.test.ts tests/app/controls.browser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CopyLinkButton' src/app/components/ParameterColumn/ParameterColumn.tsx` is 0.
    - `grep -c 'CopyLinkButton' src/app/components/ResultColumn/ExportRow.tsx` is at least 2 (import plus render).
    - `grep -c 'data-export-exclude' src/app/components/ResultColumn/HoverReadout.tsx` is 1, and `grep -rc 'data-export-exclude' src/app/components/ResultColumn/HeatmapPanel.tsx` is 0.
    - `git diff --stat src/app/components/ParameterColumn/CopyLinkButton.tsx` shows no change to that file: the relocation composes it, it does not rewrite it.
    - `npx vitest run --project app tests/app/export-png.browser.test.ts` exits 0 including the three new sweep-mode assertions and the flush-discipline assertion.
    - `npx vitest run --project app tests/app/permalink.browser.test.ts tests/app/controls.browser.test.ts` exits 0 with their Copy-link position assertions updated rather than removed (`git diff` on those files shows modified assertions, not deleted `test(` blocks: `git diff -U0 tests/app/permalink.browser.test.ts tests/app/controls.browser.test.ts | grep -c '^-.*test('` is 0).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Copy link, Export PNG and Export CSV sit in one row in the result column in both result modes. A sweep-mode capture keeps the committed crosshair and drops the transient hover readout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove D-02's theme parity and D-03's viewport independence</name>
  <reversibility rating="costly">D-03: the export width is what people paste and compare; changing it later makes new exports non-comparable with every export already shared.</reversibility>
  <files>src/export/png-export.ts, tests/app/export-png.browser.test.ts, src/app/styles.css</files>
  <read_first>
    - `src/export/png-export.ts` — the module Task 1 created, specifically its `backgroundColor` resolution and its `style` override.
    - `src/app/theme.ts` — how `data-theme` is written and read, so the test drives a real theme switch rather than a synthetic attribute set.
    - `src/app/styles.css` lines 11-45 — the two theme blocks and their `--color-bg` values (`#f5f6f7` light, `#14161a` dark).
    - `tests/app/theme.browser.test.ts` — the existing pattern for driving a theme change in a browser test.
    - `tests/app/narrow-viewport.browser.test.ts` — the existing narrow-viewport check class 04-UI-SPEC.md E5 established, which the backstop truth in this plan's `must_haves` reuses.
    - `.planning/phases/08-export-and-the-canonical-arguments/08-RESEARCH.md` Assumption A1 (the `width`/`height` reflow caveat that must be verified empirically, not assumed).
  </read_first>
  <behavior>
    - A capture taken in light theme has a top-left frame pixel equal to the light background color; the same capture in dark theme equals the dark background color. Neither is transparent.
    - A capture taken at a 1440px viewport and a capture of the same run at an 800px viewport have identical pixel dimensions.
    - Both captures at those two viewports are pixel-identical, which is the real proof that the fixed export width reflowed the region rather than only scaling it.
    - Every captured pixel has alpha 255.
  </behavior>
  <action>
Close 08-RESEARCH.md's Assumption A1 empirically rather than by assertion. A1 flags that `html-to-image`'s `width`/`height` options do not always reflow a node's internal children; the two `.screenshot-region` trees are believed immune because their canvases size from fixed pixel constants, but 08-RESEARCH.md explicitly says to verify this against the real DOM tree rather than trust the library's stated option behaviour. The verification is the cross-viewport pixel-identity assertion below. If the two captures differ, the fix is to set an explicit `width` on the cloned region through the `style` override AND to re-run the region's own layout at that width before `toBlob` reads it, not to relax the assertion.

Extend `tests/app/export-png.browser.test.ts` with a theme-parity block and a viewport-independence block. Theme parity: capture in light theme, decode the blob to an `ImageBitmap`, read the pixel at (2, 2) — inside `EXPORT_FRAME_PX`'s margin — and assert it equals the light `--color-bg` value; assert its alpha channel is 255; toggle to dark theme via the real `ThemeToggle` control, capture again, assert the same pixel equals the dark value and alpha is still 255. Viewport independence: set the browser viewport to 1440x900, capture, record dimensions and a content hash of the decoded pixel buffer; set the viewport to 800x900 (below the 900px stacking breakpoint), capture the same run again, and assert both the dimensions and the content hash match. Use the same decode helper for both blocks; put it in the test file, not in `src/`.

If the theme-parity assertion shows the captured background is transparent rather than opaque, the cause is `backgroundColor` receiving an empty string from `getPropertyValue` (leading whitespace in the custom property value is common); trim it at the call site in `png-export.ts` and assert the trimmed value is non-empty before calling `toBlob`, throwing a named error otherwise rather than silently capturing transparent.

Add a `.export-row` narrow-viewport rule to `src/app/styles.css` only if the wrap behaviour at 800px does not already hold from `flex-wrap: wrap`; verify by measuring the row's `scrollWidth` against its `clientWidth` in the browser test rather than by eye.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project app tests/app/export-png.browser.test.ts tests/app/narrow-viewport.browser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The theme-parity test asserts the frame pixel equals `#f5f6f7` in light and `#14161a` in dark, with alpha exactly 255 in both; both assertions pass.
    - The viewport-independence test asserts identical dimensions AND an identical decoded-pixel-buffer hash across 1440px and 800px viewports; both assertions pass.
    - The export row's `scrollWidth` is less than or equal to its `clientWidth` at an 800px viewport, asserted in the browser test.
    - `npx vitest run --project app tests/app/export-png.browser.test.ts` exits 0.
    - `npx vitest run --project app tests/app/narrow-viewport.browser.test.ts` exits 0 (unchanged behaviour for every element it already covers).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The same run exported from a phone-width viewport and a wide-monitor viewport is byte-identical, and neither theme produces a transparent-background image. Assumption A1 is closed by measurement, not by assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → app state | Untrusted permalink parameters cross here at boot via `decodeParams`, then reach the rendered region this plan rasterizes and the filename this plan builds. |
| App DOM → clipboard / downloaded file | Data leaves the app's control here. The user's OS and any application they paste into is the consumer. |
| npm registry → runtime bundle | `html-to-image` is new third-party code that executes in the page with full DOM read access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `pngFilename()` in `src/export/png-export.ts` | medium | mitigate | The filename is built from `backtestRequest()` fields that arrive via `decodeParams`, which is already a total function over arbitrary `URLSearchParams` with per-field allow-list validation (`symbol` against the manifest, `entryDate` against `ISO_DATE_PATTERN`, `leverage` as a number rendered to fixed decimals). Do not interpolate any raw query value into the filename; only these already-validated fields. Strip anything outside `[A-Za-z0-9._-]` from the assembled name before it reaches the `download` attribute, so a path separator or a control character cannot reach the OS save dialog. |
| T-08-02 | Information disclosure | `exportRegionAsPng` node filter | medium | mitigate | The capture serializes a live DOM subtree. Scope it to `.screenshot-region` only — never `document.body` — so nothing outside the result region can be rasterized into an image the user then pastes publicly. The `data-export-exclude` filter subtracts from that scope; it never widens it. |
| T-08-03 | Information disclosure | `navigator.clipboard.write` | low | mitigate | `ClipboardItem` is constructed with exactly one explicit MIME key, `image/png`, matching the Blob's own type. No second clipboard format is written, so a clipboard read by another application recovers the image and nothing else. |
| T-08-04 | Denial of service | `triggerDownload` object-URL lifetime | low | mitigate | `URL.revokeObjectURL` runs in a `finally`, so a repeated export cannot accumulate live object URLs and grow page memory without bound. Asserted by exporting three times in the browser test and confirming no retained blob URLs. |
| T-08-05 | Elevation of privilege | `html-to-image` at runtime | low | accept | The package executes with the same privileges as the app itself, which is a static page with no credentials, no backend and no storage beyond the URL. Version is pinned exactly (`1.11.13`), the package cleared the legitimacy gate with no postinstall script, and `tests/app/static-build.test.ts` continues to prove the emitted build reaches no external origin. Residual risk is a future supply-chain compromise on upgrade, accepted at ASVS L1 for a credential-free static site. |
| T-08-SC | Tampering | npm install of `html-to-image` | high | mitigate | 08-RESEARCH.md's Package Legitimacy Audit returned verdict `OK` (npm registry, 6.24M weekly downloads, MIT, repo `github.com/bubkoo/html-to-image`, no postinstall script) with no `[ASSUMED]` or `[SUS]` rows, so no blocking human checkpoint is owed. Install the exact pinned version and commit the lockfile change in the same commit as the dependency, so the resolved tree is reviewable. |

Threats rated `high`: T-08-SC only, and it is dispositioned `mitigate` with the audit already
complete. No unresolved `high` threat blocks this plan.
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npm run test` (the full `unit` project) exits 0, including the inverted `static-build.test.ts`.
- `npm run test:app` exits 0, including `export-png.browser.test.ts` and every pre-existing browser test whose Copy-link assertions moved.
- `npm run build` succeeds and `dist/` still contains no non-relative script or link host.
- A manual read of the diff confirms `src/app/components/ParameterColumn/CopyLinkButton.tsx` is unchanged.
</verification>

<success_criteria>
- SHARE-04's user-facing capability exists: a completed result becomes a PNG on the clipboard in one click, in either theme, in either result mode.
- The exported image is deterministic across viewports and carries every receipt inside `.screenshot-region`.
- F-01's guard is inverted, not deleted: it now asserts capture code exists and is confined to `src/export/`.
- D-22's relocation is complete in both directions: present in the export row, absent from the parameter column, with the flush discipline intact.
</success_criteria>

<output>
Create `.planning/phases/08-export-and-the-canonical-arguments/08-01-SUMMARY.md` when done.
</output>

---
context: phase
phase: 08-export-and-the-canonical-arguments
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-08-26T04:57:14.223Z
---

# Phase 08 paused after wave 2

Waves 1 and 2 are done and merged. Waves 3 and 4 have not started. The phase tail gates have
not run.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executor self-reports the wrong worktree base | 08-02's executor returned its own HEAD (`1754000`) as `expected_base` in its `<worktree_metadata>`, not the commit it forked from (`f4c30dd`). Recording that verbatim into the wave manifest would have tripped `cleanup-wave`'s fail-closed base check and blocked an otherwise clean merge. | advisory | Before `worktree.record-agent`, verify with `git merge-base HEAD <worktree-branch>` and record the verified value, not the self-reported one. |
| Declaring green from `npm run test` alone | `npm run test` is the `unit` project and EXCLUDES `tests/app/**/*.browser.test.ts`. Every export assertion in this phase lives in a browser test, so a unit-only run reports green while proving nothing about the export paths. | advisory | Run both `npm run test` and `npm run test:app`, and state both counts separately when reporting. |
| Treating a browser-process disconnect as a test failure | This sandbox has ~1.9GB RAM. Plan 08-01 saw the browser process disconnect partway through a full `npm run test:app` run, and the same signature appeared in `slice-charts.browser.test.ts`, which that plan never touched. It is memory exhaustion, not a regression. | advisory | If it recurs, run browser test files individually or in partial batches. Note: the post-merge gate after wave 2 ran all 27 browser files in one continuous run with no disconnect, so this is intermittent rather than reliable. |

<current_state>
On branch `gsd/phase-08-export-and-the-canonical-arguments` at `e5abcf9`, working tree clean,
no executor worktrees remaining, branch unpushed.

Post-merge gate on the merged wave-2 tree, all green:
typecheck clean, build clean, 844/844 unit, 180/180 app across all 27 browser files in one run.
</current_state>

<completed_work>

- Wave 1, plan 08-01 (merged at `8172379`): PNG export path end to end plus the export row every
  later export lands in. Resumed in place from the previous session's rescue commit `d48b4f7`.
  Fixed that rescue's known UI-SPEC E1 violation by hoisting `<ExportRow />` out of the
  `loadStatus() === 'ready' && resultMode() === 'single'` guard to an unconditional sibling.
  Relocated Copy link out of ParameterColumn into the export row (D-22), excluded the transient
  hover readout from capture (F-02), and closed D-02 theme parity and D-03 viewport independence
  empirically. Also repaired three real test-infrastructure bugs the plan's own detection
  patterns had (F-01's capture-call-site detector never matched html-to-image's bare `toBlob(`).
- Wave 2, plan 08-03 (merged at `050dddd`): the preset library plus `scripts/compute-presets.ts`,
  which runs the real kernel at build time and emits `presets.generated.ts` byte-identically
  across runs, so headline figures are derived rather than hand-entered. `computeDerivedMetrics`
  exported from `state.ts` for Node callers. No deviations.
- Wave 2, plan 08-02 (merged at `b266ee1`): CSV export path with a dedicated `csv.worker.ts` so
  serialization stays off the main thread, `csv-preamble.ts` provenance, and the
  recompute-to-finalValue proof. One deviation: fixed a stale 08-01-era assertion in
  `export-png.browser.test.ts` that expected Export CSV to ship disabled, which this plan's own
  change made incorrect.
</completed_work>

<remaining_work>

- Wave 3, plan 08-04: put the canonical arguments one click from the landing state. 3 tasks,
  `autonomous: true`, requirements [SHARE-06]. Consumes 08-03's preset library.
- Wave 4, plan 08-05: measure what the two export paths and the DCA preset actually cost the main
  thread, and record it. 3 tasks, `autonomous: false`, requirements [SHARE-04, SHARE-05,
  SHARE-06]. This plan carries a human checkpoint, so wave 4 cannot run unattended.
- Phase tail gates, none of which have run: aggregate_results, code_review_gate, regression_gate,
  verify_phase_goal, update_roadmap.
</remaining_work>

<decisions_made>

- Resumed 08-01 in place rather than reverting the rescued commit `d48b4f7`. Task 1's work was
  correct apart from one precisely diagnosed E1 violation, so reverting would have cost a full
  re-run for no gain.
- Recorded 08-02's worktree `expected_base` as the git-verified fork base, not the executor's
  self-reported value. See the anti-pattern table above.
- Left SHARE-04 and SHARE-05 Pending in REQUIREMENTS.md. 08-05 claims all three SHARE
  requirements and is the phase closure plan, so marking them now would overstate completion.
  SHARE-06 was marked Complete by 08-03's executor.
</decisions_made>

<blockers>
- None for this phase.
- Pre-existing and unrelated: PERF-03 remains FAILED by decision, its lever list measured and
  exhausted. It does not gate phase 8.
</blockers>

## Required Reading (in order)

1. `.planning/phases/08-export-and-the-canonical-arguments/08-04-PLAN.md`: the next plan to execute
2. `.planning/phases/08-export-and-the-canonical-arguments/08-03-SUMMARY.md`: the preset library 08-04 consumes
3. `.planning/phases/08-export-and-the-canonical-arguments/08-CONTEXT.md`: the locked D-xx decisions both remaining plans cite
4. `.planning/phases/08-export-and-the-canonical-arguments/08-UI-SPEC.md`: the design contract whose E1 section the wave 1 defect violated
5. `.planning/STATE.md`: project position and the accumulated decision log

## Infrastructure State

- Branch: `gsd/phase-08-export-and-the-canonical-arguments` at `e5abcf9`, unpushed.
- Worktrees: none. All three wave worktrees merged and removed cleanly.
- Isolation resolves to `harness-worktree`; `workflow.use_worktrees` is `true` and
  `worktree.baseRef` is `head`, so `worktree.base-check` returns `shouldDegrade: false` and
  parallel worktree execution is available for the remaining waves.
- No background processes, no async external jobs, no `.planning/async-jobs/` directory.
- Test container has ~1.9GB RAM. See the anti-pattern table.

<context>
This session picked up phase 8 with 08-01 in a half-finished state: its Task 1 production code
had been rescued onto the branch by a previous session after a container death, but no SUMMARY.md
existed, which is exactly what the execute-phase safe-resume gate is built to catch. The gate
tripped correctly and the chosen recovery was to resume in place.

Wave 2's two plans had disjoint `files_modified` lists so they ran in parallel with no conflict,
and both merged clean with no manual resolution. The post-merge gate on the combined tree is the
real evidence that they compose, since neither executor could see the other's changes.
</context>

<next_action>
Run `/gsd-execute-phase 8`. It will find 3 of 5 plans summarized, no blocked plans, and resume at
wave 3 with 08-04. Expect wave 4 (08-05) to stop for a human checkpoint.
</next_action>

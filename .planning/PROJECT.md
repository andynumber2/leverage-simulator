# Leverage Simulator

## What This Is

A static web app that simulates daily-rebalanced leveraged exposure to a bundled set of
market indices and ETFs, with rigorous cost modelling (financing, expense ratio, volatility
drag) rather than the naive `return * L` that most online arguments assume. It exists to
answer, with receipts, the recurring internet question of whether you should hold leveraged
ETFs long-term, and specifically how sensitive the answer is to your entry point and your
leverage level.

Audience: the project owner first, and then anyone in an argument who needs a link that
settles it.

## Core Value

Given a symbol, a leverage level, an entry point, and a contribution schedule, produce a
defensible outcome and show **which mechanism** consumed the money (volatility drag vs
financing vs fees), in a form that can be pasted into an argument.

If everything else fails, the simulation math must be right and must be demonstrably right
against real leveraged ETF price history.

## Requirements

### Validated

(None yet — ship to validate)

### Active

**Simulation engine**

- [ ] Simulate daily-rebalanced leverage at an arbitrary positive multiple (1x through 20x+,
      fractional allowed), where 1x reproduces the unlevered series exactly
- [ ] Model each trading day's portfolio return as leveraged price return minus financing on the
      borrowed portion minus expense ratio, not `L*r`. Financing and fees accrue on **calendar
      days elapsed since the prior trading day**, not per trading row: a three-day weekend costs
      three days of interest. Roughly
      `r_port = L*r - (L-1)*(short_rate + spread)*days/360 - ER*days/365`,
      with the exact day-count conventions to be decided and documented
- [ ] The 1x case must reproduce the unlevered series **exactly**, as a tested invariant
- [ ] Cost parameter defaults must be sourced independently and never tuned to make the
      UPRO/TQQQ validation pass. Residual tracking error is reported honestly, not fitted away
- [ ] Support an initial investment amount plus a recurring contribution amount at daily,
      monthly, quarterly, or yearly frequency
- [ ] Support a dividend-reinvest toggle backed by two bundled series per symbol
      (total-return and price-return), so the toggle switches data rather than approximating
- [ ] Detect and flag ruin (position value reaching or crossing zero), and handle it correctly
      rather than producing negative-value nonsense downstream
- [x] Validate synthetic leverage against real leveraged ETF history: synthetic 3x S&P 500 must
      track UPRO since 2009, and synthetic 3x NDX must track TQQQ, within a documented tolerance
      (Phase 3: the tracking gate; Phase 5: the comparison surfaced in-app, not only in tests)

**Cost attribution**

- [x] For any single run, decompose the gap between naive `L*r` and the actual result into
      volatility drag, financing cost, and expense ratio, reported in both dollars and percent
      (Phase 5: Shapley decomposition, reconciling to the total gap with no residual)
- [x] Expense ratio and financing spread are user-editable, defaulting to values sourced from
      real products (roughly 0.90% ER and ~0.5% over the short rate)
      (Phase 5: every default labelled as a default and editable in place)

**Outcome metrics**

- [ ] IRR (money-weighted return), used instead of CAGR whenever contributions are non-zero
- [ ] Maximum drawdown
- [ ] Final value as a multiple of total contributed
- [ ] Ruin flag (did it ever go to zero)

**Visualization**

- [ ] Fixed leverage, sweep entry date: how outcome varies by when you started
- [ ] Fixed entry date, sweep leverage: how outcome varies by how much leverage you took
- [ ] 2D heatmap over entry date x leverage, colored by the selected outcome metric
- [ ] Sweep mode toggle: fixed holding period (every entry holds N years, apples-to-apples) vs
      hold-to-today (every entry runs to the present)
- [ ] Sweeps stay responsive; a full heatmap is on the order of 10,000 backtests and must not
      block the UI

**Data**

- [x] Bundled binary data assets, no runtime backend and no external API calls
      (Phase 2: compiled data bundle)
- [x] A CLI bundle compiler in the repo that ingests raw CSVs and emits versioned binary
      bundles plus a JSON manifest, so adding a symbol is "drop in a CSV, recompile, redeploy"
      (Phase 2: compiled data bundle)
- [x] Bundled universe: S&P 500 (daily to 1928), Nasdaq-100/QQQ, real leveraged ETFs
      (UPRO, TQQQ, SSO, QLD) for model validation, and VTI, EFA, EEM, TLT
      (Phase 2: compiled data bundle)
- [x] Bundled short-rate series (Fed funds / 3M T-bill) covering the full date range
      (Phase 2: compiled data bundle)
- [x] Two user-selectable history tiers: **strict** (every input genuinely daily) and
      **extended** (deep history, with monthly rate and dividend data interpolated to daily)
      (Phase 2: tier ranges computed from seam records)
- [x] The GUI states explicitly and prominently what each tier means, including the actual
      seam dates and data sources. Provenance is visible, never a footnote
      (Phase 5: provenance strip rendered from the manifest, plus the extended-tier bias warning
      carrying its measured magnitude)

**Sharing**

- [ ] URL permalink encoding every parameter, so a pasted link reproduces the exact run
- [ ] PNG export of the current chart
- [ ] CSV export of a run's underlying daily series, so the math can be checked independently
- [ ] Named preset scenarios for the canonical arguments (e.g. 3x S&P from 1929, TQQQ from 2000,
      the 2010s in isolation)

### Out of Scope

- **Arbitrary live ticker lookup** — bundled-only was chosen deliberately. Reproducibility
  matters more than coverage for a tool whose purpose is settling disputes; vendor data
  revisions would silently change past conclusions. The bundle compiler is the escape hatch.
- **D1 / any backend database** — the full dataset is on the order of 1MB as binary assets, so
  a query round-trip buys nothing. More importantly the sweep cannot run server-side at all:
  Cloudflare Workers have hard CPU-time limits and a heatmap is ~250M operations, so compute
  must be client-side regardless, which reduces D1 to a slower asset fetch.
- **Intraday or minute-level data** — daily rebalancing is what real leveraged ETFs do; finer
  granularity adds volume without changing any conclusion.
- **Portfolio construction, rebalancing across multiple assets, tax modelling** — single-symbol
  leverage is the argument being settled. Broadening dilutes it.
- **Trading, brokerage integration, live prices** — this is an analysis tool, not an app that
  touches money.
- **User accounts, persistence, server-side state** — permalinks carry all state.

## Context

- **Motivation**: recurring, unresolvable internet arguments about whether leveraged ETFs are
  viable long-term holds. Both sides typically argue from either a naive `return * L` model
  (overstates leverage badly) or from a cherry-picked window (usually 2010-2021).
- **Why entry point is the hard visualization**: the owner's intuition is that entry sensitivity
  is the crux and that it wants something like a pork chop plot. The 2D entry-date x leverage
  heatmap is the chosen representation, with a holding-period mode switch because "sweep the
  entry date" is ambiguous between fixed-horizon and hold-to-today, and the two produce very
  different pictures.
- **Why CAGR is avoided**: with a recurring contribution schedule, money has heterogeneous
  holding periods and there is no single meaningful annualized return. Comparing a DCA outcome
  against a lump-sum CAGR is a common source of wrong conclusions in exactly these arguments.
  IRR is used instead.
- **Data seams that constrain deep history**: daily S&P 500 price data reaches back to 1928, but
  daily total-return data effectively begins in 1988 and daily short-rate data does not exist
  before 1954 at all (FRED `DFF` starts 1954-07-01, `DTB3` starts 1954-01-04; the monthly
  `TB3MS` reaches back only to 1934-01-01). Reaching 1929 therefore *requires* interpolating
  monthly or annual rate data to daily. There is no way around it. The 1929-1932 drawdown is the
  single most persuasive data point against high leverage, so the extended tier exists to reach
  it; the strict tier exists so nobody can dismiss a result as an interpolation artifact.
- **Overlapping windows are not independent observations**: adjacent entry dates in a sweep share
  nearly all their underlying data. A 10,000-cell heatmap is a sensitivity analysis over one
  history, not 10,000 backtests. The UI must say so in visible copy, not in a methodology
  footnote, or the tool overstates its own evidence and hands the argument back.
- **Credibility anchor**: real UPRO and TQQQ price history is bundled not to simulate but to
  validate. If the model does not reproduce them, its conclusions carry no weight.
- **Deployment target**: Cloudflare Pages, static hosting only.

## Constraints

- **Tech stack**: Must deploy as static assets to Cloudflare Pages. No backend, no database,
  no runtime external API calls.
- **Performance**: Heatmap sweeps are ~10,000 backtests over ~25,000 daily bars each. Compute
  runs off the main thread (Web Worker, and possibly WASM) so the UI stays responsive.
- **Data**: Bundled and versioned at build time. Every series must carry its source and date
  range, surfaced in the UI.
- **Correctness**: The cost model is the product. Any simplification that changes a conclusion
  must be surfaced to the user, not hidden.
- **Offline**: Once loaded, the app should work without a network connection.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| App, not a spreadsheet | 10k backtests per heatmap render is far beyond spreadsheet ergonomics, and the target visualization is not expressible in one | — Pending |
| Static binary assets, not D1 | Dataset is ~1MB; sweep must run client-side due to Worker CPU limits, so a backend adds latency and moving parts for no gain | — Pending |
| Full cost model, not `return * L` | The naive model is the root cause of the arguments this tool exists to end | — Pending |
| IRR instead of CAGR | CAGR is undefined in any meaningful sense once recurring contributions are on | — Pending |
| Bundled data only, plus a compiler | Reproducibility beats coverage for a dispute-settling tool; the compiler keeps the universe extensible | — Pending |
| Both price-return and total-return series bundled | Makes the dividend toggle exact rather than a modelled approximation | — Pending |
| Two explicit history tiers | Reaching 1929 requires interpolation; a strict tier means the extended tier's methodology cannot be used to dismiss the whole tool | — Pending |
| Validate against real UPRO/TQQQ | Without it, every conclusion is an unfalsifiable claim | — Pending |
| Both sweep modes (fixed-horizon and hold-to-today) | They answer different questions and each alone is misleading | — Pending |
| Attribution breakdown as headline feature | Naming which mechanism cost the money is what actually ends an argument; a number alone does not | — Pending |
| Financing accrues on calendar days, not trading rows | Interest runs over weekends and holidays; per-trading-row `/252` systematically understates leverage cost | — Pending |
| Heatmap UI must state that windows overlap | 10,000 cells over one shared history is a sensitivity analysis, not 10,000 independent trials; claiming otherwise is the kind of overstatement that loses the argument | — Pending |
| Plain JS with a Worker pool, not WASM, for the sweep/kernel compute path | Measured on the D-17 baseline (GitHub Actions `ubuntu-latest`, 4 logical cores, run 31965951474): PERF-03 at 807.92ms normalized (856.40ms raw, workerCount=3, chunkCount=12), 80.8% of the 1000ms budget, calibration score 1.0600. This dev sandbox separately measured 327.40ms normalized (185.80ms raw, 8 workers/32 chunks), 32.7% of the same budget, on 9 logical cores, calibration score 0.57 (informational only, not the D-17 baseline). The JS-over-WASM conclusion survives the worse baseline number: a throwaway Rust to WASM microbenchmark of the identical branchy per-bar recurrence (proven equal to the JS output within 1e-9 relative tolerance) measured **~1.20x SLOWER than JS**, not near parity as CONTEXT.md's rationale predicted, so escalating to WASM would spend the headroom rather than recover it | See `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` §3 for the full method, both arms' figures, and the honest floor-value caveat on the single-call comparison |
| Hand-rolled Canvas 2D (`putImageData`), not a charting library, for the heatmap | Measured on the D-17 baseline (GitHub Actions `ubuntu-latest`, run 31965951474): PERF-05 at 0.37ms of the 16ms budget. This dev sandbox separately measured `putImageData` at 0.11ms versus `fillRect`-per-cell's 5.92ms, informational only, not the D-17 baseline. No charting library was separately benchmarked, per D-14: `.claude/CLAUDE.md` §"Q2, Charting" already documents each rejected on record (uPlot has no heatmap mark; ECharts/Plotly degrade well under 10k cells; Observable Plot defaults to SVG, the specific 10k-cell failure mode this project needs to avoid) | See `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` §2 for both arms' raw figures and the equivalence proof that preceded trusting either |
| PERF-03 D-20 escalation: accept the measurement, keep the budget locked, carry headroom forward as a Phase 6/7 constraint | PERF-03 measured 807.92ms normalized (856.40ms raw, workerCount=3, chunkCount=12), 80.8% of the 1000ms budget, on the D-17 baseline (GitHub Actions `ubuntu-latest`, 4 logical cores, calibration score 1.0600, run 31965951474). The prior baseline run 31963076671 attempt 2 measured 700.38ms (70.0%) on the same baseline, so the D-20 70% trigger is crossed on two consecutive runs, not a single-sample artifact; attempt 1 of that run drew a 2-core runner and failed PERF-03 outright, a live risk to the gate itself. This is a deliberate escalation under D-20, not a budget relaxation: PERF-03's threshold stays 1000ms, no `thresholdMs` moves, D-19's lock holds on all eight budgets, and `NOMINAL_REFERENCE_MS` stays 40, on 01-01-PLAN.md's PERF-01a prohibition against altering the calibration reference loop in response to a measurement whose only effect would be to un-trip an escalation (retuning to the measured 37.30ms would read PERF-03 at 65.3% instead of 70.3%). A third baseline run, 31980066804, measured 702.73ms normalized (655.30ms raw), 70.3%, `escalate=yes`, so the trigger is crossed on all three baseline runs to date. That third sample also weakens the noise argument originally recorded alongside the prohibition: the reference loop measured 37.30ms, 42.40ms, and 37.30ms, making 37.30ms the modal value and 42.40ms the outlier rather than a spread centered near 40. The prohibition, not the spread, is what keeps the anchor at 40. Of D-20's three levers, the WASM ratio from D-11 is already spent: 01-04 measured Rust at about 1.20x SLOWER than JS, so adopting it would make PERF-03 worse. Pool tuning and a coarser default grid both belong to code that does not exist until Phase 7 and Phase 6 respectively. The choice recorded here is to accept the measurement, keep the budget locked, and carry the headroom question forward as a live constraint: any work added on the sweep path in Phase 6 or Phase 7 must be measured against PERF-03 before it lands. At 80.8% with the real kernel, attribution passes, and progressive paint still to be layered on top, roughly 19% headroom remains against the 30% that D-20's 70% rule was written to reserve. This is weaker than literally naming a lever; the lever choice is deferred to the phase that has real code to measure against, not settled here | See `.planning/phases/01-performance-spike-and-budget-lock/01-SPIKE-RESULTS.md` §4 addendum for the dated addendum recording this escalation against the D-17 baseline |
| SUPERSEDED (D-28): Stooq's redistribution licence terms are unclear and are a knowingly accepted risk (D-06) | FRED and Shiller are explicitly redistributable, recorded and authored once in each source's sidecar (`license`/`termsUrl`/`retrievedAt`), copied by the compiler into the manifest, and rendered by Phase 5's methodology page from the manifest, never hand-typed per file. Stooq's terms are permissive for personal use but unclear for redistribution of the bundled data itself; recording the terms per source and proceeding is D-05's resolution, and promoting the reasoning here (rather than leaving it as an implementation detail) is what D-06 requires | Accepted; `tools/fetch-data/src/sources.ts` carries the licence/terms text once per source. Superseded by the "Yahoo and Nasdaq licence positions" row below (D-05, D-06), implemented in plan 02-06/02-07. See `02-CONTEXT.md`'s Source Stack Reversal banner |
| SUPERSEDED (D-28): Stooq is refreshed by a human through a real browser, not by `npm run fetch-data` (Route C) | Stooq serves a JavaScript proof-of-work bot challenge to every plain-https request, confirmed this session; no response body from a scripted request is real data. No anti-bot bypass was written, no alternative vendor was substituted, and no symbol was dropped from the universe. `fetch.ts` reads a manually-placed vendor file from `raw/manual/<stem>.csv` and normalizes it through the identical `normalizeStooq` contract a live fetch would have used, so there is one code path and one coverage check regardless of how the bytes arrived. Shiller's `ie_data.xls` follows the same manual pattern (Route B) for a different reason: this environment has no network path to `econ.yale.edu` | Documented in `tools/fetch-data/README.md` and `tools/fetch-data/MANUAL-DOWNLOAD.md`; equity/ETF and Shiller raw inputs await a human download before `raw/` is complete. Superseded by the "Yahoo and FRED are fetched automatically" row below (D-27), implemented in plan 02-06/02-07. See `02-CONTEXT.md`'s Source Stack Reversal banner |
| Yahoo and Nasdaq licence positions are knowingly accepted risks (D-05, D-06) | Yahoo's terms are personal-use with an undocumented chart endpoint and no published redistribution grant; Nasdaq's index site carries its own terms of use with no explicit redistribution grant. FRED and Shiller remain explicitly redistributable. The terms text is authored once per source in `tools/fetch-data/src/sources.ts`, copied by the compiler into the manifest, and rendered by the Phase 5 methodology page, never hand-typed per file | Accepted |
| Yahoo and FRED are fetched automatically, Nasdaq and Shiller stay manual, and Yahoo falls back to committed vendor bytes (D-27) | Yahoo's chart API returns HTTP 429 to this development sandbox's shared egress address on the first request, from both `query1` and `query2`, regardless of headers, while the vendor's own web host and a real browser both succeed; that is an address-level block, not a malformed request, so the same command must work on a developer machine and on a shared-address runner. Three guards keep the fallback from becoming a trap: per-series route reporting in the coverage table, a staleness gate measured from the newest observation in the data (never file mtime) that fails the run outright, and no bypass flag | Implemented; declared thresholds are 10 days for every daily Yahoo/Nasdaq manual file and 75 days for Shiller's monthly input (`tools/fetch-data/src/sources.ts`) |
| Heatmap treatment: `form-2-filled-contour` ("filled contour", the pork-chop plot), not the dense grid, small multiples, or grid-with-contour-overlay (D-26) | Phase 6 built all four candidate forms to real scale against the same committed 200x50 sweep fixture and judged them on breakeven readability, categorical-state separation, dark-mode and screenshot-crop survival, and caveat placement. `form-1-dense-grid` lost for having no boundary mark at all, so breakeven has to be inferred from a colour step rather than read from a line. `form-3-small-multiples` lost because its 50 strip gaps break vertical continuity, so the breakeven boundary is not traceable as a shape across strips. `form-4-grid-with-contour` lost because it keeps the hard per-cell mosaic under its contour line, so the field reads as discrete samples rather than a continuous surface. No rejection reason cites implementation cost or a repaint figure. The owner accepted, with eyes open, that the winner is the most expensive of the four: measured on this dev sandbox (Linux aarch64 container, 9 logical cores, calibration score 0.58) at each form's own display geometry, form 2 measured 12.80ms normalized against the 16ms PERF-05 budget on the authoritative ubuntu-latest CI baseline, after an allocation fix cut it from an initially failing 23.92ms, versus 0.65ms (form 1), 0.69ms (form 3), and 0.98ms (form 4) for the other three. All four pass individually; this is not a ranking (D-12, F-02) | See `.planning/phases/06-heatmap-design-pass/06-HEATMAP-SPEC.md` for the full implementable treatment: palette stops, symlog domain, categorical cell rendering, legend structure, caveat copy, per-form geometry, and the O(display area) cost obligation the winning form creates for Phase 7's pan/zoom |
| PERF-03 D-17 baseline escalation (Phase 7): the zero-contribution default view fails the budget outright, and the `solveIrr` contribution-schedule branch fails it far worse; both are resolved by spending pool tuning first, with the coarser default grid held in reserve | The real D-17 baseline (GitHub Actions `ubuntu-latest`, 4 logical cores, calibration score 0.8150, CI run 32654061079, headSha `d81bc3501c1cd1e12c1b2f0e9eb86798f2ccc31b`, `source=production`) had never rendered a verdict for the production-pool sweep before this run, because the phase branch had no upstream and no PR (07-PERF-03-BASELINE.md section 1). Once it ran: the headline, zero-contribution figure -- the app's default view -- measured `measuredMs=952.40`, `normalizedMs=1168.59` against the 1000ms budget, **116.9% of budget, verdict=fail**, workerCount=3. This corrects an earlier framing in this phase's own planning that treated the default view as inside budget; it is not, on the real baseline. The same run's contribution-schedule (`solveIrr`) branch, sampled at `sampleCount=1` (informational, not a `MeasurementRow`), measured `measuredMs=7303.90`, `normalizedMs=8961.84`, roughly **8.96x over the same 1000ms budget** and roughly 7.67x worse than this run's own headline figure. Both figures carry `hardwareConcurrency=4` and are the D-17 baseline; this dev sandbox's own figures (`hardwareConcurrency=9`, informational, non-baseline) were 469.60ms/831.15ms (headline) and 2010.20ms/3557.88ms (`solveIrr`) on the same code, and 07-03-SUMMARY.md and 07-VERIFICATION.md separately recorded ~3453ms and 3351.07ms normalized on other 9-core sandboxes for the `solveIrr` branch -- all non-baseline figures agree the branch is several times over budget, and the D-17 figure is the largest of the group because it also carries this run's own headline slowdown. Confidence band: the contribution-schedule arm is `sampleCount=1`, and 07-PERF-03-BASELINE.md's own measurement-band info line (`normalize()` residual 6.36% relative over 13 recorded D-17 runs) puts roughly +/-13% (2 standard deviations) on any single normalized figure; even at the low end of that band both figures remain over budget. What was already tried: 07-03's log/exp hoist in `npv` (already shipped, unchanged by this row), and D-24's proposed Horner-loop reduction over `buildCashFlows`, checked and rejected because calendar gaps are irregular, so a Horner loop would compute a wrong answer rather than a slower one. Of D-20's three levers: WASM is spent and counterproductive (01-04 measured Rust at roughly 1.20x slower than JS on this recurrence, so it would make PERF-03 worse, not better); pool tuning is genuinely unspent, and this baseline measures at `workerCountForCores(4)=3` (`src/sweep/sweep-pool.ts`), so chunking, transfer discipline and partition shape have never been tuned against either branch's real per-cell cost profile; the coarser default grid is D-03's reserved lever, explicitly kept unspent there so PERF-03's headline figure keeps describing the fixed 200x50 default view rather than a configuration-dependent one. **Decision: spend pool tuning first, on both overruns together, and hold the coarser grid in reserve.** Pool tuning is spent first, not the coarser grid, because D-03 reserved the grid specifically to preserve headline-figure comparability across phases, and spending it now would forfeit that before pool tuning -- the actually-unspent lever -- has been tried. The two overruns plausibly share a root cause in worker-pool throughput on this 4-core baseline (`workerCount=3`, `hardwareConcurrency=4`); this is stated as an inference from the pool configuration, not a profiled finding, and is not to be read as measured. This resolves the escalation half of PERF-01a's rule: `thresholdMs` for PERF-03 is unchanged, `NOMINAL_REFERENCE_MS` is unchanged, D-19's lock holds on all eight budgets, and no calibration constant or run-level cap moved in response to this measurement. **PERF-03 is FAILED, not met**: the phase's third roadmap success criterion does not close on this record. Implementation is a follow-up phase's job: naming pool tuning here is a decision, not code, and its effect must be measured on the D-17 baseline before PERF-03 can be claimed met. Carried forward, unresolved: this same baseline run's own total bench-suite runtime (43,259ms) also exceeded `BENCH_TOTAL_RUNTIME_CAP_MS` (30,000ms), masked because the verdict-fail check in `assertRunInvariants` fires first; the follow-up performance work will encounter this and should not be surprised by it | See `.planning/phases/07-sweep-engine-and-the-heatmap/07-PERF-03-BASELINE.md` sections 2 and 3 for the full CI artifact transcriptions, environment blocks, and per-host figure table this row's numbers are copied from |
| Worker-count tried and reverted (Phase 7.1): `workerCountForCores` was changed from `cores - 1` to `cores`, adopting width 4 on the 4-core D-17 baseline pending the abort condition's own authoritative run, and was then reverted back to `cores - 1` once that run answered (see the correction paragraph below, ahead of the residual-risk paragraph, for the measured outcome and the revert). Plan 07.1-04's own stated threshold (a clearly-under-budget `maxRecomputeMs`/`maxFrameGapMs` at width 4, no measurable regression against width 3, with margin comparable to WINDOWS.md's own +/-13% single-run noise band) is recorded here as NOT satisfied by the evidence: the evidence is unmeasurable at the informational arm's current sampling, a different and more useful finding than a clean threshold pass | Plan 07.1-04's Task 2 deferred the worker-count decision to two `hardwareConcurrency=4` production CI runs of `bench/sweep-contention.bench.test.ts` (width parameterized explicitly, not read from `workerCountForCores`, so both widths are measured regardless of production code), against an explicit pre-stated threshold. Runs 32673398579 (`calibrationScore=0.7275`) and 32674615017 (`calibrationScore=1.0550`) answered, and disagree on the one figure the threshold leaned hardest on: `maxRecomputeMs` at width 4 measured 10.0000ms on the first run and 1.2000ms on the second, a swing of 8.8ms on a statistic whose own budget (PERF-07b) is 16ms -- both individual readings are technically under budget, but a max-of-a-single-run statistic that swings by more than half its own budget across two runs of identical code is not a signal that resolves "clearly under budget with real margin," it is a measurement whose own noise floor is unknown. What IS consistent across both runs: `sweepDurationUnderLoadMs` improved 15.0% (909.5ms to 772.9ms, run 32673398579) and 11.2% (858.1ms to 762.1ms, run 32674615017) at width 4 versus width 3, and `maxFrameGapMs` (PERF-07a, 50ms budget) read identically at 16.8000ms at both widths on both runs -- essentially exactly one frame interval, completely unmoved by the fourth worker. So the decision recorded here is explicitly NOT "plan 07.1-04's threshold was met": the informational arm cannot answer the interactivity-safety question at its current sampling (a max-of-one-run statistic, where a min-of-N or median across repeated in-flight-sweep trials was needed to separate signal from noise -- a defect in the arm's own design, flagged here for whoever next relies on `maxRecomputeMs` as a decision input, not fixed in this plan since fixing it is out of this plan's authorized scope). What the evidence DOES support, unambiguously: a real 11-15% throughput gain at width 4, and a frame-gap figure (the harder interactivity constraint, since a >50ms gap is a visibly dropped frame) that shows no measurable movement at all across either run. The question of whether width 4 is interactivity-safe on the real baseline therefore moves from this informational arm to the gated rows PERF-07a and PERF-07b themselves, which are budget-enforced and measured on every CI run going forward, not deferred to a second look at a statistic already shown to be unreliable at n=2. **The abort condition binding this decision:** if on either authoritative production run (the two consecutive PERF-03 baseline runs this phase's merge bar requires) PERF-07a or PERF-07b turns red, or crosses D-20's 70%-of-budget escalation trigger, `workerCountForCores` reverts to `cores - 1` in both declarations and PERF-03 is recorded as an escalation rather than a pass -- the interactivity budgets locked under D-19 are not spent to buy PERF-03's headline figure. **CORRECTED, 07.1-06: the abort condition fired and the adoption was reverted.** The authoritative production D-17 baseline run this abort condition names (CI run 32676218114, `hardwareConcurrency=4`, `calibrationScore=0.9350`, `source=production`, the first and only run of the width-4 code) measured PERF-07b at 12.30ms normalized against its 16ms budget, 76.9% of budget, crossing D-20's 70% escalation trigger. That is the abort condition's own trigger firing, and on its own, from a single run, it would be thin evidence: the three width-3 runs already ranged 56.4% to 68.7% on the same statistic. **It is not the deciding ground.** The deciding ground is that the same run measured the PERF-03 headline row at 1191.34ms normalized, landing inside the three width-3 runs' own spread (1115.92 to 1208.38ms), not below it -- width 4 bought no measurable throughput gain on the headline figure at all, refuting 07.1-PERF-03-PROFILE.md section 5's 25.00% linear-scaling projection (851.39ms to 638.54ms), which that section's own text had already flagged as untested arithmetic rather than a profiled number. With no measured upside to weigh against even a noisy interactivity risk, `workerCountForCores` was reverted to `cores - 1` in both `src/sweep/sweep-pool.ts` and `bench/sweep-pool.ts`; `BASELINE_WORKER_COUNT` returns to 3, `chunkCount` to 12; correctness clamps re-run and pass at the restored width. See `07.1-PERF-03-BASELINE.md` section 8 for the full measurement and both grounds, and `07.1-PERF-03-PROFILE.md` section 5's own correction for the refuted projection. No budget value, calibration constant, cap value, or grid dimension moved to reach this disposition. **Residual untested risk, carried forward rather than closed:** no arm in this project, including `sweep-contention.bench.test.ts`, exercises a real pointer drag through the production reactive path (`src/app/state.ts`'s `scheduleRun`) concurrently with an in-flight sweep -- the contention arm's own `runBacktest` call is a direct kernel call, not a driven UI interaction, so the automated evidence bounds the numbers but does not settle whether a human perceives stutter, which is why Task 2's own `human-check` exists as a required companion to this figure, not a formality | See `bench/sweep-contention.bench.test.ts`'s own info lines from CI runs 32673398579 and 32674615017 (both `hardwareConcurrency=4`, downloaded and independently verified against the artifact, not trusted from a transcription) for the full per-width figures this row's numbers are drawn from, `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-04-SUMMARY.md` for the deferred threshold this decision resolves, and `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` section 8 for the authoritative run (32676218114) that fired the abort condition and the revert it produced |
| PERF-03 D-17 baseline escalation (Phase 7.1 continuation): the `solveIrr` contribution-schedule branch improved 1.83x measured locally but remains far over budget on the real baseline; escalated with a named remaining lever | The prior D-17 baseline (CI run 32654061079, `hardwareConcurrency=4`) measured this branch at `measuredMs=7303.90`, `normalizedMs=8961.84`, `sampleCount=1`, roughly 8.96x over the 1000ms budget. 07.1-03 spent two levers against it, both aimed by 07.1-PERF-03-PROFILE.md's measured `npvEvaluationsPerSolve=105.01` and `cashFlowCount=1185`. Lever A replaced the loop's sole termination test (`|npv(mid)| < TOLERANCE`, a dollar-denominated NPV magnitude that was driving the loop to exhaust `MAX_ITERATIONS=100` almost every solve, since a residual below $1e-9 demands far more refinement than an 11-unit rate bracket collapsing to 1e-9 would) with an added rate-space bracket-width test (`hi - lo <= 1e-9`), measured to cut `npvEvaluationsPerSolve` from 105.01 to approximately 36.3, close to 07.1-RESEARCH.md's own naive ~33-34-iteration estimate. Lever B replaced the `CashFlow[]` per-flow object-array representation with `CashFlows`, a pair of parallel `Float64Array` buffers plus a count, eliminating the per-flow heap allocations that dominated at the measured cash-flow count. Combined, the two levers measured a 1.83x wall-clock reduction on a dev sandbox (min-of-3 samples, `hardwareConcurrency=9`: 2003.00ms to 1097.60ms raw), materially short of 07.1-PERF-03-PROFILE.md section 5's 3.14x projection, which assumed nearly all of the branch's cost was `npv` evaluation. That assumption is now known to be wrong: `buildCashFlows`, `runBacktest`, and pool/dispatch overhead do not shrink with fewer `npv` evaluations, so 07.1-PERF-03-PROFILE.md section 5's 3.14x wall-clock figure is superseded by this measured 1.83x on this specific point; its iteration-count finding (the root cause and the ~33-34-iteration prediction) stands confirmed. Plan 07.1-03's own local-to-baseline projection (approximately 4871.75ms) is now superseded by the real measurement: two `hardwareConcurrency=4` production CI runs on this branch's shipped code (32673398579, `calibrationScore=0.7275`, `headSha=e6184a3`; 32674615017, `calibrationScore=1.0550`, `headSha=5d25895`) measured `normalizedMs=3888.66` and `normalizedMs=3952.70` respectively, both `sampleCount=2`, both `source=production`, both independently downloaded and verified against the artifact rather than trusted from a transcription. Against the pre-phase 8961.84ms baseline that is roughly a 2.30x and 2.27x improvement, beating the 4871.75ms projection by a wide margin, but the branch remains roughly 3.89x to 3.95x over the 1000ms budget on the real baseline. Of D-20's remaining levers, the only one with plausible headroom against a residual multiple-x gap is Newton-Raphson with the existing bisection kept as a fallback (when a derivative step leaves the bracket or fails to converge in a small iteration budget), scoped to the sweep call site only, per 07.1-RESEARCH.md's own Don't-Hand-Roll guidance: Newton converges in roughly 5-10 iterations against bisection's tens for the same tolerance. This reopens D-08, which rejected a two-solver path on the premise that IRR "runs once per parameter change, over a few hundred cash flows, where speed is not the constraint." The sweep call site runs `solveIrr` 10,000 times per sweep, over cash-flow sequences up to roughly 1,185 flows; the premise D-08 was written against does not hold at this call site, which is why a Newton-with-bisection-fallback path needs its own recorded Key Decision rather than a silent edit, not because D-08 was wrong where it was written. **Decision: escalate.** Adopting Newton-with-bisection-fallback is deferred to a future planning cycle (`/gsd-plan-phase 7.1 --gaps` or a later phase), scoped to the sweep call site only, with `solveIrr`'s existing signature and its single-run caller in `src/app/state.ts` untouched, and the existing bisection kept as the correctness-proven fallback (Descartes' rule, exactly one sign change by construction) rather than a from-scratch replacement. **No budget value, threshold, anchor, calibration constant, run-level cap, or grid dimension moved to reach this disposition**: `PERF-03.thresholdMs` stays 1000, `NOMINAL_REFERENCE_MS` stays 40, `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000, the default grid stays 200x50. **PERF-03 remains FAILED for this branch, not met**, exactly as Phase 7's own D-17 baseline escalation recorded for the headline row | See `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-03-SUMMARY.md` for the full precision-proof evidence (a nine-case corpus proving no displayed IRR moved by more than a raw-rate difference of 3.20e-10 across the two levers), `07.1-PERF-03-PROFILE.md` for the profiled root cause both levers were aimed at, and CI runs 32673398579 and 32674615017 for the real baseline measurement this row's figures are drawn from |
| `BENCH_TOTAL_RUNTIME_CAP_MS` shortfall (Phase 7.1 continuation): the bench suite's runtime remains far over its 30,000ms cap even after both of this phase's own runtime-reduction tasks | The D-17 baseline (CI run 32669644628, `hardwareConcurrency=4`) measured `totalRuntimeMs=66527` against the cap, a rise of 23,268ms since Phase 7's own baseline (43,259ms), of which 07.1-01's new `bench/sweep-pool-profile.bench.test.ts` accounted for 11,835ms (50.9%) at its original size. 07.1-05 spent every lever available to it: Task 1 declared standing cost budgets for this phase's two new diagnostic arms (`sweep-pool-profile`: 600ms; `sweep-contention`: 1500ms, both `hardwareConcurrency=4`) and shrunk both toward those budgets (`PROFILE_COLS` 24->12, `IRR_PROFILE_COLS` 8->4, a shared npv/solveIrr batch split into separate smaller batches measured per-call; `sweep-contention`'s warm-baseline and in-flight sweeps both narrowed from the full 200-column grid to a shared 135-column axis, bounded by its own 10-frame minimum guard), cutting those two files' combined local (dev-sandbox) cost by 55.3% (3712ms to 1873ms). Task 2 worked down `07.1-PERF-03-PROFILE.md` section 4's per-file cost table among the five pre-existing files it could touch and found exactly one genuine reduction candidate: `heatmap-form-2.bench.test.ts`'s informational polygon-repaint arm, which re-measured an already-REJECTED path (D-9's `'resample'` over `'polygon'`, 07-04-PLAN.md Task 3) whose historical figure (149.71ms) was already transcribed in committed documents -- removed, cutting that file's local cost by 43.0% (2518ms to 1434ms). The other four files (`heatmap-panzoom`, `bundle-size`, `sweep-progressive`, `perf-07`) were found to have no reduction candidate under the same no-gated-workload-touched discipline: each file's cost is either entirely gated (two arms feeding one `MeasurementRow` via a `Math.max`, or each arm recording its own row directly) or the measured quantity IS the cost itself (`bundle-size`'s brotli-compressed byte count, where lowering compression quality would change what is measured, not merely its cost). `perf-07.bench.test.ts`'s only remaining lever, `INTERACTION_DRAG_STEP_COUNT` (this plan's own named step-4 last resort), was considered and declined: even halving it saves at most a few thousand ms against a remaining gap measured in the tens of thousands, for a real cost (a weaker sample size behind D-03's coalescing evidence and PERF-07a/07b's own maximum-over-a-smaller-sample). Plan 07.1-06's own CI run at `hardwareConcurrency=4` replaces the prior projection (52,958ms-60,791ms) with the measurement: production CI run 32674615017 (`calibrationScore=1.0550`, `source=production`, independently downloaded and verified) measured `totalRuntimeMs=57717` against the 30,000ms cap, inside the projected range; a second run, 32673398579 (`calibrationScore=0.7275`), measured `totalRuntimeMs=45709`, below the projected range -- real evidence that run-to-run CI variance on this suite is itself large, not just a projection artifact. Against the pre-reduction D-17 baseline (66527ms), the 57717ms figure is a real reduction (13.2%), but the suite remains roughly 27,717ms (92.4% over the cap) short on that run, and 15,709ms (52.4% over the cap) short even on the faster of the two runs -- a genuine, measured shortfall, not closed by this phase's own two runtime-reduction tasks. **No budget value, threshold, anchor, calibration constant, run-level cap, or grid dimension moved to reach this disposition**: `PERF-03.thresholdMs` stays 1000, `NOMINAL_REFERENCE_MS` stays 40, `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000, the default grid stays 200x50. **The named remaining lever, for whichever future plan takes this up:** the two largest remaining costs are both fully gated files this plan had no authority to touch -- `bench/sweep.bench.test.ts` (18161ms at the original CI baseline, now likely lower after 07.1-03's `irr.ts` levers but still the single largest file) and `bench/perf-07.bench.test.ts` (8863ms, un-reducible without weakening its own gate's sample size). Closing this gap requires either a scoped plan with explicit authority to touch one or both of those gated files' own measured workload (a Key Decision in itself, since T-07.1-12's no-relaxation discipline would need a different, explicitly-approved tradeoff), or a restructuring of what runs inside the `BENCH_TOTAL_RUNTIME_CAP_MS`-gated CI job versus a separate, non-gating diagnostic job -- neither of which this plan's own scope or file list authorized it to decide | See `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-05-SUMMARY.md` for the full per-file breakdown, the ratio derivation and its cross-check, and the two-method projection; CI runs 32673398579 and 32674615017 for the real measured totals this row's figures are drawn from |
| PERF-03 headline outcome (Phase 7.1 closure): the zero-contribution headline row FAILED on all four `hardwareConcurrency=4` production runs at both worker widths this phase tried; escalated with two named remaining levers, both held in reserve | Four D-17 baseline runs, all `hardwareConcurrency=4`, `source=production`, `verdict=fail`, independently downloaded and re-parsed from their own artifacts: 32669644628 (`calibrationScore=1.0525`, width 3) measured `normalizedMs=1120.86` (112.1% of the 1000ms budget); 32673398579 (`calibrationScore=0.7275`, width 3) measured `normalizedMs=1208.38` (120.8%); 32674615017 (`calibrationScore=1.0550`, width 3) measured `normalizedMs=1115.92` (111.6%); 32676218114 (`calibrationScore=0.9350`, width 4, the authoritative run) measured `normalizedMs=1191.34` (119.1%). The width-4 run landed inside the width-3 three-run spread (1115.92 to 1208.38ms), not below it, refuting 07.1-PERF-03-PROFILE.md section 5's 25.00% linear-scaling projection for a fourth worker and producing the "Worker-count tried and reverted" row above. All four normalized figures cluster within roughly 4% of each other despite raw `measuredMs` swinging 879.10ms to 1180.00ms (roughly 34%, tracking each run's own `calibrationScore`) -- run 32673398579's own raw figure (879.10ms) is under the 1000ms budget yet its normalized figure is the worst of the four, the clearest demonstration on this project's own record of why PERF-01a forbids retuning `NOMINAL_REFERENCE_MS` or any other calibration constant in response to a measurement: the raw number tracks the runner, the normalized number does not, and chasing the raw number would un-trip a real, if modest, gap rather than close it. **PERF-03 is FAILED, not met.** The phase's second roadmap success criterion (the headline row at or under 1000ms, `verdict=pass`) does not close on this record, exactly as Phase 7's own D-17 baseline escalation recorded before this phase began. Every lever this phase spent against the headline figure is named plainly: 07.1-PERF-03-PROFILE.md's attributed breakdown (closed, criterion 1, and remains the correct account of where the wall clock goes regardless of the worker-count outcome), the `solveIrr` convergence fix (real on that branch, zero effect on this headline row since the two branches share no code path it touched), and worker width 4 (tried, measured worthless on this figure, reverted, per the row above). **Two levers remain unspent, both deliberately held in reserve, named here so a future escalation does not have to invent one under pressure:** (1) the kernel's write-only per-bar output arrays in `src/kernel/backtest.ts` -- `runBacktest` writes three per-bar scratch arrays on every bar of every cell that the sweep worker never reads back, reasoned from reading the source, not measured, and a kernel change rather than pool tuning, so it needs its own measurement and its own Key Decision; (2) D-03's coarser default grid, still unspent and still reserved specifically to preserve the headline figure's comparability across phases as a fixed 200x50 view rather than a configuration-dependent one -- spending it now would forfeit that comparability before the first lever has been tried. **No budget value, threshold, anchor, calibration constant, run-level cap, or grid dimension moved to reach this disposition**: `PERF-03.thresholdMs` stays 1000, `NOMINAL_REFERENCE_MS` stays 40, `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000, the default grid stays 200x50, confirmed by `git diff --exit-code` against the phase base commit (`7c21f0bc2f5fe7637ea7e898320b84f5824199eb`) exiting 0 after this plan's own revert commit landed -- see `07.1-PERF-03-BASELINE.md` section 6 | See `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` for the full seven-section transcription, the six-criterion disposition table, and both grounds for the revert, and `.planning/REQUIREMENTS.md`'s PERF-03 mapping row for the requirement's own updated status |
| PNG export Safari defect found and fixed (Phase 8, plan 08-05 Task 3): every region canvas rendered blank on the FIRST capture after any chart rebuild in real Safari, present since plan 08-01, never caught by the repo's Chromium-only `app` test project. A scoped WebKit test project was adopted to close the coverage gap that let it through, not extended to the whole suite | The gate this plan built specifically to catch a Safari-only defect (08-05 Task 3's human-verify checkpoint) did its job: the first real-Safari run failed. Root cause, established in Playwright WebKit 2336 against the real production build: `html-to-image` never routes canvas pixels into its output. `cloneCanvasElement` swaps each live `<canvas>` for an `<img src="data:image/png;...">`, `svgToDataURL` serializes the whole clone into one `data:image/svg+xml` URL, and `toCanvas` loads that into a single `Image`. The per-canvas images are therefore subresources of a different document, and WebKit resolves the outer image's `load`/`decode()` before those subresources decode, so they rasterize as nothing; the second capture happens to work, which is what made it look like a caching problem. Two cache-shaped candidate fixes (pre-decoding the identical `toDataURL()` string; substituting an already-decoded `<img>`) were tested directly in real WebKit and both disproven before the shipped fix was written. `src/export/png-export.ts` now shadows each region canvas's `toDataURL` during capture so the library embeds a 1x1 transparent PNG instead of chart pixels, then composites each live canvas's own backing store into html-to-image's output canvas with `destination-over` in reverse document order, filling the opaque theme background last. All seven region canvases were affected (equity curve, heatmap, crosshair overlay, both slice charts, legend ramp, legend swatches), not only the equity curve. A new `tests/app/export-png-canvas-fidelity.browser.test.ts` (5 tests) plus a scoped `app-webkit` vitest project was added: it fails 2/5 against the ORIGINAL code in WebKit and passes 5/5 against that same original code in Chromium, proving the gap was structural and a Chromium-only matrix could not have caught it. Running the whole `app` suite under WebKit was evaluated and rejected for now: 52 of 189 tests fail, dominated by WebKit's `history.replaceState` rate limit (which Chromium does not enforce) and Chromium-generated `__screenshots__` baselines, neither of which this fix touches. PERF-07a's PNG figure was re-measured after the fix against a pre-fix baseline taken on the same host, not trusted from Task 2's other-host figure: `png rawMs=0.0000, longTaskCount=0, verdict=pass`, identical to the pre-fix figure. An intermediate fix revision that suppressed the library's canvas embedding with `'data:,'` measured 52ms, a real long task over PERF-07a's 50ms budget, because that value routes html-to-image down a synchronous `cloneNode(false)` branch that removes the per-canvas awaits keeping its work split into short tasks; the shipped fix uses a browser-encoder-generated 1x1 transparent PNG instead, which keeps those task boundaries and measured back at 0ms. No threshold, anchor, or normalization constant was altered at any point (PERF-01a) | Confirmed fixed in real Safari by the user against the post-fix bundle (`assets/index-vwbqaOH_.js`, read from the page's resource list, not assumed), with the service worker eliminated first via Settings > Privacy > Manage Website Data so no stale precache could serve. Full record: `.planning/debug/resolved/png-export-blank-canvas-safari.md` |
| **PERF-03 merge bar released (2026-08-25): the 1000ms budget is measured unreachable on the D-17 4-core host by every lever this project has identified, so the two-consecutive-pass merge bar is released and Phase 7 ships with PERF-03 recorded as a standing, failing escalation rather than blocking the milestone indefinitely** | The merge bar set on 2026-08-23 required the PERF-03 headline to read at or under 1000ms normalized, `source=production`, `hardwareConcurrency=4`, on TWO CONSECUTIVE runs. It was never met. Six recorded D-17 production runs, every one `verdict=fail`: normalized 1120.86, 1208.38, 1115.92, 1191.34 (worker width 4), 1411.05 and 1270.99ms against a 1000ms budget. **Five levers are now measured, not reasoned, and all five are refuted.** (1) WASM (01-04): a Rust microbenchmark of the identical recurrence measured roughly 1.20x SLOWER than JS, so it would make PERF-03 worse. (2) Worker width 4 (07.1-06): measured 1191.34ms normalized, inside the width-3 spread of 1115.92-1208.38ms rather than below it, refuting 07.1-PERF-03-PROFILE.md's own 25.00% linear-scaling projection; reverted on two independent grounds. (3) The kernel's write-only per-bar output arrays (quick-260824-46s): a five-sample A/B at the real ~25,000-bar series measured variant/shipped ratio min=0.9810 median=0.9841 max=0.9904, roughly 1-2% of kernel compute, gated behind a bit-identical `KernelResult` equivalence proof across four canonical cases. (4) Pool and dispatch overhead (quick-260824-52h): one real 10,000-cell sweep instrumented at the production pool boundary measured buffer allocation at 0.03ms, `mergeChunkResult` at 0.01ms and wire-write at 0.10ms across the WHOLE grid, worker-drain imbalance at 7.50ms (1.6%), and `concurrencyFactor` at 1.05; real per-chunk `computeMs` summed and divided by `workerCount` explains 98.3% of wall clock with no extrapolation, so no pool-overhead lever remains. That task also measured `spanRatio=0.51`, proving 07.1-PERF-03-PROFILE.md section 2's `perCellKernelUs=255.42` was measured at a 2-column chunk span while the grid actually runs 17-column chunks: its `idealParallelFullGridMs=851.39` is roughly double the real cost, and correcting it GROWS the residual to a projected 691.16ms (58.6%) rather than shrinking it, so the 273.98ms residual was substantially an artifact of that document's own narrow-arm extrapolation and never a separately closeable overhead. (5) Per-cell kernel compute itself (quick-260824-r5d): nine ablation arms measured at the real 17-column chunk span, each proven bit-identical on all eight `KernelResult` fields across four canonical cases before any clock started (28 proven cases, runtime-gated by a proven-case counter). The combined bit-preserving arm, stacking all six candidates, measured min=0.9740 median=1.0036 max=1.1012. Six of eight arms measured SLOWER than the shipped kernel at the median (worst: deduplicated drawdown site at 1.1605), which is direct evidence the hand micro-optimizations regress what V8's JIT already does well on this monomorphic typed-array loop. **The pass thresholds and the arithmetic:** normalization divides raw by `calibrationScore`, so a pass needs `raw <= 1052.5` (a 10.8% cut) at run 32669644628's `calibrationScore=1.0525`, or `raw <= 910.0` (a 21.3% cut) at run 32686531154's `calibrationScore=0.9100`; the two-consecutive-pass bar makes 21.3% the robust target. The precomputed decision rule (`rCombined <= 0.7833` clears 21.3%, `<= 0.8907` clears 10.8%) is missed at EVERY measured point including the single best round observed, falling short of even the weaker threshold by more than 9 percentage points. **The two levers still unspent cannot change this.** D-03's coarser default grid does not close the number, it redefines what the number measures, forfeiting headline comparability across all six recorded runs and contradicting the '~10,000 backtests' figure in the project's own stated constraints. Newton-with-bisection-fallback for `solveIrr` cannot touch the gated headline at all: the zero-contribution branch has no cash flows and never calls `solveIrr`. It remains worth adopting on its own merits for the contribution-schedule branch, where `npvEvaluationsPerSolve` measured 105.01 against a Newton expectation near 10, and it still requires its own Key Decision because it reopens D-08. **A separate finding recorded here because it bears on the bar rather than on the code:** between runs 32669644628 and 32686531154 the RAW measured figure FELL 23.10ms (1179.70 to 1156.60) while the NORMALIZED figure ROSE 150.13ms (1120.86 to 1270.99). The entire move came from `calibrationScore` swinging 1.0525 to 0.9100, a 13.5% shift in the normalizer, on a run whose measured work was slightly faster. Most of the visible instability in the gated metric is in the normalizer, not the sweep, which means a two-consecutive-pass bar was being asked to clear a moving target. This is recorded, NOT acted on: retuning or reweighting the calibration anchor in response to a failing measurement is exactly what PERF-01a prohibits and what the 01-04 escalation already litigated once, and its only effect would be to un-trip a gate rather than make anything faster. **Decision: release the merge bar, not the budget.** `PERF-03.thresholdMs` stays 1000. `NOMINAL_REFERENCE_MS` stays 40. `BENCH_TOTAL_RUNTIME_CAP_MS` stays 30000. D-19's lock holds on all eight budgets. The default grid stays 200x50. No budget value, threshold, anchor, calibration constant, run-level cap or grid dimension moved to reach this disposition, and `src/kernel/backtest.ts` is byte-identical across all three measurement tasks. **PERF-03 remains FAILED and is not claimed met.** What changes is only that the project stops blocking Phase 8 and `main` behind a budget no available lever can reach, and stops spending phases against levers that measurement has already refuted. This satisfies PERF-01a's escalation path: the requirement is escalated in the open with its evidence, never silently relaxed. Re-opening PERF-03 requires a genuinely new lever, and the bar for 'new' is a MEASUREMENT, not a projection: every one of the six projections this project has tested against measurement was refuted | See `.planning/quick/260824-46s-.../260824-46s-FINDINGS.md`, `.planning/quick/260824-52h-.../260824-52h-FINDINGS.md` and `.planning/quick/260824-r5d-.../260824-r5d-FINDINGS.md` for the three measurement transcriptions, and `.planning/phases/07.1-sweep-pool-tuning-inserted/07.1-PERF-03-BASELINE.md` for the five-run baseline this row's figures extend |

## Current State

**Shipped: v1.0 on 2026-08-27.** 9 phases, 66 plans, 604 commits, 12 days. 71 of 72 requirements
met. Closed as an override closeout with two acknowledged escalations. See
[MILESTONES.md](MILESTONES.md) for what shipped and what did not, and STATE.md's Deferred Items for
the measurement history behind the escalations.

The whole product loop is live: a sourced and versioned bundled dataset, a simulation kernel proven
against real UPRO and TQQQ history, a single-run backtest with cost attribution, a 10,000-cell
entry-date by leverage heatmap, and export as PNG, CSV or a shareable permalink.

**Core Value check.** Still the right priority, and it survived contact. The one thing the project
said it could not get wrong ("if everything else fails, the simulation math must be right and must
be demonstrably right against real leveraged ETF price history") holds: the v1.0 audit confirmed a
single-implementation kernel with no drifted second copy anywhere in production code, and the
UPRO/TQQQ tracking gate passes against the real bundled series rather than a fixture.

**What v1.0 got wrong, in its own terms.** Not the math. The measurement apparatus. Two of the
three things that went wrong late were failures of instrumentation rather than of the product:
a performance gate that could not distinguish 5ms from 49ms and therefore certified a path that was
2.4x over budget, and a benchmark whose run-to-run variance is wider than several of the
improvements it was asked to detect.

## Next Milestone Goals

Not yet scoped. Run `/gsd-new-milestone` to define v1.1. Four items carry forward:

1. **Decide what to do about the CI benchmark.** The project owner's position, recorded 2026-08-27,
   is that it is not useful given run-to-run variability. The project's own data supports the
   concern: `normalize()`'s residual is 6.36% relative over 13 D-17 baseline runs, so a single run
   supports a headroom claim only to roughly plus or minus 13%, and PERF-03's observed spread
   (1115.92 to 1411.05ms) is wider than several refuted levers' effect sizes. Separately, PERF-07a's
   longtask-observer instrument is structurally incapable of evidencing headroom at all. Keep,
   rebuild or retire is undecided, and this decision plausibly changes the status of PERF-03 itself.
2. **PERF-03**, not met, five levers refuted, only D-03's coarser default grid left and it changes
   what is measured rather than making it faster. Whether this stays a requirement depends on 1.
3. **Bench suite total runtime**, 56,141ms against a 30,000ms cap. Also depends on 1.
4. **Nyquist coverage**, 3 of 9 phases reconciled; six carry a `VALIDATION.md` at `status: draft`.

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-08-27 at the v1.0 milestone close*
